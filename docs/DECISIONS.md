# FracVibe — decision ledger (append-only)

One row per decision. **Append-only**: a decision is history and history never
rots, so never edit another landing's row. Each row names its provenance and what
was rejected. Where the owner's words are on file they are quoted; where they are
not, the row says so rather than inventing an authority.

| # | Decision | Why / evidence | Rejected | Provenance |
|---|----------|----------------|----------|------------|
| 1 | **Make it work again before anything else.** Phase 0 = B1–B3 only. | The deployed app was a blank canvas: `Uncaught TypeError: Cannot read properties of null (reading 'addEventListener')` at `app.js:181` aborted module evaluation, so no worker, no WebGL init, no render (`MODERNIZATION.md` §1 row 1). Nothing else was observable, let alone improvable. | Jumping to Phase 2 (Vite / TypeScript / `three` from npm), which would have modernised a blank page. | `MODERNIZATION.md` §5 step 1. The owner's verbatim words for this round are not on file. |
| 2 | **`public/` stays the publish dir; no build step yet.** | `netlify.toml` has `publish = "public"`, `command = ""`, and the app is already static ES modules. | Moving output to `dist/` now — it churns `netlify.toml` and every path for no behaviour change. Deferred to the Vite landing. | `MODERNIZATION.md` §4 "Delivery". |
| 3 | **Defer WebGPU, WASM/SIMD kernels and OffscreenCanvas.** | None fixes a current defect; each is a rewrite with its own risk, and would add unverified surface while B4–B8 are open. | Doing them as part of "modernization". | `MODERNIZATION.md` §5 "Explicitly deferred". |
| 4 | **One gate command: `bash scripts/gate.sh`, two tiers.** | A hand-rolled `npm test` cannot hold a host-wide lock, cannot distinguish "did not run" from "passed", and does not keep a raw log. `exit 2 = compile-only` exists so a syntax check can never be quoted as verification. | Calling `npm test` directly, and a single tier. A compile tier is a convenience here (the suite is ~6s), but it keeps "did the suite run?" explicit and answerable off-browser. | Dispatcher, `session-f6d26a74`, 2026-09-21. |
| 5 | **`reuseExistingServer: false`, and the gate refuses a busy `:3000`.** | With reuse on, a server left by a dev session or another worktree is reused, and a green suite silently reports on code the run never loaded — a wrong-tree verification with no symptom. | Leaving it `true` for convenience; or letting Playwright fail on the port, which would surface as RED ("the checks ran and failed") when in fact nothing ran. | Dispatcher, `session-f6d26a74`, 2026-09-21. Low risk, trivially reversible by the owner. |
| 6 | **The objective is "resilient and correct", and the METHOD is the dispatcher's judgement, executed as a sequenced campaign.** The bar is defined as CORRECT (every renderer agrees with one mathematical definition at every reachable control setting) and RESILIENT (no single failure leaves a blank canvas, a wedged UI, or an uncaught exception), each behaviour PINNED by a test shown red on a deliberately broken build. | Owner's verbatim words, 2026-09-21: *"I want this to be resilient and correct, you can take your time to achieve that state. You be the judge how to achieve this best."* The audit's own defect inventory (`MODERNIZATION.md` §2–§3) is the raw material; the bar is stated so a successor can tell done from not-done. | (a) Treating the mandate as a feature list and shipping B4–B8 in the audit's written order — the audit orders by section, not by risk, and puts hygiene before the silent-failure defects. (b) Dispatching several writers at once: every slice touches `public/app.js`, so they would collide. The campaign therefore SERIALIZES on `app.js`. | Owner, 2026-09-21, via the chief-of-staff session `session-f6d26a74`; plan at `docs/PLAN.md`. |
| 7 | **S1: the zoom clamp lives in `FractalViewer` alone (`setZoomLimit`/`clampScale`), and the cap is applied to the STORED view, not to a copy the animation can overwrite.** `setView`, `onWheel`, the startup animation and `renderWebGL` all read the one clamp, so the three pre-S1 paths cannot disagree; both `setView` monkey-patches, the render-time clamp and the dead wheel guard are deleted. | The probe found the cap written three ways (`app.js:221-241`, `601-623`, `651-665`) with the only correct copy re-installed per GPU entry, and `fractalViewer.js:116-121` unreachable because `WEBGL_MIN_SCALE`/`webglCheckbox` are `app.js` module-locals. The animation mutated `viewer.view.scale` directly, so even a correct patch would have been overwritten on the next frame. Pinned, all three paths to the same value, by `tests/zoom-clamp.spec.js`. | Keeping the `viewer.setView` patch and fixing only its re-installation; clamping at render time instead of at view-set time (that leaves `viewer.view` itself lying about the scale it will draw). | Writer, `session-s1-canvas`, 2026-09-21, from the brief's item 4 and the probe's "one clamp, not three". |
| 8 | **S1: a lost or released WebGL context replaces the `<canvas>` element; renderer teardown is idempotent and accounts for itself.** `destroy()` deletes the program, both shaders and the quad buffer, then releases the context via `WEBGL_lose_context`; `window.__fvLiveWebglRenderers` (from the renderer itself) is the observable the suite asserts. | A canvas whose context has been created and then lost never returns a new context from `getContext`, so a GPU→CPU→GPU toggle would silently degrade to CPU forever. The old `destroy()` only nulled `gl`, leaking a program, two shaders, a buffer and a context per toggle. The suite may not INFER the leak from pixels, so the renderer exposes a construction/destruction counter and the pin asserts 0 after each teardown. Pinned by `tests/webgl-fallback.spec.js` (5 toggles, plus a simulated context loss). | Asserting "one context" by counting `getContext` calls or by pixel-diffing (both are inference, not observation); `preventDefault()` alone with no element swap (restoration is async and never arrives in practice). | Writer, `session-s1-canvas`, 2026-09-21, from the brief's item 3 and the "make it observable" pin. |
| 9 | **S1: `window.confirm` at the GPU zoom cap is KEPT (deferred by 10 ms), but every other WebGL failure reports through a new non-modal `#appMessage` element.** The one-shot switch-to-CPU offer stays a modal because the brief scoped only `alert`; it is recorded here as a smell. | The B9 finding was that `alert` + `viewer.render()` left a blank canvas with no CPU calculation; the container's decision (`docs/PLAN.md` S1) was "fall back to the CPU worker path and report through the existing info/status UI rather than `alert`". Moving the zoom-cap offer off `confirm` is a behaviour change S1 does not need, and `deniedCpuSwitchAtZoomCap` already makes the prompt once-only. | Silently dropping the offer (removes a user-visible capability), and keeping the three modal sites while adding a separate non-modal one (the duplication item 4 exists to remove). | Writer, `session-s1-canvas`, 2026-09-21. Flagged for the dispatcher: if the owner wants no modals at all, that is a one-row change in `handleZoomLimitReached`. |
| 10 | **S2: cancellation is `worker.terminate()` + respawn, not job slicing.** `cancelJob()` counts a real cancellation (`cancelledWorkers`), terminates the live worker and constructs a fresh one; every view change routes through it, and `startFractalCalculation` also cancels a superseding job. | A worker inside its synchronous kernel cannot receive `postMessage({type:'abort'})`, so the pre-S2 abort was a no-op (the probe's B6); and a *sliced* worker that yields between chunks is only cancellable at a yield point — it needs message handling and re-entrancy in the kernel, and a cancel still waits for the current slice. Terminate is immediate, unconditional and leaves nothing to reuse. `workerGeneration` (spawns) and `cancelledWorkers` (kills) make both halves countable; the pin asserts the kill counter rises on a cancel during a long job, and an inert `cancelJob` makes it go red. | Slicing + yielding (more kernel surface, cancellable only at yield points); keeping the `abort` post and relying on the `calcToken` alone (that is exactly the pre-S2 no-op). | Writer, `session-s2-worker`, 2026-09-21, from the brief's item 1 and the probe's flow-3 row. |
| 11 | **S2: progressive refinement is APPLIED, not deleted.** The worker refines one buffer 8 → 4 → 2 → 1 and posts a frame for **every** level (earlier levels copy the buffer, the final one transfers it); `app.js` applies `progress` frames exactly as it applies `done`. | The halving existed precisely so a coarse image could appear early, but `app.js` handled only `'done'`, so all three intermediate frames were computed and thrown away — the latency work was paid and the benefit discarded. Applying them makes a slow render converge instead of staying stale until the last pixel, which is the resilience the campaign's bar names. It is *observable*: `appliedFramesForJob(jobId) > 1` (one frame can be final, every further one is intermediate) and `progressFrames()` are counted, so the pin goes red when no non-final frame is posted. | Deleting the halving and going straight to `gridStep 1` (simpler, and it drops the redundant per-level copy, but it throws away the only progressive feedback the app has — a slow render then shows nothing until it is finished). | Writer, `session-s2-worker`, 2026-09-21, from the brief's item 3. |
| 12 | **S2: `worker.onerror` surfaces a message and retires the worker.** It reports through the existing non-modal `#appMessage` (S1) with the worker's message, drops the in-flight job, and `terminate()`s the failed worker so the next job gets a clean one. | Pre-S2 there was no `onerror` at all, so a worker failure was completely silent: the canvas kept its last frame and nothing was reported. The S1 window error net is not a substitute — a worker script error reaches the page as an `ErrorEvent` on the worker, and the pin asserts a sentinel that only the worker handler produces, plus the worker count going to zero (which the window net cannot do). | Auto-restarting the failed job (an unbounded retry loop on a worker that always fails); leaving the worker in place (the next job would post to a worker whose script may not have loaded). | Writer, `session-s2-worker`, 2026-09-21, from the brief's item 2 and the probe's failure-path row. |
| 13 | **S2: a malformed frame is validated before it becomes pixels, and the `calcToken`/generation checks are DEFENCE IN DEPTH, not the net.** `applyWorkerFrame` rejects a payload that is not a whole-`Int32Array` or whose length is not exactly `width × height`, reports through `#appMessage` and ends the job; the net that actually stops a late frame from a cancelled job is clearing `progressiveState` on cancel. | The probe called the bad-payload path an uncaught `Int32Array(undefined)`; measured on V8 that constructor does **not** throw (it yields an empty array), so the real defect was a *silently wrong* frame (an empty or short buffer passes `FractalViewer.render`'s length guard and paints the background). The guards were measured, not assumed: with both `calcToken` and generation checks deleted the cancel pin still passed, so the state clear is what protects; the checks stay because they also catch a frame that raced in before `terminate()` took effect, and removing them is not worth the risk. `terminate()` itself was measured to be **asynchronous with respect to a synchronous kernel** — the level already running still finishes (CPU 1.04 s with terminate vs 1.02 s without) — so "cancel stops the CPU immediately" is NOT claimed and is not pinned. | Inferring a leak from pixels; asserting the cancel stop by CPU time (it does not distinguish, measured); shipping without the length check and calling an empty buffer "handled". | Writer, `session-s2-worker`, 2026-09-21, from the brief's item 4 and the writer's own control runs. |
| 14 | **S3: Option A — ONE kernel file with no `import` and no `export`, publishing `globalThis.FractalKernel`, so the classic worker (`importScripts`) and the ES-module main thread load the same source.** `public/fractalEngine.js` and `public/fractalEngineMain.js` are deleted; the worker's inline override is deleted; `fractalWorker.js` only calls `self.FractalKernel.calcFractalChunk`, and `app.js` / `fractalViewer.js` / `webglFractal.js` / `colorSchemes.js` load the kernel with a side-effect `import`. | The worker is CLASSIC — `app.js` does `new Worker('fractalWorker.js')` and `fractalWorker.js:1` does `importScripts(...)` — while the main thread is ES-modules-only. A file with no import/export statements is simultaneously a valid classic script and a valid ES module, so one source serves both with **no build step and no change to worker loading**; the compile tier parses the file as both `.cjs` and `.mjs`, and the 25-test suite renders through both loaders (pins 1–3). | **Option B** (`new Worker(url, { type: 'module' })` + a normal ESM kernel). Cleaner in the abstract, but it changes worker loading and still cannot serve the classic path; Option A proved adequate in the browser, so B was not taken. Also rejected: keeping a copy of the kernel for the worker (the pre-S3 duplication itself). | Writer, `session-s3-kernel`, 2026-09-21, from the brief's "Design decision". |
| 15 | **S3: the ONE iteration cap is `MAX_ITER = 2000` in `public/fractalKernel.js`.** The slider's `max`, the kernel's clamps (worker chunk and 3D heightmap), the fragment shader's loop bound and the `u_maxIter` uniform clamp all derive from it: `index.html`'s hardcoded `max="2000"` is deleted (app.js sets the attribute from the constant) and the four `for (int i = 0; i < 1024; i++)` literals become `i < MAX_ITER`, templated in by `webglFractal.js` as `#define MAX_ITER <cap>` from the kernel. | B5: `index.html:33` offered up to 2000 while the shader looped to a hardcoded 1024, so every pixel needing >1024 iterations was coloured as escaped (GPU and CPU silently disagreed). GLSL ES 1.00 requires a **constant expression** as a loop bound, so a uniform cannot bound the loop — the source must be generated. 2000 (not 1024) preserves the iteration depth the UI already advertised and the CPU always honoured. | Lowering the cap to 1024 (an equally valid fix, but it deletes CPU capability the app already had); a uniform loop bound (illegal in GLSL ES 1.00); clamping per renderer only (leaves the UI advertising more than the GPU honours — the exact B5 symptom); pinning `cap > 1024` in the suite (it would freeze this slice's choice and fail the alternative fix). | Writer, `session-s3-kernel`, 2026-09-21, from the brief's "one constant" mandate and the measured control arms. |
| 16 | **S3: ONE type/palette table, in the kernel.** `FRACTAL_TYPES` and `COLOR_SCHEMES` (value, label, shader index, GLSL identifier, palette fn) are the only such lists: app.js populates both `<select>`s from them, the GPU type/palette indices come from `indexForType`/`indexForColorScheme`, the 2D LUT and the 3D heightmap palette come from the same table, and `webglFractal.js` templates `#define FT_*` / `#define CS_*` from it so a GLSL branch cannot drift from the map. The palette implementations move into the kernel; `public/colorSchemes.js` becomes a two-line ES-module re-export for its existing importers. | The audit's "type/palette index maps are duplicated too": `app.js`'s `typeMap` and `colorSchemeMap`, the shader's integer `== 1/2/3` branches, and the `<option>` lists in `index.html` were four to five restatements of the same two ordered lists — exactly the kind that drifts silently when a type or palette is added. Pinned per type and palette by `tests/kernel-parity.spec.js` pin 3. | Leaving the `<option>` markup and the two app.js maps as constants and unifying only the iteration math; keeping the palette functions in `colorSchemes.js` while the kernel holds only the index table (two tables whose ORDER must agree, which is the drift this row removes). | Writer, `session-s3-kernel`, 2026-09-21. |
| 17 | **S3 pins: compare CPU and GPU through a sampled grid with stated thresholds, and observe the shader template rather than infer it.** Pin 1 renders at maxIter = the cap in a deliberately interior-heavy view and requires mean per-channel \|CPU−GPU\| < 6 and >48-off fraction < 0.06 (measured correct 0.911 / 0.0105; with the four loop bounds hardcoded to 1024 the mean \|Δ\| is 82.56, i.e. >13× the threshold). Pin 2 reads the slider's `max`, `__fv.maxIterCap`, the value templated into `#define MAX_ITER` and the bound token of all four loop sites (`['MAX_ITER', …]`, length 4). Pin 3 samples 32×32 pixels per type and palette and asserts inside/outside disagreement < 0.06 and mean \|Δ\| < 20 (correct worst 0.011 / 8.1; a drifted type index gives 0.111; a drifted palette index gives 0.81–0.89 xor or \|Δ\| 39.5–97.0). | The residual CPU/GPU difference is real and is not a defect: the CPU kernel samples pixel **corners** while a fragment shader samples fragment **centers**, so a thin band along the set boundary flips. An exact-equality pin would be red on every correct build; stating the measured residual and the margin makes the pin honest. The shader's bound is an artifact, so it is READ from the generated source (the four loop tokens + the `#define` value exposed on the renderer) instead of inferred from pixels — a `#define` of 4096 with the uniform still clamped to 2000 renders identically to correct code, yet pin 2 must (and does) go red on it. | Pixel-exact equality; screenshot/eyeball agreement; inferring the shader bound from pixels; a single "looks similar" threshold without separating type drift (shape) from palette drift (colour). | Writer, `session-s3-kernel`, 2026-09-21; thresholds and all five control arms recorded in the landing report. |
| 18 | **S4: the imported record schema lives in `public/memoryRepository.js` as `validateLocation`/`validateRecords`, and reuses the kernel's type table.** Required per record: `id` a non-empty string ≤ 128 chars; `name` a non-empty string ≤ 200 chars; `scale` a finite number > 0; `centerX`/`centerY` finite numbers; `fractalType` ∈ `FractalKernel.FRACTAL_TYPES`; `maxIter` an integer in [`index.html`'s slider `min` = 50, `FractalKernel.MAX_ITER` = 2000]; `renderer` ∈ {`GPU`,`CPU`}; `timestamp` a finite number. The app's own Save path now generates a **string** id (`loc-<base36 time>-<counter>`; the old `Date.now() + Math.random()` was a number and would not have round-tripped through its own export). | B7 named only `name`, but the pre-S4 sink at `app.js:180` interpolated FIVE fields (`name`, the formatted `timestamp`, `fractalType`, `maxIter`, `renderer`) and all five are attacker-controlled in an imported file. The type list is not restated here: `FractalKernel.FRACTAL_TYPES` is the ONE table (S3 row 16), so the schema cannot drift from what the renderers will actually accept. The checks are strict (`typeof` and `Number.isInteger`, no coercion): `'3'` is not a number, `256.5` is not an integer, and `0` is not a positive scale — a value the app cannot faithfully render is refused rather than silently coerced into one it can. Pinned by `tests/import-validation.spec.js` (per-field rejection with its own reason); the dispatcher turned it RED by deleting the schema consultation, and by deleting the load-path dedup for the counted pin. | Accepting the shapes `memoryRepository.save()` used to coerce (truthy name, any timestamp, numeric-string id) — that is the "render `NaN`/`undefined` instead of an error" behaviour B7 is about; validating in `app.js` and leaving the store unvalidated (the store is the boundary every future caller will use); restating the fractal-type list in the schema. | Writer, `session-s4-input`, 2026-09-21, from the brief's item 2 and the five-field sink the dispatcher measured. |
| 19 | **S4: a per-record defect is rejected PER RECORD and the file's valid part still loads; a file-level defect (too many records, or a non-array document) is rejected WHOLE.** The user sees one `#appMessage` line: `Imported N location(s); M record(s) were rejected by the import schema. First problem — record <i>: <reason>.` | The two failure kinds are different things. "One record is malformed" is local: refusing the other 20 locations a user saved because line 7 is truncated would destroy more data than the hostile input ever could, and the schema makes the accepted part safe by construction (it is normalized to exactly the nine fields). "This file has 10,000 records" or "this is not an array" is a property of the FILE, so there is no trustworthy "valid part" to keep — taking the first 200 records of a file we have just decided not to trust would be the worst of both policies. The message names the first problem so a rejection is actionable rather than silent. Pinned by the mixed-validity pin (1 accepted / 15 rejected in one file, and the accepted one loads) and the bounds pin (201 records → nothing stored). | All-or-nothing per record defect (data loss); partial acceptance for oversized files (contradicts the reason the bound exists); a modal/`alert` for rejects (the brief routes every user-facing failure through the S1 `#appMessage`). | Writer, `session-s4-input`, 2026-09-21, from the brief's item 2 ("state your judgement"). |
| 20 | **S4: the import is bounded at three points — 200 records per file, 1 MiB of raw text, 200 chars per name / 128 per id.** `LOCATION_LIMITS` in `public/memoryRepository.js` is the single declaration; `app.js` checks the raw text length **before** `JSON.parse` and again on `File.size` before `FileReader`, and `validateRecords` checks the array length before validating any record. | "A hostile file cannot wedge the UI" needs the work bounded before it is done, not after: parsing is the expensive step, so the byte bound is checked first, and record validation is O(n) so the count bound precedes it. The numbers are deliberately the app's own scale (the store is session-only; a location is nine scalar fields) rather than tight against today's usage, so a legitimate export of a heavy session still round-trips. Pinned by the bounds pin, which the dispatcher turned RED by deleting all three checks — with them gone the 201-record file is accepted and the over-limit payload reaches `JSON.parse`. | An unbounded import with per-record validation (validation is O(n), so it bounds correctness but not cost); counting characters rather than bytes (fine for the ASCII this app writes, and the `File.size` check is the authoritative one). | Writer, `session-s4-input`, 2026-09-21, from the brief's item 4. |
| 21 | **S5: the mesh work is SPLIT — `evaluateHeightmap()` owns the ONE kernel call and counts it; `applyColors()` rewrites only the geometry's `color` attribute from the CACHED heightmap and height range.** `setColorOffset` and `setColorScheme` call `applyColors()`; `regenerateMesh()` builds positions from the fresh heightmap and then calls the same `applyColors()`, so a rebuild and a colour tick share ONE palette implementation. The colour buffer is reused in place (`attr.needsUpdate = true`) rather than replacing the attribute. | B8: `setColorOffset` → `regenerateMesh()` → `calculateHeightmap` meant a full `resolution²` fractal evaluation *per animation frame* whenever 3D mode was active with colour cycling on. Caching `this.heightmap`/`this.heightMin`/`this.heightMax` makes a colour tick O(n) palette lookups with zero kernel calls. Replacing the `color` attribute object each tick (what the old code did as a side effect of rebuilding) orphans its GL buffer, so the live buffer is overwritten instead. Pinned by `tests/3d-truth.spec.js`: the counted evaluation total does not advance across real colour-cycle ticks, and the resulting colours match the kernel palette for the geometry's own heights. | A palette **shader uniform** for 3D (correct in principle, but it rewrites the `MeshStandardMaterial`/GLSL and is a bigger surface than this slice needs — recorded as the alternative the brief allowed); recreating the geometry/material per colour tick (same fractal cost as the bug, plus a leak); caching colours rather than heights (would not track a resolution rebuild). | Writer, `session-s5-3d`, 2026-09-21, from the brief's item 1 and B8 (`MODERNIZATION.md` §2). |
| 22 | **S5: `enter3DMode()` is `async` and AWAITS `Fractal3DViewer.init()`; a failure tears the viewer down, restores the pre-3D UI through the normal exit path (`restore2DFrom3D`), and reports through `#appMessage`.** The 2D canvases are hidden only AFTER a successful init, and a `threeDGeneration` counter invalidates an init that is still in flight if the user leaves 3D mode. `Fractal3DViewer.exit()` now also `dispose()`s the three.js renderer and hides the stuck progress bar. | `init()` is async but three.js constructs its `WebGLRenderer` synchronously inside it, throwing `Error creating WebGL context.` when no context is available. Pre-S5 `app.js:489` called `init()` with no `await` and no `catch` *after* hiding both 2D canvases, so the page black-screened with an unhandled rejection and no message (control B reproduced exactly that: the only text was the global net's "Unhandled promise rejection: Error creating WebGL context."). Reuses the S1 `#appMessage` surface rather than adding a second failure UI. Pinned by `tests/webgl-fallback.spec.js:228` using the S1 WebGL-denial helper (shared via `tests/helpers/webgl.js`, not copied). | Leaving `init()` unawaited with a bare `.catch(showError)` (the canvases would still be hidden, so there is no restore); reporting the failure but making the user press Space again; an overlay on top of the black canvas instead of restoring the 2D UI; a second WebGL-denial helper in the new spec. | Writer, `session-s5-3d`, 2026-09-21, from the brief's item 2 and the probe's 3D-init failure row. |
| 23 | **S5 pins: count the evaluations and the colour-cycle ticks on the observation surface, and check the colour attribute against the palette derived from the geometry's OWN positions (stated tolerance 1/255).** New `window.__fv` members `heightmapCalls`, `colorCycleTicks`, `threeDReady`, `colorOffset3D`, `terrainHeightRange`, `sample3DTerrain`, plus `set3DColorOffset`/`set3DColorScheme` that drive the real setters (as `forceFallback`/`importPayload` drive theirs). | "Zero evaluations" is invisible in pixels and ambiguous in frame time, so the viewer counts its own kernel calls (`fractal3d.js:131`) and the pin ALSO requires the real cycle to have ticked and moved the 3D offset — a stopped loop would otherwise make the pin pass vacuously. The palette pin derives its expectation from `position.getZ` (what is actually rendered), not from the cached `heightmap`, so a rewrite that used stale heights or a stale range is caught; 1/255 ≈ 0.0039 covers float32 storage only and is ~4 orders of magnitude above the measured residual. Control C (repaint dropped) turns the palette pin RED while the evaluation pin stays GREEN, so the two pins are independent. | Inferring the guarantee from frame time or pixel stability; asserting only that the counter is unchanged (a stopped cycle passes); restating the palette formula in the test instead of reading `FractalKernel.paletteFunction`; one combined pin that cannot separate "did not recompute" from "did not repaint". | Writer, `session-s5-3d`, 2026-09-21; all three control arms recorded in the landing report. |
| 24 | **S5 fix-forward: add an UPLOAD pin — `window.__fv.colorAttributeVersion()` must advance across an offset tick and a scheme change — because the value pin alone could not see a missing `attr.needsUpdate = true;`.** The version is three.js `BufferAttribute.version` (`three.module.js:1813`), incremented only by the `needsUpdate = true` setter (`:1817`) and used by `WebGLAttributes.update` to decide a re-upload (`:13525`,`:13548`). The new pin is deterministic (a counter, not pixels) and the value pin is kept unchanged, so the tick is pinned end to end: values correct AND values sent. | The dispatcher's probe deleted `fractal3d.js:168` `attr.needsUpdate = true;`. `applyColors()` still wrote every vertex colour into the geometry's `color` ARRAY, so the value pin (which reads that array through `sample3DTerrain`) passed, as did the zero-evaluation pin — while a user would see a frozen mesh and Cycle Colors would do nothing. This is the same class as the S1 pin that called the context-loss handler directly and missed the wiring: the assertion covered the data, not the effect. The exact same class of defect is what the campaign's bar calls "silently wrong"; the CPU array looked right. Control D (below) reproduces the dispatcher's probe: with `needsUpdate` removed the NEW pin goes RED and the value pin stays GREEN, so the two pins are demonstrably independent. | Inferring the upload from pixels (no deterministic threshold; the version counter is the very mechanism three.js uses); deleting or weakening the value pin (it is correct for values and catches a different defect); asserting only that the version is non-zero (the initial upload would satisfy that even if every later tick skipped `needsUpdate`). | Writer, `session-s5-3d`, 2026-09-21, in response to the dispatcher's probe log `/tmp/fv-armS5-palette.log` — the gap was found, not predicted. |
| 25 | **S6: BOTH dialogs are replaced by in-page non-modal surfaces — `window.prompt` (save/naming) by `#saveLocationPanel`, `window.confirm` (the GPU zoom-cap offer) by `#zoomCapOffer`.** The naming flow keeps the same contract: the field is prefilled with the SAME generated default the prompt offered, Save (or Enter) commits through the ONE `commitNamedLocation()`, Cancel (or Escape) stores nothing — the old `prompt === null` branch. The cap offer is shown at most once, synchronously, and the cap itself still lives only in `FractalViewer.clampScale()`, so it is applied by the CLAMP on every path (wheel, startup animation, render) whatever the user answers — or does not answer. "Switch to CPU" unchecks the WebGL checkbox and fires its ONE `change` handler (the real CPU path); "Stay on GPU" sets the denial flag. "Once" stays observable: `fv-zoom-limit`'s `prompted` is still exactly "the user was actually asked", and `__fv.zoomCapPrompted()`/`zoomCapOffered()` expose the ask as a DOM fact. | This is the smell `docs/DECISIONS.md` row 9 deliberately carried out of S1 ("if the owner wants no modals at all, that is a one-row change in `handleZoomLimitReached`"); S6 is that row. A modal blocks the page, cannot be styled or dismissed by the app, and is the class the campaign's RESILIENT bar names. Both retired sites were re-located rather than trusted (`app.js:88`, `app.js:394` at `5537ca7`). Control B (restore `window.prompt` in the save path) turns the new dialog pin RED with a recorded `"Name this location (optional):"` prompt; control D (`setZoomLimit(null, …)`, the cap disabled completely) turns the cap-application assertion RED (`1e-9` returned against a `1e-4` cap). | Keeping `window.confirm` (the smell row 9 flagged for exactly this slice); silently dropping the offer (removes a user-visible capability); a custom HTML `<dialog>` (ARIA/focus/touch work is explicitly out of this campaign, per `docs/PLAN.md` §3); ANY behaviour change to the clamp, so the S1 zoom-clamp pins and `tests/smoke.spec.js`'s modal assertions stay green unchanged. | Writer, `session-s6-hygiene`, 2026-09-21, from the brief's item 2 and row 9. |
| 26 | **S6: the four surviving `[FractalMouse]` `console.log`s are DELETED and replaced by NOTHING; the log is pinned by a console-URL filter, not by counting only `[FractalMouse]` text.** Every console method is wrapped in an `addInitScript` BEFORE app.js runs, and each call records the URL of the script that made it (`Error().stack`); the pin asserts no recorded call came from a URL containing `app.js`. The four dead state declarations (`currentResult`, `aborting`, `debounceTimer`, `lastJobParams`) and the four dead files are deleted in the same landing. | These logs sit on the hottest paths — every `mousedown`, every `wheel` tick, every view-change render — so they are per-event work on the interaction loop, and the `MutationObserver` that existed only to log was already deleted by S1 (S6 confirms its absence; it did not remove it). Replacement is not wanted: a hot-path log is exactly what the slice removes. The filter is stated because a text-based filter would be satisfied by renaming the tag, and because a pin that only counted calls could be satisfied by a stopped app — so the test ALSO requires the app to have run (a completed render and a settled startup animation). Control A (re-adding ONE `console.log` to `onMouseDown`) turns the pin RED with 9 recorded app-origin calls; restoring `window.prompt` (control B) turns the dialog pin RED with a recorded `prompt` call. | Counting `[FractalMouse]`-prefixed messages only (a renamed log would pass); asserting a call-count of zero without proving the interaction ran (a stopped app would pass); filtering out `console.error`/`console.warn` to keep the pin easy (that is the filter bending to the code); excusing three.js's own `console.warn`s by adding them to the filter (the pin is scoped to the app's own source and says so). | Writer, `session-s6-hygiene`, 2026-09-21, from the brief's item 1 and the measured controls. |
| 27 | **D1: colour is a CONTINUOUS function of the escape value, defined ONCE in `public/fractalKernel.js` and evaluated with the identical expression by the kernel and by the GLSL. The value is `v = n + 1 − log2( log(\|z\|²) · (1/ln(BAILOUT_SQ)) )`, where `n` is the 1-based index of the update whose result exceeded the bailout and `\|z\|²` is that result; a point that never escaped returns EXACTLY `maxIter` (inside ⇒ black).** The iterated buffer is a **`Float32Array` with `NaN` as the "not yet calculated" sentinel** (`new Float32Array(w*h).fill(NaN)`, skip test `value === value`, payload validated as a 4-byte-aligned `Float32Array` payload of exactly `width×height`); `-1` in an `Int32Array` cannot represent the value. Colour is produced from a table over the **quantised continuous value** at `COLORS_LUT_STRIDE = 256` entries per iteration — a FIXED resolution, independent of `maxIter` — with an inside entry at exactly `maxIter` (black) and one placeholder entry (`40,40,40`) for `NaN`; colour cycling (`u_colorOffset`/`setColorOffset`) applies to the continuous value. | The staircase was the amplifier the owner asked to remove: with one colour per INTEGER iteration, a precision change of any size can push a pixel across a step and flip it a whole band, and those pixels cluster at the boundary — which is what is on screen while zooming. Measured on the correct build: at `maxIter` 50 a hop of 0.01 iterations (1/100 of a band) moves the colour by 1/255 in every palette and at most 0.25 of a band; the boundary-crossing zoom sweep shows 0 hopping pixels at all 14 steps from scale 0.05 to the cap's 1e-4, against 2–21% of escaped pixels per step with the integer staircase (control A). A `maxIter`-proportional table was rejected because at `maxIter` 2000 it has 2000 entries — it cannot resolve neighbouring escape values at all, which is exactly the resolution this slice needs; direct per-pixel palette evaluation was rejected because it allocates per pixel in the inner loop, while the table keeps the pixel loop a lookup. Measured table-vs-exact residual over all five palettes: 1 RGB unit (the display cannot show less). The `NaN` and placeholder choices are pinned so a finished frame carries ZERO uncalculated cells and a cell without a value can never be painted as a real one (`tests/smooth-color.spec.js` pins 3 and 4). | Keeping the integer LUT and "smoothing" only the shader (one renderer stays a staircase — arm D, which the S3 parity pins do NOT catch at cap 2000); a `maxIter`-proportional fine table (see above); `-1` in a `Float32Array` (collides with a legitimate value near −1) or a separate `Uint8Array` mask (a second buffer and a second place for the two to disagree); painting `NaN` as black (indistinguishable from set interior) or as a palette colour (indistinguishable from a rendered value); keeping a per-integer colour and interpolating in RGB (it is not the palette's definition, and it breaks GPU/CPU agreement). | Writer, `session-d1-smooth-color`, 2026-09-21, from the owner's verbatim requirement ("C but with special care around precision hops … the zoom needs to stay visually as smooth as possible") and the dispatcher's slice D1 brief. |
| 28 | **D1: the two renderers must agree on `n`, and that is a measured property, not a convention to be assumed. The kernel's loop tests at the TOP of an iteration, so the count it returns already INCLUDES the escaping update; the shader breaks BEFORE its own `iter++`, so its count EXCLUDES it. The kernel therefore passes `iter` and the GLSL uses `float(iter) + 1.0`.** The GPU is pinned twice: (a) the GENERATED fragment source is read back and must contain the kernel's exact expression and the templated constants (`SMOOTH_LOG_BAILOUT`, `BAILOUT_SQ` — emitted as GLSL float literals because a bare `4` is an int/float type error that fails the fragment compile and silently falls back to the CPU), and (b) at a cap where one iteration is a visible band (`maxIter` 50) each GPU fragment is compared with the KERNEL's colour at that fragment's own centre coordinate. | The first implementation used `iter + 1` on both sides. Measured against a faithful GLSL emulation it differed from the shader by exactly 1.0 iteration at **all 14932** sampled escaped points, and the browser comparison showed a whole 21-unit band per pixel at `maxIter` 50 — while the S3 parity pins stayed GREEN, because at `maxIter` 2000 one iteration is 0.13 of an RGB unit (arm D, measured). So the S3 thresholds cannot see a one-iteration GPU-side colour difference at all; that is why the check is both axial (source) and numerical at a low cap. With the conventions aligned, the coordinate-matched check measures mean \|Δ\| **0.354** (worst 1) at cap 50; control E (the `u_maxIter` uniform off by one, source unchanged) turns it RED on the inside-agreement assertion. The comparison is made at the fragment's OWN centre because the CPU kernel samples pixel corners and the shader fragment centres — at low iteration counts that half-pixel offset alone is worth an iteration, and a corner-vs-centre pixel comparison at cap 50 measures mean 34 units even on a correct build (that pre-existing sampling difference is NOT part of this slice). | Comparing CPU-canvas pixels with GPU-canvas pixels at a low cap (it measures the sampling convention, not the formula — measured mean 34 on a correct build); inferring the shader from pixels at cap 2000 (a one-iteration error is 0.13 units there); trusting a comment about iteration conventions instead of measuring them (that is what produced the off-by-one); a shared `n` constant in a comment rather than the templated expression (the constants are templated; the expression is checked as source text). | Writer, `session-d1-smooth-color`, 2026-09-21, from the same slice; the off-by-one was found by the writer's own low-cap parity measurement before landing, not by a reviewer. |
| 29 | **D1 fix-forward (dispatcher's probe): `applyWorkerFrame` passes `progress.jobParams.maxIter` to `viewer.setData`, and a pin now holds it — a finished frame is indexed at the cap ITS OWN JOB ran at, never at whatever `viewer.maxIter` holds when the frame lands.** The reachable mismatch is: **a CPU job in flight while the viewer's cap changes, on the GPU renderer** — `applyLoadedRecord` sets the slider, `viewer.setMaxIter` and the record's renderer, then calls `renderWebGL()` (not `startFractalCalculationWithTiming()`), so on the default GPU renderer a **Load of a schema-valid location whose `maxIter` differs from the slider does NOT cancel the running job**, and that job's frame arrives afterwards. The pin builds exactly that through the real paths — the slider's own `input` handler starts a job at cap 50, a schema-valid record differing only in `maxIter` (2000) is imported through `__fv.importDocument` and loaded through `__fv.loadLocation` in the SAME synchronous page task (so no worker frame can interleave), the load takes the GPU branch, and the job's final frame lands afterwards. It then asserts: the mismatch was real (a job was in flight, jobCap 50, viewer cap 2000 after the load); the finished frame carries **no uncalculated/NaN cells**; the frame was indexed at jobCap; and every cell the JOB called inside renders **black** when the stored frame is painted once through the app's ORIGINAL 2D renderer (`__fv.paintCpuFrame`, the same function CPU mode calls). | The dispatcher reverted `public/app.js` to `viewer.setData(smoothResult, viewer.maxIter)` (log `/tmp/fv-armD1.log`) and **all five D1 pins stayed green**, so the invariant was unguarded and a P1 refactor of this buffer/cap plumbing could reintroduce it silently. Measured with the revert: the frame is indexed at cap 2000 instead of 50, all **3989** cells the job called inside paint as the palette colour **[255,26,0]** instead of black, and **nan stays 0** — i.e. the "no uncalculated cells" half alone does NOT catch it; the inside-at-the-job's-cap half is the discriminator. With the fix: capUsed 50, interior 3989, black 3989, notBlack 0, nan 0. The pin was shown RED on the revert (`Expected: 50, Received: 2000`; writer log `/tmp/d1-round2/spec-reverted.log`) while the other five pins stayed GREEN, and the reverted file was restored byte-identically (`app.js` sha256 `b724460918225a44aec1aa73c40da607365cc3a536fe92dd0c199c68c69e0770`, verified before AND after). | Relying on the S2 cancel/generation nets to make the mismatch unreachable (it is reachable: the GPU branch of the load path deliberately does not cancel); pinning only the cap without the painted inside-black half (it would assert the plumbing rather than the effect — both are asserted together); constructing the mismatch by writing `viewer.setMaxIter` directly from the test (that is a hook, not a user path); relying on the "no uncalculated cells" half alone (measured: it stays green under the revert). | Dispatcher's verification probe of D1, `session-f6d26a74`, 2026-09-21 (log `/tmp/fv-armD1.log`), executed as a fix-forward by the D1 writer on top of `8e752d8`. |
| 30 | **D2: the iteration budget scales with the zoom, and the user's slider value is a FLOOR. `effective = max(clampMaxIter(sliderValue), iterBudgetForScale(scale))`, where `iterBudgetForScale(scale)` is `MIN_ITER` (no floor) at and above the scale of the shipped GPU zoom cap and `clampMaxIter(ceil(ITER_BUDGET_PER_DECADE * log10(1/scale)))` strictly below it, with `ITER_BUDGET_PER_DECADE = 512` and `ITER_BUDGET_MIN_SCALE = 1e-4`.** The effective budget, the floor and the pure rule are observable at `window.__fv` (`maxIter`, `maxIterFloor`, `iterBudget`, `iterBudgetForScale`), and the `#maxIterValue` readout prints `"<effective> (auto)"` whenever the floor raised it, so the UI never silently shows a number the job is not using. | PROBE item 7: the probe's view centre escapes only at iteration 3086 (float64, stable across budgets 2e4-2e6), so at the shipped 512 the centre is "inside" and a 1e8 frame is 100% black; the probe needed 4096 at 1e8. Measured with this kernel at 200x150 on the probe's centre: scale 1e-8 is **100.0% inside at cap 512** and **0.0% inside at cap 4096**; 1e6/512 is 90.9% inside. The SHAPE is the measured scaling of the escape count (n ~ log2(1/d) for the quadratic map), i.e. linear in the LOGARITHM of the zoom, not in the zoom; one decade of zoom is 3.32 doublings of the boundary distance, and 512/decade is 154/doubling, which covers the measured 3086 with the budget the probe actually used — anchors 1e5 -> 2560, 1e6 -> 3072, 1e7 -> 3584, 1e8 -> 4096, 1e15 -> 7680. The knee at the shipped cap's own scale is what keeps D1 green: D1's band pins deliberately hold a cap of 50 at zooms up to exactly 1e4 to make one iteration a visible colour band, and a floor over that range would override the explicit user budget (a global log rule gives 666 at zoom 20, where one iteration is 0.38 RGB units, so D1 pin 1's "one iteration must be a resolvable band >= 3 units" cannot hold). The knee is not a silent duplicate of the app's zoom cap: pin 2 holds `__fv.iterBudgetMinScale` EXACTLY equal to `__fv.minScale`, so the slice that lifts the cap (P4) is forced to revisit the rule. | A global log rule with no knee (it overrides D1's cap-50 band at zoom 20-1e4 and turns D1 pins 1-3 red — measured arithmetic: 666 at zoom 20); a doubling-per-decade rule (exponential in log10, 2-4x the probe's measured budget at 1e8, paying 2-4x table and iteration cost for no measured benefit); a rule tuned to be under 50 at 1e4 yet 4096 at 1e8 (a steep polynomial in log10, not the measured logarithmic scaling, and erratic near the knee); letting the auto budget REPLACE the slider instead of flooring it (a user's explicit cap would be silently ignored, and D1 pin 6's contract — a finished frame indexed at the cap its own job ran at — would be violated, since that pin deliberately holds a job cap of 50 at a shallow view); making the slider a CEILING (then deep zoom could never raise the budget at all, which is the whole slice). | Writer, `session-d2-iter-budget`, 2026-09-21, from the brief's item 1 and PROBE item 7. |
| 31 | **D2: the ONE iteration cap `MAX_ITER` is raised 2000 -> 8192 (`public/fractalKernel.js`). Nothing else restates it: the slider's `max` is still derived at `app.js:64`, the kernel clamps the worker chunk and the 3D heightmap to it, and `webglFractal.js` still templates `#define MAX_ITER` and its four loop bounds from it (S3 pin 2 stays green).** | The rule reaches 4096 at zoom 1e8 and 7680 at zoom 1e15 — the practical reach of a float64 reference orbit (P6) — so 8192 is the smallest power of two that lets the RULE, not the cap, decide over that whole reachable range; the rule clamps at 8192 only past zoom 1e16, beyond where the orbit itself has precision. Measured: a fragment shader recompiled with `#define MAX_ITER` at 8192 (and at 4096/16384/32768) compiles AND links under ANGLE/SwiftShader on this host with compile time below the measurement resolution, so the raised bound costs no shader-compile budget (SwiftShader software proxy; a real GPU is not established). Raising the cap does NOT raise any frame's cost: a job runs at its own scaled budget, the shader loop breaks at its `u_maxIter`, and the colour table is sized by that job's cap (row 32). | 16384 or 32768 (no reachable view needs them; they double the worst-case table and shader work for zoom past the float64 orbit); 4096 (the rule would be truncated at 1e8, leaving nothing for 1e9-1e15 even though the probe's own trend shows the needed budget keeps rising); keeping 2000 and raising only a separate "slider max" (that is two constants and is exactly the B5 defect S3 removed); a cap so large it can never bind (the table's worst case becomes unbounded for no reachable benefit). | Writer, `session-d2-iter-budget`, 2026-09-21, from the brief's item 2 and the measured rule anchors. |
| 32 | **D2: the 2D colour table is SIZED BY THE CAP THE FRAME'S OWN JOB RAN AT and BUILT ONCE per key. `FractalKernel.colorTableSize(maxIter) = clampMaxIter(maxIter) * COLORS_LUT_STRIDE + 1` replaces D1's `COLORS_LUT_SIZE = MAX_ITER * COLORS_LUT_STRIDE + 1` (a module-maximum constant that nothing allocated), and `FractalViewer.buildColorTable` caches the `Uint32Array` on `(colorScheme, colorOffset, maxIter)`; `render` reuses one `ImageData` and its `Uint32Array` view while the backing-store size holds. The counters `colorTableBuilds`, `colorImageDataBuilds` and `colorTableEntries` are on `window.__fv`.** | MODERNIZATION.md's still-open per-frame-allocation finding, which the D2 cap raise makes bite: sized by the module maximum (8192) EVERY frame would allocate 2 097 155 entries (~8.0 MB) whatever cap it ran at, where a 512-cap frame needs 131 075 entries (0.5 MB) and the rule's 4096-cap deep frame needs 1 048 579 (4.0 MB). D1's own report measured ~2 MB per frame at cap 2000 with the table (re)built on every render; during a progressive job that is one build per frame of the 8/4/2/1 refinement. Measured in `tests/iter-budget.spec.js` pin 3 through the app's own counters: five consecutive renders at the same key build **0** tables and **0** ImageData; a palette change builds exactly **1** (so the counter is live and the cache cannot pass by never building); a frame at cap 512 has 131 075 entries, not 2 097 155; and the deep frame at cap 4096 has 1 048 579 entries (4.0 MB), not 2 097 155 (8.0 MB). | Reducing `COLORS_LUT_STRIDE` at high caps to save memory (the table-vs-exact residual D1 pinned at <= 1 RGB unit is a half-stride palette step, so a smaller stride grows it and breaks D1 pin 1); evaluating the palette per pixel instead of through a table (D1 rejected it at cap 2000 for allocation inside the inner loop, and the reason has not changed — the table keeps the pixel loop a lookup and the residual is unchanged); sizing by the module max and caching only (still 8.0 MB for a 512-cap frame); caching the `ImageData` across a resize (it must match the backing store, so it is rebuilt on a size change); dropping the inside/placeholder entries to shrink the table (they are what keep a finished frame black and an uncalculated cell unpaintable, D1 pins 3-4). | Writer, `session-d2-iter-budget`, 2026-09-21, from the brief's colour-table item and MODERNIZATION.md's per-frame-allocation finding. |
| 33 | **P1: the per-pixel delta is REBASED by the reference's own rule — when `|Z_m + z_n| < |z_n|`, `z_n <- Z_m + z_n` and the reference iteration resets to `m = 0` — and that is the mechanism that buys the depth. The comparison is made on the same absorbed `x*x + y*y` the escape test uses; the expanded magnitude `|Z|^2 + 2 Re(Z conj z) + |z|^2` was tried as the rebase test and measured far WORSE (mean per-channel |ΔRGB| 47.5 and a 15000-px blob against 0.45 and 3), so the absorbed form is the one shipped.** | The committed naive probe (`docs/PROBE-2026-09-21-perturbation.md`) had no rebasing and was structurally wrong at 1e15: 26.4 % of pixels misclassified, 74.6 % glitched, 6 distinct colours against the reference's 53. Re-running that probe with rebasing added, and then the SHIPPED shader, measures the jump. Shipped shader at 200×150, centre `-0.743643887037151 + 0.13182590420533i`, scale 1e-15, budget 7680, against a float64 PERTURBATION reference computed from the app's own orbit: **misclassification 0.00000 %, mean per-channel |ΔRGB| 0.453, largest 4-connected blob of pixels differing by >32 = 3 px, 136 distinct colours — exactly the reference's 136**; rebases actually occur (163 651 over 30 000 pixels). The control (rebase block deleted, file restored byte-identically: `webglFractal.js` sha256 `86b104c78d1e9ae15cc2696bb6fbefa88ef327b2ea9d4e9d4fc8c6765b04cc5a` before and after) measures mean |ΔRGB| **3.182**, a **90-px** blob and **6** distinct colours, and turns `tests/perturb-core.spec.js` pin 1 RED on the mean-|Δ| assertion. | Rejecting the absorbed rebase comparison in favour of the "more accurate" expanded one (measured 100× worse — the rebase must re-anchor on the same quantity the arithmetic actually carries); rebasing on a schedule instead of on the rule (the rule is what the sources give and what the probe's improvement is attributable to); treating the naive 26 % collapse as a precision ceiling rather than a missing mechanism (`docs/RESEARCH-2026-09-21-arbitrary-depth.md` §3). | Writer, `session-p1-perturb-core`, 2026-09-21, from the brief's item 4 and the writer's own probe (`/tmp/p1-probe/probe.html`) and control runs. |
| 34 | **P1: RESCALING is implemented as the sources describe — `z = S·w`, `dc = S·d`, `w -> 2 Z_m w + S w^2 + d`, with `S` renormalised to a power of two near `|z|` every `PERTURB_RESCALE_INTERVAL = 256` iterations (`public/fractalKernel.js`) — and it is measured to be INERT everywhere a float64 reference orbit can reach. It is shipped anyway because it is what the range below needs and it is exactly range-preserving.** | The sources say "typically a few hundred iterations" and publish no exact value; 256 is a power of two so the rescale is an exact float32 scaling and the trigger is exact. Measured in the probe (arms `reb` vs `both` vs `both64` vs `both1024`) and in the shipped shader: at every scale from 1e-8 to 1e-15 the image is BIT-IDENTICAL with the interval at 256, at 64, at 1024 and with the rescale block deleted entirely (all four metrics identical: mean |ΔRGB| 0.453, blob 3, 136 colours at 1e15). The reason is a range argument, not an accident: rescaling exists to stop the delta's square UNDERFLOWING in the low-precision type, and in float32 that happens at `|z| < ~1e-19`, i.e. a zoom of about 1e19 — while a float64 view centre has already lost every pixel to its own ULP (~1.1e-16 near `|c| ~ 0.74`) by zoom 1e16. Past 1e16 the frame is degenerate for the reference AND for every arm (measured at 1e20: `distinctRef` 1, reference spread 0.00024 iterations), so there is nothing to adjudicate. **Consequence for the brief: "rebasing and rescaling BOTH matter" is only half true in this slice — rebasing is the mechanism that reaches the depth; rescaling's contribution is not measurable until P2's arbitrary-precision orbit exists, and the brief's pin 4 for rescaling cannot go RED.** | A larger or smaller interval (measured to change nothing in the reachable range); skipping rescaling and shipping rebasing alone (it would be honest about the measurement but would leave the deep-range mechanism out of the code that P2/P6 extend); using the expanded `|Z+z|^2` in the rescaled escape test (the committed probe measured the literal absorbed form and the expanded form BIT-IDENTICAL at these depths, so the literal form — which the D1 source pin also requires — is kept). | Writer, `session-p1-perturb-core`, 2026-09-21, from the brief's item 3 and the writer's probe (`/tmp/p1-probe/probe.html`, arms `reb`/`res`/`both`/`both64`/`both1024`) and control run (rescale block deleted: metrics unchanged, pin 1 stays GREEN). |
| 35 | **P1: Pauldelbrot glitch detection is `|Z + z|^2 < G·|Z|^2` with the CHOSEN constant `PERTURB_GLITCH_G = 1e-4` in `public/fractalKernel.js`, templated into the GLSL as `#define PERTURB_GLITCH_G`, and exposed to the GPU-side detector as the diagnostic `u_diag` pass so its firing rate is MEASURED from the GPU, not asserted from the source.** | The literature publishes no principled G (Claude Heiland-Allen's blog says choosing it "remains open"; KF exposes it as a user slider) and only bounds it at 1e-2..1e-8, so a value must be CHOSEN and stated; 1e-4 is the middle of that band and the threshold the committed perturbation probe reported its glitch fractions at, so the two measurements are comparable. Measured through the shipped shader, deep lane, 200×150 at scale 1e-15: **73.8 % of pixels glitch at some iteration** — i.e. at this G the detector is a DIAGNOSTIC, not a repair trigger (the committed probe measured 74.6 % for the naive arm: the rate is a property of the reference/pixel geometry, not of the transport). The plain lane reports 0 because it has no reference to glitch against. Control (the detection line deleted, file restored byte-identically): the deep-lane diagnostic reports **0** and `tests/perturb-core.spec.js` pin 3 goes RED. Wiring the detector into the rebase condition as an additional trigger was measured BIT-IDENTICAL to the plain rebase rule, so it is kept as detection only; acting on glitches is P3's slice, not this one. | Hiding the detector behind a source-only assertion (a shader that computed it and dropped it would pass); acting on it by forcing a rebase (measured to change nothing, and would misattribute P3's job); claiming a "good" G on the literature's say-so rather than measuring its firing rate here. | Writer, `session-p1-perturb-core`, 2026-09-21, from the brief's item 5 and the writer's probe and control runs. |
| 36 | **P1: the reference orbit is the float64 orbit of the VIEW CENTRE, computed on the CPU ONCE PER VIEW (`ensureReferenceOrbit`, counted by `orbitComputations`), cached on the key `(centerX, centerY, maxIter)`, and uploaded as an RGBA FLOAT texture with one texel per reference iteration (`.r = Re Z_m`, `.g = Im Z_m`, NEAREST + CLAMP_TO_EDGE). `MAX_TEXTURE_SIZE` (8192 here) bounds the width, so at the shipped 8192 cap the orbit is one texel short of `maxIter + 1` and the shader clamps `m`; below the cap the extra `Z_maxIter` is uploaded. The single `MANDEBROT` lane is chosen at COMPILE time by `OES_texture_float` (two linked programs) and at DRAW time by the SAME scale constant as the shipped GPU zoom cap (`ITER_BUDGET_MIN_SCALE`, the one D2 pin 2 holds equal to `__fv.minScale`).** | The committed probe measured the transport EXACT here (NEAREST round-trips 53 significant bits; NPOT fine; 8192-wide fine), so transport is not the problem and one float32 word per component is sufficient — a hi/lo split measured INERT under a float32 delta (probe arms A and D bit-identical). "Once per view" is counted rather than inferred: pin 2 measures five re-draws of one view building **0** orbits and each centre/budget change building exactly **1**; control (the key check deleted, restored byte-identically) turns it RED with **5** rebuilds. The depth lane is gated because measurement, not taste, demanded it: with the perturbation lane ALWAYS on, the full gate went from **2.9 min to 5.9 min** (the startup animation alone draws ~60 full-window frames, each now paying a texture fetch per iteration); with the lane starting at the scale the zoom cap already owns, shallow rendering is the byte-for-byte pre-P1 shader and the P1 pins drive the deep lane through `setDeepView`, which lifts only the cap (P5's job). | hi/lo orbit transport (measured inert under a float32 delta); a uniform array instead of a texture (GLSL ES 1.00 forbids dynamic uniform-array indexing in a fragment shader); a runtime lane switch inside one shader (a fifth `for` loop would break the S3/D1 pins that hold four loop sites to `MAX_ITER`); recomputing the orbit per DRAW (that is the defect pin 2 exists to catch); always-on perturbation (measured to double the suite's wall time for no shallow-zoom accuracy the plain lane does not already have). | Writer, `session-p1-perturb-core`, 2026-09-21, from the brief's items 1-2 and the committed probe's transport measurements. |
| 37 | **P1 FINDING (the slice's blocker): the brief's pin 3 — "a zoom sweep across every rebase shows no spike … count must be 0 at every step" — is NOT met at any depth, and the criterion as written cannot decide this slice. Measured with the reference-based hop classification on the SHIPPED shader at a fixed budget of 7680 and a 0.1 %-per-step scale sweep: 1-4 hopping pixels per step at 1e-8, 2-8 at 1e-10, 1-6 at 1e-12 and 7-16 at 1e-13 (0.07-0.15 % of the 10 500 sub-iteration pixels), each a pixel whose float64 reference escape value moved < 0.5 iterations while the GPU colour moved 3-62 RGB units. The hops are not rebase-specific and are not removed by more precision: the rebase-disabled build scores 0-1 hops — it looks SMOOTHER only because it has collapsed to 6 distinct colours (mean step-to-step change 0.90 against the correct build's 9.8), i.e. the metric passes the broken build vacuously.** | The plan's rule 1 was designed for D1, where the value compared across steps is the SAME renderer's own escape value; applied to the GPU through an independent float64 reference it measures per-pixel ACCURACY at chaotic near-boundary pixels, not smoothness. Those pixels' float32 delta error is already known (mean Δ 6.6 iterations, max 2656 against the float64 perturbation reference), and a 0.1 % view change can legitimately flip such a pixel's escape count by O(100); the correct-build hops are that, not a rebase pop. Lowering the tolerance or using the GPU's own packed value would make the count 0 while also making it GREEN on the rebase-disabled control, which is the "pin asserted the data rather than the user-visible effect" failure the brief names. The honest statement is therefore: P1 reaches 1e15 with 0 % misclassification and a 0.45-unit mean colour error (pins 1-3, all with controls), and it does NOT reach zero hopping pixels; the zero-hop target belongs with the slice that adds new references (P3) or an arbitrary-precision orbit (P2), which is where the plan's own artifact list puts it. | Shipping a nonzero hop threshold (that is massaging a threshold to make the brief's 0 pass — forbidden); shipping the pin at 0 (a red suite, which must be fixed forward immediately); using the lazy "GPU's own value" formulation (measured non-discriminating: a continuous palette makes it 0 on every build, including the broken one); reporting LANDED with the sweep omitted (the parent would not know the requirement failed). | Writer, `session-p1-perturb-core`, 2026-09-21, from the brief's pin 3 and the writer's own sweeps (`/tmp/p1-probe/sweep-check.cjs`) with and without rebasing. |
| 38 | **P2: the reference orbit is computed at ARBITRARY precision in BigInt fixed point, with the working precision `bits = max(64, ceil(log2(1/scale)) + 64)` QUANTISED UP to a multiple of `BIGORBIT_STEP_BITS = 64` (`public/bigOrbit.js`). The MARGIN is 64 bits — rust-fractal-core's `precision = max(64, -radius.exponent + 64)`, chosen over the bare bound's 0 — and it is MEASURED sufficient rather than assumed.** | The research (`docs/RESEARCH-2026-09-21-arbitrary-depth.md` §1) settles the SHAPE (3.3219 bits per decimal digit of zoom) but not the margin: 0 in the bare bound, +64 in rust-fractal-core, implicit in KF/F3's type ladders. The margin covers the reference orbit's own fixed-point rounding, which the orbit's chaotic dynamics amplify, so it cannot be argued from the bare bound. Measured at 1e-30 with the exact centre `-1.4303576324512 + 2^-70` (below that centre's float64 ULP): the shipped setting produces a GPU frame matching a DIRECT per-pixel BigInt reference computed 256 bits WIDER with **mean \|Δ escape index\| 0.007 and 0 misclassification** — the residual worst pixel (23 iterations) is the float32 DELTA arithmetic (row 37), not the orbit. A deliberately starved setting (`bits = log2(1/scale) - 96`, F = 84) measures mean \|Δ\| **12.9**, so the margin is not vacuous. Anchors of the rule: 1e-19 -> 128, 1e-20 -> 192, 1e-30 -> 192, 1e-38 -> 192, 1e-39 -> 256. | The bare bound with margin 0 (measured to work at 1e-30 here, but it leaves the orbit's amplified rounding with zero headroom and is the one value the sources explicitly do NOT standardise); a per-frame unquantised precision (the step would move on every zoom nudge, so the orbit key would almost never hit); a fixed 64/128-bit precision (that is the float64 wall this slice removes). | Writer, `session-p2-bigint-orbit`, 2026-09-21, from the brief's item 2 and the writer's own margin sweep. |
| 39 | **P2: the orbit reaches the shader as ONE float32 word per component, one texel per iteration — P1's existing transport, unchanged — and that is a MEASURED decision. The binding precision wall at this reach is NOT the orbit's mantissa but the float32 RANGE of the per-pixel coordinate: the shader forms `dc = (offset) * u_scale` in float32, so below float32's smallest normal (~1.175e-38) the product underflows/flushes and the whole frame collapses to the inside sentinel.** | The brief names the transport as the central engineering question and lists more channels / a second texture / an exponent split. The measurement says the first three are not what limits this reach: the float32-transported BigInt orbit matches the independent direct-BigInt reference with 0.00000 % misclassification and mean \|Δ escape index\| 0.007 at 1e-30 (pin 1), and P1 already measured hi/lo INERT under a float32 delta (row 36; the probe's arms A and D were bit-identical, because `float(hi+lo)` returns `hi`). The real wall was located by sweeping the scale through the REAL shader with the same orbit: **42 distinct escape indices at 1e-20, 39 at 1e-30, 17 at 5e-38, and 1 (the entire frame on the inside sentinel) at 2e-38, 1.5e-38, 1.2e-38 and 1e-38** — i.e. the frame dies between 5e-38 and 2e-38, at float32's smallest normal, NOT at the orbit's 192-bit working precision. Depth reached and verified: **1e-30 correct** (pin 1); the frame is still structured at 5e-38; the ceiling is ~2e-38. Passing it needs the research's per-iteration scale/exponent transport ("float32 + shared exponent", the RESCALING half of §3) applied to `u_scale`/`dc`, which is a RANGE mechanism, not more mantissa — deliberately NOT built here, because the slice's pin is "beyond the P1 wall" and the slice must not fabricate reach. | Shipping a hi/lo or 4-float-per-component orbit to "reach deeper" (measured inert under a float32 delta and not the limit; it would add texture bandwidth and GLSL surface for no measured depth); a separate texture per precision level (P1's lane already re-uploads one texture per view; a second level has no measured benefit); claiming the 1e-45 emulation reach (a `Math.fround` emulation survives to 1e-40 because JS keeps subnormals, but the REAL ANGLE/SwiftShader fragment stage flushes them — the emulation was superseded by the browser measurement, and only browser numbers are claimed). | Writer, `session-p2-bigint-orbit`, 2026-09-21, from the brief's item 3 and the writer's transport/range sweeps. |
| 40 | **P2: the precision STEP is invisible by construction AND pinned differentially. Working precision is quantised UP to 64-bit steps, so a step change moves the exact orbit by ~2^-bits while the transport is float32 (2^-24) — the transported words are bit-identical, so the step can change nothing on screen. The pin renders ONE fixed view on both sides of the 128 -> 192 step at scale 1e-20 through the real Worker path and requires the escape-index and colour frames to be IDENTICAL (measured: colour mean 0.0000, escape-index mean 0.0000). The failing baseline is IN-PIN: the only documented literature defect in this class is Kalles Fraktaler 2.13.10 (2018) "corrupt image at transition between number types", injected by making the higher-precision side parse its centre at a COARSER number type (32 bits), which measures escape-index mean 11.69, 98.4 % of pixels differing, worst 86.** | The owner's hard requirement is that a hop never shows, and the research names the precision/number-type transition as the under-documented risk class with exactly one known defect. A differential at one fixed view is the only formulation that isolates the transition from the view change itself (two different scales would differ legitimately). The reference is keyed on `(exact centre, budget, bits)` and NOT on the scale, so a scale change within a step reuses the orbit (measured: 0 rebuilds) while crossing a step rebuilds exactly once — that is what makes the step a real, countable event rather than a per-frame accident. | Asserting the step from a picture or from a tolerance on two DIFFERENT scales (which cannot separate the precision change from the view change); a control that removes a different mechanism; leaving the KF-class corruption untested (the pinned failure would then be an assumption, not a measurement). | Writer, `session-p2-bigint-orbit`, 2026-09-21, from the brief's item 5 and the writer's control runs. **Sweep status, stated so it is not quietly dropped:** `docs/PLAN-DEEPZOOM.md` §3's strict zoom-sweep criterion is RE-ATTEMPTED here only at the PRECISION STEP, as the differential above (0 moved values across the step, with the KF-class baseline in-pin). The full zoom sweep across every REBASE remains P3's: this slice changes the ORBIT's precision, not the rebasing (unchanged from P1), and P1 measured that sweep non-discriminating for the rebasing mechanism (row 37). |
| 41 | **P2: the orbit is computed OFF the main thread in ONE long-lived CLASSIC Worker (`public/orbitWorker.js` + `importScripts('bigOrbit.js')`), ONCE PER VIEW keyed on `(exact centre string, budget, bits)`, and the orbit is transferred back as the float32 texture words. The main thread never blocks: while a request is in flight the draw falls back to the existing float64 lane, so the canvas is never left blank, and the Worker's ready message re-renders through the REAL `renderWebGL` path.** | `public/app.js` already loaded its CPU renderer from a classic Worker (S2) and `public/fractalKernel.js` is the no-import/no-export pattern that lets ONE source serve both the worker and the ES-module main thread (row 14); the BigInt module uses the same pattern. "Once per view" is COUNTED, not inferred: five re-draws of one view post **0** requests and complete **0** builds; a centre change completes exactly **1**; a scale change inside one precision step reuses the orbit (**0**); crossing a precision step completes exactly **1**; and exactly **one** Worker is spawned for the renderer's life and terminated in `destroy()`. The fallback matters because a deep draw must never wait on a message before showing anything. | Computing the orbit on the main thread (a 192-bit, 8192-iteration BigInt orbit blocks the interaction loop, and the owner's standing requirement is that zoom stays smooth); a Worker per request/view (measured contract is one long-lived Worker; `bigOrbitWorkerSpawns === 1`); returning the orbit as BigInt to the main thread (not transferable and not needed — the shader wants the float32 words; the independent reference is computed in the test from the centre string); posting a request per draw (the counters pin this RED: control 2 posts 5 for 5 redraws). | Writer, `session-p2-bigint-orbit`, 2026-09-21, from the brief's item 1 and the writer's counted control runs. |
| 42 | **P2 FINDING (the measured ceiling): arbitrary-precision ORBIT precision is not what stops depth in this design — float32 RANGE is. The BigInt orbit is correct and the transport is sufficient through the verified 1e-30 and the structured 5e-38; the real shader's `u_scale`/`dc` product dies at ~2e-38 (float32's smallest normal, subnormals flushed), which is 24+ orders of magnitude beyond P1's measured ~1e16 coordinate wall but still not "arbitrary". The mechanism the research already names for it is the per-iteration scale/shared-exponent ("float32 + shared exponent", rescaling), applied to the coordinate rather than to the orbit mantle.** | The slice's pin is "depth beyond P1's wall (past ~1e16)" and it is met with a large margin; the brief also says to MEASURE where it now fails and why rather than assume. Reaching further was attempted by the honest route (transporting more orbit precision) and is measured not to be the limiter, so building it would have added surface without depth. The 1e-30 correctness, the 5e-38 structured frame and the 2e-38 collapse are the three measured points. **Also recorded: the brief's premise that "P1 packed hi/lo (about 48 bits) for both components into RGBA" is not true of the shipped P1 code — `ensureReferenceOrbit` stores one `Math.fround` word per component in `.r`/`.g`; the hi/lo experiment lived in the committed PROBE, and row 36 records it as INERT. The brief's premise that the orbit-transport MANTISSA is the central question is likewise contradicted by measurement.** | Claiming LANDED-with-arbitrary-depth on the emulation (the browser flushes subnormals and collapses); massaging the step-pin's view to a scale the shader cannot really render (that is what made an earlier version of pin 3 pass VACUOUSLY: at 1e-39 the shader produced one value on both sides of the step, so the differential proved nothing — caught by the writer before the full gate and moved to 1e-20, inside float32's normal range); treating the 2e-38 collapse as "the orbit needs more bits" (measured false). | Writer, `session-p2-bigint-orbit`, 2026-09-21, from the writer's scale sweep and the brief's item 4. |
