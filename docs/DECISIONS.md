# FracVibe — decision ledger (append-only)

One row per decision. **Append-only**: a decision is history and history never
rots, so never edit another landing's row. Each row names its provenance and what
was rejected. Where the owner's words are on file they are quoted; where they are
not, the row says so rather than inventing an authority.

| # | Decision | Why / evidence | Rejected | Provenance |
|---|----------|----------------|----------|------------|
| 1 | **Make it work again before anything else.** Phase 0 = B1–B3 only. | The deployed app was a blank canvas: `Uncaught TypeError: Cannot read properties of null (reading 'addEventListener')` at `app.js:181` aborted module evaluation, so no worker, no WebGL init, no render (`MODERNIZATION.md` §1 row 1). Nothing else was observable, let alone improvable. | Jumping to Phase 2 (Vite / TypeScript / `three` from npm), which would have modernised a blank page. | `MODERNIZATION.md` §5 step 1. The owner's verbatim words for this round are not on file. |
| 2 | **`public/` stays the publish dir; no build step yet.** | `netlify.toml` has `publish = "public"`, `command = ""`, and the app is already static ES modules. | Moving output to `dist/` now — it churns `netlify.toml` and every path for no behaviour change. Deferred to the Vite landing. | `MODERNIZATION.md` §4 "Delivery". |
| 3 | **Defer WebGPU, WASM/SIMD kernels and OffscreenCanvas.** | None fixes a current defect; each is a rewrite with its own risk, and would add unverified surface while B4–B8 are open. | Doing them as part of "modernization". | `MODERNIZATION.md` §5 "Explicitly deferred". |
| 4 | **One gate command: `bash scripts/gate.sh`, two tiers.** | A hand-rolled `npm test` cannot hold a host-wide lock, cannot distinguish "did not run" from "passed", and does not keep a raw log. `exit 2 = compile-only` exists so a syntax check can never be quoted as verification. | Calling `npm test` directly, and a single tier. A compile tier is a convenience here (the suite is ~6s), but it keeps "did the suite run?" explicit and answerable off-browser. | Dispatcher, `session-f6d26a74`, 2026-09-21. |
| 5 | **`reuseExistingServer: false`, and the gate refuses a busy `:3000`.** | With reuse on, a server left by a dev session or another worktree is reused, and a green suite silently reports on code the run never loaded — a wrong-tree verification with no symptom. | Leaving it `true` for convenience; or letting Playwright fail on the port, which would surface as RED ("the checks ran and failed") when in fact nothing ran. | Dispatcher, `session-f6d26a74`, 2026-09-21. Low risk, trivially reversible by the owner. |
| 6 | **The objective is "resilient and correct", and the METHOD is the dispatcher's judgement, executed as a sequenced campaign.** The bar is defined as CORRECT (every renderer agrees with one mathematical definition at every reachable control setting) and RESILIENT (no single failure leaves a blank canvas, a wedged UI, or an uncaught exception), each behaviour PINNED by a test shown red on a deliberately broken build. | Owner's verbatim words, 2026-09-21: *"I want this to be resilient and correct, you can take your time to achieve that state. You be the judge how to achieve this best."* The audit's own defect inventory (`MODERNIZATION.md` §2–§3) is the raw material; the bar is stated so a successor can tell done from not-done. | (a) Treating the mandate as a feature list and shipping B4–B8 in the audit's written order — the audit orders by section, not by risk, and puts hygiene before the silent-failure defects. (b) Dispatching several writers at once: every slice touches `public/app.js`, so they would collide. The campaign therefore SERIALIZES on `app.js`. | Owner, 2026-09-21, via the chief-of-staff session `session-f6d26a74`; plan at `docs/PLAN.md`. |
| 7 | **S1: the zoom clamp lives in `FractalViewer` alone (`setZoomLimit`/`clampScale`), and the cap is applied to the STORED view, not to a copy the animation can overwrite.** `setView`, `onWheel`, the startup animation and `renderWebGL` all read the one clamp, so the three pre-S1 paths cannot disagree; both `setView` monkey-patches, the render-time clamp and the dead wheel guard are deleted. | The probe found the cap written three ways (`app.js:221-241`, `601-623`, `651-665`) with the only correct copy re-installed per GPU entry, and `fractalViewer.js:116-121` unreachable because `WEBGL_MIN_SCALE`/`webglCheckbox` are `app.js` module-locals. The animation mutated `viewer.view.scale` directly, so even a correct patch would have been overwritten on the next frame. Pinned, all three paths to the same value, by `tests/zoom-clamp.spec.js`. | Keeping the `viewer.setView` patch and fixing only its re-installation; clamping at render time instead of at view-set time (that leaves `viewer.view` itself lying about the scale it will draw). | Writer, `session-s1-canvas`, 2026-09-21, from the brief's item 4 and the probe's "one clamp, not three". |
| 8 | **S1: a lost or released WebGL context replaces the `<canvas>` element; renderer teardown is idempotent and accounts for itself.** `destroy()` deletes the program, both shaders and the quad buffer, then releases the context via `WEBGL_lose_context`; `window.__fvLiveWebglRenderers` (from the renderer itself) is the observable the suite asserts. | A canvas whose context has been created and then lost never returns a new context from `getContext`, so a GPU→CPU→GPU toggle would silently degrade to CPU forever. The old `destroy()` only nulled `gl`, leaking a program, two shaders, a buffer and a context per toggle. The suite may not INFER the leak from pixels, so the renderer exposes a construction/destruction counter and the pin asserts 0 after each teardown. Pinned by `tests/webgl-fallback.spec.js` (5 toggles, plus a simulated context loss). | Asserting "one context" by counting `getContext` calls or by pixel-diffing (both are inference, not observation); `preventDefault()` alone with no element swap (restoration is async and never arrives in practice). | Writer, `session-s1-canvas`, 2026-09-21, from the brief's item 3 and the "make it observable" pin. |
| 9 | **S1: `window.confirm` at the GPU zoom cap is KEPT (deferred by 10 ms), but every other WebGL failure reports through a new non-modal `#appMessage` element.** The one-shot switch-to-CPU offer stays a modal because the brief scoped only `alert`; it is recorded here as a smell. | The B9 finding was that `alert` + `viewer.render()` left a blank canvas with no CPU calculation; the container's decision (`docs/PLAN.md` S1) was "fall back to the CPU worker path and report through the existing info/status UI rather than `alert`". Moving the zoom-cap offer off `confirm` is a behaviour change S1 does not need, and `deniedCpuSwitchAtZoomCap` already makes the prompt once-only. | Silently dropping the offer (removes a user-visible capability), and keeping the three modal sites while adding a separate non-modal one (the duplication item 4 exists to remove). | Writer, `session-s1-canvas`, 2026-09-21. Flagged for the dispatcher: if the owner wants no modals at all, that is a one-row change in `handleZoomLimitReached`. |
| 10 | **S2: cancellation is `worker.terminate()` + respawn, not job slicing.** `cancelJob()` counts a real cancellation (`cancelledWorkers`), terminates the live worker and constructs a fresh one; every view change routes through it, and `startFractalCalculation` also cancels a superseding job. | A worker inside its synchronous kernel cannot receive `postMessage({type:'abort'})`, so the pre-S2 abort was a no-op (the probe's B6); and a *sliced* worker that yields between chunks is only cancellable at a yield point — it needs message handling and re-entrancy in the kernel, and a cancel still waits for the current slice. Terminate is immediate, unconditional and leaves nothing to reuse. `workerGeneration` (spawns) and `cancelledWorkers` (kills) make both halves countable; the pin asserts the kill counter rises on a cancel during a long job, and an inert `cancelJob` makes it go red. | Slicing + yielding (more kernel surface, cancellable only at yield points); keeping the `abort` post and relying on the `calcToken` alone (that is exactly the pre-S2 no-op). | Writer, `session-s2-worker`, 2026-09-21, from the brief's item 1 and the probe's flow-3 row. |
| 11 | **S2: progressive refinement is APPLIED, not deleted.** The worker refines one buffer 8 → 4 → 2 → 1 and posts a frame for **every** level (earlier levels copy the buffer, the final one transfers it); `app.js` applies `progress` frames exactly as it applies `done`. | The halving existed precisely so a coarse image could appear early, but `app.js` handled only `'done'`, so all three intermediate frames were computed and thrown away — the latency work was paid and the benefit discarded. Applying them makes a slow render converge instead of staying stale until the last pixel, which is the resilience the campaign's bar names. It is *observable*: `appliedFramesForJob(jobId) > 1` (one frame can be final, every further one is intermediate) and `progressFrames()` are counted, so the pin goes red when no non-final frame is posted. | Deleting the halving and going straight to `gridStep 1` (simpler, and it drops the redundant per-level copy, but it throws away the only progressive feedback the app has — a slow render then shows nothing until it is finished). | Writer, `session-s2-worker`, 2026-09-21, from the brief's item 3. |
| 12 | **S2: `worker.onerror` surfaces a message and retires the worker.** It reports through the existing non-modal `#appMessage` (S1) with the worker's message, drops the in-flight job, and `terminate()`s the failed worker so the next job gets a clean one. | Pre-S2 there was no `onerror` at all, so a worker failure was completely silent: the canvas kept its last frame and nothing was reported. The S1 window error net is not a substitute — a worker script error reaches the page as an `ErrorEvent` on the worker, and the pin asserts a sentinel that only the worker handler produces, plus the worker count going to zero (which the window net cannot do). | Auto-restarting the failed job (an unbounded retry loop on a worker that always fails); leaving the worker in place (the next job would post to a worker whose script may not have loaded). | Writer, `session-s2-worker`, 2026-09-21, from the brief's item 2 and the probe's failure-path row. |
| 13 | **S2: a malformed frame is validated before it becomes pixels, and the `calcToken`/generation checks are DEFENCE IN DEPTH, not the net.** `applyWorkerFrame` rejects a payload that is not a whole-`Int32Array` or whose length is not exactly `width × height`, reports through `#appMessage` and ends the job; the net that actually stops a late frame from a cancelled job is clearing `progressiveState` on cancel. | The probe called the bad-payload path an uncaught `Int32Array(undefined)`; measured on V8 that constructor does **not** throw (it yields an empty array), so the real defect was a *silently wrong* frame (an empty or short buffer passes `FractalViewer.render`'s length guard and paints the background). The guards were measured, not assumed: with both `calcToken` and generation checks deleted the cancel pin still passed, so the state clear is what protects; the checks stay because they also catch a frame that raced in before `terminate()` took effect, and removing them is not worth the risk. `terminate()` itself was measured to be **asynchronous with respect to a synchronous kernel** — the level already running still finishes (CPU 1.04 s with terminate vs 1.02 s without) — so "cancel stops the CPU immediately" is NOT claimed and is not pinned. | Inferring a leak from pixels; asserting the cancel stop by CPU time (it does not distinguish, measured); shipping without the length check and calling an empty buffer "handled". | Writer, `session-s2-worker`, 2026-09-21, from the brief's item 4 and the writer's own control runs. |
| 14 | **S3: Option A — ONE kernel file with no `import` and no `export`, publishing `globalThis.FractalKernel`, so the classic worker (`importScripts`) and the ES-module main thread load the same source.** `public/fractalEngine.js` and `public/fractalEngineMain.js` are deleted; the worker's inline override is deleted; `fractalWorker.js` only calls `self.FractalKernel.calcFractalChunk`, and `app.js` / `fractalViewer.js` / `webglFractal.js` / `colorSchemes.js` load the kernel with a side-effect `import`. | The worker is CLASSIC — `app.js` does `new Worker('fractalWorker.js')` and `fractalWorker.js:1` does `importScripts(...)` — while the main thread is ES-modules-only. A file with no import/export statements is simultaneously a valid classic script and a valid ES module, so one source serves both with **no build step and no change to worker loading**; the compile tier parses the file as both `.cjs` and `.mjs`, and the 25-test suite renders through both loaders (pins 1–3). | **Option B** (`new Worker(url, { type: 'module' })` + a normal ESM kernel). Cleaner in the abstract, but it changes worker loading and still cannot serve the classic path; Option A proved adequate in the browser, so B was not taken. Also rejected: keeping a copy of the kernel for the worker (the pre-S3 duplication itself). | Writer, `session-s3-kernel`, 2026-09-21, from the brief's "Design decision". |
| 15 | **S3: the ONE iteration cap is `MAX_ITER = 2000` in `public/fractalKernel.js`.** The slider's `max`, the kernel's clamps (worker chunk and 3D heightmap), the fragment shader's loop bound and the `u_maxIter` uniform clamp all derive from it: `index.html`'s hardcoded `max="2000"` is deleted (app.js sets the attribute from the constant) and the four `for (int i = 0; i < 1024; i++)` literals become `i < MAX_ITER`, templated in by `webglFractal.js` as `#define MAX_ITER <cap>` from the kernel. | B5: `index.html:33` offered up to 2000 while the shader looped to a hardcoded 1024, so every pixel needing >1024 iterations was coloured as escaped (GPU and CPU silently disagreed). GLSL ES 1.00 requires a **constant expression** as a loop bound, so a uniform cannot bound the loop — the source must be generated. 2000 (not 1024) preserves the iteration depth the UI already advertised and the CPU always honoured. | Lowering the cap to 1024 (an equally valid fix, but it deletes CPU capability the app already had); a uniform loop bound (illegal in GLSL ES 1.00); clamping per renderer only (leaves the UI advertising more than the GPU honours — the exact B5 symptom); pinning `cap > 1024` in the suite (it would freeze this slice's choice and fail the alternative fix). | Writer, `session-s3-kernel`, 2026-09-21, from the brief's "one constant" mandate and the measured control arms. |
| 16 | **S3: ONE type/palette table, in the kernel.** `FRACTAL_TYPES` and `COLOR_SCHEMES` (value, label, shader index, GLSL identifier, palette fn) are the only such lists: app.js populates both `<select>`s from them, the GPU type/palette indices come from `indexForType`/`indexForColorScheme`, the 2D LUT and the 3D heightmap palette come from the same table, and `webglFractal.js` templates `#define FT_*` / `#define CS_*` from it so a GLSL branch cannot drift from the map. The palette implementations move into the kernel; `public/colorSchemes.js` becomes a two-line ES-module re-export for its existing importers. | The audit's "type/palette index maps are duplicated too": `app.js`'s `typeMap` and `colorSchemeMap`, the shader's integer `== 1/2/3` branches, and the `<option>` lists in `index.html` were four to five restatements of the same two ordered lists — exactly the kind that drifts silently when a type or palette is added. Pinned per type and palette by `tests/kernel-parity.spec.js` pin 3. | Leaving the `<option>` markup and the two app.js maps as constants and unifying only the iteration math; keeping the palette functions in `colorSchemes.js` while the kernel holds only the index table (two tables whose ORDER must agree, which is the drift this row removes). | Writer, `session-s3-kernel`, 2026-09-21. |
| 17 | **S3 pins: compare CPU and GPU through a sampled grid with stated thresholds, and observe the shader template rather than infer it.** Pin 1 renders at maxIter = the cap in a deliberately interior-heavy view and requires mean per-channel \|CPU−GPU\| < 6 and >48-off fraction < 0.06 (measured correct 0.911 / 0.0105; with the four loop bounds hardcoded to 1024 the mean \|Δ\| is 82.56, i.e. >13× the threshold). Pin 2 reads the slider's `max`, `__fv.maxIterCap`, the value templated into `#define MAX_ITER` and the bound token of all four loop sites (`['MAX_ITER', …]`, length 4). Pin 3 samples 32×32 pixels per type and palette and asserts inside/outside disagreement < 0.06 and mean \|Δ\| < 20 (correct worst 0.011 / 8.1; a drifted type index gives 0.111; a drifted palette index gives 0.81–0.89 xor or \|Δ\| 39.5–97.0). | The residual CPU/GPU difference is real and is not a defect: the CPU kernel samples pixel **corners** while a fragment shader samples fragment **centers**, so a thin band along the set boundary flips. An exact-equality pin would be red on every correct build; stating the measured residual and the margin makes the pin honest. The shader's bound is an artifact, so it is READ from the generated source (the four loop tokens + the `#define` value exposed on the renderer) instead of inferred from pixels — a `#define` of 4096 with the uniform still clamped to 2000 renders identically to correct code, yet pin 2 must (and does) go red on it. | Pixel-exact equality; screenshot/eyeball agreement; inferring the shader bound from pixels; a single "looks similar" threshold without separating type drift (shape) from palette drift (colour). | Writer, `session-s3-kernel`, 2026-09-21; thresholds and all five control arms recorded in the landing report. |
| 18 | **S4: the imported record schema lives in `public/memoryRepository.js` as `validateLocation`/`validateRecords`, and reuses the kernel's type table.** Required per record: `id` a non-empty string ≤ 128 chars; `name` a non-empty string ≤ 200 chars; `scale` a finite number > 0; `centerX`/`centerY` finite numbers; `fractalType` ∈ `FractalKernel.FRACTAL_TYPES`; `maxIter` an integer in [`index.html`'s slider `min` = 50, `FractalKernel.MAX_ITER` = 2000]; `renderer` ∈ {`GPU`,`CPU`}; `timestamp` a finite number. The app's own Save path now generates a **string** id (`loc-<base36 time>-<counter>`; the old `Date.now() + Math.random()` was a number and would not have round-tripped through its own export). | B7 named only `name`, but the pre-S4 sink at `app.js:180` interpolated FIVE fields (`name`, the formatted `timestamp`, `fractalType`, `maxIter`, `renderer`) and all five are attacker-controlled in an imported file. The type list is not restated here: `FractalKernel.FRACTAL_TYPES` is the ONE table (S3 row 16), so the schema cannot drift from what the renderers will actually accept. The checks are strict (`typeof` and `Number.isInteger`, no coercion): `'3'` is not a number, `256.5` is not an integer, and `0` is not a positive scale — a value the app cannot faithfully render is refused rather than silently coerced into one it can. Pinned by `tests/import-validation.spec.js` (per-field rejection with its own reason); the dispatcher turned it RED by deleting the schema consultation, and by deleting the load-path dedup for the counted pin. | Accepting the shapes `memoryRepository.save()` used to coerce (truthy name, any timestamp, numeric-string id) — that is the "render `NaN`/`undefined` instead of an error" behaviour B7 is about; validating in `app.js` and leaving the store unvalidated (the store is the boundary every future caller will use); restating the fractal-type list in the schema. | Writer, `session-s4-input`, 2026-09-21, from the brief's item 2 and the five-field sink the dispatcher measured. |
| 19 | **S4: a per-record defect is rejected PER RECORD and the file's valid part still loads; a file-level defect (too many records, or a non-array document) is rejected WHOLE.** The user sees one `#appMessage` line: `Imported N location(s); M record(s) were rejected by the import schema. First problem — record <i>: <reason>.` | The two failure kinds are different things. "One record is malformed" is local: refusing the other 20 locations a user saved because line 7 is truncated would destroy more data than the hostile input ever could, and the schema makes the accepted part safe by construction (it is normalized to exactly the nine fields). "This file has 10,000 records" or "this is not an array" is a property of the FILE, so there is no trustworthy "valid part" to keep — taking the first 200 records of a file we have just decided not to trust would be the worst of both policies. The message names the first problem so a rejection is actionable rather than silent. Pinned by the mixed-validity pin (1 accepted / 15 rejected in one file, and the accepted one loads) and the bounds pin (201 records → nothing stored). | All-or-nothing per record defect (data loss); partial acceptance for oversized files (contradicts the reason the bound exists); a modal/`alert` for rejects (the brief routes every user-facing failure through the S1 `#appMessage`). | Writer, `session-s4-input`, 2026-09-21, from the brief's item 2 ("state your judgement"). |
| 20 | **S4: the import is bounded at three points — 200 records per file, 1 MiB of raw text, 200 chars per name / 128 per id.** `LOCATION_LIMITS` in `public/memoryRepository.js` is the single declaration; `app.js` checks the raw text length **before** `JSON.parse` and again on `File.size` before `FileReader`, and `validateRecords` checks the array length before validating any record. | "A hostile file cannot wedge the UI" needs the work bounded before it is done, not after: parsing is the expensive step, so the byte bound is checked first, and record validation is O(n) so the count bound precedes it. The numbers are deliberately the app's own scale (the store is session-only; a location is nine scalar fields) rather than tight against today's usage, so a legitimate export of a heavy session still round-trips. Pinned by the bounds pin, which the dispatcher turned RED by deleting all three checks — with them gone the 201-record file is accepted and the over-limit payload reaches `JSON.parse`. | An unbounded import with per-record validation (validation is O(n), so it bounds correctness but not cost); counting characters rather than bytes (fine for the ASCII this app writes, and the `File.size` check is the authoritative one). | Writer, `session-s4-input`, 2026-09-21, from the brief's item 4. |
| 21 | **S5: the mesh work is SPLIT — `evaluateHeightmap()` owns the ONE kernel call and counts it; `applyColors()` rewrites only the geometry's `color` attribute from the CACHED heightmap and height range.** `setColorOffset` and `setColorScheme` call `applyColors()`; `regenerateMesh()` builds positions from the fresh heightmap and then calls the same `applyColors()`, so a rebuild and a colour tick share ONE palette implementation. The colour buffer is reused in place (`attr.needsUpdate = true`) rather than replacing the attribute. | B8: `setColorOffset` → `regenerateMesh()` → `calculateHeightmap` meant a full `resolution²` fractal evaluation *per animation frame* whenever 3D mode was active with colour cycling on. Caching `this.heightmap`/`this.heightMin`/`this.heightMax` makes a colour tick O(n) palette lookups with zero kernel calls. Replacing the `color` attribute object each tick (what the old code did as a side effect of rebuilding) orphans its GL buffer, so the live buffer is overwritten instead. Pinned by `tests/3d-truth.spec.js`: the counted evaluation total does not advance across real colour-cycle ticks, and the resulting colours match the kernel palette for the geometry's own heights. | A palette **shader uniform** for 3D (correct in principle, but it rewrites the `MeshStandardMaterial`/GLSL and is a bigger surface than this slice needs — recorded as the alternative the brief allowed); recreating the geometry/material per colour tick (same fractal cost as the bug, plus a leak); caching colours rather than heights (would not track a resolution rebuild). | Writer, `session-s5-3d`, 2026-09-21, from the brief's item 1 and B8 (`MODERNIZATION.md` §2). |
| 22 | **S5: `enter3DMode()` is `async` and AWAITS `Fractal3DViewer.init()`; a failure tears the viewer down, restores the pre-3D UI through the normal exit path (`restore2DFrom3D`), and reports through `#appMessage`.** The 2D canvases are hidden only AFTER a successful init, and a `threeDGeneration` counter invalidates an init that is still in flight if the user leaves 3D mode. `Fractal3DViewer.exit()` now also `dispose()`s the three.js renderer and hides the stuck progress bar. | `init()` is async but three.js constructs its `WebGLRenderer` synchronously inside it, throwing `Error creating WebGL context.` when no context is available. Pre-S5 `app.js:489` called `init()` with no `await` and no `catch` *after* hiding both 2D canvases, so the page black-screened with an unhandled rejection and no message (control B reproduced exactly that: the only text was the global net's "Unhandled promise rejection: Error creating WebGL context."). Reuses the S1 `#appMessage` surface rather than adding a second failure UI. Pinned by `tests/webgl-fallback.spec.js:228` using the S1 WebGL-denial helper (shared via `tests/helpers/webgl.js`, not copied). | Leaving `init()` unawaited with a bare `.catch(showError)` (the canvases would still be hidden, so there is no restore); reporting the failure but making the user press Space again; an overlay on top of the black canvas instead of restoring the 2D UI; a second WebGL-denial helper in the new spec. | Writer, `session-s5-3d`, 2026-09-21, from the brief's item 2 and the probe's 3D-init failure row. |
| 23 | **S5 pins: count the evaluations and the colour-cycle ticks on the observation surface, and check the colour attribute against the palette derived from the geometry's OWN positions (stated tolerance 1/255).** New `window.__fv` members `heightmapCalls`, `colorCycleTicks`, `threeDReady`, `colorOffset3D`, `terrainHeightRange`, `sample3DTerrain`, plus `set3DColorOffset`/`set3DColorScheme` that drive the real setters (as `forceFallback`/`importPayload` drive theirs). | "Zero evaluations" is invisible in pixels and ambiguous in frame time, so the viewer counts its own kernel calls (`fractal3d.js:131`) and the pin ALSO requires the real cycle to have ticked and moved the 3D offset — a stopped loop would otherwise make the pin pass vacuously. The palette pin derives its expectation from `position.getZ` (what is actually rendered), not from the cached `heightmap`, so a rewrite that used stale heights or a stale range is caught; 1/255 ≈ 0.0039 covers float32 storage only and is ~4 orders of magnitude above the measured residual. Control C (repaint dropped) turns the palette pin RED while the evaluation pin stays GREEN, so the two pins are independent. | Inferring the guarantee from frame time or pixel stability; asserting only that the counter is unchanged (a stopped cycle passes); restating the palette formula in the test instead of reading `FractalKernel.paletteFunction`; one combined pin that cannot separate "did not recompute" from "did not repaint". | Writer, `session-s5-3d`, 2026-09-21; all three control arms recorded in the landing report. |
| 24 | **S5 fix-forward: add an UPLOAD pin — `window.__fv.colorAttributeVersion()` must advance across an offset tick and a scheme change — because the value pin alone could not see a missing `attr.needsUpdate = true;`.** The version is three.js `BufferAttribute.version` (`three.module.js:1813`), incremented only by the `needsUpdate = true` setter (`:1817`) and used by `WebGLAttributes.update` to decide a re-upload (`:13525`,`:13548`). The new pin is deterministic (a counter, not pixels) and the value pin is kept unchanged, so the tick is pinned end to end: values correct AND values sent. | The dispatcher's probe deleted `fractal3d.js:168` `attr.needsUpdate = true;`. `applyColors()` still wrote every vertex colour into the geometry's `color` ARRAY, so the value pin (which reads that array through `sample3DTerrain`) passed, as did the zero-evaluation pin — while a user would see a frozen mesh and Cycle Colors would do nothing. This is the same class as the S1 pin that called the context-loss handler directly and missed the wiring: the assertion covered the data, not the effect. The exact same class of defect is what the campaign's bar calls "silently wrong"; the CPU array looked right. Control D (below) reproduces the dispatcher's probe: with `needsUpdate` removed the NEW pin goes RED and the value pin stays GREEN, so the two pins are demonstrably independent. | Inferring the upload from pixels (no deterministic threshold; the version counter is the very mechanism three.js uses); deleting or weakening the value pin (it is correct for values and catches a different defect); asserting only that the version is non-zero (the initial upload would satisfy that even if every later tick skipped `needsUpdate`). | Writer, `session-s5-3d`, 2026-09-21, in response to the dispatcher's probe log `/tmp/fv-armS5-palette.log` — the gap was found, not predicted. |
| 25 | **S6: BOTH dialogs are replaced by in-page non-modal surfaces — `window.prompt` (save/naming) by `#saveLocationPanel`, `window.confirm` (the GPU zoom-cap offer) by `#zoomCapOffer`.** The naming flow keeps the same contract: the field is prefilled with the SAME generated default the prompt offered, Save (or Enter) commits through the ONE `commitNamedLocation()`, Cancel (or Escape) stores nothing — the old `prompt === null` branch. The cap offer is shown at most once, synchronously, and the cap itself still lives only in `FractalViewer.clampScale()`, so it is applied by the CLAMP on every path (wheel, startup animation, render) whatever the user answers — or does not answer. "Switch to CPU" unchecks the WebGL checkbox and fires its ONE `change` handler (the real CPU path); "Stay on GPU" sets the denial flag. "Once" stays observable: `fv-zoom-limit`'s `prompted` is still exactly "the user was actually asked", and `__fv.zoomCapPrompted()`/`zoomCapOffered()` expose the ask as a DOM fact. | This is the smell `docs/DECISIONS.md` row 9 deliberately carried out of S1 ("if the owner wants no modals at all, that is a one-row change in `handleZoomLimitReached`"); S6 is that row. A modal blocks the page, cannot be styled or dismissed by the app, and is the class the campaign's RESILIENT bar names. Both retired sites were re-located rather than trusted (`app.js:88`, `app.js:394` at `5537ca7`). Control B (restore `window.prompt` in the save path) turns the new dialog pin RED with a recorded `"Name this location (optional):"` prompt; control D (`setZoomLimit(null, …)`, the cap disabled completely) turns the cap-application assertion RED (`1e-9` returned against a `1e-4` cap). | Keeping `window.confirm` (the smell row 9 flagged for exactly this slice); silently dropping the offer (removes a user-visible capability); a custom HTML `<dialog>` (ARIA/focus/touch work is explicitly out of this campaign, per `docs/PLAN.md` §3); ANY behaviour change to the clamp, so the S1 zoom-clamp pins and `tests/smoke.spec.js`'s modal assertions stay green unchanged. | Writer, `session-s6-hygiene`, 2026-09-21, from the brief's item 2 and row 9. |
| 26 | **S6: the four surviving `[FractalMouse]` `console.log`s are DELETED and replaced by NOTHING; the log is pinned by a console-URL filter, not by counting only `[FractalMouse]` text.** Every console method is wrapped in an `addInitScript` BEFORE app.js runs, and each call records the URL of the script that made it (`Error().stack`); the pin asserts no recorded call came from a URL containing `app.js`. The four dead state declarations (`currentResult`, `aborting`, `debounceTimer`, `lastJobParams`) and the four dead files are deleted in the same landing. | These logs sit on the hottest paths — every `mousedown`, every `wheel` tick, every view-change render — so they are per-event work on the interaction loop, and the `MutationObserver` that existed only to log was already deleted by S1 (S6 confirms its absence; it did not remove it). Replacement is not wanted: a hot-path log is exactly what the slice removes. The filter is stated because a text-based filter would be satisfied by renaming the tag, and because a pin that only counted calls could be satisfied by a stopped app — so the test ALSO requires the app to have run (a completed render and a settled startup animation). Control A (re-adding ONE `console.log` to `onMouseDown`) turns the pin RED with 9 recorded app-origin calls; restoring `window.prompt` (control B) turns the dialog pin RED with a recorded `prompt` call. | Counting `[FractalMouse]`-prefixed messages only (a renamed log would pass); asserting a call-count of zero without proving the interaction ran (a stopped app would pass); filtering out `console.error`/`console.warn` to keep the pin easy (that is the filter bending to the code); excusing three.js's own `console.warn`s by adding them to the filter (the pin is scoped to the app's own source and says so). | Writer, `session-s6-hygiene`, 2026-09-21, from the brief's item 1 and the measured controls. |
| 27 | **D1: colour is a CONTINUOUS function of the escape value, defined ONCE in `public/fractalKernel.js` and evaluated with the identical expression by the kernel and by the GLSL. The value is `v = n + 1 − log2( log(\|z\|²) · (1/ln(BAILOUT_SQ)) )`, where `n` is the 1-based index of the update whose result exceeded the bailout and `\|z\|²` is that result; a point that never escaped returns EXACTLY `maxIter` (inside ⇒ black).** The iterated buffer is a **`Float32Array` with `NaN` as the "not yet calculated" sentinel** (`new Float32Array(w*h).fill(NaN)`, skip test `value === value`, payload validated as a 4-byte-aligned `Float32Array` payload of exactly `width×height`); `-1` in an `Int32Array` cannot represent the value. Colour is produced from a table over the **quantised continuous value** at `COLORS_LUT_STRIDE = 256` entries per iteration — a FIXED resolution, independent of `maxIter` — with an inside entry at exactly `maxIter` (black) and one placeholder entry (`40,40,40`) for `NaN`; colour cycling (`u_colorOffset`/`setColorOffset`) applies to the continuous value. | The staircase was the amplifier the owner asked to remove: with one colour per INTEGER iteration, a precision change of any size can push a pixel across a step and flip it a whole band, and those pixels cluster at the boundary — which is what is on screen while zooming. Measured on the correct build: at `maxIter` 50 a hop of 0.01 iterations (1/100 of a band) moves the colour by 1/255 in every palette and at most 0.25 of a band; the boundary-crossing zoom sweep shows 0 hopping pixels at all 14 steps from scale 0.05 to the cap's 1e-4, against 2–21% of escaped pixels per step with the integer staircase (control A). A `maxIter`-proportional table was rejected because at `maxIter` 2000 it has 2000 entries — it cannot resolve neighbouring escape values at all, which is exactly the resolution this slice needs; direct per-pixel palette evaluation was rejected because it allocates per pixel in the inner loop, while the table keeps the pixel loop a lookup. Measured table-vs-exact residual over all five palettes: 1 RGB unit (the display cannot show less). The `NaN` and placeholder choices are pinned so a finished frame carries ZERO uncalculated cells and a cell without a value can never be painted as a real one (`tests/smooth-color.spec.js` pins 3 and 4). | Keeping the integer LUT and "smoothing" only the shader (one renderer stays a staircase — arm D, which the S3 parity pins do NOT catch at cap 2000); a `maxIter`-proportional fine table (see above); `-1` in a `Float32Array` (collides with a legitimate value near −1) or a separate `Uint8Array` mask (a second buffer and a second place for the two to disagree); painting `NaN` as black (indistinguishable from set interior) or as a palette colour (indistinguishable from a rendered value); keeping a per-integer colour and interpolating in RGB (it is not the palette's definition, and it breaks GPU/CPU agreement). | Writer, `session-d1-smooth-color`, 2026-09-21, from the owner's verbatim requirement ("C but with special care around precision hops … the zoom needs to stay visually as smooth as possible") and the dispatcher's slice D1 brief. |
| 28 | **D1: the two renderers must agree on `n`, and that is a measured property, not a convention to be assumed. The kernel's loop tests at the TOP of an iteration, so the count it returns already INCLUDES the escaping update; the shader breaks BEFORE its own `iter++`, so its count EXCLUDES it. The kernel therefore passes `iter` and the GLSL uses `float(iter) + 1.0`.** The GPU is pinned twice: (a) the GENERATED fragment source is read back and must contain the kernel's exact expression and the templated constants (`SMOOTH_LOG_BAILOUT`, `BAILOUT_SQ` — emitted as GLSL float literals because a bare `4` is an int/float type error that fails the fragment compile and silently falls back to the CPU), and (b) at a cap where one iteration is a visible band (`maxIter` 50) each GPU fragment is compared with the KERNEL's colour at that fragment's own centre coordinate. | The first implementation used `iter + 1` on both sides. Measured against a faithful GLSL emulation it differed from the shader by exactly 1.0 iteration at **all 14932** sampled escaped points, and the browser comparison showed a whole 21-unit band per pixel at `maxIter` 50 — while the S3 parity pins stayed GREEN, because at `maxIter` 2000 one iteration is 0.13 of an RGB unit (arm D, measured). So the S3 thresholds cannot see a one-iteration GPU-side colour difference at all; that is why the check is both axial (source) and numerical at a low cap. With the conventions aligned, the coordinate-matched check measures mean \|Δ\| **0.354** (worst 1) at cap 50; control E (the `u_maxIter` uniform off by one, source unchanged) turns it RED on the inside-agreement assertion. The comparison is made at the fragment's OWN centre because the CPU kernel samples pixel corners and the shader fragment centres — at low iteration counts that half-pixel offset alone is worth an iteration, and a corner-vs-centre pixel comparison at cap 50 measures mean 34 units even on a correct build (that pre-existing sampling difference is NOT part of this slice). | Comparing CPU-canvas pixels with GPU-canvas pixels at a low cap (it measures the sampling convention, not the formula — measured mean 34 on a correct build); inferring the shader from pixels at cap 2000 (a one-iteration error is 0.13 units there); trusting a comment about iteration conventions instead of measuring them (that is what produced the off-by-one); a shared `n` constant in a comment rather than the templated expression (the constants are templated; the expression is checked as source text). | Writer, `session-d1-smooth-color`, 2026-09-21, from the same slice; the off-by-one was found by the writer's own low-cap parity measurement before landing, not by a reviewer. |
| 29 | **D1 fix-forward (dispatcher's probe): `applyWorkerFrame` passes `progress.jobParams.maxIter` to `viewer.setData`, and a pin now holds it — a finished frame is indexed at the cap ITS OWN JOB ran at, never at whatever `viewer.maxIter` holds when the frame lands.** The reachable mismatch is: **a CPU job in flight while the viewer's cap changes, on the GPU renderer** — `applyLoadedRecord` sets the slider, `viewer.setMaxIter` and the record's renderer, then calls `renderWebGL()` (not `startFractalCalculationWithTiming()`), so on the default GPU renderer a **Load of a schema-valid location whose `maxIter` differs from the slider does NOT cancel the running job**, and that job's frame arrives afterwards. The pin builds exactly that through the real paths — the slider's own `input` handler starts a job at cap 50, a schema-valid record differing only in `maxIter` (2000) is imported through `__fv.importDocument` and loaded through `__fv.loadLocation` in the SAME synchronous page task (so no worker frame can interleave), the load takes the GPU branch, and the job's final frame lands afterwards. It then asserts: the mismatch was real (a job was in flight, jobCap 50, viewer cap 2000 after the load); the finished frame carries **no uncalculated/NaN cells**; the frame was indexed at jobCap; and every cell the JOB called inside renders **black** when the stored frame is painted once through the app's ORIGINAL 2D renderer (`__fv.paintCpuFrame`, the same function CPU mode calls). | The dispatcher reverted `public/app.js` to `viewer.setData(smoothResult, viewer.maxIter)` (log `/tmp/fv-armD1.log`) and **all five D1 pins stayed green**, so the invariant was unguarded and a P1 refactor of this buffer/cap plumbing could reintroduce it silently. Measured with the revert: the frame is indexed at cap 2000 instead of 50, all **3989** cells the job called inside paint as the palette colour **[255,26,0]** instead of black, and **nan stays 0** — i.e. the "no uncalculated cells" half alone does NOT catch it; the inside-at-the-job's-cap half is the discriminator. With the fix: capUsed 50, interior 3989, black 3989, notBlack 0, nan 0. The pin was shown RED on the revert (`Expected: 50, Received: 2000`; writer log `/tmp/d1-round2/spec-reverted.log`) while the other five pins stayed GREEN, and the reverted file was restored byte-identically (`app.js` sha256 `b724460918225a44aec1aa73c40da607365cc3a536fe92dd0c199c68c69e0770`, verified before AND after). | Relying on the S2 cancel/generation nets to make the mismatch unreachable (it is reachable: the GPU branch of the load path deliberately does not cancel); pinning only the cap without the painted inside-black half (it would assert the plumbing rather than the effect — both are asserted together); constructing the mismatch by writing `viewer.setMaxIter` directly from the test (that is a hook, not a user path); relying on the "no uncalculated cells" half alone (measured: it stays green under the revert). | Dispatcher's verification probe of D1, `session-f6d26a74`, 2026-09-21 (log `/tmp/fv-armD1.log`), executed as a fix-forward by the D1 writer on top of `8e752d8`. |
| 30 | **D2: the iteration budget scales with the zoom, and the user's slider value is a FLOOR. `effective = max(clampMaxIter(sliderValue), iterBudgetForScale(scale))`, where `iterBudgetForScale(scale)` is `MIN_ITER` (no floor) at and above the scale of the shipped GPU zoom cap and `clampMaxIter(ceil(ITER_BUDGET_PER_DECADE * log10(1/scale)))` strictly below it, with `ITER_BUDGET_PER_DECADE = 512` and `ITER_BUDGET_MIN_SCALE = 1e-4`.** The effective budget, the floor and the pure rule are observable at `window.__fv` (`maxIter`, `maxIterFloor`, `iterBudget`, `iterBudgetForScale`), and the `#maxIterValue` readout prints `"<effective> (auto)"` whenever the floor raised it, so the UI never silently shows a number the job is not using. | PROBE item 7: the probe's view centre escapes only at iteration 3086 (float64, stable across budgets 2e4-2e6), so at the shipped 512 the centre is "inside" and a 1e8 frame is 100% black; the probe needed 4096 at 1e8. Measured with this kernel at 200x150 on the probe's centre: scale 1e-8 is **100.0% inside at cap 512** and **0.0% inside at cap 4096**; 1e6/512 is 90.9% inside. The SHAPE is the measured scaling of the escape count (n ~ log2(1/d) for the quadratic map), i.e. linear in the LOGARITHM of the zoom, not in the zoom; one decade of zoom is 3.32 doublings of the boundary distance, and 512/decade is 154/doubling, which covers the measured 3086 with the budget the probe actually used — anchors 1e5 -> 2560, 1e6 -> 3072, 1e7 -> 3584, 1e8 -> 4096, 1e15 -> 7680. The knee at the shipped cap's own scale is what keeps D1 green: D1's band pins deliberately hold a cap of 50 at zooms up to exactly 1e4 to make one iteration a visible colour band, and a floor over that range would override the explicit user budget (a global log rule gives 666 at zoom 20, where one iteration is 0.38 RGB units, so D1 pin 1's "one iteration must be a resolvable band >= 3 units" cannot hold). The knee is not a silent duplicate of the app's zoom cap: pin 2 holds `__fv.iterBudgetMinScale` EXACTLY equal to `__fv.minScale`, so the slice that lifts the cap (P4) is forced to revisit the rule. | A global log rule with no knee (it overrides D1's cap-50 band at zoom 20-1e4 and turns D1 pins 1-3 red — measured arithmetic: 666 at zoom 20); a doubling-per-decade rule (exponential in log10, 2-4x the probe's measured budget at 1e8, paying 2-4x table and iteration cost for no measured benefit); a rule tuned to be under 50 at 1e4 yet 4096 at 1e8 (a steep polynomial in log10, not the measured logarithmic scaling, and erratic near the knee); letting the auto budget REPLACE the slider instead of flooring it (a user's explicit cap would be silently ignored, and D1 pin 6's contract — a finished frame indexed at the cap its own job ran at — would be violated, since that pin deliberately holds a job cap of 50 at a shallow view); making the slider a CEILING (then deep zoom could never raise the budget at all, which is the whole slice). | Writer, `session-d2-iter-budget`, 2026-09-21, from the brief's item 1 and PROBE item 7. |
| 31 | **D2: the ONE iteration cap `MAX_ITER` is raised 2000 -> 8192 (`public/fractalKernel.js`). Nothing else restates it: the slider's `max` is still derived at `app.js:64`, the kernel clamps the worker chunk and the 3D heightmap to it, and `webglFractal.js` still templates `#define MAX_ITER` and its four loop bounds from it (S3 pin 2 stays green).** | The rule reaches 4096 at zoom 1e8 and 7680 at zoom 1e15 — the practical reach of a float64 reference orbit (P6) — so 8192 is the smallest power of two that lets the RULE, not the cap, decide over that whole reachable range; the rule clamps at 8192 only past zoom 1e16, beyond where the orbit itself has precision. Measured: a fragment shader recompiled with `#define MAX_ITER` at 8192 (and at 4096/16384/32768) compiles AND links under ANGLE/SwiftShader on this host with compile time below the measurement resolution, so the raised bound costs no shader-compile budget (SwiftShader software proxy; a real GPU is not established). Raising the cap does NOT raise any frame's cost: a job runs at its own scaled budget, the shader loop breaks at its `u_maxIter`, and the colour table is sized by that job's cap (row 32). | 16384 or 32768 (no reachable view needs them; they double the worst-case table and shader work for zoom past the float64 orbit); 4096 (the rule would be truncated at 1e8, leaving nothing for 1e9-1e15 even though the probe's own trend shows the needed budget keeps rising); keeping 2000 and raising only a separate "slider max" (that is two constants and is exactly the B5 defect S3 removed); a cap so large it can never bind (the table's worst case becomes unbounded for no reachable benefit). | Writer, `session-d2-iter-budget`, 2026-09-21, from the brief's item 2 and the measured rule anchors. |
| 32 | **D2: the 2D colour table is SIZED BY THE CAP THE FRAME'S OWN JOB RAN AT and BUILT ONCE per key. `FractalKernel.colorTableSize(maxIter) = clampMaxIter(maxIter) * COLORS_LUT_STRIDE + 1` replaces D1's `COLORS_LUT_SIZE = MAX_ITER * COLORS_LUT_STRIDE + 1` (a module-maximum constant that nothing allocated), and `FractalViewer.buildColorTable` caches the `Uint32Array` on `(colorScheme, colorOffset, maxIter)`; `render` reuses one `ImageData` and its `Uint32Array` view while the backing-store size holds. The counters `colorTableBuilds`, `colorImageDataBuilds` and `colorTableEntries` are on `window.__fv`.** | MODERNIZATION.md's still-open per-frame-allocation finding, which the D2 cap raise makes bite: sized by the module maximum (8192) EVERY frame would allocate 2 097 155 entries (~8.0 MB) whatever cap it ran at, where a 512-cap frame needs 131 075 entries (0.5 MB) and the rule's 4096-cap deep frame needs 1 048 579 (4.0 MB). D1's own report measured ~2 MB per frame at cap 2000 with the table (re)built on every render; during a progressive job that is one build per frame of the 8/4/2/1 refinement. Measured in `tests/iter-budget.spec.js` pin 3 through the app's own counters: five consecutive renders at the same key build **0** tables and **0** ImageData; a palette change builds exactly **1** (so the counter is live and the cache cannot pass by never building); a frame at cap 512 has 131 075 entries, not 2 097 155; and the deep frame at cap 4096 has 1 048 579 entries (4.0 MB), not 2 097 155 (8.0 MB). | Reducing `COLORS_LUT_STRIDE` at high caps to save memory (the table-vs-exact residual D1 pinned at <= 1 RGB unit is a half-stride palette step, so a smaller stride grows it and breaks D1 pin 1); evaluating the palette per pixel instead of through a table (D1 rejected it at cap 2000 for allocation inside the inner loop, and the reason has not changed — the table keeps the pixel loop a lookup and the residual is unchanged); sizing by the module max and caching only (still 8.0 MB for a 512-cap frame); caching the `ImageData` across a resize (it must match the backing store, so it is rebuilt on a size change); dropping the inside/placeholder entries to shrink the table (they are what keep a finished frame black and an uncalculated cell unpaintable, D1 pins 3-4). | Writer, `session-d2-iter-budget`, 2026-09-21, from the brief's colour-table item and MODERNIZATION.md's per-frame-allocation finding. |
| 33 | **P1: the per-pixel delta is REBASED by the reference's own rule — when `|Z_m + z_n| < |z_n|`, `z_n <- Z_m + z_n` and the reference iteration resets to `m = 0` — and that is the mechanism that buys the depth. The comparison is made on the same absorbed `x*x + y*y` the escape test uses; the expanded magnitude `|Z|^2 + 2 Re(Z conj z) + |z|^2` was tried as the rebase test and measured far WORSE (mean per-channel |ΔRGB| 47.5 and a 15000-px blob against 0.45 and 3), so the absorbed form is the one shipped.** | The committed naive probe (`docs/PROBE-2026-09-21-perturbation.md`) had no rebasing and was structurally wrong at 1e15: 26.4 % of pixels misclassified, 74.6 % glitched, 6 distinct colours against the reference's 53. Re-running that probe with rebasing added, and then the SHIPPED shader, measures the jump. Shipped shader at 200×150, centre `-0.743643887037151 + 0.13182590420533i`, scale 1e-15, budget 7680, against a float64 PERTURBATION reference computed from the app's own orbit: **misclassification 0.00000 %, mean per-channel |ΔRGB| 0.453, largest 4-connected blob of pixels differing by >32 = 3 px, 136 distinct colours — exactly the reference's 136**; rebases actually occur (163 651 over 30 000 pixels). The control (rebase block deleted, file restored byte-identically: `webglFractal.js` sha256 `86b104c78d1e9ae15cc2696bb6fbefa88ef327b2ea9d4e9d4fc8c6765b04cc5a` before and after) measures mean |ΔRGB| **3.182**, a **90-px** blob and **6** distinct colours, and turns `tests/perturb-core.spec.js` pin 1 RED on the mean-|Δ| assertion. | Rejecting the absorbed rebase comparison in favour of the "more accurate" expanded one (measured 100× worse — the rebase must re-anchor on the same quantity the arithmetic actually carries); rebasing on a schedule instead of on the rule (the rule is what the sources give and what the probe's improvement is attributable to); treating the naive 26 % collapse as a precision ceiling rather than a missing mechanism (`docs/RESEARCH-2026-09-21-arbitrary-depth.md` §3). | Writer, `session-p1-perturb-core`, 2026-09-21, from the brief's item 4 and the writer's own probe (`/tmp/p1-probe/probe.html`) and control runs. |
| 34 | **P1: RESCALING is implemented as the sources describe — `z = S·w`, `dc = S·d`, `w -> 2 Z_m w + S w^2 + d`, with `S` renormalised to a power of two near `|z|` every `PERTURB_RESCALE_INTERVAL = 256` iterations (`public/fractalKernel.js`) — and it is measured to be INERT everywhere a float64 reference orbit can reach. It is shipped anyway because it is what the range below needs and it is exactly range-preserving.** | The sources say "typically a few hundred iterations" and publish no exact value; 256 is a power of two so the rescale is an exact float32 scaling and the trigger is exact. Measured in the probe (arms `reb` vs `both` vs `both64` vs `both1024`) and in the shipped shader: at every scale from 1e-8 to 1e-15 the image is BIT-IDENTICAL with the interval at 256, at 64, at 1024 and with the rescale block deleted entirely (all four metrics identical: mean |ΔRGB| 0.453, blob 3, 136 colours at 1e15). The reason is a range argument, not an accident: rescaling exists to stop the delta's square UNDERFLOWING in the low-precision type, and in float32 that happens at `|z| < ~1e-19`, i.e. a zoom of about 1e19 — while a float64 view centre has already lost every pixel to its own ULP (~1.1e-16 near `|c| ~ 0.74`) by zoom 1e16. Past 1e16 the frame is degenerate for the reference AND for every arm (measured at 1e20: `distinctRef` 1, reference spread 0.00024 iterations), so there is nothing to adjudicate. **Consequence for the brief: "rebasing and rescaling BOTH matter" is only half true in this slice — rebasing is the mechanism that reaches the depth; rescaling's contribution is not measurable until P2's arbitrary-precision orbit exists, and the brief's pin 4 for rescaling cannot go RED.** | A larger or smaller interval (measured to change nothing in the reachable range); skipping rescaling and shipping rebasing alone (it would be honest about the measurement but would leave the deep-range mechanism out of the code that P2/P6 extend); using the expanded `|Z+z|^2` in the rescaled escape test (the committed probe measured the literal absorbed form and the expanded form BIT-IDENTICAL at these depths, so the literal form — which the D1 source pin also requires — is kept). | Writer, `session-p1-perturb-core`, 2026-09-21, from the brief's item 3 and the writer's probe (`/tmp/p1-probe/probe.html`, arms `reb`/`res`/`both`/`both64`/`both1024`) and control run (rescale block deleted: metrics unchanged, pin 1 stays GREEN). |
| 35 | **P1: Pauldelbrot glitch detection is `|Z + z|^2 < G·|Z|^2` with the CHOSEN constant `PERTURB_GLITCH_G = 1e-4` in `public/fractalKernel.js`, templated into the GLSL as `#define PERTURB_GLITCH_G`, and exposed to the GPU-side detector as the diagnostic `u_diag` pass so its firing rate is MEASURED from the GPU, not asserted from the source.** | The literature publishes no principled G (Claude Heiland-Allen's blog says choosing it "remains open"; KF exposes it as a user slider) and only bounds it at 1e-2..1e-8, so a value must be CHOSEN and stated; 1e-4 is the middle of that band and the threshold the committed perturbation probe reported its glitch fractions at, so the two measurements are comparable. Measured through the shipped shader, deep lane, 200×150 at scale 1e-15: **73.8 % of pixels glitch at some iteration** — i.e. at this G the detector is a DIAGNOSTIC, not a repair trigger (the committed probe measured 74.6 % for the naive arm: the rate is a property of the reference/pixel geometry, not of the transport). The plain lane reports 0 because it has no reference to glitch against. Control (the detection line deleted, file restored byte-identically): the deep-lane diagnostic reports **0** and `tests/perturb-core.spec.js` pin 3 goes RED. Wiring the detector into the rebase condition as an additional trigger was measured BIT-IDENTICAL to the plain rebase rule, so it is kept as detection only; acting on glitches is P3's slice, not this one. | Hiding the detector behind a source-only assertion (a shader that computed it and dropped it would pass); acting on it by forcing a rebase (measured to change nothing, and would misattribute P3's job); claiming a "good" G on the literature's say-so rather than measuring its firing rate here. | Writer, `session-p1-perturb-core`, 2026-09-21, from the brief's item 5 and the writer's probe and control runs. |
| 36 | **P1: the reference orbit is the float64 orbit of the VIEW CENTRE, computed on the CPU ONCE PER VIEW (`ensureReferenceOrbit`, counted by `orbitComputations`), cached on the key `(centerX, centerY, maxIter)`, and uploaded as an RGBA FLOAT texture with one texel per reference iteration (`.r = Re Z_m`, `.g = Im Z_m`, NEAREST + CLAMP_TO_EDGE). `MAX_TEXTURE_SIZE` (8192 here) bounds the width, so at the shipped 8192 cap the orbit is one texel short of `maxIter + 1` and the shader clamps `m`; below the cap the extra `Z_maxIter` is uploaded. The single `MANDEBROT` lane is chosen at COMPILE time by `OES_texture_float` (two linked programs) and at DRAW time by the SAME scale constant as the shipped GPU zoom cap (`ITER_BUDGET_MIN_SCALE`, the one D2 pin 2 holds equal to `__fv.minScale`).** | The committed probe measured the transport EXACT here (NEAREST round-trips 53 significant bits; NPOT fine; 8192-wide fine), so transport is not the problem and one float32 word per component is sufficient — a hi/lo split measured INERT under a float32 delta (probe arms A and D bit-identical). "Once per view" is counted rather than inferred: pin 2 measures five re-draws of one view building **0** orbits and each centre/budget change building exactly **1**; control (the key check deleted, restored byte-identically) turns it RED with **5** rebuilds. The depth lane is gated because measurement, not taste, demanded it: with the perturbation lane ALWAYS on, the full gate went from **2.9 min to 5.9 min** (the startup animation alone draws ~60 full-window frames, each now paying a texture fetch per iteration); with the lane starting at the scale the zoom cap already owns, shallow rendering is the byte-for-byte pre-P1 shader and the P1 pins drive the deep lane through `setDeepView`, which lifts only the cap (P5's job). | hi/lo orbit transport (measured inert under a float32 delta); a uniform array instead of a texture (GLSL ES 1.00 forbids dynamic uniform-array indexing in a fragment shader); a runtime lane switch inside one shader (a fifth `for` loop would break the S3/D1 pins that hold four loop sites to `MAX_ITER`); recomputing the orbit per DRAW (that is the defect pin 2 exists to catch); always-on perturbation (measured to double the suite's wall time for no shallow-zoom accuracy the plain lane does not already have). | Writer, `session-p1-perturb-core`, 2026-09-21, from the brief's items 1-2 and the committed probe's transport measurements. |
| 37 | **P1 FINDING (the slice's blocker): the brief's pin 3 — "a zoom sweep across every rebase shows no spike … count must be 0 at every step" — is NOT met at any depth, and the criterion as written cannot decide this slice. Measured with the reference-based hop classification on the SHIPPED shader at a fixed budget of 7680 and a 0.1 %-per-step scale sweep: 1-4 hopping pixels per step at 1e-8, 2-8 at 1e-10, 1-6 at 1e-12 and 7-16 at 1e-13 (0.07-0.15 % of the 10 500 sub-iteration pixels), each a pixel whose float64 reference escape value moved < 0.5 iterations while the GPU colour moved 3-62 RGB units. The hops are not rebase-specific and are not removed by more precision: the rebase-disabled build scores 0-1 hops — it looks SMOOTHER only because it has collapsed to 6 distinct colours (mean step-to-step change 0.90 against the correct build's 9.8), i.e. the metric passes the broken build vacuously.** | The plan's rule 1 was designed for D1, where the value compared across steps is the SAME renderer's own escape value; applied to the GPU through an independent float64 reference it measures per-pixel ACCURACY at chaotic near-boundary pixels, not smoothness. Those pixels' float32 delta error is already known (mean Δ 6.6 iterations, max 2656 against the float64 perturbation reference), and a 0.1 % view change can legitimately flip such a pixel's escape count by O(100); the correct-build hops are that, not a rebase pop. Lowering the tolerance or using the GPU's own packed value would make the count 0 while also making it GREEN on the rebase-disabled control, which is the "pin asserted the data rather than the user-visible effect" failure the brief names. The honest statement is therefore: P1 reaches 1e15 with 0 % misclassification and a 0.45-unit mean colour error (pins 1-3, all with controls), and it does NOT reach zero hopping pixels; the zero-hop target belongs with the slice that adds new references (P3) or an arbitrary-precision orbit (P2), which is where the plan's own artifact list puts it. | Shipping a nonzero hop threshold (that is massaging a threshold to make the brief's 0 pass — forbidden); shipping the pin at 0 (a red suite, which must be fixed forward immediately); using the lazy "GPU's own value" formulation (measured non-discriminating: a continuous palette makes it 0 on every build, including the broken one); reporting LANDED with the sweep omitted (the parent would not know the requirement failed). | Writer, `session-p1-perturb-core`, 2026-09-21, from the brief's pin 3 and the writer's own sweeps (`/tmp/p1-probe/sweep-check.cjs`) with and without rebasing. |
| 38 | **P2: the reference orbit is computed at ARBITRARY precision in BigInt fixed point, with the working precision `bits = max(64, ceil(log2(1/scale)) + 64)` QUANTISED UP to a multiple of `BIGORBIT_STEP_BITS = 64` (`public/bigOrbit.js`). The MARGIN is 64 bits — rust-fractal-core's `precision = max(64, -radius.exponent + 64)`, chosen over the bare bound's 0 — and it is MEASURED sufficient rather than assumed.** | The research (`docs/RESEARCH-2026-09-21-arbitrary-depth.md` §1) settles the SHAPE (3.3219 bits per decimal digit of zoom) but not the margin: 0 in the bare bound, +64 in rust-fractal-core, implicit in KF/F3's type ladders. The margin covers the reference orbit's own fixed-point rounding, which the orbit's chaotic dynamics amplify, so it cannot be argued from the bare bound. Measured at 1e-30 with the exact centre `-1.4303576324512 + 2^-70` (below that centre's float64 ULP): the shipped setting produces a GPU frame matching a DIRECT per-pixel BigInt reference computed 256 bits WIDER with **mean \|Δ escape index\| 0.007 and 0 misclassification** — the residual worst pixel (23 iterations) is the float32 DELTA arithmetic (row 37), not the orbit. A deliberately starved setting (`bits = log2(1/scale) - 96`, F = 84) measures mean \|Δ\| **12.9**, so the margin is not vacuous. Anchors of the rule: 1e-19 -> 128, 1e-20 -> 192, 1e-30 -> 192, 1e-38 -> 192, 1e-39 -> 256. | The bare bound with margin 0 (measured to work at 1e-30 here, but it leaves the orbit's amplified rounding with zero headroom and is the one value the sources explicitly do NOT standardise); a per-frame unquantised precision (the step would move on every zoom nudge, so the orbit key would almost never hit); a fixed 64/128-bit precision (that is the float64 wall this slice removes). | Writer, `session-p2-bigint-orbit`, 2026-09-21, from the brief's item 2 and the writer's own margin sweep. |
| 39 | **P2: the orbit reaches the shader as ONE float32 word per component, one texel per iteration — P1's existing transport, unchanged — and that is a MEASURED decision. The binding precision wall at this reach is NOT the orbit's mantissa but the float32 RANGE of the per-pixel coordinate: the shader forms `dc = (offset) * u_scale` in float32, so below float32's smallest normal (~1.175e-38) the product underflows/flushes and the whole frame collapses to the inside sentinel.** | The brief names the transport as the central engineering question and lists more channels / a second texture / an exponent split. The measurement says the first three are not what limits this reach: the float32-transported BigInt orbit matches the independent direct-BigInt reference with 0.00000 % misclassification and mean \|Δ escape index\| 0.007 at 1e-30 (pin 1), and P1 already measured hi/lo INERT under a float32 delta (row 36; the probe's arms A and D were bit-identical, because `float(hi+lo)` returns `hi`). The real wall was located by sweeping the scale through the REAL shader with the same orbit: **42 distinct escape indices at 1e-20, 39 at 1e-30, 17 at 5e-38, and 1 (the entire frame on the inside sentinel) at 2e-38, 1.5e-38, 1.2e-38 and 1e-38** — i.e. the frame dies between 5e-38 and 2e-38, at float32's smallest normal, NOT at the orbit's 192-bit working precision. Depth reached and verified: **1e-30 correct** (pin 1); the frame is still structured at 5e-38; the ceiling is ~2e-38. Passing it needs the research's per-iteration scale/exponent transport ("float32 + shared exponent", the RESCALING half of §3) applied to `u_scale`/`dc`, which is a RANGE mechanism, not more mantissa — deliberately NOT built here, because the slice's pin is "beyond the P1 wall" and the slice must not fabricate reach. | Shipping a hi/lo or 4-float-per-component orbit to "reach deeper" (measured inert under a float32 delta and not the limit; it would add texture bandwidth and GLSL surface for no measured depth); a separate texture per precision level (P1's lane already re-uploads one texture per view; a second level has no measured benefit); claiming the 1e-45 emulation reach (a `Math.fround` emulation survives to 1e-40 because JS keeps subnormals, but the REAL ANGLE/SwiftShader fragment stage flushes them — the emulation was superseded by the browser measurement, and only browser numbers are claimed). | Writer, `session-p2-bigint-orbit`, 2026-09-21, from the brief's item 3 and the writer's transport/range sweeps. |
| 40 | **P2: the precision STEP is invisible by construction AND pinned differentially. Working precision is quantised UP to 64-bit steps, so a step change moves the exact orbit by ~2^-bits while the transport is float32 (2^-24) — the transported words are bit-identical, so the step can change nothing on screen. The pin renders ONE fixed view on both sides of the 128 -> 192 step at scale 1e-20 through the real Worker path and requires the escape-index and colour frames to be IDENTICAL (measured: colour mean 0.0000, escape-index mean 0.0000). The failing baseline is IN-PIN: the only documented literature defect in this class is Kalles Fraktaler 2.13.10 (2018) "corrupt image at transition between number types", injected by making the higher-precision side parse its centre at a COARSER number type (32 bits), which measures escape-index mean 11.69, 98.4 % of pixels differing, worst 86.** | The owner's hard requirement is that a hop never shows, and the research names the precision/number-type transition as the under-documented risk class with exactly one known defect. A differential at one fixed view is the only formulation that isolates the transition from the view change itself (two different scales would differ legitimately). The reference is keyed on `(exact centre, budget, bits)` and NOT on the scale, so a scale change within a step reuses the orbit (measured: 0 rebuilds) while crossing a step rebuilds exactly once — that is what makes the step a real, countable event rather than a per-frame accident. | Asserting the step from a picture or from a tolerance on two DIFFERENT scales (which cannot separate the precision change from the view change); a control that removes a different mechanism; leaving the KF-class corruption untested (the pinned failure would then be an assumption, not a measurement). | Writer, `session-p2-bigint-orbit`, 2026-09-21, from the brief's item 5 and the writer's control runs. **Sweep status, stated so it is not quietly dropped:** `docs/PLAN-DEEPZOOM.md` §3's strict zoom-sweep criterion is RE-ATTEMPTED here only at the PRECISION STEP, as the differential above (0 moved values across the step, with the KF-class baseline in-pin). The full zoom sweep across every REBASE remains P3's: this slice changes the ORBIT's precision, not the rebasing (unchanged from P1), and P1 measured that sweep non-discriminating for the rebasing mechanism (row 37). |
| 41 | **P2: the orbit is computed OFF the main thread in ONE long-lived CLASSIC Worker (`public/orbitWorker.js` + `importScripts('bigOrbit.js')`), ONCE PER VIEW keyed on `(exact centre string, budget, bits)`, and the orbit is transferred back as the float32 texture words. The main thread never blocks: while a request is in flight the draw falls back to the existing float64 lane, so the canvas is never left blank, and the Worker's ready message re-renders through the REAL `renderWebGL` path.** | `public/app.js` already loaded its CPU renderer from a classic Worker (S2) and `public/fractalKernel.js` is the no-import/no-export pattern that lets ONE source serve both the worker and the ES-module main thread (row 14); the BigInt module uses the same pattern. "Once per view" is COUNTED, not inferred: five re-draws of one view post **0** requests and complete **0** builds; a centre change completes exactly **1**; a scale change inside one precision step reuses the orbit (**0**); crossing a precision step completes exactly **1**; and exactly **one** Worker is spawned for the renderer's life and terminated in `destroy()`. The fallback matters because a deep draw must never wait on a message before showing anything. | Computing the orbit on the main thread (a 192-bit, 8192-iteration BigInt orbit blocks the interaction loop, and the owner's standing requirement is that zoom stays smooth); a Worker per request/view (measured contract is one long-lived Worker; `bigOrbitWorkerSpawns === 1`); returning the orbit as BigInt to the main thread (not transferable and not needed — the shader wants the float32 words; the independent reference is computed in the test from the centre string); posting a request per draw (the counters pin this RED: control 2 posts 5 for 5 redraws). | Writer, `session-p2-bigint-orbit`, 2026-09-21, from the brief's item 1 and the writer's counted control runs. |
| 42 | **P2 FINDING (the measured ceiling): arbitrary-precision ORBIT precision is not what stops depth in this design — float32 RANGE is. The BigInt orbit is correct and the transport is sufficient through the verified 1e-30 and the structured 5e-38; the real shader's `u_scale`/`dc` product dies at ~2e-38 (float32's smallest normal, subnormals flushed), which is 24+ orders of magnitude beyond P1's measured ~1e16 coordinate wall but still not "arbitrary". The mechanism the research already names for it is the per-iteration scale/shared-exponent ("float32 + shared exponent", rescaling), applied to the coordinate rather than to the orbit mantle.** | The slice's pin is "depth beyond P1's wall (past ~1e16)" and it is met with a large margin; the brief also says to MEASURE where it now fails and why rather than assume. Reaching further was attempted by the honest route (transporting more orbit precision) and is measured not to be the limiter, so building it would have added surface without depth. The 1e-30 correctness, the 5e-38 structured frame and the 2e-38 collapse are the three measured points. **Also recorded: the brief's premise that "P1 packed hi/lo (about 48 bits) for both components into RGBA" is not true of the shipped P1 code — `ensureReferenceOrbit` stores one `Math.fround` word per component in `.r`/`.g`; the hi/lo experiment lived in the committed PROBE, and row 36 records it as INERT. The brief's premise that the orbit-transport MANTISSA is the central question is likewise contradicted by measurement.** | Claiming LANDED-with-arbitrary-depth on the emulation (the browser flushes subnormals and collapses); massaging the step-pin's view to a scale the shader cannot really render (that is what made an earlier version of pin 3 pass VACUOUSLY: at 1e-39 the shader produced one value on both sides of the step, so the differential proved nothing — caught by the writer before the full gate and moved to 1e-20, inside float32's normal range); treating the 2e-38 collapse as "the orbit needs more bits" (measured false). | Writer, `session-p2-bigint-orbit`, 2026-09-21, from the writer's scale sweep and the brief's item 4. |
| 43 | **WORKER-CANCEL: the fatal CPU-lane zoom banner is an ABORTED `importScripts`, and the fix has three parts: (1) a Worker announces it has finished loading with a `{type:'ready'}` message (`public/fractalWorker.js`) and is NEVER terminated before it arrives — a worker that is still loading and is superseded is simply KEPT and its pending job replaced, so no kernel fetch is ever aborted and a wheel storm reuses one worker instead of spawning one per tick; (2) the worker's `onerror` is no longer detached by `terminateWorker`, and it calls `preventDefault()` UNCONDITIONALLY before its guards, so even an aborted load cannot escape to the `window.onerror` net; (3) `sendProgressiveJob()` never posts to a worker that is not ready — the ready handler posts only the CURRENT job, so a superseded job never runs.** | **The firing site was PROVEN, not guessed** (the two earlier attempts failed because they assumed `w.onerror` was the banner source). Instrumented run with `fractalKernel.js` delayed 400 ms and 45 real wheel ticks: on `cancelJob` the just-spawned worker was killed at t=4545+133 ms while `importScripts` was in flight; the browser fired an `error` event on the Worker at t=4696 with `event.error === null`, `filename=fractalWorker.js`, `lineno=5`, `colno=1`, and `defaultPrevented=false` on BOTH the Worker event and the `window` event that immediately followed — i.e. `terminateWorker` had already set `w.onerror = null`, so nothing called `preventDefault()` and the event escaped to the global handler at `app.js:683`, which alone produces the `Error: <msg>` text with NO `(file:line)` suffix. Measured before: **44 worker error events, 44 window error events, 44 aborted `fractalKernel.js` requests, and the exact banner**; after: **0 / 0 / 0**. `Playwright pageerror` sees NONE of these (0 logged) — which is why the owner's DevTools console was empty while `#appMessage` was not. The abort window is load-time-dependent: 0 aborts at ~5 ms local load, 44 at 400 ms. | The first two attempts: deleting `cancelJob`'s eager `spinUpWorker()` (removes some aborts but the abort still escapes, and `startFractalCalculation` respawns anyway) and a `_fvSpoke` load-phase guard inside `w.onerror` (never runs — `terminateWorker` nulled the handler first). Both reverted as recorded on the board before this work. Also rejected: leaving `terminateWorker` to null `onerror` and relying on the abort being harmless (it is the banner); reusing a loading worker but still posting the superseded job to it (the worker would run both jobs back-to-back, undoing S2's "a superseded job must not keep burning CPU"); a worker pool (out of scope, `MODERNIZATION.md` §3). | Writer, `session-wc-worker-cancel` (`fix/worker-cancel`), 2026-09-21, from its own instrumented probe `/tmp/fv-wc-probe-before.json` / `/tmp/fv-wc-probe-after.log`. |
| 44 | **WORKER-CANCEL: a live worker failure SELF-HEALS — it is retired and the in-flight job is RETRIED on a fresh Worker, bounded at `MAX_WORKER_FAILURE_RETRIES = 2` (3 attempts per user action); the failure is reported as an ACTIONABLE retry message. A completed final frame resets the budget, so a healthy worker regains a full budget.** | The pre-fix failure path called `terminateWorker()` with no respawn, so the slot stayed empty and the canvas stayed frozen until the user changed the view — failure mode (b) of the WORKER-CANCEL row. The retry starts synchronously inside `onerror` and calls `startFractalCalculation` directly (not the `…WithTiming` wrapper, which resets the budget) so the bound is real; a worker that fails every load stops after 3 attempts with "change the view to retry". This CHANGES S2's worker-failure pin, which asserted the old terminal state (`liveWorkers() === 0`, generation unchanged): the pin is updated in the same commit to the new contract (a fresh generation exists, the live slot is occupied, the retried job reaches a final frame with no view change) and it was watched RED against the pre-fix build. The CANCELLATION pin from S2 is unchanged and stays green (a READY worker inside its kernel is still terminated outright). | Not retrying (the frozen canvas this row exists to remove); an unbounded retry loop on a worker that always fails; retrying by re-sending to the same failed worker (its script may never have loaded); resetting the retry budget on every retry (makes the bound infinite). | Writer, `session-wc-worker-cancel`, 2026-09-21; control arms `/tmp/fv-wc-arm-whole.log` (acceptance pin RED with the exact defect banner and aborted requests) and `/tmp/fv-wc-arm-selfheal.log` (self-heal pin RED: "change the view to retry"). |
| 45 | **WORKER-CANCEL does NOT make any claim in `MODERNIZATION.md` false.** `MODERNIZATION.md` §B6 says "cancel instead by `worker.terminate()` + respawn"; that is still exactly what a READY worker gets, and this change only qualifies the case where no job has been posted yet (there is nothing to cancel, so the worker is kept). §3's "single worker / per-pass buffer copies" and the worker-pool remainder are untouched, and §1 remains the pre-fix revision. No MODERNIZATION edit is owed. | Grep of MODERNIZATION.md for worker/cancel/abort/onerror claims, and a read of §B6 and §3 before landing. | Editing MODERNIZATION to "bless" a refinement it already permits (churn with no false claim to fix). | Writer, `session-wc-worker-cancel`, 2026-09-21. |
| 46 | **The CPU lane is not a performance fallback, and depth must never trigger a switch to it. The per-pixel work of a fractal render is embarrassing parallel (identical arithmetic, no inter-pixel communication), which is the exact shape a GPU is built for — so for pixels the GPU wins at every depth, and "deep zoom is slower on GPU" is false.** The CPU offer existed for a CORRECTNESS reason that no longer holds: before the perturbation lane, the GPU path was plain float32 and produced a visibly wrong image past ~1e4x, so CPU was the only correct renderer. P2's arbitrary-precision orbit removed that reason, leaving the offer as a fossil. | Where a CPU genuinely wins is the opposite shape — latency on small SERIAL work — and this architecture already uses it that way: the reference orbit is serial (each Z_{n+1} needs Z_n) and arbitrary-precision (BigInt in 64-bit limbs, whose carry propagation does not vectorise), so it belongs on the CPU, in a worker, exactly where it is. The rule is therefore **serial + wide-integer -> CPU; parallel + fixed-width -> GPU**, and deep zoom never moves a pixel between them. The owner's directive ("CPU only when there is no GPU") is not a preference but the correct engineering answer. | A measured GPU/CPU crossover (the writer's first instinct, and mine in the brief) — REJECTED by the owner explicitly; it would trade correctness and simplicity for a heuristic that cannot pay off, and it must not be reintroduced as a "smart" optimisation. Also rejected: any depth- or time-based automatic fallback. | Owner directive 2026-09-21, stated on the board as an INVARIANT (docs/STATE.md FORK-RESOLVED row). |
| 47 | **Two honest limits on the "GPU is always faster" claim, recorded so it is not oversold:** (1) a GPU wins on THROUGHPUT, not latency — for a shallow view the fixed costs (GL context, shader compile, orbit build + texture upload, JS/driver marshalling) can exceed the arithmetic, so the argument is for avoiding extra passes, never for switching engines; (2) **on this development host Chrome renders WebGL on SwiftShader (a software rasteriser), so every "GPU" timing taken here is a CPU-implementing-GLSL number and understates real hardware — report such numbers as software bounds, never as hardware performance.** A pathological device GL path is a device-quality problem to detect, not a depth threshold to trigger a switch. | Keeps the no-crossover invariant defensible: the exceptions are fixed-cost and device-quality, not depth-dependent, so they cannot justify a depth-triggered CPU offer. The manual post-checkbox is retained for exactly this case — user-chosen once, never automatic. | Treating the SwiftShader timings as hardware GPU performance; using the fixed-cost caveat to justify a crossover (it does not follow). | Dispatcher, 2026-09-21, answering the owner's question "is there EVER a point where CPU might be actually quicker?" |
| 48 | **NORTH STAR (owner, 2026-09-21): "I just want this to be the best it can be on no matter what machine. Thats the goal. Use what is available."** The renderer must be a *capability ladder*, not a two-way switch: dispatch is a pure function of what the machine actually exposes — WebGPU (compute) > WebGL2/WebGL1 + `OES_texture_float` (the perturbation lane) > WebGL without float textures (plain float32 loop, shallow) > CPU with WASM SIMD tiled across cores — and the app renders on the best tier PRESENT. **This is capability dispatch, not performance benchmarking: there is still no timing-based or depth-based crossover** (owner directive, row 46). "Best" means best-achievable on that machine, never a fixed tier. | Today the selection is a checkbox plus a failure flag: `app.js` has no `hardwareConcurrency`, no `WebAssembly`, no `navigator.gpu`, and `webglFractal.js` detects only `OES_texture_float` + `MAX_TEXTURE_SIZE`. The CPU path is one worker (row for CPU-PATH). So on a WebGPU machine we are leaving the better engine unused; on a GPU-less machine we are leaving 15 of 16 cores idle. Both are the same defect: no ladder. | Rejected: treating GPU as mandatory and CPU as a degraded afterthought; timing-based engine selection; assuming one engine is right for all hardware. | Owner directive 2026-09-21; see docs/STATE.md NORTH-STAR row. |
| 49 | **"Best on every machine" CANNOT mean "identical pixels on every machine" — and saying so is what keeps the goal honest.** Engines have different precision CEILINGS (a WebGPU/WebGL perturbation lane reaches ~1e-30+; a float32-only lane or a CPU lane without the perturbation path reaches far less), so the deepest depth a given machine renders CORRECTLY genuinely differs. The achievable guarantee is the half that actually matters to a user: **for the same view parameters, every engine renders the same colours from the same iteration counts — output is a function of (centre, scale, maxIter, scheme, offset), not of which engine ran it — and no engine silently degrades QUALITY as you zoom; where a machine cannot reach a depth correctly, it must refuse or say so rather than draw a wrong image.** Depth REACH varies by machine; the picture at a given depth does not. | The app already saves and shares locations (`memoryRepository.js`, import/export), so a location opened on two machines must look the same or the feature is a lie. S3's `tests/kernel-parity.spec.js` already pins CPU-vs-GPU agreement — but only at SHALLOW scale (mean |Δ| < 6, measured 0.911 against the half-pixel sampling mismatch). **At deep scale there is no such pin:** the CPU lane has no perturbation path at all, so at 1e-15 the two engines may legitimately disagree — which means "identical everywhere" is currently UNVERIFIED exactly where the owner cares most. That gap is recorded here rather than discovered later. | Assuming the shallow parity pins cover deep views (they do not); adding engines that each round differently and calling the result "the same"; shipping a per-machine quality difference as if it were a precision difference. | Dispatcher, 2026-09-21, from the owner's goal statement. |
| 50 | **NPU verdict: real hardware, wrong tool, and currently unreachable from a browser anyway — LOW priority, as the owner judged.** NPUs are fixed-function accelerators for quantized neural-network inference (INT8/INT4 matrix/tensor ops). Every defining property is wrong for escape-time fractal rendering: (1) the iteration is a SERIAL recurrence (`z_{n+1} = z_n^2 + c`), while NPUs are built for parallel tensor contraction; (2) deep zoom requires per-bit-correct extended-range arithmetic, while NPU throughput comes FROM low-precision quantization — the exact thing that destroys a deep frame; (3) the workload is branchy per pixel (escape test, glitch/rebasing control flow), which suits neither tensor ops nor ML operator graphs; (4) no web API exposes an NPU for general compute — WebNN surfaces ML OPERATORS, not a kernel language, so a fractal iteration cannot be expressed there without pretending it is a neural net. | Recorded so the question is settled with reasons instead of re-raised: the owner's own guess ("they probably cant be") is correct, and correct for structural reasons rather than a missing API. If an NPU ever exposes a general-purpose or high-precision integer path, this row is the thing to revisit — not the goal. | Pursuing NPU acceleration now; treating WebNN as a compute API. | Dispatcher, 2026-09-21, from the owner's remark. |
| 51 | **The WebGL lane is NOT made obsolete by WebGPU — it is the tier that covers the majority of machines WebGPU cannot reach, and dropping it would break ~1 in 8 visitors.** Measured 2026-09-21 from caniuse (August 2026 usage): WebGPU is **85.72% + 1.63% partial = ~87.35% global**. Concretely: Chrome 113+ / Edge 113+ / Opera 99+ / Samsung Internet 24+ supported; **Firefox is DISABLED BY DEFAULT at every version through 159** (i.e. all current Firefox users); Safari is only **partial** and iOS requires **26+**; Opera Mini, UC, QQ, Baidu, KaiOS and Android Browser have none. | **THIS HOST IS ITSELF AN EXAMPLE**, which is why the question was easy to answer: `navigator.gpu` is present in a secure context but `requestAdapter()` returns null by default, so WebGL runs and WebGPU does not (see the `MEASURED ... WebGPU availability` row). Machines that do WebGL but not WebGPU are therefore not hypothetical — they are common, and one is in this room. WebGL is also the universal floor: it works on hardware and OS versions that will never receive a WebGPU-capable browser. | Deleting or freezing the WebGL backend on the assumption that WebGPU "supersedes" it (caniuse's own summary line says "meant to supersede WebGL" — meant is not shipped). Reimplementing the CPU lane as WebGPU-only. | Dispatcher, 2026-09-21, answering the owner's question directly. Sources: https://caniuse.com/webgpu . |
| 52 | **Three backends is affordable ONLY IF the MATH is shared and exactly one copy of it exists — that is the architectural condition for the capability ladder.** The expensive parts of this project are not the drawing API; they are the reference orbit (BigInt, `public/bigOrbit.js`), the iteration budget rule (D2, `fractalKernel.js`), rebasing/glitch rules, the colour mapping, and the exponent-carrying delta formulation. Those must live in engine-neutral modules that every backend consumes, so a WebGPU backend is a new *scheduler* over shared math rather than a second implementation of it. `public/fractalKernel.js` already sets this precedent: it is deliberately dependency-free with no `import`/`export` so the classic Worker and the ES-module main thread load the SAME file (DECISIONS row 14) — the WebGPU backend must follow that pattern, not fork it. | The failure mode this prevents is concrete and has already happened here once: S3 existed because the app had "one fractal kernel in four copies" (MODERNIZATION B5/P1), and the parity pins that closed it are what keep CPU and GPU from drifting. Adding a third backend with its own copy of the arithmetic would recreate that defect at three times the size. The `docs/RENDERER-CONTRACT.md` deliverable of the WebGPU groundwork slice exists precisely to make the shared-vs-per-engine split explicit before any second implementation is written. | Three independent implementations of the fractal arithmetic; a WebGPU backend that re-derives the precision/iteration rules; letting the WebGPU path become the reference for the others. | Dispatcher, 2026-09-21. |
| 53 | **WEBGPU-GROUNDWORK: capability detection has THREE WebGPU states, never a boolean — `absent` / `api-present-no-adapter` / `adapter-available` — and `selectTier(report)` consults ONLY the AWAITED adapter verdict, never `navigator.gpu`.** `public/capabilities.js` publishes the status vocabulary, an awaited `detect()` whose `webgpu.status` comes from `requestAdapter()` (a rejection AND a null both become `api-present-no-adapter`, with the reason recorded), the adapter's own `info`, `isFallbackAdapter`, `features` and limits, and the rest of the ladder measured: `webgl2`, `webgl1`, `oesTextureFloat`, `maxTextureSize`, `hardwareConcurrency`, `wasmSimd` (by `WebAssembly.validate` on a real v128 module, never assumed), `sharedArrayBuffer`, `crossOriginIsolated`, `secureContext`. The file has NO `import`/`export` (the dual-consumption pattern, row 14, and exactly the shape row 52 requires of shared engine-neutral code); `public/capabilitiesModule.js` is the named-export ES-module face, mirroring `colorSchemes.js`. | The trap is MEASURED on this host, not hypothetical: `navigator.gpu` is PRESENT in a secure context and `requestAdapter()` returns **null** by default, so a naive `if (navigator.gpu)` selects a tier the machine cannot run; and on `about:blank` (`isSecureContext === false`) `navigator.gpu` is `undefined` ENTIRELY, which cost three probe attempts before it was spotted (STATE `MEASURED ... WebGPU availability`). The spec pins the semantics with SYNTHETIC reports whose ONLY difference is the verdict — `api-present-no-adapter` → `webgl-perturbation`, `adapter-available` → `webgpu`, nothing → `cpu` — so the pin is independent of this host. Measured here: normal config `hasNavigatorGpu=true` / `status=api-present-no-adapter`, `maxTextureSize=8192`, `hardwareConcurrency=12`, `wasmSimd=true`, `sharedArrayBuffer=false`; with `--enable-unsafe-webgpu --enable-features=Vulkan,WebGPU` `status=adapter-available`, vendor `google`, architecture `swiftshader`. | A boolean `hasWebGPU` (the defect this row exists to prevent); choosing the tier from API presence, a UA string or `hardwareConcurrency`; collapsing a REJECTED `requestAdapter()` into "absent" (the API IS present, and that distinction is the diagnostic); assuming WASM SIMD from the WASM version rather than validating a module. | Writer, `session-webgpu-groundwork`, 2026-09-21; `tests/webgpu-feasibility.spec.js` test 1, logs `/tmp/fv-webgpu-all2.log` and `/tmp/fv-webgpu-normalcfg.log`. |
| 54 | **WEBGPU-GROUNDWORK: the storage-buffer ceiling is REAL and is NOT the texture cap — on this host `maxStorageBufferBindingSize` = 134 217 728 B (128 MiB) ⇒ 16 777 216 orbit samples, against WebGL's 8192 texels (`webglFractal.js:116`, `maxOrbitWidth = min(MAX_TEXTURE_SIZE, MAX_ITER)`). A 20 001-entry `vec2<f32>` orbit buffer (160 008 B) was read by a compute pass at every index up to 20 000 and returned BYTE-FOR-BYTE (0 mismatches; index 8192 → `[8192, 4096]`, index 20 000 → `[20000, 10000]`).** `public/webgpuProbe.js` reads the device limits, uploads the long orbit, runs the perturbed iteration AND a ceiling pass that reads/writes back every entry, then reads pixels, escape indices and the max orbit index back. Device limits measured: `maxBufferSize` 268 435 456 B, `maxTextureDimension2D` 8192, `maxComputeWorkgroupStorageSize` 32 768, `maxComputeInvocationsPerWorkgroup` 256. | Reason 1 of the brief is exactly this: the WebGL orbit travels as a TEXTURE, so the orbit width is capped by a texture limit and at the shipped 8192 cap the orbit is one texel short of `maxIter + 1` (row 36). **Two honest limits are recorded with the number:** (1) a perturbation renderer samples the orbit only up to the REFERENCE ORBIT'S OWN escape, and rebasing resets that index — so at a normal boundary view (scale 1e-12, budget 20 000) the perturbed pass read only index ≈3015, and `escapedBeyond8192` measured **0** (max escape index 8172). The `>8192` claim therefore rests on the ceiling pass plus a perturbed run whose reference orbit does NOT escape (centre inside the main cardioid, measured max orbit index **20 000**) — not on an escape index. (2) **No performance claim:** on this host's SOFTWARE adapters (SwiftShader for BOTH lanes) the WebGPU compute pass measured ≈ **188–201 ms/frame vs WebGL ≈ 137–176 ms/frame** on the same view (160×120, scale 1e-8, cap 2048) — WebGPU was SLOWER in both runs; the spec asserts no timing relationship (row 47). | Claiming hardware performance from a SwiftShader number; claiming "WebGPU is faster"; using `escapedBeyond8192 > 0` as the proof that the perturbed pass addresses the long orbit (false in a rebasing renderer, measured); comparing the long orbit against itself instead of against a synthetic ramp whose exact values can be named (a 20 000-iteration boundary orbit is NaN past its escape, and byte-equality of NaN proves nothing a reader can check). | Writer, `session-webgpu-groundwork`, 2026-09-21; `tests/webgpu-feasibility.spec.js` test 2. |
| 55 | **WEBGPU-GROUNDWORK: the renderer contract is CLEANLY SATISFIABLE by a WebGPU backend but is NOT satisfied yet, and it has TWO shapes — a synchronous GPU `draw` and an ASYNCHRONOUS CPU frame pipeline — so the ladder must dispatch on "request a frame", not "draw".** `docs/RENDERER-CONTRACT.md` states C1–C9 with `file:line` anchors plus a per-backend satisfaction matrix. The CPU backend produces a SEQUENCE of frames for one job (one full-size `Float32Array` of smooth escape values with `NaN` for uncalculated cells, one frame per grid step `8→4→2→1`, `fractalWorker.js:31-72`), applied through `viewer.setData(array, JOB cap)` (`app.js:887`); the WebGL backend is ONE synchronous full-resolution pass (`webglFractal.js:742-744`, `app.js:1353-1390`). The matrix marks WebGPU as satisfying C1–C9 only in part: device limits (C2) and a real frame (C3-ish) are proven, but it has no canvas binding, no progressive frames, no `device.lost` path, no `window.__fv` observables and no shared orbit cache. | Every element the ladder needs is ALREADY declared in the code and was read, not invented: construction needs an ATTACHED canvas (`webglFractal.js:86-91`); sizing is CSS × devicePixelRatio in device pixels (`app.js:1105-1126`); the draw signature and its type/Julia/cap conventions are `webglFractal.js:742-881` with the call site `app.js:1372-1381`; the orbit is keyed `(centre, budget)` for float64 (`webglFractal.js:590`) and `(exact centre, budget, bits)` for the BigInt orbit (`:650`) with a REQUIRED float64 fallback while a request is in flight (`:824-836`); failure has ONE exit that must leave a REAL CPU render (`app.js:1320-1351`) with `preventDefault` and an active-canvas guard (`:1433-1445`); `destroy()` is idempotent and releases the context (`webglFractal.js:883-932`); observability is mandatory because every pin reads a counted fact (`window.__fvLiveWebglRenderers`, `webglFractal.js:16-20`). The gaps the wiring slice owns are listed in the contract's §5 (dispatch seam, backend object, shared orbit cache, progressive GPU frames, `__fv` equivalents). | Treating the GPU's synchronous `draw` as THE interface (it makes the CPU lane second-class and hides the progressive-frame requirement, which the owner's `FORK-RESOLVED` row demands); declaring WebGPU "done" from the probe's byte-exact ceiling result — the contract's §2 says the probe is an experiment, not a backend; wiring dispatch in THIS slice, which is explicitly later (and `app.js`/`webglFractal.js` are held by another writer). | Writer, `session-webgpu-groundwork`, 2026-09-21, from `public/webglFractal.js`, `public/fractalViewer.js`, `public/fractalWorker.js`, `public/app.js`. |
| 56 | **WEBGPU-GROUNDWORK: the WebGPU experiment runs under a DEDICATED `/tmp` Playwright config, and under the NORMAL project config its compute tests SKIP with a recorded reason — they never fail.** `/tmp/fv-webgpu.config.js` reuses the repo config's settings but sets `testDir` to this worktree, `testMatch` to the one spec, `baseURL` to a `/tmp` static server on :3011 (so the project's :3000 and the gate's suite lock are untouched), and `launchOptions.args = ['--enable-unsafe-webgpu', '--enable-features=Vulkan,WebGPU']`. `playwright.config.js` is NOT modified. | The gate must stay honest in BOTH worlds. Measured: with the flags, **3/3 pass** and an adapter is granted; under the normal project config this host reports `api-present-no-adapter`, test 1 passes (the detector semantics run everywhere) and tests 2–3 are `skipped` with the reason recorded as a Playwright `skip` annotation naming the status, the reason and the exact config/flags needed — the run exits **0**, and the skip is verified from the JSON reporter rather than assumed. A spec that FAILED here would make every gate on a GPU-less machine red for a capability the machine does not have, which invites the next writer to weaken the experiment; a spec that silently passed would be worse than either. | Editing `playwright.config.js` (forbidden by this slice's scope, and it would grant a software adapter to the WHOLE suite, changing what the existing WebGL pins measure); a hand-rolled `chrome --headless` invocation outside Playwright (no gate vocabulary, no recorded skip reason); asserting a timing relationship so the measurement could flake. | Writer, `session-webgpu-groundwork`, 2026-09-21; `/tmp/fv-webgpu-all2.log` (flagged, 3 passed) and `/tmp/fv-webgpu-normalcfg.log` + `/tmp/fv-webgpu-normalcfg.json` (normal config, 1 passed / 2 skipped with the annotation). |
| 57 | **GPU-ARBITRARY: the float32 delta-coordinate RANGE wall is removed by a UNIFORM SPLIT, not a new algorithm. The deep lane seeds its per-pixel delta in float32's NORMAL range and carries the scale's exponent in the rescaled representation's own `S`: `u_scale <- scale * 2^shift`, `u_scaleShift <- shift`, and the shader starts at `S = exp2(-u_scaleShift)`.** The physical per-pixel offset is unchanged (`S * dc` is EXACTLY what the pre-fix code computed, because `2^shift * 2^-shift === 1`), while neither the seeded delta nor `S` is ever subnormal; the loop's EXISTING rescaling then walks `S` from `2^-shift` toward `|z|` exactly as before. Rebasing, the glitch detector and the S-normalisation are untouched. With `shift == 0` the expressions are byte-for-byte the pre-change ones, which is why the shallow lane is unchanged. | **THE MECHANISM WAS CHOSEN BY MEASUREMENT, not by plausibility.** The defect is one float32 uniform: `dcx = (v_uv.x - 0.5) * u_scale * u_aspect` underflows past ~2e-38, so every pixel lands on the reference point before any rescaling can act. Measured through the REAL render path at 160x120 against an independent per-pixel BigInt iteration (the P2 reference): 1e-30 and 1e-35 match at **0.00000 % misclassified / mean |Δ escape index| 0.0**, 1e-37 begins to break (**23.3 % misclassified**), 2e-38 is **100 % misclassified with ONE distinct value**, and everything deeper is one value — i.e. the frame dies at float32's smallest normal, NOT at the orbit's precision (P2's own wall, row 42). With the split: **1e-30 0.00 %, 1e-35 0.00 %, 1e-40 0.00 % (mean 0.0, worst 16 at 128x96)**, and the SAME view through the pre-fix seed is 100 % misclassified with 1 value. **A per-pixel-exponent variant (a full boxed `S`, and a two-factor `S = S1*S2`) was implemented and measured WORSE and is NOT shipped**: the two-factor form's quadratic term `(S1*w)*(S2*w)` double-rounds, turning the exact 1e-40 result into **32 % misclassification** and degrading 1e-30 from 0.0 to **mean 2109** against the legacy seed's 0.613. At 1e-42 and beyond the single-factor form degrades on its own (7.4 % at 1e-42, 5.0 % at 1e-45, and the shift saturates at 120 past ~1e-57); those are its measured limits and they are stated, not hidden. | The route the brief named as candidate (a) — `u_scale` as (mantissa, exponent) with `dc` assembled as `mantissa * exp2(exponent)` — was NOT taken: measured, it needs BOTH the scaled uniform and `S` normal at once, and past ~1e-42 their exponents cannot both be, so it collapses at the SAME wall with extra surface. The two-factor scale was rejected on the numbers above. A CPU fallback at any depth was rejected by the owner's invariant (rows 46-47) and is not in the code. | Writer, `session-gpu-arbitrary`, 2026-09-21, from the mechanism sweep in `/tmp/fv-arb/` (logs `arb1.log`/`reach3.log`) and the standalone float32 simulation of the shipped loop (`mech5.js`). |
| 58 | **GPU-ARBITRARY: `WEBGL_ZOOM_CAP` is decoupled from precision and set from the measured COST curve: `1e6` (scale `1e-6`), against `1e4` before. Measured ms per FULL image at 640x480 through the readout (ANGLE + SwiftShader SOFTWARE rasteriser, so these are software numbers, not hardware ones): 1e-4 -> 14 ms, 1e-8 -> 106 ms, 1e-12 -> 150 ms, 1e-16 -> 211 ms, 1e-20 -> 285 ms, 1e-24 -> 356 ms, 1e-28 -> 429 ms, 1e-32 -> 501 ms, 1e-36 -> 566 ms, 1e-40 -> 640 ms.** `1e6` is where a full image is still comfortably under a tenth of a second for a user pressing the wheel, and it is **100x past the old cap**; the D2 budget floor inside it stays at its knee's own 512 (`ITER_BUDGET_MIN_SCALE = 1e-4`), so the startup animation and shallow rendering are as cheap as they were. | The deliverable requires the cap to be "whatever your MEASURED reach supports" and forbids it from being the thing that stops zoom at 1e4. The precision wall is gone (row 48) and verified to 1e-40, so the only remaining bound is TIME — and the cost curve above is flat-ish per decade, so a cap at the far end (1e-40) would cost **~0.64 s per image on this host even at 640x480** (and seconds at full window). Setting it at the measured cost knee is the honest reading of "performance-bound". **The depth where the range wall used to be (2e-38) is no longer special in any way**: it is not the cap, not a lane boundary, and the deep-view path renders correctly through it. | A cap at 1e-20 or 1e-40 (measured, correct, but 285-640 ms per image on a software rasteriser and seconds on a full window, with no user benefit the interaction can reach — the wheel's own step lands around 2e-5 from the default view); leaving the cap at 1e4 and relying on the D2 floor (that is exactly the "cap has to do with standard lib capabilities" the owner rejected). | Writer, `session-gpu-arbitrary`, 2026-09-21; perf log `/tmp/fv-arb/perf3.log`, readout asserted by `tests/gpu-arbitrary.spec.js` pin 2. |
| 59 | **GPU-ARBITRARY: the per-full-image render time is USER-VISIBLE and OBSERVABLE, and its measurement is a ONE-PIXEL readback sync inside `draw`. The readout is the `#renderTime` element (already in the page), the observable is `__fv.renderTimeMs()`/`renderTimeText()`/`fullImagePasses()`.** The sync is chosen by measurement: a wall clock around `drawArrays` alone reads **0.0 ms at every depth** (WebGL queues and returns), and `gl.finish()` does NOT wait on this host's ANGLE/SwiftShader (measured **0.2 ms against a true 288 ms image**). A 1x1 `readPixels` DOES wait, and costs the image itself — measured median frame **243.5 ms with it against 226.7 ms without** on a 1280x800 animated zoom, i.e. ~7 % over an image that already takes ~227 ms. That is affordable for a per-IMAGE (not per-frame) readout, so the number the user sees is the real cost of the image they are looking at. | The owner asked for "the rendering time per full image" and the deliverable requires it to reflect a FULL image, not a partial pass. The renderer issues exactly ONE full-frame `drawArrays` per image (no tiling, no partial pass), and `fullImagePasses` advances by exactly one per image, so "the number covers a full image" is COUNTED rather than asserted; pin 2 checks both. | `gl.finish()` (measured NOT to sync here, so it would have shipped a readout that always reads 0.0 ms); a wall clock without any sync (same failure); the full-frame readback the hooks use (accurate but transfers a megabyte per image and is far slower than the ~7 % the 1x1 sync costs); the WebGL timer-query extensions (NOT PRESENT on this host — checked `getSupportedExtensions()`). | Writer, `session-gpu-arbitrary`, 2026-09-21; timing logs `/tmp/fv-arb/tim1.log`, `/tmp/fv-arb/tim2.log`, `/tmp/fv-arb/anim1.log`, `/tmp/fv-arb/perf3.log`. |
| 60 | **GPU-ARBITRARY: the automatic CPU-switch OFFER is REMOVED; the cap is reported as a brief non-modal `#appMessage` notice and the renderer never changes itself. The manual renderer checkbox and the genuine no-GPU path are untouched.** `#zoomCapOffer`, `#zoomCapSwitchToCpu` and `#zoomCapDismiss` are DELETED from `public/index.html`; `showZoomCapOffer`/`acceptCpuSwitchAtZoomCap`/`declineCpuSwitchAtZoomCap` are DELETED from `public/app.js`; `handleZoomLimitReached` only throttles a message (at most one per 2 s, and the throttle is inclusive so the FIRST capped tick always reports) and dispatches `fv-zoom-limit` with `prompted: false` on every tick. `__fv.zoomCapOffered()`/`zoomCapPrompted()` are kept as inert observables so a regression that reintroduced an ask would move them. | The owner's invariant is explicit: *"I do not want a GPU to cpu crossover if a GPU is present. If a GPU is present, complete depth needs to be calculated there, period."* A mode switch at the cap is therefore wrong, not merely optional, and the S6 offer (row 25) was the last remaining site. The CPU lane stays reachable ONLY as the genuine no-GPU path (WebGL absent/failed — S1's `handleWebGLFailure`/`handleWebGLLoss`) and through the manual checkbox the owner kept. Pinned by `tests/hygiene.spec.js` (the offer's markup is absent, nothing is offered or prompted, the renderer stays checked, the notice still appears) and `tests/zoom-clamp.spec.js` (`prompted` is false on EVERY capped tick). | Keeping the offer but making it rarer (still a mode switch, still forbidden); silently dropping the notice (removes the one piece of information the user gets); reusing `#zoomCapOffer` as the notice (it is a two-button decision surface, not a status line). | Writer, `session-gpu-arbitrary`, 2026-09-21, from the owner's verbatim directive (STATE row `FORK-RESOLVED 2026-09-21`). |
| 61 | **GPU-ARBITRARY FINDINGS, recorded because two of them are NOT what the brief assumed. (a) NO REGRESSION, proven byte-for-byte: the shallow plain lane is UNCHANGED. FNV-1a hashes of the raw RGBA readback at four shallow views (scale 3, 1e-4, 1e-8, 1.05e-4) are IDENTICAL between the PRISTINE pre-change build (base `ab59aaa`, restored by `git stash`) and the changed build, and two consecutive pre-fix captures are byte-identical to each other, so the comparison is deterministic rather than lucky. (b) The DEEP lane's own 1e-15 P1 pins are unaffected: `tests/perturb-core.spec.js` stays green, and at 1e-15 the shift is 0 for the same reason as (a). (c) The new deep path is CORRECT, not merely non-collapsed: at 1e-40 the GPU matches the independent direct-BigInt reference at 0.00000 % misclassified / mean |Δ escape index| 0.003 / worst 2 (128x96), while the pre-fix seed in the same program is 100 % misclassified with one value. (d) A THIRD finding the brief did not anticipate: with the cap lifted to 1e-6, the perturbation lane's boundary at 1e-4 is INSIDE the reachable range, and that boundary is a PROGRAM SWITCH whose step-to-step hop fraction measured 0.469 (13404 of 13404 sub-iteration pixels) at scale 6.7e-5. The hop sweep's subject is the COLOUR mapping, so its sweep was bounded above the boundary (`SWEEP_HI_SCALE = 1.2e-4`) and the switch is recorded here rather than silently measured as a hop; whether the switch should become invisible belongs with the coarse-to-fine/refinement slice, which must not be blocked by it. (e) The two-factor and boxed variants were measured and NOT shipped (see row 48).** | Each measurement is a control for a specific claim the slice makes, and (a) is the owner's own binding criterion. The hashes are in `tests/gpu-arbitrary.spec.js` pin 4 as the permanent pin; the 1e-15 result is P1's own pin. (d) is recorded because a green pin that hides a real boundary would be the exact "pin asserted the data, not the effect" failure the campaign already names twice. | Reporting "no regression" from the absence of failures rather than from a byte comparison; deleting P1's pins instead of leaving them green; moving the sweep's bound silently; claiming the lane switch is invisible without measuring it. | Writer, `session-gpu-arbitrary`, 2026-09-21; captures `/tmp/fv-arb/cap-base2.log` (pre-change) vs `/tmp/fv-arb/cap2-1.log`/`cap2-2.log` (post-change, twice). |
| 62 | **OWNER DIRECTIVE 2026-09-21 (binding, and it settles the cap question): "I do not want such boundaries, the math needs to work regardless by extending precision when needed. Do not ask me for exact borders, i am no mathematician. The goal is to get to arbitrary precision … and i accept that this gets slower the deeper it gets. But no hard stop."** Consequences: (1) **no hard stop and no precision-defined cap** — depth is bounded by TIME, which the user can feel, never by a number chosen from a library limit; (2) **no boundary in the MATH** — precision must extend *as needed*, continuously, so a depth must never be special because a constant flips there; (3) **the dispatcher must not ask the owner to choose constants** — that is engineering judgement, and asking it back is a failure to decide. | This is exactly why the `DEEP-LANE-SWITCH` defect is a defect rather than a tuning question: at `1e-4` TWO independent mechanisms fire together (`ITER_BUDGET_MIN_SCALE` starts D2's budget floor at 512→2058 — **corrected: I first wrote 50→2048 (41×), having misread the slider's `min="50"` attribute instead of `fractalViewer.js:33`'s `maxIterFloor = clampMaxIter(512)`; the real step is 4.02×**, and `webglFractal.js:945` swaps the whole shader program), producing a measured 31.6-unit colour jump in one zoom step. The math does not need a boundary there; the IMPLEMENTATION put one there, twice. The fix is to make both continuous — ramp the effective budget instead of switching a floor on, and choose the lane by whichever is better rather than by a scale constant — not to move the constant. | A cap chosen from a library's numeric range; a depth that is special because a constant flips; asking the owner for a border value; treating "it gets slower" as a reason to stop. | Owner directive 2026-09-21. |
| 63 | **CPU LANE — the probe's two reframing findings, both measured (`/tmp/fv-cpu-probe/REPORT.md`).** **(A) The CPU lane cannot render depth at all and needs a perturbation+orbit PORT, not an optimisation.** It projects every pixel in direct float64 (`fractalKernel.js:470`) with no orbit and no perturbation, so below ~1e-13 it paints a coordinate staircase (distinct x per 200 sampled columns: 200 @1e-12 → 26 @1e-14 → 3 @1e-15 → 1 from ~1e-17). The probe's own 1e-22/1e-30 timings are 700k copies of ONE point, not frames. **(B) The CPU lane is clamped to `scale >= 1e-4`** — `WEBGL_MIN_SCALE` is applied by ONE call on the SHARED viewer (`app.js:495`) and no production path ever nulls it, so **the old "switch to CPU for deeper zoom" offer could never have delivered deeper zoom.** That retroactively explains why that offer was always a lie, and it means CPU deep zoom is a FEATURE, not a speed-up. | The probe also FALSIFIED a plan premise and corrected a plan fact: **in THIS kernel's formulation, the branchless rewrite did not pay and V8 never auto-vectorised it** (branchless fixed-count 0.72–1.21×; `--print-opt-code` shows ZERO packed-double and ZERO ymm — V8 never auto-vectorised; hand-written wasm f64x2 SIMD only 1.09–1.24× over scalar wasm). What DOES pay is **loop SHAPE — 2.4–2.7× on one core at every depth, bit-identical per pixel** (inline the per-pixel path; no `%`/`|0` division, no per-pixel object return) and **row-band tiling — 11.6× at N=12** (2.0×/4.0× at N=2/4, knee N≈4–6 for frames ≤1 s). And `MODERNIZATION.md` §3's "copies per pass" is wrong in detail: `job.prior` is ALWAYS NULL so `job.prior.slice()` is DEAD CODE, the `{...job, chunk}` spread is per LEVEL (4×) not per chunk, and the real overhead in that bucket is `levelIndices` (13.7 ms/frame) — total ~19.2 ms/frame = 7.9 % shallow but **0.2 % at depth**. The BigInt orbit is 3–7 ms once per view = **0.02–0.05 % of one deep CPU frame**; per-pixel work is >99.95 % of at-depth cost. | Prioritising SIMD/branchless (measured not to pay); treating the copy fixes as a depth win (they are shallow-only); assuming the CPU lane deep-zooms today. Probe caveats, recorded: the run overlapped a GPU gate (loadavg 5–20 on 12 CPUs) so absolute ms are inflated and short-frame tiling numbers are lower bounds; Node `worker_threads` are not browser Web Workers; and the probe documented three of its own harness bugs rather than passing them off as findings. | Read-only probe `dfff36bb`, 2026-09-21, repo untouched. |
| 64 | **CORRECTION to rows 62/63 and to the "accuracy wall" framing, forced by the owner: "The arithmetic only degrades if you use the same functions. You can get arbitrarily precise with other math libraries." The 1e-42 degradation is OUR CURRENT SOLVER'S LIMIT, not a numerical floor, and must never be recorded or spoken of as one.** The distinction that matters: **precision is MANTISSA WIDTH; underflow was RANGE.** The shipped mechanism (rows 57–61) extended the *exponent* — the `u_scaleShift` split — so the delta is no longer subnormal; it did **nothing** to the mantissa. The accuracy loss at depth is the float32 delta mantissa (24 bits) accumulating relative error, and the known remedy is **more mantissa**, not more exponent. | This is not speculation — **our own record already measured a worked example and then abandoned it**: DECISIONS 37 recorded compensated float32 (hi+lo) holding to **>=1e10** with **0.000% misclassification** (mean |dRGB| 0.31/0.42/0.99 at 1e8/1e9/1e10), at a cost of **10.4-12.5x fragment work**, and the fork note says that route was **"deliberately NOT built."** So "the arithmetic degrades at 1e-42" is a statement about a solver that uses ONE float32 for the delta instead of two (or four, or k). Cost grows with k; precision grows with k. **Slower, never stopped** \u2014 which is exactly the owner's stated acceptance. | Recording our own solver choice as a property of the mathematics; telling the owner a depth is a floor when it is a branch we did not take; treating "GPU has no arbitrary-precision library" as the end of the discussion \u2014 the GPU route is TECHNIQUES (multi-component / compensated arithmetic, a mantissa ladder), not a package, and it is a real, measured-in-this-repo option. | Owner correction 2026-09-21. Supersedes the "both must be stated" framing in row 62 and the plan's "do not claim arbitrary depth past the measured reach" phrasing: the honest statement is "past the reach of the CURRENTLY-BUILT solver, the next thing to build is a wider mantissa." |
| 65 | **What "arbitrary precision" actually means PER LANE, stated so the word stops being ambiguous (owner asked for it repeatedly and we kept answering with a cap).** **(a) CPU lane: genuinely arbitrary precision is AVAILABLE TODAY** \u2014 `public/bigOrbit.js` is BigInt fixed point with `bits = max(64, ceil(log2(1/scale)) + 64)`, and MPFR/GMP-class libraries are exactly the "other math libraries" the owner means. Bound = memory and time, not mantissa. **(b) GPU lane: no library exists and none can be imported into a shader** \u2014 GLSL ES has no `double` in any version \u2014 so precision there comes from TECHNIQUES: the exponent split (shipped), compensated/multi-component mantissa (measured but unbuilt), and a mantissa ladder (k components -> ~k*24 bits, cost ~k). Bound = the same time/memory tradeoff, never a numeric wall. **(c) The GPU already renders 24+ orders deeper than the old cap** and is correct at 1e-40; its next depth step is a BUILD, not a discovery. | Consequence for scheduling: **"what limits depth" has three different answers that were being conflated** \u2014 (i) the app's cap, which row 62 says must go; (ii) the RANGE of the delta, which the shipped split already fixed; (iii) the MANTISSA of the delta, which is the thing to widen next and which no constant can fix. Any future record about depth must say WHICH of the three it means. | Saying "the math degrades" without naming the solver; answering "how deep can it go" with a number that is an app constant; assuming the CPU lane deep-zooms today (it cannot \u2014 DECISIONS 63: it has no orbit and is clamped to scale >= 1e-4). | Owner correction 2026-09-21, with the per-lane facts measured in this repo. |
| 66 | **OWNER DECISION: "Arbitrary it is. Take the best fit for that." The chosen route is a k-COMPONENT EXPANSION LADDER whose component count GROWS WITH DEPTH — not any single fixed-precision library.** The reasoning follows from a property we already ship: the reference orbit's precision is already depth-dependent (`bigOrbit.js`: `bits = max(64, ceil(log2(1/scale)) + 64)`). Delta precision must track it, because the perturbed sum `Z + z` must resolve the pixel scale, so the required significand also grows with `log(1/scale)`. Therefore a FIXED component count is merely a bigger wall — double-single (~48 bits) would move our 1e-42 limit to roughly 1e-47 and stop — whereas a ladder makes depth a function of time, which is exactly the owner's stated acceptance ("slower the deeper it gets … but no hard stop"). | **THE BUILD, in order:** (1) **GLSL first, WGSL later and only integer-controlled** — WGSL permits reassociation and fusion and fixes no rounding direction, so the classic error-free transforms are NOT portable there (a `TwoSum` low term is algebraically zero over the reals and a reassociating compiler erases it); GLSL has no such rule, which is why luma.gl's full `fp64` library is GLSL-only; (2) **k=2 (double-single) as the first rung** — already MEASURED in this repo at ~48 bits and 10.4–12.5× fragment cost (DECISIONS 37), so it is the cheapest rung with a known price; (3) **parameterise the representation in k** so k=4/8 is a constant change rather than a rewrite — that parameterisation is what makes it a ladder instead of a new cap; (4) the orbit stays CPU BigInt and arbitrary (rows 64–65); (5) only the DELTA needs expansion, since it only ever has to be small (research §3). **Prior art to build FROM, not re-derive:** luma.gl `fp64arithmetic` (Møller–Knuth `TwoSum`, Dekker split/`TwoProd`, renormalisation), `glsl-arbitrary-precision`, `glsl-arb-prec` (OpenGL **ES**). | **THE CERTIFICATION LIMIT, stated up front so it cannot be discovered as a surprise:** this host runs SwiftShader for WebGL *and* WebGPU, and hardware validation is required to show that residual terms survive a real compiler and driver (a value can look plausible after the expansion silently collapses to f32). So our pins must compare against an independent BigInt reference AND assert a **required-nonzero low component**, and the final "this survives real GPUs" claim needs hardware we do not have — it will be reported as unverified-on-hardware, not assumed. | Rejected: picking one fixed-precision library and calling the result arbitrary; a bigger constant cap; classic double-single on WGSL without integer control or per-adapter validation. | Owner 2026-09-21; prior art in `docs/RESEARCH-2026-09-21-arbitrary-depth.md` §7. |
| 67 | **"Arbitrary precision in both paths" requires TWO depth-scaling mechanisms, and we have neither fully. Naming them so no future attempt can build one and claim the other** (owner pressed five times; the dispatcher had been treating the delta as the whole job). **(a) THE ORBIT: already arbitrary ON THE CPU, but NOT on the GPU.** `bigOrbit.js` computes BigInt at `bits = max(64, ceil(log2(1/scale)) + 64)`, then `orbitWorker.js`/`webglFractal.js` transport and sample it as **one float32 per component** (~24 bits). So the reference is computed precisely and then quantised on the way to the shader — the reach past ~1e-30 is bounded by the DELTA arithmetic (P2 probe) but the orbit's own 24-bit transport is a second, independent cap that must be lifted (multi-component orbit words) before deep depths are trustworthy. **(b) THE DELTA: the k-component expansion ladder** (DECISIONS 66) — variable k with depth. **In addition (c) the CPU lane has NO perturbation and NO orbit at all** (probe: 0 orbit/perturb hits in `fractalWorker.js`), so "arbitrary precision in both paths" additionally requires PORTING the orbit+perturbation architecture to the CPU path. | Consequence: the honest completion criterion for "arbitrary in both paths" is a **depth-scaling precision stack** — CPU BigInt orbit (exists) -> multi-component ORBIT TRANSPORT (missing) -> multi-component DELTA on the GPU (missing) -> CPU lane port (missing). Each is separately dispatchable and separately measurable; none of them is a constant bump. | Treating the delta ladder as the whole job (the dispatcher's error); claiming arbitrary depth while the orbit is still quantised to 24 bits in transit; assuming the CPU lane deep-zooms. | Dispatcher, 2026-09-21, in answer to the owner's push. |
| 68 | **AUTONOMOUS MODE (owner, 2026-09-21): "Assume that i will be gone for a long time and build everything speculatively, to be adjusted later with results."** The dispatcher therefore proceeds WITHOUT further owner answers, on these rules: **(1) nothing waits for a missing answer** — a real fork is decided, recorded with the reasoning and the rejected option, and marked SPECULATIVE; **(2) prefer work that survives being wrong** — mechanisms go behind seams, so a hardware result can change a CHOICE rather than invalidate a build; **(3) never finalise a precision-dependent decision on SwiftShader evidence alone** — every such decision is labelled UNVERIFIED-ON-HARDWARE until the GPU-DIAGNOSTIC report arrives; **(4) the disqualifying rules do not relax**: no timing- or depth-based CPU fallback (row 46), no hard stop or precision-defined cap (row 62), the math must exist once and be shared between backends (row 52), and every landing still needs the dispatcher's own gate plus its own control arm before publish. | **THE BIGGEST SPECULATION IN THE TREE, stated so it is not mistaken for fact:** the shipped deep lane (rows 57–61) is correct at 1e-40 **on SwiftShader only**. Every "GPU" number and every correctness claim this project has for the deep lane is a SOFTWARE-rasteriser measurement (rows 47, 65). The GPU-DIAGNOSTIC battery exists to replace that with a real-hardware reading, and until it reports, the deep lane's correctness, the float-texture transport, and the float32 delta arithmetic are all **UNVERIFIED on hardware**. | Waiting for the owner to answer an engineering question; building a fixed-width precision choice as if settled when row 66 chose a depth-scaling ladder; treating SwiftShader results as hardware results; letting "speculative" become an excuse to skip the gate/control discipline. | Owner 2026-09-21; see the AUTONOMOUS-MODE row in docs/STATE.md. |
| 71 | **PRECISION-CHAIN AUDIT (writer 2e791076, `docs/PRECISION-CHAIN.md`) — headline: no precision mechanism is actually FAILING at 1e-30 or 1e-40, and MY ORBIT-TRANSPORT HYPOTHESIS WAS HALF WRONG.** I claimed the orbit's one-float32-per-component transport caps orbit precision at ~24 bits and is a depth limiter. Measured against an independent direct-BigInt reference: the shipped 24-bit transport gives **0.0000% misclassified, mean |Δ| 0.007–0.017 at 1e-20…1e-40**, and raising BigInt precision from 192 → 448 → 704 bits changes **0.0000% of pixels** — because the perturbation only ever samples the first ~364 orbit indices (worst transported-word divergence sits at index 612). So the 24-bit transport is confirmed as a WIDTH and **falsified as the DEPTH CAP**. What binds first is the **float32 DELTA mantissa** (incremental 0.003 @1e-30, 0.005 @1e-40; breakdown 7.4% @1e-42) \u2014 so DECISIONS 66's k-component ladder is correctly the next rung, and the orbit transport is the rung AFTER it. | **TWO FURTHER CORRECTIONS/FINDINGS, both mine to own:** (a) **RECORD CORRECTION \u2014 the CPU lane's clamp is `scale >= 1e-6`, not 1e-4** (DECISIONS 63 said 1e-4): `WEBGL_ZOOM_CAP` was lifted to 1e6 and ONE shared clamp serves both lanes, so the CPU lane can render 100x deeper than 63 claimed. The conclusion in 63 is unchanged \u2014 the CPU lane still has no orbit/perturbation and still cannot render depth \u2014 but the number was wrong and is corrected here. (b) **NEW GAP \u2014 THE DEEP LANE IS UNREACHABLE FROM THE SHIPPED UI.** `centerXExact`/`centerYExact` (the exact decimal centre that names a point past a float64's own ULP) has exactly ONE producer: the `__fv.setDeepView` TEST HOOK (`app.js:1620–1635`). Every production path stores the centre as a float64 double (`fractalViewer.js:25/165–173/215–226`, `app.js:80–82`, `memoryRepository.js:97`). I independently confirmed this by grepping the tree: `centerXExact` appears in production code only as a CONSUMER (`webglFractal.js:960,964`) and in the hook's own guard. **Consequence: a user who zooms by hand never triggers the BigInt orbit at all \u2014 the arbitrary-precision machinery we have been building is, today, reachable only from the console. Producing a serialisable exact centre is a PREREQUISITE for "arbitrary in both paths" and was NOT named in DECISIONS 67's three mechanisms.** | Ranked limiters at 1e-30 / 1e-40 (audit): app cap (24 / 34 decades shallow of the view, and now REMOVED) → float32 delta mantissa → 24-bit orbit transport. The CPU port prototype runs the shared `BigOrbit` and shared kernel colour/iteration rules, is correct at 1e-30/1e-40/1e-120 (0.00000% vs an independent reference, where the shipped lane is 2.78% misclassified with 19 distinct values on the same view), costs 118–251 ms/frame at 320x240 on ONE core, and is **GENUINELY ARBITRARY IN ORBIT BUT NOT IN DELTA** (its per-pixel delta is float64, so it degrades ~1e-300). | Treating the 24-bit transport as the current wall (my error); assuming the deep lane is user-reachable; quoting DECISIONS 63's 1e-4 clamp for the CPU lane. | Writer 2e791076, gated 1bda851, landing 5c80d70; UNVERIFIED-ON-HARDWARE (SwiftShader host). |
| 74 | **THE GPU-DIAGNOSTIC INSTRUMENT WORKS, AND ITS FIRST READING IS ALREADY A RESULT (writer 9ca927d9; DECISIONS 69 was theirs, this row is the dispatcher's reading of it).** The Test button produces a 33-line structured console report and was green in the gate on this host. **The confirmation that matters is that it MEASURED the research §7 warning rather than arguing it:** the LITERAL-operand `TwoSum(1, 2^-25).lo` is folded to **ZERO** by SwiftShader's compiler, while the VARIABLE-operand compensated accumulation still **CHANGES the result** (1.0000019 vs 1.0000000) and the Dekker `TwoProd` residual **SURVIVES** (5.960e-8). **DESIGN RULE THIS FORCES, binding on MANTISSA-LADDER:** `dsLow` must NEVER be read alone. `ZERO` there is a portability signal about ONE formulation, not a verdict on the technique; usability is decided by `ds_low_changes_result` + `ds_twoprod_low`, and the ladder's pins must therefore assert **both** that a low component is non-zero **and** that it changes the result. A ladder built and pinned on the naive "is `.lo` non-zero" test would have been declared broken on a healthy compiler. | Secondary readings from the same run that update the record: the deep lane **did engage** (`deepLane=float64+bigint`, and `deep_ref_bigint` at 1e-20 measured **0.000% misclassified, mean |dn| 0.176**, capIter 8192); float textures **genuinely sample** (`float_texture_sampleable` maxDelta **0**); `gl_error` NO_ERROR and readback plausible, so the other numbers are trustworthy; and `ds_precision` reports highp = **23 bits**, so there is no hidden >24-bit float to lean on. **The SwiftShader cost baseline is now on the record for comparison with the owner's hardware**: 1e-4 70.9 ms, 1e-6 **7397.5 ms**, 1e-8 3306.9 ms (full-window software render under load; DECISIONS 47 applies). | Not established and must not be implied: hardware certification (this is a software rasteriser — that is the instrument's whole purpose); the cost figures as any kind of performance claim. | Dispatcher, 2026-09-21, from the writer's gate log `/tmp/fracvibe-gate/gate-full-b5eae26-20260921T230334Z.log`. |
| 75 | **PUBLISH HAZARD FOUND AND FIXED BY SEQUENCING, not by code: the diagnostic's own script would have been served from a fixed, edge-cached URL.** `public/gpuDiagnostic.js` is loaded by a `<script>` tag that **nothing imports**, so `scripts/stage.sh` — which content-addresses exactly the graph reachable from its entry list — left it UNVERSIONED. It works (staging exits 0), but that URL then carries the **4 h Cloudflare edge TTL** (the PUBLISH-CACHE trap), so a stale diagnostic could be served for hours — including to the owner, whose press is the one piece of evidence we cannot obtain any other way. **The fix is to add `gpuDiagnostic.js` to `stage.sh`'s entry list**, which makes `index.html`'s tag point at `gpuDiagnostic.<hash>.js` and therefore fresh with the app. **ORDERING CONSTRAINT discovered by trying it early: the change CANNOT precede the file.** Attempting it on `main` before the diagnostic landed made `stage.sh` fail RED (`referenced file missing: gpuDiagnostic.js`) — correct behaviour, wrong order — so the sequence is: land the diagnostic -> then add the entry -> then publish. `capabilities.js`, `capabilitiesModule.js` and `webgpuProbe.js` remain unversioned orphans by design (nothing fetches them yet); they must be added to the entry list when WEBGPU-WIRING references them. | Recorded because it is a general trap, not a one-off: **any file the page fetches must be an entry `stage.sh` knows about, or it is cache-stuck.** The moment the WEBGPU wiring references `capabilities.js`, the same failure mode applies. | Editing stage.sh before the target file exists; assuming a `<script>` tag is versioned just because it is in `index.html`; shipping the diagnostic unversioned and having the owner test a stale build. | Dispatcher, 2026-09-21. |
| 78 | **THE EXPANSION LAYER IS BUILT AND MEASURED, AND ITS RESULT RETIRES A RUNG I HAD PLANNED (writer b7af28b5; DECISIONS 69–70 are theirs).** `public/expansions.js` + `public/expansions.glsl.js` implement a k-component expansion where a value is **k float32 terms, most-significant first**, built from `TwoSum`/`FastTwoSum`/Dekker `TwoProd` and renormalised by **Priest distillation**, with **constant operation counts per k** \u2014 so `k` is a genuine BUILD parameter, not a per-k cascade. `k=1` emits the plain expressions and is **bit-identical** to hand-written float32 (0 mismatches over 96 scalars and a 384-px recurrence), which is precisely the property that keeps the shallow byte-identity pins passing after integration. | **THE FINDING THAT MATTERS: at a deep view, k=2 recovers the delta error to BELOW the 24-bit float32 ORBIT-TRANSPORT floor, so k=4 buys NOTHING for the escape index.** Escape index vs a 512-bit direct reference: at 1e-6, k1 **0.5208%** misclassified → k2 **0.0000%** → k4 **0.0000%**; at 1e-20, k1 0.5208% → k2 **0.2604%** → k4 **0.2604%** (identical). Delta-error table: k2 2.95e-12 (2^-38.3) at 1e-6 and 9.996e-15 (2^-46.5) at 1e-20, against a transport floor of ~2^-24. **So DECISIONS 67's mechanism (a) \u2014 the multi-component ORBIT TRANSPORT \u2014 is the NEXT depth slice, not more mantissa rungs.** The ladder stops at **k=2** for the delta; buying k=4 would be 1740 `TwoSum`/64 `TwoProd` per step (measured ~1975–2113 ms at 32x24x512 on this software rasteriser) for no measurable escape-index gain. | **Cost, so the choice is informed:** pinned structural counts per delta step are k1 0/0, k2 **138/20**, k3 594/39, k4 1740/64 (`TwoSum`/`TwoProd`); measured k1 ~21–39 ms, k2 ~105–139 ms, k4 ~1975–2113 ms. **k=2 costs ~3–5x k=1 and is the last rung the transport floor justifies.** | **On the dispatch: I do not descend the ladder further in the DELTA.** The next dispatch is the wide orbit transport, then the exact-centre producer (DECISIONS 71's gap), then integration of the k=2 delta into `webglFractal.js` \u2014 and integration must re-run these pins against the integrated path, since this slice deliberately left `webglFractal.js`/`fractalKernel.js`/`app.js` untouched. | UNVERIFIED ON HARDWARE (SwiftShader), and the bit-exact compiler result is evidence about THIS compiler only. Two preconditions are pinned so they cannot bite silently: `TwoProd`'s Dekker split needs `|operand| < 2^128/4097 = 8.306e34`, and subnormal operands are non-exact here \u2014 neither binds our recurrence, whose operands are O(1). The writer also RECORDED A LIMIT of their own control: collapsing `EX_MUL` to f32 turns pin 5 RED but leaves pin 7 green, because the delta step's linear term is an `EX_SCALE`, so a mul-only collapse is not numerically significant \u2014 stated rather than smoothed. | Dispatcher, 2026-09-22, from the writer's gate log `/tmp/fracvibe-gate/gate-full-098bc6a-20260921T231551Z.log`. |
| 79 | **LANE-CONTINUITY (2026-09-22): the 1e-4 discontinuity was the BUDGET FLOOR TURNING ON, not the lane switch, and the floor is now introduced CONTINUOUSLY. REPRODUCED first, through the REAL render path at the COAST view (1000x700, log-spaced, mean red): `9.00e-5 254.418`, `9.60e-5 254.425`, `1.00e-4 222.825`, `1.05e-4 222.690`, `1.20e-4 219.502`, `1.50e-4 207.668`, MAX STEP **31.600 at 9.60e-5 -> 1.00e-4**, with the effective budget stepping 2072/2058 (deep lane) -> 512 (plain lane). SEPARATED by holding the budget FIXED (slider 4096 and 8192, so the floor could not contribute): the SAME lane switch moves the frame by **0.013** mean-red units at the boundary (254.846 -> 254.860) and 0.011-0.036 across the sweep — the same order as the sweep's own non-boundary steps — so the two lanes AGREE at 1e-4 (both sub-pixel accurate there) and the program switch is measurably invisible. THE FIX is therefore candidate (c)/(a): `iterBudgetForScale` ramps the floor from 0 AT the knee to the long-standing 512-per-decade rule ONE decade below it — `floor = ceil(512*L*min(1, max(0, L - ITER_BUDGET_KNEE_DECADES)))` with `L = log10(1/scale)` and `ITER_BUDGET_KNEE_DECADES = log10(1/ITER_BUDGET_MIN_SCALE) = 4`, `ITER_BUDGET_RAMP_DECADES = 1`. The deep anchors are UNCHANGED (1e-5 2560, 1e-6 3072, 1e-8 4096, 1e-15 7680), so D2's pins still hold and only the (1e-4, 1e-5) decade changes. MEASURED AFTER: the boundary step is **0.019** and is not the sweep's largest (max 11.834 is a genuine non-boundary step), against the removed rule's **31.600** measured in the SAME pin; the RULE is continuous too — over 240 log-spaced samples from 1e-3 to 1e-6 the largest adjacent change is **38** iterations where the removed rule changes **1543**. | REJECTED, with the number that rejected it: (b) blending/overlapping the two programs, or keeping the deep lane always on — the lane's own colour delta is 0.013, below the frame's own step-to-step change, so a second draw in the band would buy nothing, and the always-on lane already measured 5.9m vs 2.9m on the full gate; moving the constant 1e-4 — the owner explicitly rejects a depth that is special because a constant flips (row 62); ramping over more than one decade — it would break D2's 1e-5 = 2560 anchor or under-provision the depth the probe measured. **CORRECTION to the brief's narrative, recorded rather than inherited: the measured budget step is 512 -> 2058 (4.02x), NOT 50 -> 2048 (41x).** The running app's slider floor is 512 (`FractalViewer` sets `maxIterFloor = 512`; `index.html`'s `value="256"` is overwritten), and the dispatcher's own raw log shows `cap=512` above the boundary. The 31.600-unit colour jump is unchanged and is the baseline. | Pin: `tests/lane-continuity.spec.js` pins 1-3, with the removed rule's arm IN-PIN as the failing baseline (`legacyBudget()` drives the REAL render path at the pre-fix effective budget; setting the slider to the old effective budget is pixel-equivalent because the cap, the lane and the shader are then identical). COMPOSITION WITH THE NEXT SLICE: the exponent split is ORTHOGONAL to mantissa width — a multi-component delta scales every component by the same exact power of two `S` — so widening the mantissa composes cleanly, and the ramp is orthogonal to both. | Writer, `session-lane-continuity`, 2026-09-22; probe logs `/tmp/fv-lane/`, gate log in the landing report. |
| 80 | **LANE-CONTINUITY (2026-09-22): the app's zoom cap is REMOVED — depth is bounded by TIME, not by a number — and the three DIFFERENT limits are stated separately so a branch we did not take cannot be mistaken for a property of the mathematics.** `WEBGL_ZOOM_CAP`/`WEBGL_MIN_SCALE` are gone from `public/app.js`; `viewer.setZoomLimit(null, null)`; `__fv.minScale` and `__fv.zoomCap` are `null`; S1's clamp machinery is retained but disabled and is exercised by `__fv.setZoomLimitForTest` so the S1 property stays pinned. The ONE depth signal left is informational: `MEASURED_CORRECT_MIN_SCALE = 1e-40`, a throttled non-modal `#appMessage` notice and the `fv-deep-precision` event, neither of which changes the view, the lane or the renderer. THE LIMITS, named: **(i) the app cap — REMOVED**; **(ii) the delta RANGE — fixed by the exponent split** (rows 57/61: the delta is seeded in float32's normal range and the shift saturates at 2^120, i.e. the seed stops tracking the view scale around 1e-49); **(iii) the delta MANTISSA — the limit of the CURRENT single-factor solver**, the 24-bit float32 delta accumulating relative error: measured CORRECT to 1e-40 (0.00000 % misclassified against an independent per-pixel BigInt reference, `tests/gpu-arbitrary.spec.js` pin 1) and 7.4 % misclassified at 1e-42. **1e-42 is OUR SOLVER'S LIMIT, not a numerical floor and not where "precision" ends**: the orbit's precision grows continuously and is not the wall, and the remedy for what is left is MORE MANTISSA — compensated / multi-component arithmetic, k components ~ k*24 bits at ~k* cost — which the committed precision probe already MEASURED (`docs/PROBE-2026-09-21-gpu-precision.md` §8 and `docs/STATE.md` MEASURED-FINDINGS: compensated float32 hi+lo holds to >=1e10 at 0.000 % misclassification at 10.4-12.5x fragment cost) and which was deliberately NOT built. It is the NEXT slice and edits the same shader, so it is serialized after this one. **COST CURVE AFTER (ms per FULL image, 640x480, median of 5 draws through the app's own readout, which includes the 1x1 readback sync; this host's Chrome renders WebGL on SwiftShader SOFTWARE, so these are software numbers, not hardware): 1e-4 18.5, 1e-6 101.3, 1e-8 107.1, 1e-12 151.0, 1e-16 219.2, 1e-20 295.1, 1e-30 513.1, 1e-40 632.9.** Unchanged from GPU-ARBITRARY's 14/106/150/211/285/-/-/640 within noise, i.e. the ramp and the cap removal changed no cost: depth costs time. | NO REGRESSION, all four classes measured: (a) the shallow plain lane is BYTE-FOR-BYTE identical at and above 1e-4 — `tests/gpu-arbitrary.spec.js` pin 4 stays green (the ramp is a no-op at and above the knee, and at 1e-8 it already equals the old rule); (b) P1's 1e-15 pins stay green (`tests/perturb-core.spec.js`: 0.00000 % misclassified) and `gpu-arbitrary` pin 1 still measures 1e-40 at 0.00000 % against the independent per-pixel BigInt reference; (c) the deep lane stays correct; (d) the CPU-switch offer stays DELETED, the manual checkbox and the real no-GPU path are untouched, and the cap-dependent assertions were MOVED to the new contract rather than deleted (`zoom-clamp.spec.js`, `hygiene.spec.js`, `iter-budget.spec.js` pin 2, `gpu-arbitrary.spec.js` pin 3, `perturb-core.spec.js` pin 3, `smooth-color.spec.js` pin 3). | The mantissa ladder was NOT built (same shader, next slice). Removing the cap is what makes it reachable at all: the old 1e-6 cap sat ~34 decades above the measured-correct reach, so a deeper solver would have been unreachable behind a stop. `MODERNIZATION.md` §1's B1 "zoom-cap logic" is now historical (the machinery exists, the cap is disabled); a note is added there. | Writer, `session-lane-continuity`, 2026-09-22. |
| 81 | **GPU-DIAGNOSTIC: the hardware-certification gap is closed by an INSTRUMENT, not by a guess. A `Test` button in the app (`public/index.html`) plus `public/gpuDiagnostic.js` (new file, own module tag, no build step) runs a battery and prints ONE copy-pasteable console block (`===== GPU DIAGNOSTIC (FracVibe) =====`, a one-line SUMMARY, one section per group, one line per item). The battery: IDENTITY (`navigator.userAgent`, `WEBGL_debug_renderer_info`'s UNMASKED vendor/renderer, the WebGL version string, WebGL1 vs WebGL2, and a HARDWARE/SOFTWARE/UNKNOWN classification); FEATURE GATES (`OES_texture_float`, `OES_texture_float_linear`, `MAX_TEXTURE_SIZE`, `MAX_VERTEX_TEXTURE_IMAGE_UNITS`, and whether a float texture is ACTUALLY SAMPLEABLE — a known 1x1 RGBA float texture uploaded, sampled in a real draw, `readPixels`ed and compared against the written values); THE DOUBLE-SINGLE RESIDUAL (a standalone test shader — NOT the shipped shader — computes `TwoSum(1, 2^-25).lo`, asks whether the low term CHANGES a 64-term compensated accumulation, and computes a Dekker `TwoProd(1+2^-12, 1+2^-12)` residual, with a readback sentinel so a broken mapping is distinguished from an erased residual); THE REAL DEEP LANE end to end against an independent reference computed IN THE PAGE (the CPU float64 recurrence at 1e-6, and a direct BigInt iteration with its own parser/rounder at 1e-20) reporting `%` misclassified, mean `|Δ iteration|` and `orbitSource`; READBACK (`gl.getError()` after everything, and whether `readPixels` returned plausible, varied data); and COST (`renderTimeMs()` at 1e-4/1e-6/1e-8). A FIXED 24-item id set is emitted on EVERY path — a phase that cannot run emits `SKIP (reason)` or `FAIL (reason)` rather than dropping the item — so one unsupported feature can never silently cost the owner information. The button refuses re-entry while running, restores the view it changed, and never touches `app.js`/`webglFractal.js`/`fractalKernel.js` (it integrates ONLY through the frozen `window.__fv` observables).** | DECISIONS 66 states the certification limit up front — this host runs SwiftShader for WebGL, so a residual can only be certified on the owner's hardware — and the research §7 warns that **a value can look plausible after an expansion has silently collapsed to f32**, which is why the instrument reports the LOW component AND whether it is USED rather than a picture that "looks fine". Every item maps to a failure this repo has already paid for: float-texture sampling IS the deep lane's transport (P1/P2), readback plausibility is what makes the escape-index pins mean anything, and the CPU/BigInt reference is the campaign's own rule that an independent reference must not share the code path under test. | **What it CANNOT do, stated so the press is not oversold:** it cannot certify the SHIPPED k-component delta ladder, because that ladder does not exist yet (DECISIONS 67) — it measures whether a driver WOULD preserve an expansion. It cannot remove the need for the owner's press, and one press samples one driver/OS/browser. Every number taken on THIS host is a SwiftShader software baseline, not hardware performance (DECISIONS 47). It does not exercise WGSL/WebGPU precision (the app's lanes are WebGL1). **Rejected:** a decorative GPU-info dump (no failure modes); asserting the VALUES in the spec on this host (machine-specific — the spec asserts structure, and a zero low component is an accepted outcome here); running the probe on the SHIPPED shader (it would couple the instrument to a slice and be erased by the same compiler it must detect); aborting the battery on the first unsupported feature. | Writer, `session-9ca927d9`, 2026-09-21, from DECISIONS 65-67 and `docs/RESEARCH-2026-09-21-arbitrary-depth.md` §7. |
| 82 | **GPU-DIAGNOSTIC: the hardware-certification gap is closed by an INSTRUMENT, not by a guess. A `Test` button in the app (`public/index.html`) plus `public/gpuDiagnostic.js` (new file, own module tag, no build step) runs a battery and prints ONE copy-pasteable console block (`===== GPU DIAGNOSTIC (FracVibe) =====`, a one-line SUMMARY, one section per group, one line per item). The battery: IDENTITY (`navigator.userAgent`, `WEBGL_debug_renderer_info`'s UNMASKED vendor/renderer, the WebGL version string, WebGL1 vs WebGL2, and a HARDWARE/SOFTWARE/UNKNOWN classification); FEATURE GATES (`OES_texture_float`, `OES_texture_float_linear`, `MAX_TEXTURE_SIZE`, `MAX_VERTEX_TEXTURE_IMAGE_UNITS`, and whether a float texture is ACTUALLY SAMPLEABLE — a known 1x1 RGBA float texture uploaded, sampled in a real draw, `readPixels`ed and compared against the written values); THE DOUBLE-SINGLE RESIDUAL (a standalone test shader — NOT the shipped shader — computes `TwoSum(1, 2^-25).lo`, asks whether the low term CHANGES a 64-term compensated accumulation, and computes a Dekker `TwoProd(1+2^-12, 1+2^-12)` residual, with a readback sentinel so a broken mapping is distinguished from an erased residual); THE REAL DEEP LANE end to end against an independent reference computed IN THE PAGE (the CPU float64 recurrence at 1e-6, and a direct BigInt iteration with its own parser/rounder at 1e-20) reporting `%` misclassified, mean `|Δ iteration|` and `orbitSource`; READBACK (`gl.getError()` after everything, and whether `readPixels` returned plausible, varied data); and COST (`renderTimeMs()` at 1e-4/1e-6/1e-8). A FIXED 24-item id set is emitted on EVERY path — a phase that cannot run emits `SKIP (reason)` or `FAIL (reason)` rather than dropping the item — so one unsupported feature can never silently cost the owner information. The button refuses re-entry while running, restores the view it changed, and never touches `app.js`/`webglFractal.js`/`fractalKernel.js` (it integrates ONLY through the frozen `window.__fv` observables).** | DECISIONS 66 states the certification limit up front — this host runs SwiftShader for WebGL, so a residual can only be certified on the owner's hardware — and the research §7 warns that **a value can look plausible after an expansion has silently collapsed to f32**, which is why the instrument reports the LOW component AND whether it is USED rather than a picture that "looks fine". Every item maps to a failure this repo has already paid for: float-texture sampling IS the deep lane's transport (P1/P2), readback plausibility is what makes the escape-index pins mean anything, and the CPU/BigInt reference is the campaign's own rule that an independent reference must not share the code path under test. | **What it CANNOT do, stated so the press is not oversold:** it cannot certify the SHIPPED k-component delta ladder, because that ladder does not exist yet (DECISIONS 67) — it measures whether a driver WOULD preserve an expansion. It cannot remove the need for the owner's press, and one press samples one driver/OS/browser. Every number taken on THIS host is a SwiftShader software baseline, not hardware performance (DECISIONS 47). It does not exercise WGSL/WebGPU precision (the app's lanes are WebGL1). **Rejected:** a decorative GPU-info dump (no failure modes); asserting the VALUES in the spec on this host (machine-specific — the spec asserts structure, and a zero low component is an accepted outcome here); running the probe on the SHIPPED shader (it would couple the instrument to a slice and be erased by the same compiler it must detect); aborting the battery on the first unsupported feature. | Writer, `session-9ca927d9`, 2026-09-21, from DECISIONS 65-67 and `docs/RESEARCH-2026-09-21-arbitrary-depth.md` §7. |
| 83 | **GPU-DIAGNOSTIC: the instrument's FIRST measurement on this host is a COMPILER finding — exactly the class it was built to detect — and it changes how the owner's report must be READ. Captured verbatim in the gate log `/tmp/fracvibe-gate/gate-full-b5eae26-20260921T230334Z.log` (33 lines, echoed by the spec): on SwiftShader/ANGLE WebGL1, `ds_low_component` = **ZERO** (the LITERAL-OPERAND `TwoSum(1, 2^-25).lo` reads back 0, rawByte 0) while `ds_low_changes_result` = **YES** (the VARIABLE-OPERAND compensated accumulation of 64 x 2^-25 reaches 1.0000019 against the naive 1.0000000) and `ds_twoprod_low` = **NONZERO** (Dekker residual 5.960e-8). The same block shows the real deep lane engaged and correct — 1e-6 `src=float64 misclassified=0.032% mean|dn|=2.269`, 1e-20 `src=bigint bits=192 misclassified=0.000% mean|dn|=0.176` — the float texture genuinely sampleable (`wrote=[0.25,0.5,0.75,1] read=[64,128,191] maxDelta=0`), `gl_error=NO_ERROR(0)`, and `highp=23 bits`.** | This is the research §7 warning MEASURED rather than argued: a `TwoSum` low term is algebraically zero over the reals, so a compiler may fold the literal-operand form (`bb = s - a` with a a literal) while it cannot so easily fold a loop-carried variable — and on this driver it DID. **Consequence for the owner's report: `dsLow` must NEVER be read alone.** A `ZERO` there is a compiler-portability signal about ONE formulation, and the two companion items (`ds_low_changes_result`, `ds_twoprod_low`) say whether the residual is USABLE; a report with `dsLow=ZERO-IGNORED` AND `ds_low_component_changes_result=YES` is a driver that folds the literal form, not a driver on which double-single fails. | Reading `dsLow=ZERO-IGNORED` as "double-single does not work on this GPU" (the variable-form residual changed the result on the SAME shader); treating the literal-form probe as the load-bearing one (the PAIR is); treating the software timings (1e-6 **7397.5 ms**, 1e-8 3306.9 ms, 1e-4 70.9 ms under load 5-8) as performance — they are SwiftShader software bounds (DECISIONS 47). | Writer, `session-9ca927d9`, 2026-09-21, from its own FULL gate at `b5eae26` (exit 0, 68 passed / 2 skipped, 8.4m). |
| 84 | **OWNER DECISION: "Arbitrary it is. Take the best fit for that." The chosen route is a k-COMPONENT EXPANSION LADDER whose component count GROWS WITH DEPTH — not any single fixed-precision library.** The reasoning follows from a property we already ship: the reference orbit's precision is already depth-dependent (`bigOrbit.js`: `bits = max(64, ceil(log2(1/scale)) + 64)`). Delta precision must track it, because the perturbed sum `Z + z` must resolve the pixel scale, so the required significand also grows with `log(1/scale)`. Therefore a FIXED component count is merely a bigger wall — double-single (~48 bits) would move our 1e-42 limit to roughly 1e-47 and stop — whereas a ladder makes depth a function of time, which is exactly the owner's stated acceptance ("slower the deeper it gets … but no hard stop"). | **THE BUILD, in order:** (1) **GLSL first, WGSL later and only integer-controlled** — WGSL permits reassociation and fusion and fixes no rounding direction, so the classic error-free transforms are NOT portable there (a `TwoSum` low term is algebraically zero over the reals and a reassociating compiler erases it); GLSL has no such rule, which is why luma.gl's full `fp64` library is GLSL-only; (2) **k=2 (double-single) as the first rung** — already MEASURED in this repo at ~48 bits and 10.4–12.5× fragment cost (DECISIONS 37), so it is the cheapest rung with a known price; (3) **parameterise the representation in k** so k=4/8 is a constant change rather than a rewrite — that parameterisation is what makes it a ladder instead of a new cap; (4) the orbit stays CPU BigInt and arbitrary (rows 64–65); (5) only the DELTA needs expansion, since it only ever has to be small (research §3). **Prior art to build FROM, not re-derive:** luma.gl `fp64arithmetic` (Møller–Knuth `TwoSum`, Dekker split/`TwoProd`, renormalisation), `glsl-arbitrary-precision`, `glsl-arb-prec` (OpenGL **ES**). | **THE CERTIFICATION LIMIT, stated up front so it cannot be discovered as a surprise:** this host runs SwiftShader for WebGL *and* WebGPU, and hardware validation is required to show that residual terms survive a real compiler and driver (a value can look plausible after the expansion silently collapses to f32). So our pins must compare against an independent BigInt reference AND assert a **required-nonzero low component**, and the final "this survives real GPUs" claim needs hardware we do not have — it will be reported as unverified-on-hardware, not assumed. | Rejected: picking one fixed-precision library and calling the result arbitrary; a bigger constant cap; classic double-single on WGSL without integer control or per-adapter validation. | Owner 2026-09-21; prior art in `docs/RESEARCH-2026-09-21-arbitrary-depth.md` §7. |
| 85 | **MANTISSA-LADDER (the arithmetic layer): a value is carried as an EXPANSION of k float32 components, MOST-SIGNIFICANT FIRST (`c0` is the leading term), produced by error-free transforms and renormalised by Priest's DISTILLATION. `k` is a parameter of the BUILD — `public/expansions.glsl.js` emits the GLSL for a given k and `public/expansions.js` is the facade plus the JS reference — exactly the way `webglFractal.js` already templates `MAX_ITER` into the shader.** The primitives are the published error-free transforms — Moller/Knuth `TwoSum`, Dekker `FastTwoSum`, Dekker's split and `TwoProd` — and the arbitrary-k renormalisation is a fixed-list distillation that preserves the EXACT sum (each TwoSum is exact) and leaves the leading term first, so `slice(0, k)` is an exact-sum truncation whose tail is at the 2^-24k relative level. `EX_ADD`/`EX_SUB`/`EX_SCALE` distil 2k values (C(2k,2) TwoSums); `EX_MUL` distils the k^2 TwoProd pairs (C(2k^2,2) TwoSums). The delta recurrence `w -> 2 Z w + S w^2 + d` is emitted as ONE function with `S` scaled EXACTLY (a power of two) and the per-op cost pinned. | **(1) WHY MOST-SIGNIFICANT-FIRST AND A FULL DISTILLATION, not a hand-written double-double cascade.** The cascade form is a different algorithm per k; a distillation over the exact product terms is the SAME algorithm for every k, so k=4 is a parameter rather than a rewrite (the parameterisation IS the ladder, DECISIONS 66). **(2) k=1 REDUCES EXACTLY.** `EX_ADD`/`EX_SUB`/`EX_MUL`/`EX_SQR`/`EX_SCALE` for k=1 emit `a.c0 + b.c0`, `a.c0 * b.c0`, etc. — the plain float32 expression — and the k=1 delta recurrence is BIT-IDENTICAL to the shipped two lines (pinned: 5 ops x 96 scalars + a 384-px recurrence, 0 mismatches). That is the property that lets the existing shallow-lane byte-identity pins keep passing after integration. **(3) THE GLSL MATCHES THE JS float32 REFERENCE BIT-FOR-BIT ON THIS HOST** for add/sub/mul/sqr at k=1..4 and across a 3-op chain at k=4 (0 of 128 component values differ): ANGLE/SwiftShader preserves the rounding points the transforms need. This is the compiler-fidelity half of the contract; the BigInt half is below. **(4) TWO PRECONDITIONS ARE EXPLICIT AND PINNED, not discovered as surprises.** `FastTwoSum` needs `|a| >= |b|` (fed unordered it loses the residual on 2/2 vectors while `TwoSum` stays exact), and `TwoProd`'s Dekker split needs `|operand| < 2^128/4097 = 8.306e34` (beyond it the residual is NaN, on the GPU and in the JS reference alike; `TwoSum` has no such bound). Both bounds are exported (`SPLITTER_MAX`) and neither binds the recurrence, whose operands are O(1). Subnormal operands are also excluded: `1e-38 + 1e30` is NOT exact here because the shader flushes subnormals (DECISIONS 39) — the shipped `u_scaleShift` mechanism exists precisely so the delta seed is never subnormal. | Rejected: a hand-written double-double cascade (a different algorithm per k); vendoring luma.gl's `fp64arithmetic` source (licence checked but not needed — the algorithms are published and re-derived here, see row 70); arrays and dynamic indexing in the emitted GLSL (GLSL ES 1.00 restrictions; the emitter unrolls to scalar struct fields); a branch inside the hot ops (the cost would stop being a fixed function of k). | Writer, `session-expansions`, 2026-09-21, from `public/expansions.glsl.js`, `public/expansions.js` and `tests/expansions.spec.js` pins 1-5. |
| 86 | **MANTISSA-LADDER MEASUREMENTS — the ladder works, and the FIRST rung is the one that matters at the depths the shipped orbit transport can feed.** Reference = an INDEPENDENT 512-bit BigInt fixed-point recurrence (its own dyadic oracle in the test; no code shared with the library). Frame 32x24, centre `-1.4303576324512 + 2^-70` (the P2 centre), f32 orbit from `bigOrbit.js` (the SHIPPED transport). **(A) DELTA relative error, no rebasing:** at scale 1e-6 over 20 steps, k1 = **1.80e-7**, k2 = **2.95e-12**, k4 = **2.94e-12**; at scale 1e-20 over 64 steps, k1 = **1.84e-7 (2^-22.4)**, k2 = **1.00e-14 (2^-46.5)**, k4 = **2.56e-18 (2^-58.5)** — ~24 bits per rung from k=1 to k=2 and again to k=3, with k=4 delivering ~12 more in this recurrence (measured separately: pure `EX_MUL` on TIGHT expansions delivers 2^-46.6 / 2^-70.6 / 2^-93.1 at k=2/3/4). **(B) ESCAPE-INDEX accuracy against a 512-bit DIRECT (no perturbation) reference:** at 1e-6, misclassified **4/768 (0.5208 %)** at k=1 against **0/768 (0.0000 %)** at k=2 and k=4; at 1e-20, **4/768 (0.5208 %)** at k=1 against **2/768 (0.2604 %)** at k=2 AND k=4, mean |delta| 0.0339 -> 0.0990 -> 0.0990. **(C) THE FINDING: at a deep view, k=4 buys nothing over k=2 for the ESCAPE INDEX, because the f32 ORBIT TRANSPORT (24 bits, ~6e-8 absolute) dominates both, and the escape test itself is the shipped float32 one.** That is DECISIONS 67's second mechanism arriving on schedule: the delta ladder is necessary but not sufficient, and this slice deliberately does not widen the orbit transport. **(D) COST:** structural operation counts per delta step (TwoSums/TwoProds) are **k1 0/0, k2 138/20, k3 594/39, k4 1740/64** — exact, pinned, and O(k^4) for the mul-dominated step; measured wall time on the SOFTWARE rasteriser (32x24x512): **k1 ~21-33 ms, k2 ~106-137 ms, k4 ~2059-2447 ms**, and a 64x `EX_MUL`/`EX_ADD` chain at 64 px: **k1 ~14-28 ms, k2 ~30-44 ms, k4 ~360-412 ms**. "Slower the deeper it gets" is therefore a measured curve, not a slogan. | The deliverable requires the cost-vs-depth curve and a BigInt-referenced accuracy measure, and the honest reading of (C) is the slice's most useful result: it tells the next slice WHERE to spend — the multi-component ORBIT TRANSPORT (row 67) — instead of adding rungs. A k=2-only design would have hidden the k=4 truncation weakness; a k=4-only design would have hidden that k=2 already reaches the orbit floor. The escape metric is deliberately the SHIPPED f32 escape test so the table measures what integrating the ladder would change, not an idealised all-expansion kernel. | Reporting "arbitrary depth achieved" from the delta ladder alone (the dispatcher's own error, DECISIONS 67); comparing against a float64 host expression (a different rounding, not an oracle); asserting only that the shader compiles; hiding the k=4 plateau. | Writer, `session-expansions`, 2026-09-21; raw numbers in the gate log (`[expansions pin6]`, `[expansions pin7]` lines). |
| 87 | **MANTISSA-LADDER LICENCE AND CERTIFICATION STATUS. (a) NOTHING THIRD-PARTY WAS VENDORED.** The GLSL is GENERATED from the published algorithms — Moller/Knuth `TwoSum`, Dekker's `FastTwoSum`/split/`TwoProd`, Priest's distillation — which are public methods; the JS reference is written here; the BigInt oracle in the test is written here. luma.gl's `fp64arithmetic` (vis.gl) is the project the work is modelled ON conceptually and it is MIT-licensed, but **no luma.gl source is copied**, so no licence obligation is inherited; the other surveyed libraries (`glsl-arbitrary-precision`, `glsl-arb-prec`, CAMPARY) were NOT used. **(b) HARDWARE CERTIFICATION IS UNVERIFIED AND MUST NOT BE ASSUMED.** Every number above was measured on ANGLE + **SwiftShader** (a SOFTWARE rasteriser; `docs/STATE.md` §TRAP, and this host is SwiftShader for WebGL AND WebGPU). The bit-exact GLSL-vs-reference agreement in row 68 is therefore evidence that THIS compiler preserves the rounding points — it is NOT evidence that a hardware driver does. The research's warning 2 (`docs/RESEARCH-2026-09-21-arbitrary-depth.md` §7) is exactly this case: a software adapter cannot demonstrate that residual terms survive execution, and a plausible final value can hide an expansion that silently collapsed to float32. The pins defend against that in the only way available here — they compare against an independent BigInt oracle AND assert a REQUIRED-NONZERO low component (pins 3, 4, 5, 7), with in-spec collapsed-build controls that the same assertions reject — but the "this survives real GPUs" claim needs hardware this host does not have. **(c) NOT IN THIS SLICE:** no file of the live app was modified; `public/webglFractal.js` is untouched and the ladder is not integrated into the shader. Wiring it in (and choosing the depth->k rule) is a later slice that must re-run these pins against the integrated path. | The licence question was asked explicitly ("do not vendor code whose licence you have not checked") and the answer is that nothing needed checking because nothing was copied — stated so a future reader does not assume an obligation either way. The certification limit is stated in the pin file itself so it cannot be discovered as a surprise after an integration slice claims hardware validity from these green pins. | Putting "arbitrary precision on the GPU" in a report without the hardware caveat; vendoring a library and relying on "it is MIT" without recording which files; treating a green software-adapter pin as a hardware proof. | Writer, `session-expansions`, 2026-09-21; prior art and both warnings in `docs/RESEARCH-2026-09-21-arbitrary-depth.md` §7. |
| 88 | **PRECISION-CHAIN audit: row 67(a)'s 24-bit orbit transport is CONFIRMED as a width and FALSIFIED as the depth cap, and the audit found a NEW gap: the exact centre has no production producer.** (1) **Width, confirmed by measurement:** `toF32` keeps 53 bits then `Math.fround`s to 24, and the mean delivered significand over the ~360 orbit indices the perturbation samples is **25.78 bits** (round-to-nearest gain) with a worst relative error of 5.80e-8 — so ~24 bits arrive whatever the BigInt precision. (2) **Cap, falsified:** against an independent per-pixel direct-BigInt reference the shipped 24-bit transport is **0.0000 % misclassified with mean \|Δ\| 0.007–0.017 iterations** from 1e-20 to 1e-40; changing the BigInt working precision (192 vs 448 vs 704 bits) changes **0.0000 %** of pixels, because the perturbation **never samples the orbit tail** — the largest reference index `m` reached in a full 1e-30 frame is **364** (164 at 1e-15), while the transported words first differ between precisions at index **612**. Row 67(a)'s "second, independent cap that must be lifted before deep depths are trustworthy" is therefore true of the *width* and false of the *consequence*: the transport is the **next rung once the delta ladder is built**, not the current wall. (3) **The first precision mechanism to bind is the float32 DELTA mantissa**, measured at 0.003 (1e-30) / 0.005 (1e-40) mean iterations incremental, with its breakdown at **1e-42 / 7.4 %** (DECISIONS 57) — so the k-component ladder of DECISIONS 66 is correctly the next build. (4) **NEW GAP:** the only producer of `centerXExact`/`centerYExact` in the whole tree is the test hook `window.__fv.setDeepView` (`public/app.js:1620-1635`); every production path stores the centre as a float64 double (`fractalViewer.js:25/165-173/215-226`, `app.js:80-82`, `memoryRepository.js:97`), so **the BigInt orbit is unreachable from the shipped UI** — a serialisable exact centre is a prerequisite for both paths, separately dispatchable, and not named by row 67 or the queue. (5) **Record correction, measured:** the CPU lane's clamp is **scale ≥ 1e-6**, not the 1e-4 on the board — `WEBGL_ZOOM_CAP` was lifted 1e4 -> 1e6 by GPU-ARBITRARY (`app.js:445-446`) and one shared clamp serves both lanes. The conclusion of DECISIONS 63 is unchanged. | The numbers and the full stage-by-stage map are `docs/PRECISION-CHAIN.md`; the probe was a throw-away Node harness loading the real `bigOrbit.js` plus a 40×30 float64 emulation of the shipped shader, cross-checked against the recorded browser pins. The corrected ranking, with numbers, is in that file's §7. | Recording a solver choice as a property of the maths (again — the class row 64 names); repeating the dispatcher's 24-bit claim as fact without measuring it; scheduling a multi-component orbit transport before the delta ladder when the measurement says the delta binds first. | Dispatcher/writer `feat/precision-chain`, 2026-09-21. |
| 89 | **GPU COARSE-TO-FINE (owner request, 2026-09-22 verbatim: "Deep zooms seem to work well on webgpu. They get slow of course and thats where the coarse to fine rendering should come in. Rendering that does not block further zoom. Can do that for any depth, it just wont be noticable at lower depths."): a render is a CHAIN of levels `8 -> 4 -> 2 -> 1`, the same sequence the CPU worker already walks, and each level is a REAL pass of the SAME fragment program into an offscreen target of `ceil(W/step) x ceil(H/step)` texels that is then magnified onto the canvas by a small present program.** So a level's cost is proportional to the fragments it rasterises — `1/step^2` of the final pass — and the only thing that changes between levels is the SAMPLE DENSITY, a parameter: the iteration and colour arithmetic still exists ONCE (row 52) and no second shader restates it. Repeated levels of one view REUSE the reference orbit (counted, not inferred). The level sequence is a constant that depends on no depth, no machine and no stopwatch — there is no depth- or timing-based switch and no new cap (rows 46, 62). | **THE MECHANISM WAS CHOSEN BY MEASUREMENT, and the numbers are the per-level costs at 64x48 on this host's SwiftShader SOFTWARE rasteriser (row 47 applies — these are not hardware numbers): scale 1e-15, cap 7680: `8:4.5 ms (8x6)`, `4:5.7 ms (16x12)`, `2:15.5 ms (32x24)`, `1:58.2 ms (64x48)`; each level strictly fewer fragments and (measured) fewer ms than the next. The content really is coarse then fine, measured from the presented framebuffer after each level: the fraction of horizontally adjacent red pairs that are EQUAL falls `0.902 > 0.806 > 0.656 > 0.403`, and the fraction of 8x8 canvas blocks that are ALL IDENTICAL falls `1.000 > 0.083 > 0.042 > 0.021` — a step-8 NEAREST upscale is exactly 8x8 uniform blocks, a full-resolution frame is not. **REJECTED, with the reason:** (a) a full-resolution pass with a coarser SAMPLE RASTER (a `u_sampleStep` uniform that skips the loop off-grid) — cheaper in the loop but it still rasterises and writes EVERY canvas fragment, so its floor is the framebuffer bandwidth and it cannot reach `1/step^2`; (b) a mipmap/downsample OF a full-resolution result — every pixel is rendered, so it is not refinement at all and is not cheaper (this is the trap the deliverable names); (c) refining the ITERATION BUDGET instead of the sample density — that is a DIFFERENT image rather than a coarser one, and it would collide with D2's budget rule and the deep lane's correctness pins. | Writer, `session-…-refine`, 2026-09-22; pins `tests/gpu-refine.spec.js` 1–4 (per-level ms, the blockiness/correlation metrics, the orbit-reuse count), cost curve `[REFINE pin4]` lines in `/tmp/fracvibe-gate/` raw logs. **THE SAME CURVE AT 640x480, the viewport DECISIONS 58's cost curve used** (SwiftShader SOFTWARE, `ms to first useful frame / ms to final full image / ms for the whole chain`, levels in ms): scale 3 `2.2 / 18.2 / 34.9` `[8:2.2 4:4.8 2:6.4 1:18.2]`; 1e-4 `2.5 / 30.2 / 58.6` `[8:2.5 4:4.2 2:9.4 1:30.2]`; 1e-8 (deep) `31.6 / 1372.7 / 1858.6` `[8:31.6 4:92.4 2:349.5 1:1372.7]`; 1e-15 (deep) `72.7 / 4005.5 / 5346.3` `[8:72.7 4:274.9 2:980.5 1:4005.5]`. So at depth the user sees a frame of the new view **43–55x sooner** than the full image, at the cost of ~33 % more total GPU time (the coarse levels) plus the yields; at shallow depths first and final are the same order (2.2 vs 18.2 ms at scale 3), which is the owner's "it just wont be noticable at lower depths" — and it is NOT to be "fixed". |
| 90 | **GPU COARSE-TO-FINE, REACTIVITY: a GPU pass cannot be preempted, so the chain YIELDS a macrotask between levels and checks its generation token FIRST, and a superseded chain ABANDONS its remaining levels immediately.** The first (coarsest) level runs synchronously inside `renderWebGL`, so the wheel handler itself leaves a coarse frame of the NEW view on screen; the remaining levels are scheduled and each one re-checks the token before touching the GPU. The token is bumped by EVERY render request, so a colour tick or a resize supersedes too — strictly more aggressive than the view-change case the owner asked for, and deliberate. **MEASURED, wheel dispatched from INSIDE the refinement (a real `WheelEvent` on the canvas, via the app's own listener): wheel-to-first-visible-frame `3.8 ms` shipped against `30.2 ms` for the pre-change single-pass schedule — an 8x improvement — and the superseded chain applied exactly 1 level and SKIPPED 3 (`passesSkippedOnAbandon`), i.e. the GPU time it did not spend.** THE FAILING BASELINE IS IN-PIN: `__fv.setGpuSchedule([1])` runs the pre-change single full-resolution pass through the SAME renderer, scheduler and shader; against it the pin's assertions fail exactly as stated — `gpuJobToken()` is already `null` after `renderWebGL` (one atomic pass, nothing in flight), `passesSkippedOnAbandon` cannot move (`0`), the chain is never abandoned, and the wheel's first visible response costs a FULL pass. THE FINAL FRAME IS UNCHANGED: the `[8,4,2,1]` final frame is byte-for-byte the `[1]` frame at the deep view (pin 1) and at four shallow views (pin 3), which is what makes this a latency change and not a picture change. **A readback pin must now await the final level (`__fv.whenRenderSettled()`); the readback still lands before compositing, so no `preserveDrawingBuffer` was needed — measured, the post-await readback hash equals the final level's hash.** | The alternative — doing the whole chain synchronously — was rejected by construction: with no yield NO event can arrive between levels, so "check for a pending view change between passes" would be vacuous and the owner's "must not block further zoom" would not be met. Also rejected: `requestAnimationFrame` yields (they add a display frame per level, and the macrotask queue is what lets the browser deliver already-queued INPUT first — measured to work: the 3.8 ms figure includes the input delivery) and yielding without checking the token (the superseded chain would keep drawing a view nobody will see). | Writer, `session-…-refine`, 2026-09-22; pins `tests/gpu-refine.spec.js` 2 (in-pin baseline) and 1; control arms `/tmp/fv-armA.log` (the shipped schedule forced to `[1]` -> pins 1+2 RED) and `/tmp/fv-armB.log` (the supersede check deleted -> pin 2 RED, the superseded chain still rendering after its replacement had finished). |
| 91 | **GPU COARSE-TO-FINE, NO REGRESSION: `fullImagePasses` KEEPS its row-59 meaning ("the readout covers a FULL image"), and the coarse levels are counted by a NEW `gpuPasses` instead of redefining it.** The readout still reports the step-1 pass's ms, because that is the cost of the image the user is looking at; the whole-chain time (including the cheap levels and the yields) is a separate number at `lastGpuChain().totalMs`. The chain's token is `gpuJobToken()`, the GPU twin of the worker lane's `jobToken()`. | Redefining `fullImagePasses` to advance per level would have made the "more than one frame was applied" assertion easy and corrupted the one observable that proves the render-time readout is a full image — the same class of error as a pin that asserts a proxy one step upstream. Stating both facts separately is what keeps two independent claims measured: "the chain applied N frames" (`gpuPasses`) and "the time is a full image" (`fullImagePasses` + the step-1 level's own ms). Measured: one settled render advances `fullImagePasses` by exactly 1 and `gpuPasses` by the schedule length; the shallow golden readback hashes (`tests/gpu-arbitrary.spec.js` pin 4) are unchanged, so the refinement is invisible at and above the old 1e-4 boundary exactly as the owner predicted ("it just wont be noticable at lower depths"). | Advancing `fullImagePasses` once per level (breaks row 59's counted claim and the readout's meaning); reporting the CHAIN time in the readout (it is not a full image — it is the perceived-latency number, kept separately); adding a second "did a frame apply" mechanism when the lane already had one. | Writer, `session-…-refine`, 2026-09-22; pin `tests/gpu-refine.spec.js` 1 (the counters and the readout), pin 3 (shallow invisibility), `tests/gpu-arbitrary.spec.js` pins 2 and 4 (updated to await the final level; the golden hashes themselves are UNCHANGED). |
| 95 | **ITERATION-BUDGET INVESTIGATION (owner: "the iteration depth in auto mode needs to increase more aggressively. In deeper zooms, the structures get undifferentiated.") — the premise was TESTED AND FALSIFIED at the depths this host can measure, and the measurement is recorded rather than a plausible-looking constant bump.** Method: drove the SHIPPING kernel (`public/fractalKernel.js`, `iteratePixelSmooth` / `calcFractalChunk`) headlessly in Node at the COAST centre, 300x200, and counted DISTINCT escape values against iteration budget — at and ABOVE the internal cap by calling the raw iterator so `clampMaxIter` could not mask the answer. **Result: at 1e-12 the plateau is 1366 distinct values and it is reached at 8192; 16384 / 32768 / 65536 / 131072 / 262144 all give EXACTLY 1366. At 1e-14 the plateau is 646, again flat from 8192 upward. Adding 32x the budget changed nothing.** So at those depths the iteration cap is NOT what limits detail — more iterations would have been the WRONG fix, and I would have shipped it on the owner's say-so without this check. | Second measurement, the opposite direction: below the cap, DEEP views are ALSO not obviously under-provisioned. Smallest budget reaching 95% of the plateau: 1e-6 -> 8192 (auto gives 3072), 1e-8 -> 2048 (auto 4096, i.e. auto already OVER-provisions), 1e-10 -> 8192 (auto 5120), 1e-11 -> 8192 (auto 5632). The one clear under-provision is right at the KNEE: at 1e-4 the ramp deliberately returns **1** while the view needs ~1024 for 95% — the LANE-CONTINUITY ramp is correct about CONTINUITY but it does hand a near-knee deep view almost no budget. | **THEORY vs MEASUREMENT, stated because they disagree:** the accepted answer to "why do deeper zooms need more iterations" (Mark McClure, math.SE 4824024) is that *"the closer a point is to the Mandelbrot set, the more iterates you generally need to detect if it is in the exterior"* — i.e. the requirement is set by DISTANCE TO THE SET and grows as a power of the pixel size, predicting `N ~ log(1/zoom)`. That predicts rising requirements with depth; the CPU measurement shows a FLAT plateau. **Resolution: this host's CPU lane CANNOT settle it.** It projects coordinates in float64 with no perturbation, so its coordinate staircase (DECISIONS 71) already collapses at ~1e-16-1e-17; that limits how deeply the test can look, and the owner's "undifferentiated structures" is reported at depths this host cannot render meaningfully. **The measurement is therefore a falsification of the naive fix, NOT evidence that no problem exists.** | A fourth candidate that fits the symptom and is NOT an iteration issue, recorded for the next investigation: the GPU deep lane samples the reference orbit from a texture whose width is `min(MAX_TEXTURE_SIZE, MAX_ITER)` = 8192 texels, and the shader CLAMPS the sample index (`min(float(m), u_orbitW - 1.0)`) — so on views where the orbit's meaningful length approaches that width, the reference SATURATES and the picture can flatten. That is a TRANSPORT ceiling (DECISIONS 67's mechanism (a)), not a budget one, and the orbit/transport slice is already queued for it. | NOT DONE, deliberately: no constant was changed. What is needed from the owner is the SPECIFIC view: the zoom level and the centre where the structures go undifferentiated, so the GPU lane can be measured at that exact point instead of at a guessed one. | Dispatcher, 2026-09-22; raw scripts `/tmp/fv-iter-study*.mjs`, `/tmp/fv-iter-above.mjs`. |
| 96 | **DEFECT FOUND AND MEASURED \u2014 the owner's "bouncing, rendering the same spot again from scratch" is a CHAIN STORM driven by the startup animation, not a re-render loop and not an unchanged view.** The startup `animateZoom` (`public/app.js` ~1026) calls `viewer.setView()` **once per animation frame** (~60 fps, scale ~183 -> 3). Every view change starts a GPU refinement chain whose coarsest level is drawn SYNCHRONOUSLY, and the next frame abandons it 16 ms later. Measured in a real browser against the live origin, sampling `gpuChainLog()` / `gpuPasses()` / `getView().scale`: **t=300ms passes=17, t=700ms 35, t=1200ms 164, t=3500ms 263 \u2014 110 generations, 109 of them ABANDONED**. So a full coarse->fine chain was STARTED PER FRAME and almost none ever finished; it stops once the view settles. | **WHY THE SLICE MISSED IT:** the refinement was designed and measured against SPARSE view changes \u2014 one wheel tick per ~90 ms, where a chain has time to finish and where the measured wheel->first-response is 3.8 ms. A 60 fps animation guarantees every chain is superseded before completion, so the only thing the user ever sees is the repeated COARSE level: exactly "rendering the same spot from scratch". The refinement's own acceptance test drove wheel ticks, never an animation, so it could not have caught this. | **THE FIX AND ITS BOUNDARY (why it must NOT be "disable refinement"):** while the view is animating, one cheap pass per frame and NO chain; when the view SETTLES, run the chain so the user ends with the refined image. Sparse input keeps its chains \u2014 that is when they pay \u2014 so the wheel latency property must be unchanged. | Rejected as fixes: debouncing the wheel path (it is not the problem and the latency property depends on it); disabling refinement globally (defeats the owner's request); treating it as an infinite loop (it terminates \u2014 two stationary-view probes showed passes flat at 248/263 for 8-10 s). | Dispatcher, 2026-09-22; raw scripts `/tmp/fv-bounce{,2,3}.mjs`; dispatched to writer `21e4959e` on `fix/anim-refine`. |
| 97 | **ANIMATION CHAIN STORM, THE MECHANISM: the refinement chain is gated on the app's OWN "the animation is still driving the view" state (`zoomAnimationSettled`), and while that state is false a view change costs exactly ONE full-resolution pass and starts NO chain; the chain runs ONCE, in the animation's own completion branch, on the SETTLED view.** The gate sits in `renderWebGL()` — the single funnel every render path uses (`viewer.setView` -> the re-pointed `viewer.render` -> `renderFractal` -> `renderWebGL`) — so the animation's per-frame `setView` cannot start a chain, while every sparse input AFTER the animation behaves exactly as the REFINE slice measured it. `zoomAnimationSettled` is set true and never reset, so the exception has a bounded lifetime: the startup animation and the pre-animation load frames. The pass is `renderPass(view, ..., step=1)`, i.e. `draw`, the pre-refinement single-pass frame. | **WHY A FULL-RESOLUTION PASS AND NOT A COARSE ONE.** The animation renders shallow scales (~183 -> 3) where a full pass is cheap, and step 1 is byte-for-byte the frame this path drew BEFORE the refinement slice existed — the animation the owner already accepted as smooth. A step-8 frame per animation frame is cheaper in fragments but would show 8x8 blocks for the whole ~3.5 s animation and then snap to detail: a visible regression the owner did not ask for, and "the animated view still updates smoothly" is part of the requirement, so it is REJECTED. Also REJECTED: debouncing/throttling the animation (that changes the animation the owner sees, and the chain storm is not caused by its speed); coalescing chains with rAF (still one chain per rAF while animating — the requirement is that an animation-driven view starts NO chain); disabling refinement globally or for the wheel (defeats the owner's request and its measured 3.8 ms first-response property); putting the decision in the animation loop instead of the render path (every pre-settle render, including the load path and the deferred-WebGL init render, must be covered, and they all pass through `renderWebGL`). | The state is the APP'S OWN (`__fv.animationSettled()`); no new flag duplicates it. The TEST-ONLY `__fv.setAnimationSettled(v)` only writes that same variable so a pin can script an animation-like sequence through the real render path; no production code calls it. | Writer, `session-21e4959e`, 2026-09-22; `public/app.js` (`renderAnimationSinglePass` + the `renderWebGL` branch + the completion-branch chain trigger); pins `tests/anim-refine.spec.js` 1-2. |
| 98 | **ANIMATION CHAIN STORM, BEFORE/AFTER (SwiftShader, software rasteriser — row 47 applies; these are NOT hardware numbers).** BEFORE, measured by reverting ONLY `public/app.js` to the pre-fix build and running the new pin against it (the RED control): the scripted animation-like sequence (24 view changes 16 ms apart, 1000x700) started **24 chains, 22 abandoned, 28 passes skipped, 67 GPU passes**, against the post-fix **0 chains, 0 abandoned, 0 skipped, 24 passes (exactly one full-resolution pass per frame), 24 full-image passes, nothing in flight**; the REAL startup animation started **110 chains with 108 abandoned and 297 GPU passes**, against the post-fix **1 chain**. The dispatcher's independent live measurement was 110 generations / 109 abandoned / 263 passes: the same shape, reproduced. THE FAILING BASELINE IS IN-PIN as an arm of the same pin: the identical scripted sequence with the view SETTLED (which is what the pre-fix build did on every frame) starts **24 chains, abandons 23, skips 25-29 and applies 66-70 GPU passes** (the exact point at which the last chain is superseded varies by run), while the fixed arm starts none. The SETTLED view then runs **exactly one chain on the shipped schedule `[8,4,2,1]`, completed, one full-resolution frame** — so the animation still ends on a fully refined image. | **NO REGRESSION (binding, measured).** (a) The final image is unchanged: the chain's last level is the step-1 pass, pinned byte-for-byte at four shallow views (`tests/gpu-arbitrary.spec.js` pin 4 FNV goldens) and against the `[1]` schedule (`tests/gpu-refine.spec.js` pins 1 and 3) — this fix never alters a chain, only WHEN one starts. (b) The wheel path is UNTOUCHED: after the animation `zoomAnimationSettled` is true forever, so a wheel starts a chain exactly as before and `tests/gpu-refine.spec.js` pin 2's wheel-to-first-frame property stands. (c) No CPU crossover, no depth- or time-based engine switch (row 46): the branch keys on the ANIMATION state, never on depth, elapsed time or machine. (d) LANE-CONTINUITY and the P1 1e-15 / 1e-40 BigInt-reference pins are unaffected (no shader, kernel or view arithmetic changed). | Rejected as fixes: debouncing the animation; one chain per rAF; disabling refinement; a coarse animation frame; reading depth or timing to choose a path (row 46). | Writer, `session-21e4959e`, 2026-09-22; pins `tests/anim-refine.spec.js` 1 (scripted, baseline in-pin) and 2 (the real startup animation); RED control log `/tmp/fv-animrefine-red1.log`, green log in the gate raw log. |
| 99 | **ITER-CAP (owner request, verbatim: "Iterations max needs to be much bigger, lets say 100000, for deep zooms. Also double the auto-iterations please."): `MAX_ITER` 8192 -> 100000, `ITER_BUDGET_PER_DECADE` 512 -> 1024, and the reference-orbit TRANSPORT is decoupled from `MAX_ITER` by carrying it in a 2D float texture.** The orbit was a 1D texture of width `min(MAX_TEXTURE_SIZE, MAX_ITER)` = 8192, with the shader clamping `min(float(m), u_orbitW - 1.0)`, so a 100000 budget re-sampled the LAST reference value for every index past 8191 — a frozen reference. `orbitTextureLayout(length)` is now row width `min(length, MAX_TEXTURE_SIZE)` and `ceil(length / width)` rows, so the transport carries `MAX_TEXTURE_SIZE^2` samples (8192^2 = **67 108 864** here; 16 384^2 = 268 435 456 on a typical GPU) with NO dependence on `MAX_ITER`; `u_orbitRows`/`u_orbitLen` carry the geometry and `sampleOrbit()` does the row/column fetch. If a device's capacity ever binds, the deep-lane budget is REDUCED to the transported length and the difference is recorded in the observable `orbitShortfall` — a declared ceiling, never a silent clamp. The false inline comments (`MAX_TEXTURE_SIZE == MAX_ITER == 8192`) are corrected. | **A CEILING and a SLOPE, not a step**, so DECISIONS 62 ("no hard stop", "no boundary in the math") and LANE-CONTINUITY's continuous ramp are preserved: the ramp departs from zero at the knee exactly as before and doubling a scale-invariant slope doubles the deep anchors without introducing a boundary. With 1024/decade the rule returns 5120/6144/8192/15360 at 1e-5/1e-6/1e-8/1e-15 and first reaches the 100000 cap past zoom ~1e98, so over every reachable depth the **rule**, not the cap, decides. **The 2D transport was PROBED before it was written** (standalone WebGL1, this host): `#define MAX_ITER 100000` compiles AND links under ANGLE/SwiftShader in 4.9 ms, and an 8192x13 RGBA-float orbit texture round-tripped indices 0, 1, 8191, 8192, 8193, 16384, 99999 and 100000 **exactly** through NEAREST (`getError()` 0). In the real app at cap 100000 both deep-lane sources report `length 100001, width 8192, rows 13, covered true, shortfall 0`. | Rejecting the raise (the owner's request, and the slider/UX ceiling is part of it); keeping the 1D transport and clamping the budget to 8192 (clamp-and-lie — the exact failure the brief names); a uniform array for the orbit (GLSL ES 1.00 forbids dynamic uniform indexing); an if-chain over N slice textures (sampler arrays are constant-index-only in GLSL ES 1.00, and a 13-way branch per iteration); raising the cap only past the transport (a frozen reference); treating `MAX_TEXTURE_SIZE` as the orbit length (it is the ROW limit, so the 2D layout is what frees the length). | Owner request 2026-09-22; writer session `8bb928f1`, branch `feat/iter-cap`, base `origin/main` = `429ab31`, code `09a6315`, test fix-forward `2db9bf4` (the gated head); DECISIONS 62, 95 and 67(a) are its binding context. |
| 100 | **ITER-CAP MEASUREMENTS (this SwiftShader host — DECISIONS 47 applies: SOFTWARE numbers, never hardware performance).** **(A) THE TRANSPORT.** `MAX_TEXTURE_SIZE` = **8192** here (WebGL and WebGPU both), so capacity is **67 108 864** samples against the **100 001** a 100000 budget needs (8192 x 13 rows). Both deep-lane sources carry it: the float64 lane (`ensureReferenceOrbit`) and the BigInt Worker (`computeOrbitFixed`, previously handed `maxOrbitWidth` = 8192, now the full needed length). **(B) THE CPU REFERENCE-ORBIT COST** (`BigOrbit.computeOrbitFixed`, Node): an ESCAPING reference is bounded by its own escape and padding, so cap 8192 -> 100000 costs **19 -> 12 ms at 1e-15 (escapedAt 3085)** and **11 -> 25 ms at 1e-30** — no material increase; a NON-ESCAPING reference is the ~12x worst case, **17-21 ms -> 216-235 ms** in Node and **63.5 ms** in the shipped Worker (Misiurewicz centre, bits 192). The float64 orbit loop itself is **1 ms -> 4 ms**. VERDICT: acceptable per view — one long-lived Worker, off the interaction loop, once per (centre, budget, bits) key, already cached by P2; no new worker/caching work is owed. **(C) GPU COST AT THE NEW CAP** (256x192, scale 1e-15, cap 100000, float64 lane, shipped chain 8->4->2->1): the first (coarsest, 32x24) pass is **21-80 ms**, then 49-73 / 186-191 ms, and the FINAL full image (step 1) is **728-733 ms**, chain total **1019-1044 ms** — the coarse->fine property holds at the new cap (first visible frame ~10-35x sooner than the final image). **(D) DID THE EXTRA ITERATIONS CHANGE ANY DETAIL?** Both answers, measured. NO at the depths DECISIONS 95 could reach, reproduced through the GPU lane too: at 1e-12 the CPU float64 direct projection gives **1368 distinct integer escape values, flat from 8192 to 200000 with 0 pixels changed and 0 reclassified**; at 1e-14 **640, flat**; the GPU perturbation lane at 1e-15 gives **740 distinct escape values at cap 15360 AND at cap 100000** (identical). YES at one depth this host can render: at **scale 1e-7** (256x192) the old-style auto budget of **7168** leaves **12 063 of 49 152 pixels (24.5%) classified inside** with 4446 distinct values, while cap **100000** gives **inside 0** and **6984** distinct — the budget really was the limit there, and the doubled slope plus the raised ceiling resolves it. **(E) THE FROZEN-REFERENCE FAILURE IS LATENT, MEASURED RATHER THAN CLAIMED.** The shader's reference index `m` is reset by REBASING, so at structured views it stays far below 8192: Misiurewicz (preperiodic) `max m ~ 230`; a centre whose reference escapes at 12 562 gives `max m ~ 3900` (in-pin, `maxRefIndex=3751`); COAST is bounded by its own escape (3086). Only a uniformly-INSIDE view drives `m` to the budget, and there the frame is uniform. So at every structured view measurable here the OLD 8192 transport would not have changed a painted pixel: the freeze was a LATENT lie, not an observed colour error. The transport is fixed anyway, for two measured reasons: coverage must be a function of the device, not a coincidence, and a slowly-diverging reference (near-parabolic) can hold `m` high while pixels escape — a case this host could not construct. Pin 3 therefore asserts COVERAGE (`length >= cap + 1`, `covered`, `shortfall 0`, and the values past 8191 are real, not a padded repeat) and REPORTS the measured max index; it does not claim a colour effect it cannot see. **(F) THE COLOUR-TABLE CONTRACT (verified, arithmetic shown).** `colorTableSize(maxIter) = clampMaxIter(maxIter) * COLORS_LUT_STRIDE + 1`, so a 512-cap frame carries 512*256+3 = **131 075** entries (0.50 MiB), the 8192-cap deep frame **2 097 155** (8.00 MiB), and only a frame actually run at the ceiling **25 600 003** (97.66 MiB); the D1 module-maximum constant would have been 25 600 001 entries for EVERY frame. No path sizes by the module maximum — D2 pin 3 measures `cap512=524300 B, cap8192=8388620 B, maxCap=102400012 B` through the app's own counters. **(G) NO REGRESSION, the binding measurement: FULL gate at the landing code sha `2db9bf4` -> exit 0 GREEN, 91 passed / 2 skipped / 10.0m** (`/tmp/fracvibe-gate/gate-full-2db9bf4-20260922T091624Z.log`). Every bound class is green in that log: GPU-ARBITRARY pin 4 (the three PLAIN-lane goldens byte-identical; only the deep-lane 1e-8 golden re-captured because the budget there genuinely doubled 4096 -> 8192), P1 pins 1-3, P2 pins 1-2, D1/D2, LANE-CONTINUITY pins 1-4 (no boundary outlier, no step), REFINE pins 1-4, S3 pin 1 at the raised cap, the manual/no-GPU paths and no CPU crossover. | **(G) A RED FIRST, AND WHY, recorded rather than smoothed:** the FIRST full gate at the cap raise (`09a6315`) was RED on **9** pins, and **none was a product defect** — each was a pin calibrated to the old cap or the old slope, and the fix-forward is its own commit. Four `worker-lifecycle` fixtures used `slider.max` as their "heavy but in-flight" budget, which is now 100000 and runs for MINUTES (scoped to a bounded HEAVY_ITER = 8192, the old cap); `hygiene` S6 and `zoom-clamp` drove 1e-45/1e-12 at the default viewport, where the budget is now up to 46080 (state pins, so the render was shrunk to a small viewport); `bigorbit` pin 1's non-degeneracy bound moved 5 -> 4 because the doubled budget pushes the reference past its own escape at 19200; `gpu-refine` pin 1's absolute colour-variation thresholds are tied to D1's `t = v/cap`, which a larger budget compresses (re-expressed as a monotone non-increasing uniform-8 fraction with a strictly finer final frame — the structural statement — while the byte-identical-final-frame assertion is unchanged); and `gpu-arbitrary` pin 4's deep-lane golden was re-captured. | Claiming a detail improvement at every depth (falsified at 1e-12/1e-14/1e-15); claiming the old transport painted wrong pixels at measurable structured views (falsified by (E)); quoting any timing as hardware performance; treating `MAX_TEXTURE_SIZE` as the orbit length; asserting the frozen arm differs when it does not. | ITER-CAP writer session `8bb928f1`, branch `feat/iter-cap`, base `429ab31`; code `09a6315`, test fix-forward `2db9bf4` (the gated head); raw probes `/tmp/fv-itercap/`; gate log quoted above. |
| 101 | **TOUCH-INPUT (owner request 2026-09-22: *"Zooming with smart phone or tablet should also work"*): the canvas takes TOUCH — one finger PANS and two fingers PINCH-ZOOM, on BOTH the WebGL and the 2D canvas.** The gesture lives in `FractalViewer` (the ONE view-owning seam): `onTouchStart`/`onTouchMove`/`onTouchEnd` decide between the two gestures, one finger drives the SAME `panView` the mouse drag uses, and two fingers call the new `zoomViewAt()` — a scale factor `lastDist/dist` (fingers APART = zoom IN = a SMALLER `scale`) **anchored at the midpoint** and preceded by a `panView` of the midpoint's own movement, which compose EXACTLY into "the world point under the fingers' midpoint stays under it". The binding is `app.js`'s existing `attachFractalMouseEvents` (so a WebGL canvas REPLACED after context loss is re-bound with the same four listeners), and the CSS half is `touch-action: none` on both canvases — the property is what makes the browser hand a pinch to the canvas before any event fires, and the `{ passive: false }` + `preventDefault()` is the half that keeps holding when the property is not enough. | **WHAT WAS ACTUALLY MISSING, measured before the change:** the app could already PAN on a touch device (Chrome synthesises mouse events from a one-finger drag) but could not ZOOM AT ALL — `FractalViewer.setupEvents`/`attachFractalMouseEvents` bound only `mousedown`/`mousemove`/`mouseup`/`wheel`, and no `touch-action` was declared, so a two-finger gesture zoomed the PAGE. **THE ANCHOR IS THE PART A CENTRE-ONLY ZOOM GETS WRONG, and it is derived from the app's own contract, not invented:** the ONE projection both renderers draw with is `FractalKernel.pixelToCoord`, whose world point under a screen offset `o` from the centre is `center + o·scale`; holding it fixed while `scale -> factor·scale` shifts the centre by `o·scale·(1 - factor)`, and panning by the midpoint delta first then zooming about the NEW midpoint composes into exactly that (verified numerically in the pin, against `pixelToCoord` itself, at an OFF-CENTRE midpoint so a centre-only implementation cannot pass). **CDP'S TOUCH MODEL WAS MEASURED, NOT ASSUMED:** a probe (`/tmp/touch-cdp-probe.js`) showed a two-finger `touchStart` arrives as TWO `touchstart` events (one per changed point), and — against the protocol docs' wording — `touchEnd`'s `touchPoints` list holds the points being RELEASED, so `touchEnd: [{id:2}]` lifts finger 2 and leaves finger 1 down; the pin's partial-lift test depends on that and says so in a comment. | **REJECTED:** (a) **a centre-only pinch** — it passes a scale check and slides the plane under the fingers, which is the visible defect the anchor exists to prevent; (b) **treating a pinch as a synthetic wheel tick** (`deltaY` from the finger separation) — it would reuse the wheel's centre-only zoom and could not express the midpoint, and it would couple touch to `onWheel`'s latency instrument; (c) **a gesture library** (hammer.js et al.) — a new dependency and a second event layer for two gestures that are ~40 lines through state the viewer already owns; (d) **a POINTER-EVENTS rewrite** — it would rewrite the pinned mouse seam (S1/S6/hygiene/zoom-clamp pins run on it) to deliver a gesture that touch events deliver without touching those paths; (e) **`user-scalable=no` on the viewport meta** — it disables page zoom for the WHOLE document (an accessibility cost) where `touch-action: none` on the two canvases scopes it to the gesture surface. | Writer/verifier (one actor), session `session-1616d8a1`, 2026-09-22, branch `feat/touch-input`, code `e536bd9`. **Pins:** `tests/touch-input.spec.js` (5), driven by REAL `Input.dispatchTouchEvent`, because a synthetic `new TouchEvent(...)` would pass with the CSS half missing. **RED CONTROLS, three, each isolated and restored BYTE-IDENTICALLY** (`fractalViewer.js` sha256 `401456b5cfd5…`, `app.js` `f0762ceca3fc…` before and after every arm; log `/tmp/fv-touch-arms.out`): ARM A, `zoomViewAt` reduced to a centre-only zoom → the anchor pin RED on *"the anchored point does not slide in x"* while the scale assertion stayed green; ARM B, the separation ratio flipped (`dist/lastDist`) → the scale pin RED on *"a 2x finger spread doubles the zoom"*; ARM C, the four touch listeners removed from `attachFractalMouseEvents` → BOTH the `defaultPrevented` pin and the pinch pin RED. **GATE: MY OWN FULL gate at `e536bd9` -> exit 0 GREEN, 98 passed / 2 skipped / 13.3m** (`/tmp/fracvibe-gate/gate-full-e536bd9-20260922T102745Z.log`); the suite was 93 passed before this slice and 98 after, i.e. exactly these 5 pins, and the 2 skips are the pre-existing WebGPU adapter-gated tests. |
| 101 | **HARDWARE CERTIFICATION ARRIVED (owner's RTX 5070, Windows/Chrome 153, ANGLE + D3D11) — and it FALSIFIES the premise that a real driver preserves error-free transforms.** `kind=HARDWARE`, `gl_version` WebGL 1.0 with WebGL2 available, `MAX_TEXTURE_SIZE` **16384** (2x this host), `OES_texture_float` present and `float_texture_sampleable` (maxDelta 1, i.e. it genuinely samples), `gl_error` NO_ERROR, readback plausible, **deep lane engaged and CORRECT**: `deep_ref_bigint` at 1e-20 gives **misclassified 0.000%, mean|dn| 0.000, max 0, orbitTexels 16384**. **ALL THREE DOUBLE-SINGLE PROBES FAIL: `ds_low_component` ZERO, `ds_low_changes_result` NO (`compensated=1.0000019` vs `naive=1.0000000` are IDENTICAL on this driver), `ds_twoprod_low` ZERO.** | **THE PROBE IS NOT THE PROBLEM \u2014 I CHECKED, because the operands being run-time values is what makes this a result rather than an artifact.** `gpuDiagnostic.js`'s `DS_FRAG` passes its operands as **uniforms** (`uniform float u_b`), not literals, so a compiler CANNOT constant-fold them away; `compensated` is computed from the READBACK (`1 + (at(1,0)/255)*2^-19`), not hardcoded. On SwiftShader the same probe read `ds_low_changes_result=YES` and `ds_twoprod_low=NONZERO` with a variable operand \u2014 the very distinction research \u00a77 draws (a literal operand was folded there, a variable one survived). **On D3D11 even the variable-operand residuals are erased.** That is the research's warning fully realised on a shipping driver: ANGLE/D3D11 reassociates and/or flushes, so `TwoSum`'s low term \u2014 algebraically zero over the reals \u2014 is optimised away. | **CONSEQUENCE FOR DECISIONS 66 (the k-component expansion ladder): the classic error-free transforms are NOT usable as written on this hardware.** The ladder is measured correct on SwiftShader only; the D3D11 result means the next step past the current reach must use **integer-controlled transforms** (decode f32 sign/exponent/significand into u32 limbs, do the 24x24 product in exact 48-bit integer arithmetic, renormalise) \u2014 the route luma.gl selects automatically on Apple GPUs for exactly this reason, and which the research \u00a77 already flagged as the robust option. **What this does NOT do is retroactively invalidate what is shipped:** the k=2 expansion is NOT integrated in the app (it is a standalone library, unreferenced), and the live deep lane uses the **exponent split** (range) with a float32 delta (mantissa), which this same report certifies as correct at 1e-20 on the owner's GPU. The FAILs constrain the FUTURE ladder, not the present renderer. | Also measured on this hardware, useful and improving our assumptions: `MAX_TEXTURE_SIZE` **16384** doubles the orbit transport capacity (16384^2 = 268M samples; the report shows orbitTexels 16384 against our 8192-based expectation), `mediump` is **23-bit** here (same as highp, unlike SwiftShader's 10-bit), and the **cost readout read 72.1 ms at all three depths** \u2014 flat across 1e-4/1e-6/1e-8, which is suspicious and is recorded as a diagnostic anomaly to check, NOT as a performance result. | Owner's Test-button report, 2026-09-22; probe source `public/gpuDiagnostic.js` `DS_FRAG`. |
| 102 | **OWNER CAVEAT THAT SETS THE BAR FOR ANY GPU-PRECISION WORK: "Not against it, but my hardware is just one hardware... though it is pretty typical, being nvidia."** A single-vendor, single-driver result is evidence, not proof, and the correct response is not extra caution but a different TARGET: the transform must be **robust by construction**, with the hardware run confirming rather than defining correctness. Concretely this rules out (a) tuning anything until it passes on D3D11, (b) treating "works on my GPU" as the acceptance test, and (c) a design whose correctness depends on one compiler's optimisation choices \u2014 which is precisely the trap DECISIONS 101 fell into in reverse: the FLOAT double-single path looked fine on SwiftShader and is erased on D3D11, so **either vendor alone would have misled us**. | **THE PORTABILITY ARGUMENT FOR INTEGER-CONTROLLED TRANSFORMS IS STRUCTURAL, and this is why they are the right next probe rather than a workaround:** the classic error-free transforms depend on individual `f32` ROUNDING POINTS that no shader spec guarantees (WGSL permits reassociation/fusion outright; GLSL ES is silent, and ANGLE's D3D11 backend evidently reassociates). An integer-controlled transform decodes the significand and does the 24x24 product in **exact `u32` arithmetic**, whose result is bit-exact by definition of integer maths \u2014 there is no rounding point for a compiler to reassociate away. That property is what makes it vendor-independent in principle; the probe exists to check whether it holds in practice. | Treating the RTX 5070 result as the last word; building the k-ladder on the float path because it passed here; assuming "it is integer maths so it must be safe" without measuring \u2014 integer ops can still be affected by driver-level transforms of the surrounding float conversion, which is exactly what the probe measures. | Owner, 2026-09-22; probe dispatched as `feat/int-transform-probe` (writer `b8a975cd`). |
