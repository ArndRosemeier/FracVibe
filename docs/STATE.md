# FracVibe — STATE OF RECORD (the board)

> One screen, overwritten in place. Records are `field=value` so a query is a
> `grep`. **This board is checked, never believed** — every record names something
> checkable (sha, branch, worktree, session id, path). Read this first, then
> `MODERNIZATION.md` (plan of record) and `docs/DECISIONS.md` (why).

reconciled: origin/main=e6f2f87 · local main=e6f2f87 (0 behind, 0 ahead — IN SYNC; the PUBLISHED CODE is `e6f2f87` = P2 + favicon `7735d36` + WORKER-CANCEL `7b25bf2`/`c1527d7`) · 2026-09-21T17:17Z · by=session-0e97db24-5f1e-49f6-8e63-18a285c027a0 · host=MemAvailable ~18GB, `:3000` free, no gate lock · **ONE worktree in flight: `worktrees/wc-worker-cancel` (branch `fix/worker-cancel`, writer `67ffc583`) — the WORKER-CANCEL fix; its tree is clean of `public/index.html`, and the two footprints were checked disjoint before the favicon landed** · **the CDN is FULLY P2** — the owner purged by URL and all seven assets are byte-identical to `dist/` on their BARE URLs · **`main` advanced to P2's landing `a2fe502` at 16:21Z — a WAKE EVENT this session did not cause** (the writer committed, pushed and retired itself, leaving no board row); it is **dispatcher-verified: MY OWN full gate at `a2fe502` → exit 0, 58 passed / 3.5m**, plus my own probe (no gap found) and a published-subpath suite · **FAVICON LANDED + PUBLISHED `7735d36`** (gate 59 passed, control RED, live URL now 200 `image/svg+xml` with ZERO non-2xx — the campaign-long unexplained host-root 404 is gone) · **WORKER-CANCEL is FIXED, VERIFIED AND PUBLISHED** (`e6f2f87`): never terminate a Worker mid-`importScripts`, a `ready` handshake, and a bounded self-heal; dispatcher rebased, re-gated (60 passed) and both control arms recorded; writer worktree+branch RETIRED · registry=3 PROJECT sessions (this one + two ended predecessors `f6d26a74`, `d1525624`)
CORRECTION | 2026-09-21T16:12Z | the reconciled line went stale by ONE commit at this reconcile: it still claimed `origin/main=57adf7b` while main was already `4051e91` ("docs: dispatch P2"), which is the commit that dispatched the in-flight P2 writer. The drift was one docs commit, not lost code, and the in-flight writer's base (`57adf7b`) is therefore still `origin/main`^1 — no re-base is owed. Recorded rather than silently rewritten, per the board's own rule that its most important claim is the one it is checked against.

## OWNER
OWNER | 2026-09-21 | decision=**push approved** — owner chose "Push all 5 commits to origin/main" | effect=Phase 0 + machinery land on `origin/main`; the deployed blank-canvas site is fixed
OWNER | 2026-09-21 | verbatim: *"I want this to be resilient and correct, you can take your time to achieve that state. You be the judge how to achieve this best."* | effect=standing mandate; the METHOD is the dispatcher's judgement and is written down in `docs/PLAN.md` | ledger row 6

## 🌐 PUBLISHED — https://apps.futuremagic.de/fracvibe/
PUBLISHED | 2026-09-21T11:34Z | slug=`fracvibe` | host=the apps host (`~/apps` served over HTTPS, public, no login) | mechanism=`~/apps/fracvibe -> /home/administrator/projects/FracVibe/dist` (a SYMLINK, so re-staging `dist/` goes live instantly) | source=`dist/` staged from `public/` as an exact mirror
VERIFY | local, bypassing the CDN, `http://127.0.0.1:8082/fracvibe/` → 200 for `/`, `index.html`, `app.js`, `fractalKernel.js`, `fractalWorker.js`, `three.module.js`, `styles.css`, all with the right MIME (`text/javascript`, `text/css`) | CDN `https://apps.futuremagic.de/fracvibe/` → 200, and its `index.html` is **byte-identical to the local `dist/index.html`** (so it is the NEW build, not a cached old one) | the four deleted dead files → **404** both locally and through the CDN | **the FULL suite (43 tests, all 9 spec files) was run AGAINST THE PUBLISHED SUBPATH** (`baseURL=http://127.0.0.1:8082/fracvibe/`, no `webServer`, config /tmp/fv-publish.config.js) → **43 passed / 2.1m**, log /tmp/fv-published-suite2.log
FINDING | the app is plain static ESM with every path RELATIVE (checked: no `src`/`href`/`import`/`new Worker` starting with `/`), so it needs NO base path — the host's 301 to the trailing slash is enough. That is why the suite runs unmodified under the subpath (the suite was made subpath-ready at `001cec9`).
FINDING | **FracVibe was ALREADY published and serving a STALE build.** `~/apps/fracvibe` existed before this publish (created 2026-09-21T10:11Z) pointing at the same repo `dist/`, but `dist/` had not been re-staged since 09:33 — it still contained the four deleted dead files AND the pre-S3 `fractalEngine.js`/`fractalEngineMain.js`, and was missing `fractalKernel.js`. The live site was therefore a pre-S1 build. Re-staging fixed it; the dead files now 404 and `fractalKernel.js` is live.
REPUBLISHED | 2026-09-21T16:30Z (deployed content = main code tree = P2's landing `a2fe502`) | staged exactly with `rsync -a --delete --exclude '.git*' public/ dist/` (exit 0; `diff -rq public dist` → **no differences**) | reason=the P2 landing had to be published: leaving `dist/` at P1 while `main` is P2 is the "silently stale `dist/`" failure this board already records once | local origin `http://127.0.0.1:8082/fracvibe/` → 200 for `/`, `index.html`, `app.js`, `bigOrbit.js`, `orbitWorker.js`, `webglFractal.js`, `fractalKernel.js`, and every served asset hashes **SAME as the staged file**; the new modules serve their markers (`computeOrbitFixed` in `bigOrbit.js` + `orbitWorker.js`, `import './bigOrbit.js'` in `app.js`). **CDN, measured `?cb=` per asset:** the two BRAND-NEW URLs are fresh at the edge (`bigOrbit.js` `039244edc9ed`, `orbitWorker.js` `053313302d68`, both = staged), while `app.js` and `webglFractal.js` remain **STALE P1 copies** on their bare URLs (`age: 5481`, `cf-cache-status: HIT`) until ≈20:06Z. **So the canonical URL is currently a CONSISTENT P1 site, not a broken mix**: a plain-visitor browser load returns the P1 `app.js`, which does not reference the new modules at all (`grep -c bigOrbit` on the bare `app.js` → **0**) and therefore never requests them; slider `max` 8192, 2 canvases, no pageerrors. P2 becomes visible to ordinary visitors when that cache expires, or immediately after a purge of those two URLs (owner action) or via per-asset `?cb=`. **P1's `dist/` snapshot kept at `/tmp/fv-dist-p1-20260921T1629/`.** See the owner-side note below.
DEPLOY-VERIFY-P2 | 2026-09-21T16:34Z | **the deployed build was verified TWICE over, not by curl alone.** (1) **THE FULL SUITE RAN AGAINST THE PUBLISHED SUBPATH** — `npx playwright test --config /tmp/fv-publish.config.js` (baseURL `http://127.0.0.1:8082/fracvibe/`, no `webServer`) → **exit 0, 58 passed / 3.5m** (raw log `/tmp/fv-published-p2-suite.log`); this exercises the whole app, including P2's three new pins, against the artifact actually served. (2) browser on the origin: `bigOrbitInfo`/`bigOrbitRequests` present, slider `max` 8192, **0 pageerrors**, and the deployed precision rule answers correctly — `bitsForScale(1e-30) = 192`, `(1e-20) = 192`, `(1e-19) = 128`, `margin = 64`, `stepBits = 64`. The benchmark for "published" in this project is now this: a full subpath suite plus a browser read of the live artifact.
REPUBLISH|PUBLISH-OWNER-NOTE | 2026-09-21T16:30Z | **to cut the public site over to P2 NOW rather than at ≈20:06Z, the owner purges two URLs in Cloudflare** (Caching → Configuration → Purge Cache → Custom Purge): `https://apps.futuremagic.de/fracvibe/app.js` and `https://apps.futuremagic.de/fracvibe/webglFractal.js`. There are **no Cloudflare credentials on this host**, so this is not something an agent here can do — see REPUBLISH-CORRECTION. The durable fix is the `PUBLISH-CACHE` queue row (version the asset URLs); a purge is a stopgap.
REPUBLISHED | 2026-09-21T16:15Z (deployed content = main code tree = P1's gated tree `9c6bf28`/`aa39f1c`; `origin/main` = `97ee9e1`, whose only difference is docs) | reason=owner instructed "please publish" | staged exactly with `rsync -a --delete --exclude '.git*' public/ dist/` (exit 0; `diff -rq public dist` → **no differences**) | **what the stale `dist/` was**: before staging it held `app.js` 73 084 B / `fractalKernel.js` 25 958 B / `webglFractal.js` 17 916 B — a pre-P1 AND pre-D2 build whose `webglFractal.js` sha (`a728973f3ced`) and `app.js` sha (`cdf3f94ca245`) match **no committed revision** in this repo (checked `0ed9b63`…`HEAD`), so the live site had been stale by at least the D2+D1+P1 landings. Backed up to `/tmp/fv-dist-publish-20260921T1620/` before staging.
REPUBLISH-VERIFY | 2026-09-21T16:15Z | local origin `http://127.0.0.1:8082/fracvibe/` (bypasses the CDN) → 200, browser (headless Chrome, `channel: 'chrome'`) reports slider `max = 8192`, **zero pageerrors**, and `webglFractal.js` carries ALL of D1's `smoothIterationValue`, D2's `MAX_ITER = 8192` + `clampMaxIter`, and P1's `usePerturbation` + rebasing + rescaling | **byte-identity is PROVEN, not assumed**: each served asset fetched cache-busted through the CDN hashes SAME as the staged file for all 11 (`index.html` `40eb6d089e8b`, `app.js` `714dd3ddff86`, `fractalKernel.js` `373a494ebe4b`, `webglFractal.js` `0af5eae86457`, `fractalViewer.js`, `fractalWorker.js`, `fractal3d.js`, `memoryRepository.js`, `colorSchemes.js`, `styles.css`, `three.module.js`); the two dead files → **404** through the CDN; a plain-visitor load (NO cache-buster) has **no pageerrors and no non-2xx request**. **The FULL suite was NOT re-run for this publish** — the host-wide suite lock is held by the in-flight P2 writer — so the verification rests on (a) the recorded FULL gate at `9c6bf28` (exit 0, 55 passed, /tmp/fracvibe-gate/gate-full-9c6bf28-20260921T154409Z.log), (b) the P1 rebase being byte-identical (tree hashes checked at `9c6bf28` vs `aa39f1c`: `app.js` `ac73f45f3102`, `webglFractal.js` `f2846273ef2a`, `fractalKernel.js` `8fbb019c2ef0`, `tests/perturb-core.spec.js` `0a408989afde`), and (c) `git diff --name-only aa39f1c..HEAD -- public/ tests/ scripts/ server/ package.json playwright.config.js netlify.toml` being **EMPTY**. Every commit after P1 is `docs/STATE.md` (and P1's own `docs/PLAN-DEEPZOOM.md`) only.
REPUBLISH-CAVEAT | 2026-09-21T16:15Z | **the CDN edge cache is in a MIXED state and this is measured, not feared.** Bare (what a normal visitor gets): `index.html` and `fractalKernel.js` are FRESH, while `app.js` (serving `cdf3f94ca245`) and `webglFractal.js` (serving `a728973f3ced`) are **STALE pre-P1 copies** — `cache-control: max-age=14400`, `age: 753`, `cf-cache-status: HIT`, so the stale pair persists to **≈20:02Z** (~227 min from the publish). `?cb=<nonce>` returns the ORIGIN's fresh bytes, which is how the byte-identity above was proven. A plain-visitor load in this mixed state STILL WORKS — slider `max` 8192, 2 canvases, **no pageerrors** — because `app.js` and `webglFractal.js` are only coupled in the dormant deep lane, so the mismatch is invisible at the default zoom. No purge mechanism is documented for this host (`apps-publish` covers publishing only) and I did not invent one. **If a visitor reports a stale page, this cache window is the reason; it is not a broken deploy.**
REPUBLISH-CORRECTION | 2026-09-21T16:20Z | **the OpenClaw admin (relayed by the owner) corrected two things, and I checked both:** (1) **there are no Cloudflare credentials on this host** — the secret store holds only mail entries plus the gateway password, and `~/.cloudflared/` holds the tunnel's own credential, which cannot touch the zone — so "purge the cache" is an action for the OWNER (Caching → Configuration → Purge Cache → Custom Purge) or a stopgap `?cb=<nonce>` on the **asset** URL, and it is **NOT** something I can do or invent. (2) **`?cb=` on the PAGE url does nothing**: `index.html`'s `<script src="app.js">` still requests the bare URL, which stays a `HIT`. My REPUBLISH-VERIFY line above used cache-busted *asset* fetches, which is the form that does bypass the edge; anything reading fresh code through a page must bust each asset, not the page. The admin also confirmed our numbers independently (age 4 831 s on `app.js`, 944 s on `webglFractal.js`).
REPUBLISH-ROOT-CAUSE | **the 4 h edge TTL is the DEFECT, not the incident.** This origin sends no cache headers, so Cloudflare's default (4 h, per URL) applies, and FracVibe ships **unhashed filenames** (`app.js`, `webglFractal.js`) because it has no bundler — so *every* deploy serves stale bytes for up to four hours after it. The hub app escapes this only because Vite emits `index-BYUlCwJi.js`, whose name changes with its content. The fix is to **version the asset URLs** — a content hash or `?v=<hash>` written into `index.html` at stage time — and a Cache Rule lowering the TTL would only hide it. **NOT done in this publish** (a publish was asked for, not a build change) and **NOT to be done while P2's writer is in flight**; recorded here as the next publish-path slice, with the same rule now present in the `apps-publish` skill so the next app hears it before its first deploy.
REPUBLISHED | 2026-09-21 (deployed content = main sha `0ed9b63` + `74ce20f`, the post-D2 build) | staged exactly with `rsync -a --delete --exclude '.git*' public/ dist/` | verified WITH A BROWSER, not curl: a headless Chrome on `https://apps.futuremagic.de/fracvibe/` reports `kernelMAX_ITER = 8192`, `colorTableSize` present, slider `max = 8192`, zero pageerrors — and **the FULL suite was run against the LIVE public URL through the CDN → 52 passed / 3.1m across all 11 spec files** (log /tmp/fv-live-suite.log, config /tmp/fv-live.config.js) | `curl` is NOT a trustworthy deploy check here — see the CDN-cache TRAP below
REPUBLISHED | 2026-09-21 (deployed content = main sha `86cc3d1`, the post-D1 build) | staged exactly with `rsync -a --delete --exclude '.git*' public/ dist/` | verified, NOT assumed: the CDN's `index.html` **and** `fractalKernel.js` are **byte-identical** to the staged files and carry D1's markers (`smoothIterationValue`, `COLORS_LUT_STRIDE`), so the public URL serves the NEW smooth-colour build rather than a cached old one; the dead files still 404 through the CDN; and **the FULL suite was run against the published subpath** → **49 passed / 2.5m, all 10 spec files** (log /tmp/fv-published-d1.log, config /tmp/fv-publish.config.js with `baseURL=http://127.0.0.1:8082/fracvibe/` and no `webServer`)
REPUBLISHED | 2026-09-21T18:45Z (deployed content = `86667cb`, the WORKER-CANCEL fix delivered through VERSIONED urls) | **the fix had already been committed, gated, pushed AND copied to `dist/` and still reached nobody** — Cloudflare served `app.js`/`fractalWorker.js` from its 4 h per-URL cache, and the live pair was measurably the OLD code (`hasReadyGate:false`, `hasReady:false`), reproducing the owner's banner on the live URL. Published instead via `bash scripts/stage.sh` (content-addressed names, see the REPUBLISH row). **VERIFIED ON THE LIVE URL, no purge required:** the HTML declares `app.a975bef989bd.js`; the browser received `fractalWorker.a5dcc6845cf6.js` containing `postMessage({ type: 'ready' })` and `app.a975bef989bd.js` containing the `_fvReady` gate (6 occurrences); a 48-tick wheel-zoom into the CPU lane with `fractalKernel.js` delayed 400 ms — the exact condition that reproduced the defect — now shows the **informational** zoom-cap notice, **`msgClass: app-message` (not `--error`)**, **non-2xx: NONE**, **ERRORS: NONE**, and the canvas applied 14 frames. The full suite against the staged bytes was run first: **60 passed / 3.6m** (`/tmp/fv-stage-suite.log`). This is the FIRST deploy on this host that needed no manual purge, which was the whole purpose of the versioning.
REPUBLISH | after ANY change to `public/`, run **`bash scripts/stage.sh`** (NOT the old hand-rolled rsync). It copies `public/` to `dist/`, re-emits every file in the module graph under a content-addressed name (`app.<sha12>.js`), rewrites the graph's own references **and** `index.html` to those names, verifies the artifact (no unversioned script, no dangling reference) and then swaps it in. Exit 0 = STAGED; 1 = RED, nothing half-written. `STAGE_DEST=...` stages to a scratch dir (how the script is tested without publishing). See the PUBLISH-CACHE row for why this is now required rather than optional. **`scripts/gate.sh` is still the only GATE — `stage.sh` is the stage step, and “it staged” is never “it passed”:** the published artifact must additionally be verified by running the suite against the staged bytes (`npx playwright test --config <config with baseURL at the staging port>`).
CAVEAT | this is a SECOND, independent deploy path. `netlify.toml` describes a Netlify site built from `origin/main`, and `fracvibe.netlify.app` answers 404, so the Netlify URL remains UNVERIFIED and may or may not exist. The apps host is the one that is verified above.

## ✅ CAMPAIGN COMPLETE (2026-09-21) — the owner's "resilient and correct" mandate
STATUS | all six slices S1–S6 landed and verified · `origin/main` = `310c4ae` · suite **4 → 43 tests** · `MODERNIZATION.md` §2 (B1–B9 + the fold-in) FULLY CLOSED
EVIDENCE | every landing carries the dispatcher's own FULL gate at its landing sha, the dispatcher's own injection watched RED, and a byte-identical restore. Independent probes found real COVERAGE gaps in S1 (the context-loss pin called the handler and missed the wiring) and S5 (the palette pin read the CPU buffer and missed the GPU upload); both were fixed forward in a round 2 and re-probed RED. S3 and S4 probes confirmed the pins. S6's probe found no gap. Each writer's worktree, branch and session was retired after verification.
REMAINING | deliberately DEFERRED, not unfinished (docs/DECISIONS.md rows 2–3, MODERNIZATION §5): the P1 remainder (§3 — per-frame LUT/`ImageData` allocation in `FractalViewer.render`, and the per-pass buffer copy + single worker, i.e. no pool/tiles/transferables), tooling (§4 — Vite, `three` from npm, Node 24, ESLint/Prettier, CI, ESM dev server, TypeScript), product (§5 — `<dialog>`/ARIA, pointer+touch input, persisted locations), and the WebGPU/WASM/OffscreenCanvas rewrites.
OPEN-TRAP | **`curl` is not a trustworthy post-deploy check on this host.** The apps host sets `cache-control: max-age=14400` (4 h) on static assets and Cloudflare answers `cf-cache-status: HIT` from the edge. On 2026-09-21, immediately after the D2 republish, `curl` received a **stale D1-era `fractalKernel.js`** (20 867 B, `MAX_ITER = 2000`) under **all three** `Accept-Encoding` variants — while a headless **Chrome** on the same URL received the current kernel (`MAX_ITER = 8192`) and the FULL suite passed against the live URL. **CONFIRMED AGAIN, WITH A MECHANISM, AT THE 16:15Z PUBLISH:** the edge served stale `app.js` + `webglFractal.js` (`age: 753`, `max-age=14400`, `cf-cache-status: HIT`) while `index.html` and `fractalKernel.js` were fresh — so the two URLs disagreed *with each other*, and a `?cb=<nonce>` query returned the origin's current bytes. The earlier "unexplained" discrepancy is therefore ordinary **per-object edge-cache TTL skew**: each asset expires on its own 4 h timer, so between publishes the site can serve a MIX of revisions, and `curl` on a bare URL reports whichever copy the edge holds. This also means a byte-identity check MUST cache-bust or it silently compares against cache. **Verify a deploy with a browser or by running the suite against the live URL, never by curl alone.**
TRAP | `dist/` is **NOT** a useless duplicate any more — it is the SOURCE OF THE LIVE SITE (`~/apps/fracvibe` symlinks to it). Republish after every change to `public/` with `rsync -a --delete --exclude '.git*' public/ dist/`; the `--delete` is required or removed files stay published. Still never edit `dist/` by hand — edit `public/` and re-stage. It is untracked and gitignored, so it is invisible to `git status`. **`dist/` can also go stale SILENTLY, and did:** at the 16:15Z publish it held a pre-P1/pre-D2 build whose file shas matched no committed revision, so the live site had been at least three landings behind with nothing on the board saying so. **A publish is not a copy — it is a copy plus a verification**: after staging, `diff -rq public dist` must report nothing, and the served bytes must be hashed against `dist/` with a per-**asset** cache-buster (`curl "<asset-url>?cb=<nonce>" | sha256sum` — a nonce on the page URL does NOT bust the `<script src>` requests).

## 🔭 NEW PHASE — deep zoom that stays on the GPU (owner-directed 2026-09-21)
OWNER | verbatim: *"I would like C but with special care around precision hops. I know there was a problem earlier that those can become very visible when zooming because of palette changes and things like that. The zoom needs to stay visually as smooth as possible."* | decision=**C — a GPU perturbation renderer**, so the GPU stops handing off to the CPU at deep zoom | hard constraint=**precision hops must be invisible**, and the acceptance test for EVERY slice is a continuous zoom sweep with NO step-to-step image-difference spike
PLAN | `docs/PLAN-DEEPZOOM.md` — the mechanism, slices D1–D4, and the acceptance criterion. `WEBGL_ZOOM_CAP = 10000` (`app.js:424`) is an HONEST float32 ceiling (≈1.7e4 by the arithmetic in the plan), not a timid setting, so raising it is not available: the FORMULATION must change, and GLSL ES has no `double` in any version.
AMPLIFIER | colour is a STAIRCASE in the escape value in BOTH renderers and nothing smooths it — CPU builds a `Uint32Array` LUT with one entry per INTEGER iteration (`fractalViewer.js:197-208`), GPU uses `float t = float(iter)/float(u_maxIter)`. So any precision change can flip a pixel a whole band, and colour cycling moves the bands: that is the pop the owner remembers. A continuous colour function makes a hop INVISIBLE rather than merely small.
FORK-TAKEN | smooth colour REPLACES the banded look. Rejected: keeping the integer staircase — it cannot satisfy the owner's own requirement, since any hop can flip a band. If the banded look is wanted back it becomes a colour-mode toggle, not the default.
MEASURED | probe `b9243a50` CONSUMED 2026-09-21 · evidence committed at `docs/PROBE-2026-09-21-gpu-precision.md` · full plan revision in `docs/PLAN-DEEPZOOM.md`
MEASURED-FINDINGS | **compensated float32 (hi+lo) holds to ≥1e10** — at 1e6/mi32768 ZERO pixels differ by >32 and 53 distinct colours = the float64 reference exactly; at 1e8/1e9/1e10 mean ΔRGB 0.31/0.42/0.99 with **0.000% misclassification**, at or BELOW the 1e3 baseline; failure mode is scattered speckle (largest component 2–6 px), NOT blocky artifacts; cost **10.4–12.5×** fragment work. Also: plain float32 has **no exact regime** (~0.9% misclassified already at 1e3) and **collapses between 1e5 and 1e6** (at 1e8 the frame is ONE colour vs the reference's 233); the 1e4 cap is 0.088 px and 0.5 px arrives at 5.65e4, so the cap is conservative ~5–10×; the view centre escapes only at iteration **3085**, so at the shipped maxIter deep frames saturate to black **regardless of precision** (this is slice D2); and the CPU/GPU **half-pixel sample-point offset** already makes the two renderers disagree MORE than float32 error at 1e3–1e4 (slice D6).
CORRECTION | **three of the dispatcher's own claims have now been falsified by measurement, and each is recorded rather than quietly dropped:** (1) "`WEBGL_ZOOM_CAP` is an honest ceiling" — my arithmetic, not a measurement; the probe showed it is conservative by ~5–10× with collapse one order of magnitude later. (2) "smoothing in only one renderer breaks the S3 parity pins" — WRONG at the cap: one iteration is 0.13 RGB units at maxIter 2000, so the GPU could be put back on the integer count with S3 pins 1/3 still GREEN; the parity pins do NOT guard the smoothing, which is why D1 added its own source-level plus low-cap numerical pin. (3) the acceptance criterion "a zoom sweep must show no step-to-step spike" is **NON-DISCRIMINATING** — at deep zoom genuine image change dominates (mean 9–45 L1 per step), so it passes in both a correct and a broken build. The plan's normative criterion is now a **hop-classification** rule instead. Every one of these was caught by a writer or a probe, not by me. (4) The board's own `reconciled:` line went **STALE for four commits** — it still claimed `origin/main=310c4ae` while main was already `7df9c2c`/`a7282fb`/`d4d0732`/`86cc3d1`. That line is the board's single most important claim (what the rest was verified against), so drifting is a real defect, not a cosmetic one; it is corrected in this commit. (5) **My D2 brief's central premise was WRONG**: I claimed the colour table was allocated at module-maximum size and that raising `MAX_ITER` would multiply a per-frame allocation. In fact `buildColorTable` already sized the allocation by the JOB's cap (`this.maxIter`); D1's exported `COLORS_LUT_SIZE` constant was allocated by nothing. The real per-frame cost was the REBUILD (and the ImageData) — so the concern was real and D2 fixed it, but my mechanism was not. (6) **PROCESS SLIP**: I edited main's `AGENTS.md` and the probe doc BEFORE landing D2, which made `git pull --rebase` refuse ("Please commit or stash them"). The push was still a clean fast-forward (origin was at `989c12d`), so nothing broke — but the order was wrong: land first, then edit the record. (7) **MY "~1e15 for a float64 orbit" SCOPE CLAIM was reasoning, not evidence**, and the perturbation probe falsified it: the limiter is the float32 delta arithmetic rather than the orbit, and a straightforward implementation is clean at 1e8, degraded at 1e12 and structurally wrong at 1e15. (8) **MY P1 BRIEF WAS WRONG TWICE, and the writer measured both**: (a) *"rescaling MATTERS"* is FALSE within this slice's reach — removing rescaling is **bit-identical** (interval 64/256/1024, or deleted), because float32 delta-square underflow is at ~**1e19** while a float64 **coordinate's own ULP** already degenerates the frame at ~**1e16**, so there is nothing to adjudicate; rescaling belongs with P2/P3, not P1. (b) The hop-classification rule **cannot reach 0 and does not discriminate** at this stage. Also unreproducible: the brief's "a float64 orbit reaches ~1e15" premise — rebasing FIXES the naive 1e15 collapse, and the next wall is the **coordinate** (~1e16), not the mantissa, which is exactly what P2's arbitrary-precision orbit addresses.
CORRECTION-2 | an earlier claim in `docs/PLAN-DEEPZOOM.md` — "1e4 is an honest ceiling" — was MY arithmetic from the code, not a measurement. The probe refines it: conservative by 5–10×, with collapse one order of magnitude later. Corrected in place.
FORK-RESOLVED | owner chose **Route 2 — go straight to perturbation**, fully informed: it is the only route past ~1e10, it is UNMEASURED here, and its rebasing is itself a pop mechanism. The compensated float32 route (measured to ≥1e10) is deliberately **NOT built**; its compensation measurements are retained because P1's delta arithmetic needs them. Also confirmed: continuous colour as the ONLY mode (no banded toggle), which is what D1 is already building.
DECISION-SCOPE | with a **float64 reference orbit** perturbation reaches roughly **1e15** (~1e11× beyond today's 1e4 cap) — the orbit needs about as many digits as the zoom. Past ~1e15 the ORBIT needs big-float precision (slice P6, deferred). "Unlimited" lives at P6, not P1.
HOP-SOURCES | while the orbit stays float64 there is **no precision hop inside the deep path** — the orbit is recomputed per view at fixed precision. So the only two hop sources are the **path switch** (P3) and **rebasing** (P2), each its own slice, each under the sweep-no-spike acceptance criterion. Rebasing is where the risk actually lives.
SEQUENCE | D1 continuous colour **LANDED at 86cc3d1** → D2 zoom-scaled iteration budget **LANDED at 0ed9b63** (`MAX_ITER = 8192`, one constant; rule = the slider as a FLOOR raised by `ceil(512·log10(1/scale))`, clamped to the cap; table cached and sized by the JOB's cap) → **P1 the perturbation core LANDED at `aa39f1c`** (renders scale 1e-15 at **0.00000 %** misclassification with rebasing; BLOCKED only on the deferred sweep clause) → **P2 the BigInt arbitrary-precision orbit (NEXT — it crosses the ~1e16 coordinate wall)** → P2 exact rebasing → P3 the path switch → **P4 lifts the zoom cap — which is the slice that makes D2 visible** → P5 series approximation → P6 big-float orbits only if >1e15 is wanted. D6 (align the CPU/GPU sample points) is queued and measured as mattering: the corner-vs-centre offset makes the two renderers disagree by MORE than float32 error at 1e3–1e4, and makes canvas-vs-canvas comparison meaningless at low maxIter (~34 RGB units even on a correct build).

## ✅ RESOLVED — the push landed (the live priority is now the campaign below)
LANDED | row=push | sha=5d86f40 | remote=`dea8a55..5d86f40  HEAD -> main` (exit 0) | verify=**PROVEN, not assumed**: local HEAD == `origin/main` == `5d86f40cdff28b89e3b60cbc0976c6f69789f5e4`, `git rev-list --left-right --count origin/main...main` → `0 0`, and server-side `git ls-remote origin refs/heads/main` → the same sha | gate=BEFORE the push, FULL tier at that exact sha: exit 0 GREEN, 4 passed / 6.1s, raw log /tmp/fracvibe-gate/gate-full-5d86f40-20260921T083517Z.log | effect=`origin/main` is no longer the blank-canvas build
WAS-BLOCKED | at 2026-09-21T08:31Z the push failed: `fatal: could not read Username for 'https://github.com'` (exit 128). The host then had no credential helper, no token env, no `~/.git-credentials`, no `~/.netrc`, no `gh`, no SSH key and an empty agent. **I did not look inside `~/.openclaw`** — a different platform, and the standing rule is to stop and ask. The owner then wired a global store (`~/.git-credentials` mode 600 plus `credential.helper=store` in `~/.gitconfig`); my re-check returned `auth OK` and the push succeeded. The block was always **credentials, never the repo**.
ESTABLISHED | the campaign no longer needs a local-only base: `origin/main` carries the machinery, so **new writers base on `origin/main`**, as `AGENTS.md` requires. (S1 was the one exception — dispatched against local `main` while the push was blocked — and it has since landed at `cea74cb`.)

## SESSION
SESSION | id=session-0e97db24-5f1e-49f6-8e63-18a285c027a0 | role=chief of staff (owner-designated) | state=active · created 2026-09-21T16:09Z · goal frozen and paused by design · did the 16:10Z reconcile, verified and published P2 (`a2fe502`), and dispatched WORKER-CANCEL
SESSION | id=de79af30-a99c-4b72-ab25-6e63f1e2fd94 | role=WRITER · row=GPU-ARBITRARY | state=dispatched 2026-09-21 by session-0e97db24 · worktree=`worktrees/gpu-arbitrary` · branch=`feat/gpu-arbitrary` · base=`origin/main` = `ab59aaa` · scope=the float32-range wall on the delta coordinate + render-time readout + removing the automatic CPU-switch offer · NOT this slice=coarse-to-fine GPU refinement (owner's separate ask; next slice) · verify=NOT YET
SESSION | id=9ca927d9-db15-4c03-92eb-c7064a4d5314 | role=WRITER · row=GPU-DIAGNOSTIC (the Test button) | state=dispatched 2026-09-21 · worktree=`worktrees/gpu-diagnostic` · branch=`feat/gpu-diagnostic` · base=`b300413` · **does NOT touch `public/app.js`** (lane-continuity owns it) — adds `public/gpuDiagnostic.js` + a button in `index.html`, integrating only via the frozen `window.__fv` observables · scope=identity/renderer, float-texture capability AND actual sampleability, **the double-single residual test (is the low component non-zero and does it change the result — the research's silent-collapse failure)**, the deep lane end-to-end vs an in-page reference (% misclassified, mean |Δ|), gl.getError/readback sanity, and real-hardware timing · **PURPOSE: the owner has a REAL GPU and will press it and report the console — this is the instrument for the hardware-certification gap that DECISIONS 65/66/67 record (this host is SwiftShader for WebGL and WebGPU, so precision code can be built and falsified here but never certified)** · verify=NOT YET
SESSION | id=b7af28b5-d6c2-4186-a431-17dc8effdf62 | role=WRITER · row=MANTISSA-LADDER (arithmetic layer) | state=dispatched 2026-09-21 · worktree=`worktrees/expansions` · branch=`feat/expansions` · base=`ca14755` · **NEW FILES ONLY** (shader integration is a later slice) · scope=the k-component GLSL expansion library (`TwoSum`/`Dekker` split/`TwoProd`/renormalisation, k as a BUILD parameter, k=1 reducing exactly to plain float32) + a numeric-contract spec that asserts a required-nonzero low component + the k=1/2/4 cost & accuracy curve · verify=NOT YET
SESSION | id=2e791076-1426-4334-89a5-4a43720ed425 | role=WRITER · row=PRECISION-CHAIN + CPU port prototype | state=dispatched 2026-09-21 · worktree=`worktrees/precision-chain` · branch=`feat/precision-chain` · base=`ca14755` · **NEW FILES ONLY** · scope=Part 1 `docs/PRECISION-CHAIN.md` (every precision-loss site with bits/exponent per stage, and whether the dispatcher's ~24-bit ORBIT-TRANSPORT claim is true) + Part 2 a perturbation+orbit port prototype for the CPU lane in new files, consuming `FractalKernel` so no arithmetic is duplicated · verify=NOT YET
SESSION | id=b61434ef-603a-4507-b7e4-a9bebe94933d | role=WRITER · row=DEEP-LANE-SWITCH + cap removal | state=dispatched 2026-09-21 by session-0e97db24 · worktree=`worktrees/lane-continuity` · branch=`feat/lane-continuity` · base=`origin/main` = `2a71a91` · scope=make the 1e-4 transition CONTINUOUS (both the D2 budget step and the shader-program switch) and remove the hard stop, per the owner's directive “no boundaries … no hard stop” · acceptance=log-spaced colour-delta sweep across the old boundary with the pre-fix 31.6-unit jump as the in-pin RED baseline, plus the four no-regression classes · NOT this slice=coarse-to-fine refinement · verify=NOT YET
SESSION | id=1e087e60-9696-4f06-a114-980a218f6638 | role=WRITER · row=GPU-ARBITRARY groundwork / WebGPU tier | state=dispatched 2026-09-21 by session-0e97db24 · worktree=`worktrees/webgpu-groundwork` · branch=`feat/webgpu-groundwork` · base=`origin/main` = `a018c84` · **NEW FILES ONLY** (may not modify any existing code file — the GPU-ARBITRARY writer is mid-flight in `app.js`/`webglFractal.js`) · scope=`public/capabilities.js` (capability detector incl. the api-present-no-adapter case), `docs/RENDERER-CONTRACT.md`, a WebGPU compute feasibility experiment + spec, and a storage-buffer-vs-8192-texture-ceiling measurement · NOT this slice=wiring WebGPU into the live dispatch (after GPU-ARBITRARY lands) · verify=NOT YET
SESSION | id=de79af30-a99c-4b72-ab25-6e63f1e2fd94 | role=WRITER · row=GPU-ARBITRARY | state=dispatched 2026-09-21 by session-0e97db24 · worktree=`worktrees/gpu-arbitrary` · branch=`feat/gpu-arbitrary` · base=`origin/main` = `ab59aaa` · scope=the float32-range wall on the delta coordinate + render-time readout + removing the automatic CPU-switch offer · NOT this slice=coarse-to-fine GPU refinement (owner's separate ask; next slice) · verify=NOT YET
SESSION | id=67ffc583-c941-4a6e-93c7-ba50cbd1b92b | role=WRITER · row=WORKER-CANCEL | state=dispatched 2026-09-21T16:52Z by session-0e97db24 · worktree=`worktrees/wc-worker-cancel` · branch=`fix/worker-cancel` · base=`origin/main` = `9cdc24a` · verify=NOT YET (the dispatcher must gate the landing itself and watch its own control RED)
SESSION | id=session-f6d26a74-68de-4926-b4d3-16efb7ff2421 | role=predecessor chief of staff | state=ended/idle · last session-log write 2026-09-21T15:49Z · spawned the in-flight P2 writer `e2e963f0` · left no worktree or branch of its own behind
SESSION | id=session-d1525624-6f23-4803-837e-34a1adc80fea | role=predecessor: audit + Phase 0 | state=ended 2026-09-21T10:14Z · left no worktree, no branch, no lock behind (verified)

## IN-FLIGHT / VERIFY HISTORY
IN-FLIGHT | row=**P2** | **LANDED 2026-09-21T16:21Z as `a2fe502` (pushed) — no longer in flight; verification row below** | writer=`e2e963f0-2ded-47f6-bd60-0ed3e1301cb6` | worktree=/home/administrator/projects/FracVibe/worktrees/p2-bigint-orbit | branch=feat/p2-bigint-orbit | base=`origin/main` = 57adf7b | state=dispatched 2026-09-21, no commit yet | scope=**the arbitrary-precision reference orbit** — BigInt fixed point in a Worker, precision growing with zoom (`bits ≈ log2(1/scale) + margin`, margin CHOSEN and measured, since the sources disagree 0…+64), **computed once per view** (P1's pin-2 contract). This is the slice that crosses the **~1e16 COORDINATE wall** P1 identified (float64's own ULP, not mantissa precision). **The named central risk is transport**: the shader samples the orbit from a float texture (`u_orbit`, one texel per iteration, `MAX_TEXTURE_SIZE = 8192` = the cap) and a float channel is 24 bits, so P1 packed hi/lo (≈48 bits) into RGBA — arbitrary depth needs more per value, and the writer must solve that by measurement or report BLOCKED with the measurement. **Precision STEP boundaries must change nothing visible** (the documented risk class: KF 2018 "corrupt image at transition between number types" is the only literature defect found), pinned differentially with the failing baseline in-pin. NOT in this slice: P3 glitch solving, P4 series approximation, P5 cap lift. | files=public/{app.js,webglFractal.js,fractalKernel.js}, a Worker, tests/ | dispatched_by=session-f6d26a74 | brief-carry=the `MODERNIZATION.md` clause; remove the WHOLE mechanism in controls; **CHECK that your injection took effect before concluding** (the dispatcher's own first P1 injection was VOID because a later line overwrote it); pin independence — a reference that shares the code path under test is a tautology; and BLOCKED-with-measurement is acceptable, do not massage a threshold | verify=NOT YET | salvaged-check 2026-09-21T16:10Z by session-0e97db24: the writer session `e2e963f0` is ALIVE (`session.v3.jsonl.zstd` written 16:10:08Z) and working — worktree holds `public/bigOrbit.js` (9 526 B, 15:59Z), `public/orbitWorker.js` (1 520 B, 16:01Z), `tests/bigorbit.spec.js` (28 103 B, 16:06Z), plus edits to `public/app.js` and `public/webglFractal.js` — and it has **NO COMMIT YET** on `feat/p2-bigint-orbit` (branch still at its base `57adf7b`). Per the cadence contract that partial state must be committed before the writer ends; it is NOT yet a defect because the writer has not ended, and it is recorded here so a successor can salvage it if the writer vanishes.
VERIFY-HISTORY | row=S1 round 1 | sha=398aad5 | the dispatcher's probe that caught the coverage gap · gate=FULL exit 0 GREEN · 14 passed / 28.2s · raw log /tmp/fracvibe-gate/gate-full-398aad5-20260921T084945Z.log (independently corroborates the writer's 14/27.7s) | arm1=MY OWN injection, `handleWebGLLoss` body neutered → the context-loss pin went RED (`#appMessage` Expected visible / Received hidden), log /tmp/fv-arm1.log, so the pin is NOT vacuous | arm2=MY OWN coverage probe, the real `webglcontextlost` listener at `app.js:907` deleted → that same pin **STILL PASSED (rc=0)**, log /tmp/fv-arm2.log: it reaches the handler through the `window.__fv.simulateContextLoss()` hook (`app.js:918`), so `attachContextLossGuard` (`app.js:895-907`) is never exercised. Break the real wiring and the canvas dies silently while the suite stays green — the exact B9 class S1 exists to remove. | restored=byte-identical after both arms (`app.js` sha256 `fba018c4…`) | action=returned to the writer for a real-`webglcontextlost` pin plus its own red control; the writer's own arms A–E are accepted as reported but are NOT what this row certifies
PROBE | id=dc6f39f3-2a45-4f41-9234-9ce42b13394c | state=**CONSUMED** 2026-09-21 · report kept at `docs/PROBE-2026-09-21-seams.md` (validated at 9e8661e; code unchanged since c6c87e2) · agent deleted | findings=six defects the audit did not contain (⚠ in the report): `progress` frames are never applied, there is no `worker.onerror`, a bad `done` payload throws uncaught — **FALSIFIED 2026-09-21: it does NOT throw; that claim reached this board and a brief from plausibility, see the report's CORRECTIONS**, 3D init failure leaves a black screen with both canvases hidden, there is no global error handler, and four state variables are dead | action=all six folded into the `docs/PLAN.md` slice table
NOTE | the serialization rule still holds: every slice touches `public/app.js` (docs/PLAN.md §2), so S1–S6 run ONE writer at a time unless a probe has PROVEN two footprints disjoint. S1 was the only writer in flight and is now retired.

## NEW-PHASE LANDED
LANDED | row=**P2** | sha=**`a2fe502`** | commits=a2fe502 (the writer's own commit `1bd6b92` on `feat/p2-bigint-orbit`, pushed as `a2fe502`) | **verified by MY OWN gate, at the landing sha**: FULL gate at `a2fe502` → **exit 0 GREEN, 58 passed / 3.5m** (raw log `/tmp/fracvibe-gate/gate-full-a2fe502-20260921T162440Z.log`), which independently corroborates the writer's own 58-pass run. | delivered=`public/bigOrbit.js` (209 lines, BigInt fixed point, no import/export so the classic Worker and the ESM main thread share ONE source), `public/orbitWorker.js` (43 lines, one long-lived classic Worker), `public/webglFractal.js` (+203), `public/app.js` (+83, the `__fv.bigOrbit*` observables), `tests/bigorbit.spec.js` (534 lines, 3 pins); suite 55 → 58. Precision `bits = max(64, ceil(log2(1/scale)) + 64)` quantised UP to 64-bit steps; margin +64 measured sufficient (shipped setting matches a direct-BigInt reference 256 bits wider at mean \|Δ\| **0.007**; a starved setting measures **12.9**). Reached depth in the real shader: **1e-30, 0.00000 % misclassification**, where P1's float64-orbit baseline on the same view measures mean \|Δ\| **9.47**. New measured ceiling: the wall is **float32 RANGE, not orbit precision** — the frame is structured to 5e-38 and collapses to one value at ~2e-38 (float32's smallest normal, subnormals flushed). | **MY OWN probe (no gap found)**: I read the new lane and asked the one question its pins do not answer — P2's contract says "while a request is in flight the draw falls back to the float64 lane, so the canvas is never left blank", but every pin measures the STEADY state (pin 1 vs a reference, pin 2 counters, pin 3 the step). My probe measured the **pending** draw at the 1e-30 exact centre: `orbitSource='float64'`, 1 request posted / **0** builds complete, and the frame is **0 % inside with 96 distinct escape values** — NOT blank, NOT the inside sentinel. So the fallback claim holds by measurement, and it is the ONLY pin-class on this slice that P2 did not write itself. (No RED arm: I found no gap, and I am recording that rather than manufacturing one.) | **PROCESS FINDINGS, recorded rather than smoothed over:** (1) the writer gated an **UNCOMMITTED worktree** — `bash scripts/gate.sh` from `worktrees/p2-bigint-orbit` at HEAD `57adf7b` with its P2 files uncommitted, which is this board's own TRAP ("`git worktree add` checks out the COMMITTED tree… commit before you probe"). The gap is narrow, not fatal: before the gate it snapshotted the files and after the gate it restored them with `cp` + `sha256sum -c` passing and `git diff` empty, so the gated bytes ARE the committed bytes — but the evidence is bound to file hashes, not to a sha, which is why my own gate at `a2fe502` now exists. (2) the writer **self-verified and self-labelled**: it wrote "**LANDED & VERIFIED**" into `docs/PLAN-DEEPZOOM.md` and reported "LANDED", then sent its report and **retired its own worktree, branch and session**. Verification is the dispatcher's, by the project's own doctrine — my gate above is that verification, arriving after the label. (3) the writer left **no `docs/STATE.md` row**, so the board did not know P2 had landed; a successor reading only the board would have believed it still in flight. All three are the same root: the landing and its verification were collapsed into one actor. | brief-corrections=the writer corrected FOUR of my brief's premises with measurement, and I record them as my errors: **P1 did NOT ship a hi/lo split** (it stores one `Math.fround` word per component; hi/lo lived only in the committed probe and row 36 records it INERT); **orbit-transport mantissa is not the blocker — float32 RANGE is**; **1e-39 is not a viable step-test scale** (the shader collapses there, so an earlier version of pin 3 passed VACUOUSLY until the writer moved it to 1e-20 — caught by the writer, not by me); and the brief's "arbitrary depth needs more orbit precision" premise is contradicted by measurement. | docs=docs/DECISIONS.md rows 38–42, docs/PLAN-DEEPZOOM.md §9 P2 row; MODERNIZATION.md makes no claim this falsifies | retired=writer session `e2e963f0`, worktree `worktrees/p2-bigint-orbit`, branch `feat/p2-bigint-orbit` — **all retired by the writer itself before dispatcher verification** (see PROCESS FINDINGS 2) | publish=the live site was re-staged with this landing (see the REPUBLISHED row for P2); **note that `public/bigOrbit.js` and `public/orbitWorker.js` are now part of the unhashed-filename set the PUBLISH-CACHE row must cover.**
LANDED | row=P1 | sha=aa39f1c | commits=aa39f1c (rebased from 9c6bf28) | verify=**MY OWN**: FULL gate at `9c6bf28` → exit 0 GREEN, **55 passed / 3.1m** (/tmp/fracvibe-gate/gate-full-9c6bf28-20260921T154409Z.log); after the rebase all four app/test files are **byte-identical** to what I gated (checked by hash) | arms=MY OWN probe: forcing the lane **condition** false (`webglFractal.js:634`) → **all three P1 pins RED** for the right reasons ("a deep view must build an orbit", Expected >0 / Received 0; "the deep lane must be the perturbation one"); restored byte-identical (`webglFractal.js` sha256 `0af5eae8…`). My FIRST injection (forcing `usePerturbation = false` at `:640`) was **VOID** — line 643 overwrites it — and is recorded as void rather than as a gap. | achievement=**the deep lane renders scale 1e-15 CORRECTLY**: against a float64 perturbation reference at 200×150, **mis 0.00000 %**, mean \|ΔRGB\| **0.453** (threshold <1.5), largest Δ>32 blob **3 px** (≤8), and **136 distinct colours = the reference's 136** — where the pre-P1 float32 absolute coordinate collapses to ONE value across 30 000 px and the naive probe was 26–55 % misclassified. **This is the depth jump the research predicted from adding rebasing.** | mechanisms=rebasing (absorbed magnitude; the expanded form measured 100× WORSE — rejected), **G = 1e-4** (fires on 73.8 % of pixels ⇒ diagnostic, not a repair trigger), rescale interval 256, orbit **once per view** keyed `(centre, budget)` and counted (5 redraws → 0 builds; each change → exactly 1) | lane=depth-selected at `ITER_BUDGET_MIN_SCALE` (the same constant as the shipped zoom cap, D2-pinned to `__fv.minScale`); above it the plain lane is byte-for-byte pre-P1. Forced by measurement: always-on perturbation doubled the gate (5.9m vs 2.9m) because the startup animation draws ~60 full-window frames | blocked-clause=P1 is **BLOCKED on the sweep clause ONLY**, and the writer correctly REFUSED to ship a nonzero threshold to make it pass. The plan's criterion is re-specified (CORRECTION 8); the strict sweep is deferred to P2/P3 | pins=tests/perturb-core.spec.js — 3 pins; suite 52 → 55 | retired=writer session `80cd0291`, worktree `worktrees/p1-perturb-core`, branch `feat/p1-perturb-core` | docs=docs/PLAN-DEEPZOOM.md (§3/§9 P1 rows), docs/DECISIONS.md rows 33–37; MODERNIZATION.md untouched (nothing there falsified)
LANDED | row=D2 | sha=0ed9b63 | commits=0ed9b63 (rebased from ced80e5) | verify=**MY OWN, at two shas**: FULL gate at the writer's sha `ced80e5` → exit 0 GREEN, 52 passed / 2.9m (/tmp/fracvibe-gate/gate-full-ced80e5-20260921T141731Z.log); FULL gate at the rebased LANDING sha `0ed9b63` → exit 0 GREEN, 52 passed / 2.9m (/tmp/fracvibe-gate/gate-full-0ed9b63-20260921T142058Z.log) | arms=MY OWN probe on the net the writer's arms A–D never touched — the ONE-cap BOUND: removing `clampMaxIter` from `iterBudgetForScale` (`fractalKernel.js:180`) → **pin 2 RED** (`overCap=9`: its deep anchor demands 10240 against cap 8192) with pins 1 and 3 GREEN, so the arm is isolated (/tmp/fv-armD2.log); restored byte-identical (`fractalKernel.js` sha256 `c1198235ab9923bd…`) | pins=tests/iter-budget.spec.js — 3 pins; suite 49 → 52 | result=**NO gap found** — S6 and D2 are the only two slices where my independent probe confirmed the pins outright | new-cap=`MAX_ITER = 8192` (one constant; the rule's maximum over the float64-orbit reach is 7680, so the rule decides, not the cap) | table=cached per `(scheme, offset, cap)` and sized by the JOB's cap: 131 075 entries / 0.5 MB at cap 512, 1 048 579 / 4.0 MB at 4096; the module-maximum table (8.0 MB) is never allocated; 5 repeat renders → 0 builds, 0 ImageData builds | retired=writer session `4eff9260`, worktree `worktrees/d2-iter-budget`, branch `feat/d2-iter-budget` | docs=docs/PLAN-DEEPZOOM.md (D2 row), docs/DECISIONS.md rows 30–32, MODERNIZATION.md per-frame-allocation + the "still open" line
DORMANCY | D2's auto-budget is correct and pinned but **currently INVISIBLE in the UI**: `WEBGL_ZOOM_CAP = 10000` (`app.js:424` → scale 1e-4) exactly equals `ITER_BUDGET_MIN_SCALE = 1e-4` (`fractalKernel.js:172`), so every UI path is clamped to precisely the scale where the floor begins and the floor can never fire. Pin 1 reaches depth through `__fv.setDeepView`, which lifts that one clamp for the view change and restores it (pin 2 asserts the clamp is back). **This is why D2 must land BEFORE the cap is lifted, and why its user-visible effect belongs to P4.** I verified both constants are 1e-4 myself.
PROBE-CORRECTION | the D2 writer could NOT reproduce the first probe's `1e6 / maxIter 32768 ⇒ 0.0% inside` row: at that centre, 200×150, scale 1e-6, the app's kernel is **74.9% inside at cap 8192** (90.9 → 77.2 → 75.7 → 74.9 from cap 512 → 8192). The probe's **1e8** pair (100.0% at 512, 0.0% at 4096) and the 1e6/512 case (90.9%) DO reproduce and are what the campaign relied on. `docs/PROBE-2026-09-21-gpu-precision.md` carries a CORRECTIONS section saying so, and that the centre's escape count is **3086** (1-based in the app), not 3085.
PROBE-2 | perturbation orbit transport — probe `defd8b05` CONSUMED, evidence at `docs/PROBE-2026-09-21-perturbation.md` | transport=**EXACT and cheap**: `OES_texture_float` + NEAREST round-trips the deep centre's 53 significant bits exactly, in WebGL1 AND WebGL2; `MAX_TEXTURE_SIZE = 8192` (= the shipped cap); NPOT fine | depth=**clean at 1e8** (mis 0.0026% f32 orbit / 0.0013% hi/lo), **degraded at 1e12** (0.34% / 0.97%), **STRUCTURALLY WRONG at 1e15** (26.4% / 55.2% — the hi/lo arm collapses to one blob covering 53% of the frame) | limiter=**the float32 DELTA ARITHMETIC, not the orbit**: the same 24-bit orbit with float64 arithmetic drops the 1e15 error from a mean of **1235 iterations to 64**. A hi/lo orbit is **INERT** without a compensated delta (arms A and D bit-identical — `float(hi + lo)` returns `hi`), and the specific hi/lo + Dekker-`ds_mul` compensation tested was **WORSE** at 1e12–1e15 | glitch-baseline=WITHOUT rebasing: 0.08% at 1e8, 1.37% at 1e12, **74.6% at 1e15** | budget=the D2 rule is slightly under-provisioned at 1e8 (frame needs 4669 vs a 4096 budget) and **cannot be adequate at 1e12** (needs 9011 > the 8192 cap) | 1e18=NOT A TEST (that centre's reference frame is a single value) | unproven=real-GPU behaviour (whether the compensated regression survives on hardware), other centres/types, rebasing/SA/big-float orbits, the hop criterion, timing
RESEARCH | arbitrary-depth probe `b5bc3b56` CONSUMED · cited synthesis committed at `docs/RESEARCH-2026-09-21-arbitrary-depth.md` | settled=precision follows **bits ≈ log2(1/scale) + margin** ≡ **3.3219 bits per decimal digit** of zoom (shape confirmed by two independent sources); the MARGIN is NOT standardized (0 in the bare bound, **+64** in rust-fractal-core) | architecture=**arbitrary-precision reference orbit computed ONCE per view on the CPU** (native renderers: GMP/MPFR; **browser: BigInt fixed point** — bertbaron, the closest analogue) + **float64 deltas on the GPU kept small by REBASING and RESCALING**; rescaling defeats double UNDERFLOW, which is the real wall near 1e308 | glitches=Pauldelbrot `\|Z+z\|² < G·\|Z\|²` with G∈[1e-2,1e-8] and **no principled value published** (KF exposes it as a slider); four rebasing strategies, each with a DOCUMENTED artifact | speed=series approximation via probe-point skip (the analytic test is not used in practice), or Fraktaler 3's BLA | **KEY FINDING**=this EXPLAINS the perturbation probe's 1e15 collapse as a **MISSING-MECHANISM result** — it had neither rebasing nor rescaling, and its 74.6% glitch rate is that signature — **NOT a precision ceiling** | hop-risk=the only literature defect on precision transitions is KF 2018 "corrupt image at transition between number types"; rebasing artifacts are documented per strategy | unproven=no rigorous error bound exists for G or for SA skipping (stated by the field's own author); no published account of a current renderer's transition appearance; the BigInt-reference ceiling is unpublished
ROUTE-RESOLVED | the owner's answer — **arbitrary depth** ("as deep as the user wants, getting slower") — closes the fork: **perturbation IS the route**, with the researched architecture and the revised slices in `docs/PLAN-DEEPZOOM.md` §9: **P1 the core WITH rescaling + rebasing (expected large depth jump over the naive 1e15 collapse) → P2 BigInt arbitrary-precision orbit with invisible precision steps → P3 glitch solving beyond same-reference rebasing → P4 series approximation → P5 lift the zoom cap (which is what makes any of it visible) → P6 GPU-side reference only if the CPU orbit becomes the bottleneck.** P3 and P6 are measurement-gated, not assumed.
LANDED | row=D1 | sha=86cc3d1 | commits=8e752d8 (feature) + 86cc3d1 (pin-6 fix-forward) | verify=**MY OWN, at two shas**: FULL gate at the rebased feature sha `8e752d8` → exit 0 GREEN, 48 passed / 2.5m (/tmp/fracvibe-gate/gate-full-8e752d8-20260921T134052Z.log); FULL gate at the LANDING sha `86cc3d1` → exit 0 GREEN, 49 passed / 2.4m (/tmp/fracvibe-gate/gate-full-86cc3d1-20260921T135031Z.log) | arms=MY OWN probe, TWICE: reverting `app.js:799` to `viewer.maxIter` left ALL FIVE D1 pins GREEN in round 1 — a correct, REACHABLE fix (`applyLoadedRecord`'s GPU branch does not cancel a running job, so loading a location with a different `maxIter` changes the viewer's cap mid-job) was unpinned (/tmp/fv-armD1.log); after the writer's round 2 the SAME revert turns **pin 6 RED** (`Expected 50, Received 2000`) with the other five GREEN — isolated (/tmp/fv-armD1r2.log); restored byte-identical both times (`app.js` sha256 `b7244609…`) | pins=tests/smooth-color.spec.js — 6 pins; suite 43 → 49 | retired=writer session `d50156c3`, worktree `worktrees/d1-smooth-color`, branch `feat/d1-smooth-color` | docs=docs/DECISIONS.md rows 27–29, docs/PLAN-DEEPZOOM.md, MODERNIZATION.md stale LUT claims
NOTE | a DOCS add/add conflict was resolved at the rebase: D1's branch predated the dispatcher's `docs/PLAN-DEEPZOOM.md`, so it had written its own 8,353-byte version against main's 11,277-byte one. Resolved by KEEPING MAIN's (it carries the probe evidence and the owner's Route-2 decision) after verifying nothing was lost — D1's formula/sentinel/colour decisions are in its own DECISIONS rows 27–28, which main never touched, and its code rebased byte-identically (`fractalKernel.js aa8a6275…`, `webglFractal.js a728973f…`).
CONFIG-NOTE | between the D1 dispatch and its landing the owner changed HOST config (`~/.dsh/settings.yaml` + credentials, 15:36–15:39 local). The PROJECT config was untouched (`playwright.config.js`, `scripts/gate.sh`, `package.json` all unchanged, `git status` clean), so D1's pre-change gate stood — and the landing was re-gated anyway at the rebased sha.

## LANDED
LANDED | row=S6 | sha=310c4ae | commits=310c4ae (rebased from c72726d) | verify=**MY OWN at the LANDING sha**: FULL gate at `310c4ae` → exit 0 GREEN, 43 passed / 1.9m (/tmp/fracvibe-gate/gate-full-310c4ae-20260921T112006Z.log). The worktree held ONLY tracked files (git status empty, no untracked entries), which is the campaign's definition-of-done criterion "the suite passes from a clean checkout". | arms=MY OWN injection on the net the writer's arm B did NOT cover — `window.confirm` restored into the zoom-cap path (`handleZoomLimitReached`) → the dialog pin RED with a recorded `{"kind":"confirm"}` (log /tmp/fv-armS6.log); a real modal also tripped the once-only pin as collateral; restored byte-identical (`app.js` sha256 `5f883d60…`) | note=**the FIRST slice where the dispatcher's independent probe found NO gap** (S1's found the wiring gap, S5's the upload gap; S3/S4 confirmed their pins) | pins=tests/hygiene.spec.js — 4 new pins; suite 39 → 43 | removed/replaced=4 `[FractalMouse]` logs; 4 dead state vars (`app.js:427-430`); 4 dead files (`git rm`); BOTH modals → non-modal `#saveLocationPanel` + `#zoomCapOffer` (S1's `fv-zoom-limit` `{scale, prompted}` contract kept meaningful, now also `__fv.zoomCapPrompted()`/`zoomCapOffered()`); the logging-only MutationObserver was ALREADY GONE (S1) and S6 only confirmed its absence | retired=writer session `ad34beae`, worktree `worktrees/s6-hygiene`, branch `feat/s6-hygiene` | docs=docs/PLAN.md, docs/DECISIONS.md rows 25–26, MODERNIZATION.md §2/§3/§4/§5/§6
LANDED | row=S5 | sha=3f473c8 | commits=3f473c8 (round 2; round 1 was ae94753) | verify=**MY OWN, at two shas**: FULL gate at `ae94753` → exit 0 GREEN, 38 passed / 1.7m (/tmp/fracvibe-gate/gate-full-ae94753-20260921T105127Z.log); FULL gate at `3f473c8` → exit 0 GREEN, 39 passed / 1.8m (/tmp/fracvibe-gate/gate-full-3f473c8-20260921T110259Z.log) | arms=MY OWN probe, TWICE: deleting `attr.needsUpdate = true` (`fractal3d.js:168`) → in round 1 the palette pin STILL PASSED, which IS the gap (/tmp/fv-armS5-palette.log); in round 2 the NEW upload pin goes RED with the other two GREEN — isolated (/tmp/fv-armS5r2.log, 1 failed / 2 passed); restored byte-identical both times (`fractal3d.js` sha256 `e35c4b73…`) | pins=tests/3d-truth.spec.js — 3 pins: zero fractal evaluations across a real colour-cycle tick (counted; the old code scored 7), the palette VALUES for the same heights, and the palette UPLOAD via three.js `BufferAttribute.version`; suite 35 → 39 | retired=writer session `8aee532a`, worktree `worktrees/s5-3d`, branch `feat/s5-3d` | docs=docs/PLAN.md, docs/DECISIONS.md rows 21–24, MODERNIZATION.md B8 + the 3D fold-in + the §2 heading + §6 suite size — **the writer did this itself, the first slice in three to do so**, which is the S4 brief-clause fix working | note=the gap the dispatcher found (a pin asserting the CPU buffer, not the upload) was closed in round 2; see the TRAP on pins that assert DATA and miss the EFFECT
LANDED | row=S4 | sha=55eada7 | commits=55eada7 (no rebase was needed — the writer had already based its branch on `main` `b47d648`, so the gated sha IS the landing sha) | verify=**MY OWN**: FULL gate at `55eada7` → exit 0 GREEN, 35 passed / 1.5m (/tmp/fracvibe-gate/gate-full-55eada7-20260921T102954Z.log) | arms=MY OWN injection: the ONE shared escaping helper (`appendLocationInfo`) flipped back to `innerHTML` → ALL THREE hostile-render pins RED (name / renderer / fractalType), log /tmp/fv-armS4.log; restored byte-identical (`app.js` sha256 `c625ece7…`) | structural check=I grepped the tree myself — the only remaining `innerHTML` sites are a data-free `select.innerHTML = ''` and comments, with NO interpolation anywhere | pins=tests/import-validation.spec.js — 10 new tests; suite 25 → 35 | retired=writer session `2fab6a6e`, worktree `worktrees/s4-input`, branch `feat/s4-input` | docs=docs/PLAN.md, docs/DECISIONS.md rows 18–20, and MODERNIZATION.md B7 amended BY THE DISPATCHER (second slice in a row where the writer left the audit doc alone — see the TRAP on dropped brief clauses) | declared-gap=`FileReader.onerror` HAS a handler (`app.js:212`) but has no dedicated pin; low severity, recorded rather than hidden
LANDED | row=S3 | sha=590945c | commits=590945c (rebased from afe7601) | verify=**MY OWN, at two shas**: FULL gate at the writer's sha `afe7601` → exit 0 GREEN, 25 passed / 53.7s (/tmp/fracvibe-gate/gate-full-afe7601-20260921T094928Z.log); FULL gate at the rebased LANDING sha `590945c` → exit 0 GREEN, 25 passed / 53.4s (/tmp/fracvibe-gate/gate-full-590945c-20260921T095051Z.log) | arms=MY OWN injection on a net NO writer arm touched — the `u_maxIter` uniform pinned to 100 while the cap stayed 2000 (`webglFractal.js:314`) → pin 1 went RED at mean \|Δ\| **6.53** against the <6 threshold, while pin 2 stayed GREEN (so the arm is isolated); log /tmp/fv-armS3-pin1.log, restored byte-identical (`webglFractal.js` sha256 `ff03d1e6…`) | structural check=I read the tree myself: `fractalEngine.js` + `fractalEngineMain.js` DELETED and untracked, no dangling references, the only JS escape-time kernel is `fractalKernel.js`, the shader's four loops read `MAX_ITER` from the `#define` templated at `webglFractal.js:67`, and the slider max is derived at `app.js:59` | pins=tests/kernel-parity.spec.js — 6 new tests; suite 19 → 25 | retired=writer session `83436ce7`, worktree `worktrees/s3-kernel`, branch `feat/s3-kernel` | docs=docs/PLAN.md, docs/DECISIONS.md rows 14–17, and **MODERNIZATION.md B5 + the P1 kernel paragraph amended BY THE DISPATCHER** because the writer left the audit doc untouched and it still listed both as open | note=the writer corrected MY brief wording — see the TRAP on `fractalEngine.js`
LANDED | row=S2 | sha=63f19c8 | commits=63f19c8 (rebased from 446f746) | verify=**MY OWN, at two shas**: FULL gate at the writer's sha `446f746` → exit 0 GREEN, 19 passed / 39.1s (/tmp/fracvibe-gate/gate-full-446f746-20260921T091810Z.log); FULL gate at the rebased LANDING sha `63f19c8` → exit 0 GREEN, 19 passed / 38.1s (/tmp/fracvibe-gate/gate-full-63f19c8-20260921T091948Z.log) | arms=MY OWN probe: removed ALL FOUR stale-frame nets (`terminateWorker`'s state clear, `cancelJob`'s state clear, the generation gate in `makeWorkerMessageHandler`, and the token+generation checks in `applyWorkerFrame`) → the cancel pin went RED (`a late frame must not revive a cancelled job`, Expected null / Received 5), log /tmp/fv-armS2c.log, so that pin is NOT vacuous | restored=byte-identical (`app.js` sha256 `39636b23…`) | pins=tests/worker-lifecycle.spec.js — 4 new pins; suite 15 → 19 | retired=writer session `4d4ddbb7`, worktree `worktrees/s2-worker`, branch `feat/s2-worker` | docs=docs/PLAN.md, docs/DECISIONS.md rows 10–13, docs/PROBE-2026-09-21-seams.md CORRECTIONS (the writer falsified the probe's `Int32Array` claim) | note=the dispatcher's OWN first two probe attempts were VOID — incomplete removals — and neither was reported as a finding
LANDED | row=S1 | sha=cea74cb | commits=2de5107,913fde1,cea74cb (rebased onto main from aba24ec/398aad5/42ce3b6) | verify=**MY OWN, at two shas**: FULL gate at the writer's sha `398aad5` → exit 0 GREEN, 14 passed / 28.2s (/tmp/fracvibe-gate/gate-full-398aad5-20260921T084945Z.log); FULL gate at the rebased LANDING sha `cea74cb` → exit 0 GREEN, 15 passed / 26.7s (/tmp/fracvibe-gate/gate-full-cea74cb-20260921T085313Z.log) | arms=MY OWN positive control (neuter `handleWebGLLoss`) → the context-loss pin went RED (/tmp/fv-arm1.log), so it is not vacuous; MY OWN coverage probe (delete the real `webglcontextlost` listener at `app.js:907`) → the ORIGINAL pin STILL PASSED, exposing that it only reached the handler through the `window.__fv` hook and never exercised `attachContextLossGuard`; after the writer's round 2 the SAME probe turns the NEW pin RED for the right reason while the renamed hook test still passes (/tmp/fv-arm2b-real.log, /tmp/fv-arm2b-hook.log) | restored=byte-identical after every arm (`app.js` sha256 `fba018c4…`) | pins=tests/canvas-dpr.spec.js, tests/webgl-fallback.spec.js, tests/zoom-clamp.spec.js — 11 new tests, suite 4 → 15 | retired=writer session `5e91b6a0`, worktree `worktrees/s1-canvas`, branch `feat/s1-canvas` (all verified gone) | docs=MODERNIZATION.md B4/B9 + the P1 clamp paragraph, docs/DECISIONS.md rows 7–9, docs/PLAN.md | note=the dispatcher's probe found a pin that overclaimed coverage; the writer fixed it FORWARD in round 2 rather than the landing being accepted with a false pin
LANDED | row=machinery | sha=c6c87e2 | branch=main | verify=MY OWN at this sha: `bash scripts/gate.sh` exit 0 GREEN, 4 passed / 0 failed / 6.4s, MemAvailable 18616MB, raw log /tmp/fracvibe-gate/gate-full-c6c87e2-20260921T082802Z.log · plus the lock/tier/per-tree controls in GUARD below | scope=scripts/gate.sh (one gate command, atomic mkdir lock in the git COMMON dir shared across worktrees, two tiers), AGENTS.md, docs/BRIEF.md, docs/DECISIONS.md, .gitignore worktrees/, and playwright.config.js `reuseExistingServer: true → false` | retired=nothing (no writer was dispatched) | docs=this file, docs/DECISIONS.md rows 4–5
LANDED | row=Phase0 | sha=001cec9 | branch=main | commits=b83ecca,f9a016e,001cec9 | verify=MY OWN: `bash scripts/gate.sh` exit 0 GREEN, 4 passed / 0 failed / 6.2s, MemAvailable 18661MB, raw log /tmp/fracvibe-gate/gate-full-001cec9-20260921T082600Z.log (independently repeated, 6.4s, …082622Z.log) | scope=B1 dead element IDs that aborted module evaluation, B2 the off-screen 3D canvas, B3 the CPU render desync, plus the Playwright smoke suite | caveat=none — pushed to `origin/main` on 2026-09-21 (was local-only until then) | docs=MODERNIZATION.md §2
NOTE | the predecessor's own evidence survives as /tmp/fv-a.log and /tmp/fv-b.log (both "4 passed", 2026-09-21T10:14Z). Corroborating, not durable, and superseded by the logs above.

## QUEUE
QUEUE | row=* | The sequenced campaign is `docs/PLAN.md` (slices S1–S6, with footprints, pins, order and the serialization rule). The rows below are the raw defect inventory it draws from — do not dispatch a row directly; dispatch its slice.
QUEUE-CLOSED | row=WORKER-CANCEL | **CLOSED 2026-09-21 at `b46b30d`** (branch `fix/worker-cancel`, writer `session-wc-worker-cancel`; gate at that sha: FULL exit 0 GREEN, **59 passed / 3.6m**, raw log `/tmp/fracvibe-gate/gate-full-b46b30d-20260921T170049Z.log`). **DISPATCHER VERIFICATION 2026-09-21T17:15Z — the landing, rebased and re-gated by me, not taken on report:** rebased `fix/worker-cancel` onto `origin/main` `93d35ec`; the ONLY conflict was `docs/STATE.md` and I resolved it by **keeping MAIN's board and applying only the writer's row change** (union), verified by row counts. Landing head **`c1527d7`** (fix `7b25bf2`, board `c1527d7`), pushed to `origin/main` = `c1527d7`. **MY OWN gate, twice: at the writer's head `7dd698d` → exit 0 GREEN, 59 passed / 3.6m** (`/tmp/fracvibe-gate/gate-full-7dd698d-20260921T170552Z.log`) **and at the REBASED LANDING `c1527d7` → exit 0 GREEN, 60 passed / 3.6m** (`/tmp/fracvibe-gate/gate-full-c1527d7-20260921T171108Z.log`; 60 because main now also carries the favicon pin). The rebase provably changed no code: `public/app.js` `a975bef989bd`, `public/fractalWorker.js` `a5dcc6845cf6`, `tests/worker-cancel.spec.js` `6083ae561095`, `tests/worker-lifecycle.spec.js` `3a7cd3cab41d` are **byte-identical** pre/post rebase. **MY OWN independent control arms (2), and what they FALSIFIED — recorded because a green pin is not a sensitive pin:** (1) **falsified early `ready`** (worker posts `ready` BEFORE `importScripts`, i.e. the handshake's timing premise broken) → the pin stayed **GREEN**; (2) **`preventDefault()` removed alone**, everything else intact → the pin stayed **GREEN**. Both are INFORMATIVE, not defects: with the ready-gate in place no abort ever occurs, so *after the fix* neither the handshake's timing nor `preventDefault` is individually load-bearing — `preventDefault` is defence-in-depth and the handshake's timing is only load-bearing against a build that terminates mid-load, which is exactly the writer's control (whole mechanism removed → RED, receiving the literal banner, `/tmp/fv-wc-arm-whole.log`). **So the pin is proven sensitive to the DEFECT CLASS (whole mechanism) but is NOT sensitive to either partial break; a successor must not read this pin as covering them.** **RETIRED 2026-09-21T17:15Z** by the dispatcher, after verifying the landing on the remote (`git ls-remote origin refs/heads/main` = `e6f2f870c8e1…` = local `main`): worktree `worktrees/wc-worker-cancel` and branch `fix/worker-cancel` DELETED (branch was fully merged; `git worktree list` now shows main alone, `worktrees/` empty). **PUBLISHED 2026-09-21T17:14Z**: `dist/` re-staged (`diff -rq public dist` → no differences) so the fix is the live artifact at the origin. **CDN SKEW — measured, and the reason this one wants a purge:** `app.js` and `fractalWorker.js` are both **STALE at the edge** with **~3.4 h left** (age ≈2151 s and ≈2149 s, i.e. they expire within seconds of each other), so the natural rotation keeps the OLD pair coherent and then flips both — but any window where they disagree combines the new `app.js` (which waits for `{type:'ready'}` before ever posting a job) with the old `fractalWorker.js` (which never sends it), and the CPU lane would then never render. The owner purged these two URLs for the P2 deploy, and the same two URLs are the ask here; the durable fix is the `PUBLISH-CACHE` queue row (version the asset URLs), which this release is the second concrete argument for. Verified coherent NOW: the live URL makes no non-2xx request and hard-reloads clean. The defect record below is kept as history; the PROVEN firing site, the fix and the control arms are appended at the END of this row. | **ZOOMING INTO THE CPU LANE KILLS THE RENDER WITH A FATAL BANNER — reproduced by the owner and by me, on the live P2 build.** Symptom: `Error: Uncaught NetworkError: Failed to execute 'importScripts' on 'WorkerGlobalScope': The script at '…/fractalKernel.js' failed to load.` in `#appMessage`; the canvas then never recovers until the view changes, and on the owner's machine it does **not** appear in the console. **Reproduced deterministically headless, on the LIVE build and on a local copy**: wheel-zoom ~25–40 ticks → banner (raw logs `/tmp/fv-cpulane-origin.log`, `/tmp/fv-fix1.log`, `/tmp/fv-fix2.log`). **Mechanism (measured, not inferred):** `onWheel` → `startFractalCalculation` → `cancelJob` on every tick; `cancelJob` → `terminateWorker()` **then an eager `spinUpWorker()`**, so each tick creates a Worker and kills it ~450 ms later with **`ready=false`** — terminated in the middle of its `importScripts('fractalKernel.js')`. An instrumented trace showed a repeating `terminate(ready=false) → create` cycle across 26 generations. The aborted load surfaces as the fatal banner; that handler then calls `terminateWorker()`, leaving the slot empty until the next view change. **It is NOT a P2 regression**: `git diff aa39f1c a2fe502 -- public/app.js` shows P2 did not touch `spinUpWorker`/`terminateWorker`/`cancelJob`; P2 only made the lane easy for a user to reach. **MY PARTIAL FIX IS REVERTED AND RECORDED AS A FAILURE, not left in:** (a) removing `cancelJob`'s eager `spinUpWorker()` removed the `FAILED fractalKernel.js` request but **the banner persisted**; (b) adding a load-phase suppression (`_fvSpoke`) **also failed**. So the firing handler is NOT the one my first two readings assumed. **The next attempt must PROVE which handler fires** (a `showError` stack or a counted instrumented run) **before changing code** — do not repeat my guesswork. Deliverable: (a) a Worker never terminated mid-script-load, or recreated so an aborted load can never surface as a render failure, and (b) **a worker failure must never leave a frozen canvas with no recovery** — retry/reload or self-heal, with an actionable `#appMessage`. **Acceptance test = the owner's exact interaction**: wheel-zoom from the default view into the CPU lane with **zero** banner and zero failed `fractalKernel.js` requests, run headless AND as a browser check. Footprint: `public/app.js` (worker lifecycle + `#appMessage`) and `tests/worker-lifecycle.spec.js` (S2's file — shared file means SERIALIZE). **RESOLUTION (2026-09-21, `b46b30d`) — the firing site is PROVEN, and the two failed attempts are explained.** A `page.addInitScript` instrument (no production path) recorded every `window` error event, every Worker `error` event, and every Worker create/terminate, while `fractalKernel.js` was delayed 400 ms to widen the load window. Result: on each tick `cancelJob` terminated the just-spawned worker while `importScripts` was in flight; the browser then fired an `error` event on the **Worker** and a second one on **`window`**, both with `event.error === null`, `filename=fractalWorker.js`, `lineno=5`, `colno=1`, and `defaultPrevented=false` — i.e. `terminateWorker` had already set `w.onerror = null`, so **nothing called `preventDefault()`** and the event escaped to the global net at `app.js:683`. That is the ONLY producer of the `Error: <text>` text with no `(file:line)` suffix, which is exactly the observed banner — so the firing handler was the WINDOW net, never `w.onerror`. That also explains both failed attempts: (a) removing the eager `spinUpWorker()` left `startFractalCalculation` respawning anyway, and (b) `_fvSpoke` inside `w.onerror` could never run because the handler had been detached. Measured before: **44 worker error events, 44 window error events, 44 aborted `fractalKernel.js` requests**; after: **0 / 0 / 0**. (`pageerror` logged **0** of them, which is why the owner's console was empty.) **THE FIX:** `fractalWorker.js` posts `{type:'ready'}` once loaded; `app.js` never terminates a not-yet-ready worker (a superseded loading worker is KEPT and its pending job replaced — one worker per storm, not one per tick), `sendProgressiveJob` never posts to a not-ready worker (so a superseded job never runs — S2's cancellation semantics preserved), `w.onerror` is never detached and calls `preventDefault()` unconditionally, and a live-worker failure self-heals (retire + retry on a fresh worker, bounded at 2 retries, actionable message). **EVIDENCE:** new `tests/worker-cancel.spec.js` runs the owner's real interaction (48 × `mouse.wheel(0,-120)` at 90 ms) with the kernel delayed, asserting **0** error banner, **0** aborted kernel requests, and a canvas that keeps applying frames; the S2 cancellation pin is unchanged and green; S2's worker-failure pin was updated to the self-heal contract. Control arms, both watched RED against the pre-fix build then restored byte-identically (`app.js` sha256 `a975bef989bd`, `fractalWorker.js` `a5dcc6845cf6`): `/tmp/fv-wc-arm-whole.log` (acceptance pin RED — the exact banner text and the aborted requests) and `/tmp/fv-wc-arm-selfheal.log` (self-heal pin RED — "change the view to retry"). `MODERNIZATION.md` carries **no** claim this makes false (B6's "`worker.terminate()` + respawn" still holds for a READY worker; §3's single-worker/pool remainder is untouched) — see `docs/DECISIONS.md` rows 43–45.
FORK-RESOLVED 2026-09-21 (owner, verbatim) | **“I do not want a GPU to cpu crossover if a GPU is present. If a GPU is present, complete depth needs to be calculated there, period.”** and **“Just display the rendering time per full image. Render from coarse to fine (like the cpu renderer does already i believe) so that the app stays responsive. Zooming mid-render needs to stay possible.”** | effect=**an INVARIANT, not a heuristic: when a GPU is present the GPU renders every depth, however slow. There is no depth-based or time-based fallback and no crossover to design.** Slowness at extreme depth is accepted and made visible rather than traded for a mode switch. The CPU lane is reachable ONLY when WebGL is absent or has failed (plus the manual checkbox the owner chose to keep). Two consequences written into the deliverable: (1) **a per-full-image render-time readout**; (2) **coarse-to-fine GPU refinement** — the CPU lane already posts gridStep 8→4→2→1 frames, the GPU lane currently does ONE full-resolution pass, so progressive refinement must be added on the GPU path, with zoom remaining possible mid-render (a superseded job must stay cheap). | FORK-REJECTED: my recommendation of a measured GPU-vs-CPU crossover — the owner rejected it explicitly. **Do not reintroduce it as a ‘smart’ optimisation.** | RISK RECORDED, measured later not assumed: some deep coordinates produce pertubation GLITCHES that rebasing cannot repair; if that happens the correct answer under this invariant is still the GPU (a longer/again-rebased pass), NOT the CPU — a pin must state what the app does there rather than silently degrading.
OWNER 2026-09-21 | verbatim: **“Yea, so we dont get a regression.”** | effect=**NO REGRESSION is a binding acceptance criterion on GPU-ARBITRARY, not a hope.** Three specific regression classes are pinned to the writer, each with an in-pin baseline and a measurement rather than an assertion: (1) **the SHALLOW plain lane must stay byte-for-byte identical** — every view at or above `1e-4` runs the plain shader, and the exponent work touches the SHARED uniform/vertex structure, so the default view and the view at exactly the cap must render the same pixels as the pre-change build (readback-buffer comparison); (2) **the existing deep lane must not degrade** — the 1e-15 P1 measurement (0.00000 % misclassification, mean |ΔRGB| 0.453, `tests/perturb-core.spec.js`) must stay green and un-worsened; (3) **the new deep path must be CORRECT, not merely non-collapsed** — “it shows structure instead of one colour” is not a pass; it needs an independent direct-BigInt reference with % misclassified + mean |Δ| reported, and the pre-fix collapse as the failing baseline in-pin. A fourth: the manual renderer checkbox and the genuine no-GPU path must keep working (S1/S2 pins green). **If the shallow lane is not byte-identical, that is a FINDING to report, never to smooth over.**
MEASURED | 2026-09-21 WebGPU availability on THIS host | **`navigator.gpu` is PRESENT in a secure context but `requestAdapter()` returns NULL by default** — i.e. the API is exposed, there is simply no hardware adapter. With `--enable-unsafe-webgpu --enable-features=Vulkan,WebGPU` an adapter IS granted and reports **`google / swiftshader`**, so WebGPU work is testable here only as a SOFTWARE adapter (same SwiftShader situation as the WebGL lane — no hardware numbers). **TWO CONSEQUENCES:** (1) the capability ladder must handle **‘API present, no adapter’** as a distinct case from ‘API absent’ — `navigator.gpu !== undefined` is NOT sufficient to choose the WebGPU tier, the adapter request must succeed and be awaited; (2) **a secure context is required** (`window.isSecureContext` false on `about:blank` makes `navigator.gpu` undefined entirely, which cost three probe attempts before being spotted). Recording this because it is the exact trap a naive `if (navigator.gpu)` feature test would fall into on any machine with a blocklisted or disabled GPU.
LANDED | row=WEBGPU-GROUNDWORK (tier groundwork, NEW FILES ONLY) | sha=**`b55aa56`** (commits `150b87a` code, `0697bc2`+`b55aa56` board; clean FF from `origin/main`) | **DISPATCHER-VERIFIED**: (1) **the new-files-only claim is CONFIRMED by my own `git diff --name-status`** — `M docs/DECISIONS.md`, `M docs/STATE.md`, `A docs/RENDERER-CONTRACT.md`, `A public/capabilities.js`, `A public/capabilitiesModule.js`, `A public/webgpuProbe.js`, `A tests/webgpu-feasibility.spec.js`; **no existing code file, no existing test, no config, no `dist/`** — which is what made it safe beside the in-flight GPU writer; (2) **MY OWN full gate at `b55aa56` → exit 0 GREEN, 61 passed / 2 skipped / 4.5m** (`/tmp/fracvibe-gate/gate-full-b55aa56-20260921T204648Z.log`), corroborating the writer's 61/2 at `0697bc2`; (3) **MY OWN control arm — and it took three attempts, recorded honestly:** injecting the NAIVE feature test this slice exists to prevent (`if (navigator.gpu) return 'webgpu'` inside `selectTier`) drives the pin RED for the right reason — `tiers={"fromAbsent":"webgpu","fromNoAdapter":"webgpu","fromAdapter":"webgpu","fromNothing":"webgpu"}` against `expect(out.tiers.fromNoAdapter).not.toBe('webgpu')` at `tests/webgpu-feasibility.spec.js:114` (`/tmp/fv-wg-arm3.log`), restored byte-identically. **Attempt 1 was VOID** — it died on `:3000 is already used` because MY OWN gate held the port, i.e. a startup failure miscounted as a RED; **attempt 2 is UNEXPLAINED** (injection proven present at line 343 and proven active by a dedicated diagnostic, yet the spec reported green) and is NOT counted as evidence. Attempt 3 is the valid arm. | **CAPABILITY REPORT FROM THIS MACHINE (verbatim):** `secureContext` true, `hardwareConcurrency` **12**, `webgl1`/`webgl2` true, `OES_texture_float` true, `MAX_TEXTURE_SIZE` **8192**, WebGL renderer `ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (Subzero)), SwiftShader driver)`, `wasmSimd` **true**, `sharedArrayBuffer` **false**, `offscreenCanvas` true, `hasNavigatorGpu` **true**, and `webgpu.status = **api-present-no-adapter**` by default — **the trap from the MEASURED row, now encoded AND pinned.** With `--enable-unsafe-webgpu --enable-features=Vulkan,WebGPU`: `adapter-available`, `google/swiftshader`. | **THE STRUCTURAL WIN, MEASURED: the orbit ceiling is no longer a texture limit.** WebGL carries the orbit as a texture, so `maxOrbitWidth = min(MAX_TEXTURE_SIZE, MAX_ITER)` = **8192 samples**. WebGPU's device `maxStorageBufferBindingSize` measured **134 217 728 B ⇒ 16 777 216 orbit samples** (`maxBufferSize` 268 435 456 B); a **20 001-entry `vec2<f32>` orbit storage buffer (160 008 B)** was created and read back **byte-for-byte at every index to 20 000** (index 8192 → `[8192,4096]`, index 20 000 → `[20000,10000]`), including from a compute pass whose reference is non-escaping. | **COMPUTE PRODUCED REAL PIXELS, measured:** a 96×96 perturbed frame (scale 1e-12, budget 20 000, the shipped recurrence + rebasing + rescaling) gave **715 distinct escape values, 0/9216 misclassified** against an independent float64 perturbation reference, mean |Δ escape index| **5.89**, read back through `MAP_READ` → `ImageData` → `putImageData` → `getImageData` (**121 distinct colours, nonBlackFrac 1.0**). **HONEST LIMIT recorded by the writer and kept:** in the perturbed pass the max ORBIT index actually reached was only **3015** (rebasing resets the reference index, and a boundary reference escapes ~3015), so the >8192 claim rests on the ceiling pass plus the non-escaping-reference run — NOT on the perturbed frame. | **NO PERFORMANCE CLAIM IS MADE, and the one measurement we have points the other way:** same view (160×120, scale 1e-8, cap 2048, 5 frames, completion barrier both sides) WebGPU compute **≈188–201 ms/frame** vs the shipped WebGL perturbation lane **≈137–176 ms/frame**. **Both are SOFTWARE SwiftShader numbers** on this host (WebGPU adapter `google/swiftshader`; WebGL reports ANGLE/SwiftShader), so they say nothing about real hardware — the correct reading is that the WebGPU case rests on STRUCTURAL advantages (the removed texture ceiling, real compute, a cleaner home for the exponent-delta mechanism), not on speed. | NOT ESTABLISHED: the probe is **not a backend** — no progressive frames (the owner's coarse-to-fine GPU requirement is still unbuilt), no `device.lost` path, no shared orbit cache, no `window.__fv` observables; **no WebGPU tier is wired into the live dispatch** (deliberate); the 20 001-entry orbit is far below the 16.7M-sample ceiling (the ceiling was READ, not saturated); no hardware numbers. Wiring gaps are listed in `docs/RENDERER-CONTRACT.md` §5. | PUBLISH: nothing to publish yet — no existing file references the new ones, so the live site is unchanged by this landing. **When the wiring slice adds a reference from `index.html`/`app.js`, the versioned stage step must be taught to version the NEW files too** (it rewrites the graph reachable from `app.js`/`fractalWorker.js`; an unreferenced file is simply not part of it). | RETIRED: writer session `1e087e60`, worktree `worktrees/webgpu-groundwork`, branch `feat/webgpu-groundwork`.
SESSION | id=d3ba4c07-3aeb-40d8-ac0d-aa68f0d23572 | role=WRITER · row=REFINE (coarse-to-fine + non-blocking zoom) | state=dispatched 2026-09-22 by session-0e97db24 · worktree=`worktrees/refine` · branch=`feat/refine` · base=`origin/main` = `c3bb7aa` · **OWNER REQUEST, verbatim: “Deep zooms seem to work well on webgpu. They get slow of course and thats where the coarse to fine rendering should come in. Rendering that does not block further zoom. Can do that for any depth, it just wont be noticable at lower depths.”** · scope=port the CPU lane's gridStep 8→4→2→1 progressive model to the GPU path, with the chain ABANDONED between passes when the view changes (the GPU cannot be preempted mid-pass, so the honest mechanism is a check between passes), at any depth, invisible at shallow depths by design · acceptance=ms from wheel event to first visible response during a deep refinement, plus a pin whose failing baseline is the current single full-res pass · also binds=TOTAL non-regression: shallow lane byte-identical FINAL frame, P1 1e-15 + 1e-40 pins, no CPU crossover, LANE-CONTINUITY pins green · verify=NOT YET
LANDED-BATCH | 2026-09-22 | **ALL FOUR WRITERS FROM THIS SESSION ARE LANDED, VERIFIED AND RETIRED; `origin/main` = `0a2e5d5`; only `feat/refine` remains in flight.** In landing order, each with the dispatcher's own check:
  • **LANE-CONTINUITY `b68385a`** (code `1dc3618`) — PUBLISHED AND LIVE. The 1e-4 budget discontinuity is gone (my own sweep: boundary step **31.600 → 0.019**, and my own control arm — near-zero ramp width, which keeps the NEW formula and breaks only the ramp — drove pin 1 RED at `31.5997` vs `< 1` and pin 3 RED at `1543` vs `≤200` with pins 2/4 correctly GREEN, restored byte-identical `f1542b09…`). `WEBGL_ZOOM_CAP` REMOVED: live app reports `zoomCap: null`, `minScale: null`, **zero non-2xx, zero errors**. My gate at the landing: exit 0 GREEN, 70 passed / 2 skipped / 9.5m.
  • **GPU-DIAGNOSTIC `da2170c`** (code `b5eae26`) — PUBLISHED AND LIVE, and verified working from the CDN: `#gpuDiagBtn` present, pressing flips `#gpuDiagStatus` to `done — see console (F12)`, console receives the structured report. `scripts/stage.sh` now treats `gpuDiagnostic.js` as an ENTRY, so `index.html` points at `gpuDiagnostic.a257d60d3c7c.js` and **no edge cache can serve a stale instrument** — that was DECISIONS 75's hazard, fixed in the correct order (file first, entry second).
  • **EXPANSIONS `d8c98bf`** (code `76ec487`/`098bc6a`, gate 74 passed) — the k-component expansion ladder; `k=1` bit-identical to plain float32; **k=2 is the last useful rung** (DECISIONS 78). Unreferenced by the app, so it publishes as an orphan and is fetched by nothing — correct until integration.
  • **PRECISION-CHAIN `0a2e5d5`** (code `775dd67`/`1bda851`, gate 67 passed) — `docs/PRECISION-CHAIN.md`, the CPU perturbation+orbit port prototype (0.00000 % vs an independent reference at 1e-30/1e-40/1e-120 where the shipped lane is 2.78 % misclassified), same orphan status.
  All four were rebased onto current main with the append-only docs conflicts resolved by CONTENT (rows renumbered to follow main's highest, never by trusting the writer's numbers), and every landing's code was proven **byte-identical** to the sha that was gated — that is the check that makes trusting a writer's gate honest rather than hopeful. Worktrees and branches for all four RETIRED; `git worktree list` shows main plus the active refine writer only.
DISPATCHER-PROCESS-FIX (mine, recorded): I landed docs commits SINGLY, which moved `main` under four open branches and forced repeated rebases — lane-continuity alone was rebased four times for no substantive reason. **Docs now fold into landings, batched.**
HANDOFF | 2026-09-22 | **THE OWNER'S ONE ACTION: open https://apps.futuremagic.de/fracvibe/ , press the `Test` button (top bar, next to Load Location), then copy the F12 console output back.** That report is the ONLY source of real-hardware evidence this project can get: this host is SwiftShader (software) for WebGL AND WebGPU, so every precision claim we hold is a software measurement (DECISIONS 47/65/66/67). The button is LIVE and verified working from the CDN: present on the page, pressing it flips `#gpuDiagStatus` to `done — see console (F12)`, and the console receives the structured 33-line report. Its script is content-addressed (`gpuDiagnostic.a257d60d3c7c.js`) so no edge cache can serve a stale instrument.
WHAT THE OWNER'S REPORT WILL SETTLE, and why the SUMMARY line alone may be enough: `kind=HARDWARE|SOFTWARE` (is the GPU real); `floatTex=SAMPLES|FAILS` (can we sample a float texture at all — the deep lane's load-bearing capability); **`dsLow=` and the DOUBLE-SINGLE RESIDUAL block** (does a real driver keep the expansion's low component, or fold it away like this host's compiler does for a literal operand — this decides whether the k=2 delta ladder is safe as written or must go integer-controlled, DECISIONS 74); `deepLane=float64+bigint` (did the deep lane engage); and the `cost:` triple, which is the first real-hardware timing this project will ever have. If `dsLow=ZERO-IGNORED` appears WITH `ds_low_changes_result=NO`, that is the silent-collapse failure and it changes the plan — mine, not theirs.
STATE AT HANDOFF: published and live = the lane-continuity fix (no hard stop, no 1e-4 jump) and the diagnostic. QUEUED, not yet landed = `feat/expansions` (the k=2 delta ladder, gate-green at 098bc6a) and `feat/precision-chain` (audit + CPU perturbation port, gate-green at 1bda851); both need a rebase and a fresh gate before publish, and neither is user-visible until integration, which is deliberate.
AUTONOMOUS-MODE | 2026-09-21 (owner, verbatim) | **“Assume that i will be gone for a long time and build everything speculatively, to be adjusted later with results.”** | RULES IN FORCE until the owner returns (full text in DECISIONS 68): no work waits for a missing answer — a real fork is DECIDED, recorded with the rejected option, and marked SPECULATIVE; build behind seams so a hardware result changes a CHOICE rather than invalidating a build; **never finalise a precision-dependent decision on SwiftShader evidence alone**; and the standing disqualifiers do not relax (no CPU crossover, no hard stop, shared math, dispatcher gate + own control arm before every publish). | **WHAT IS SPECULATION AND MUST BE LABELLED SO:** every deep-lane correctness and performance claim this project holds is a SWIFTSHADER measurement (rows 47, 65) — the deep lane's 1e-40 result, the float-texture transport, and the float32 delta arithmetic are UNVERIFIED ON HARDWARE until the owner reports the GPU-DIAGNOSTIC console. The **STAGED PLAN** is therefore: (1) land the diagnostic instrument and PUBLISH it — it is the one thing that converts their machine into our test hardware, and it blocks nothing; (2) land LANE-CONTINUITY (the visible 1e-4 jump, a live user-facing defect); (3) land the EXPANSIONS arithmetic library and the PRECISION-CHAIN audit, both of which are correct work regardless of what the hardware says; (4) hold SHADER INTEGRATION of the ladder until the residual test reports, because if the low component does NOT survive on the owner's driver the integration must be integer-controlled instead of classic — that is precisely the choice the instrument exists to make cheap.
NORTH-STAR | 2026-09-21 (owner, verbatim) | **“I just want this to be the best it can be on no matter what machine. Thats the goal. Use what is available and if it turns out NPUs can also be used, we might examine that next... low prio right now and they probably cant be :)”** | effect=**the renderer becomes a CAPABILITY LADDER, not a two-way switch**: WebGPU (compute) > WebGL + `OES_texture_float` (the perturbation lane) > WebGL without float textures (plain float32, shallow) > CPU with WASM SIMD tiled across cores, each machine rendering on the best tier it actually HAS. Still **capability dispatch, never timing-based** — the owner's no-crossover invariant stands (row 46). It also RESOLVES the CPU-path question: CPU speed is not “the fallback”, it is the best engine on machines with no better one. | HONEST BOUND (row 49): depth REACH varies by machine because precision ceilings differ, so “identical on every machine” cannot be promised at every depth — what IS promised is that at a given depth the picture is a function of the view parameters, not the engine, and that no engine silently degrades quality. **GAP FOUND WHILE RECORDING THIS: S3's cpu-vs-gpu parity pins only cover SHALLOW scale, and the CPU lane has no perturbation path at all, so deep cross-engine agreement is UNVERIFIED** — recorded, not discovered later. | NPU (row 50): REAL hardware, WRONG tool, and unreachable from the browser anyway — serial recurrence + branchy control flow + per-bit extended-range arithmetic are the opposite of quantized tensor inference, and WebNN exposes ML operators, not a kernel language. The owner's guess is correct for structural reasons. LOW priority confirmed. | SEQUENCE: the GPU-ARBITRARY landing first (in flight), then a capability-detection skeleton + the CPU ladder rung (the read-only probe `dfff36bb` already measuring its headroom), then WebGPU as a possible tier above WebGL. **GROUNDWORK LANDED `150b87a` (branch `feat/webgpu-groundwork`, writer `1e087e60`):** the ladder's DETECTOR is on disk — `public/capabilities.js` (+ the ES-module face `public/capabilitiesModule.js`) with the three-state WebGPU status (`absent` / `api-present-no-adapter` / `adapter-available`) and `selectTier()` reading ONLY the awaited adapter verdict; `docs/RENDERER-CONTRACT.md` states the backend interface C1–C9 with `file:line` anchors and a per-backend satisfaction matrix (the wiring slice's gaps are its §5); `public/webgpuProbe.js` + `tests/webgpu-feasibility.spec.js` MEASURE the two WebGPU wins on this host — `maxStorageBufferBindingSize` **134 217 728 B ⇒ 16 777 216 orbit samples vs WebGL's 8192 texels**, a 20 001-entry orbit storage buffer read back byte-exact at every index up to 20 000, and a real escape-time frame read back as pixels (96×96 at 1e-12: 715 distinct escape values, 0 misclassified vs a float64 reference) — with **NO performance claim** (software SwiftShader for both lanes; WebGPU measured ≈188–201 ms/frame vs WebGL ≈137–176 ms/frame). **NOT wired in: no file of the live app was modified** (only `docs/STATE.md` and `docs/DECISIONS.md` amended, rows 53–56). **GATE at `0697bc2` (the tree the landing was gated at): FULL exit 0 GREEN — 61 passed / 2 skipped / 4.9m, raw log `/tmp/fracvibe-gate/gate-full-0697bc2-20260921T204109Z.log`; the two skips are this slice's adapter-gated compute tests, recorded with their reason (the gate runs on a machine with no WebGPU adapter by default, which is the documented behaviour, not a failure).**
PROBE-DONE 2026-09-21 | read-only probe `dfff36bb` COMPLETE, repo untouched (`git status --porcelain` empty), report `/tmp/fv-cpu-probe/REPORT.md`. **Measured multipliers: row-band tiling 1.99×/4.04×/11.64× at N=2/4/12 (pixel-exact; knee N≈4–6 for frames ≤1 s); loop SHAPE 2.4–2.7× on ONE core at every depth, bit-identical per pixel; branchless NONE; wasm f64x2 SIMD only +1.09–1.24×; the copy bucket 7.9 % shallow but 0.2 % at depth.** **TWO FINDINGS THAT REFRAME THIS ROW (see DECISIONS 63): the CPU lane cannot render depth at all** (no orbit, direct float64 projection → coordinate staircase below ~1e-13, one flat colour from ~1e-17), **and it is clamped to `scale >= 1e-4` by the shared viewer** — so the old “switch to CPU for deeper zoom” offer could never have delivered deeper zoom. **A CPU perturbation+orbit PORT is therefore a PREREQUISITE for CPU deep zoom, not an optimisation.** Probe caveats: the run overlapped a GPU gate (loadavg 5–20 on 12 CPUs) so absolute ms are inflated and short-frame tiling figures are lower bounds; Node `worker_threads` ≠ browser Web Workers; smooth-value conversion and 2D merge costs were not measured; the probe documented three of its own harness bugs rather than presenting them as findings. **SCHEDULING (owner: “CPU path is less urgent … if it conflicts, keep CPU optimization as a step for when GPU is in a good state”): unchanged — and note P6 (the CPU port) is now a FEATURE prerequisite, while P1 (loop shape, 2.4–2.7×) and P2 (tiling, 11.6×) are the cheap wins, and P5 says do NOT invest in SIMD/branchless.**
QUEUE | row=MANTISSA-LADDER (**owner-chosen route: k-component expansion ladder, component count GROWS WITH DEPTH — see DECISIONS 66**; next after LANE-CONTINUITY, NOT dispatched — SAME SHADER, so SERIALIZE) | **The real path to arbitrary depth on the GPU: widen the DELTA MANTISSA.** The shipped mechanism (DECISIONS 57–61) extended RANGE via `u_scaleShift`; the depth bound that remains is the float32 delta mantissa (24 bits) accumulating relative error. That is a SOLVER CHOICE, NOT A FLOOR — and this repo has already measured the remedy and abandoned it: **DECISIONS 37 measured compensated float32 (hi+lo) holding to ≥1e10 with 0.000% misclassification (mean |ΔRGB| 0.31/0.42/0.99 at 1e8/1e9/1e10) at 10.4–12.5× fragment cost, and the fork note says it was “deliberately NOT built.”** Route = a mantissa ladder: k float32 components ≈ k×24 bits of delta precision, cost ≈ k, so depth grows with the component count and is bounded by TIME/GPU load, never by a number. Prior art matches the range fix (bertbaron float32+exponent; KF rescaled types; `docs/RESEARCH-2026-09-21-arbitrary-depth.md` §2–§3). **Deliverable:** the widened-mantissa delta path; a pin against an independent BigInt reference reporting % misclassified + mean |Δ|; the same four no-regression classes; and a measured cost-vs-depth curve for k = 1 (current), 2, 4. **Rejected in advance:** substituting a new app cap for a real mechanism (owner, DECISIONS 62), and claiming arbitrary depth without measuring the new reach. **PRIOR ART FOUND (owner-directed web search 2026-09-21, `docs/RESEARCH-2026-09-21-arbitrary-depth.md` §7): the GPU libraries DO exist** — **luma.gl `fp64`/`fp64arithmetic`** (production WebGL2/WebGPU; double-single in GLSL *and* WGSL, ~48 bits, with a deep-zoom Mandelbrot benchmark), **`glsl-arbitrary-precision`**, **`glsl-arb-prec`** (OpenGL **ES** — our exact target), **CAMPARY** (CUDA multi-precision), and **deep-fractal** (a working WebGL perturbation deep-zoom viewer). **TWO PLAN-CHANGING WARNINGS:** (1) **WGSL's fp rules permit reassociation and fusion and do not fix a rounding direction, so the CLASSIC double-single transforms are NOT portable on WebGPU** — an error-free transform is an algorithm over rounding events and a reassociating compiler can erase the low term (algebraically zero over the reals); `let`/parentheses/`bitcast`/`+0.0`/`*1.0`/WGSL `fma` are all explicitly NOT precision barriers. So a WGSL mantissa ladder must be **integer-controlled** or validated per-adapter; **GLSL is the safer route**. (2) **Validation needs REAL HARDWARE** — a software adapter cannot demonstrate that residual terms survive execution, and a final value can look plausible after the expansion silently collapses to f32, so pins must inspect BOTH the high and a required-nonzero low component; **this host is SwiftShader for WebGL *and* WebGPU, so it can develop and falsify the mechanism but cannot certify it.** | SERIALIZE: it edits `public/webglFractal.js`, which LANE-CONTINUITY owns right now — dispatch only after that landing is verified. It must compose with whatever shift/`S` representation LANE-CONTINUITY settles on; that writer has been asked to report whether the exponent split composes cleanly with hi/lo components.
QUEUE | row=CPU-PATH (new, 2026-09-21, owner-sequenced AFTER GPU, deliberately NOT dispatched) | **OWNER DECISION: CPU optimisation is LESS URGENT and must not slow the GPU work** — verbatim: *“CPU path is less urgent, but an investigation and maybe even implementation in a separate worker would be nice if it wont slow down our GPU work. If it conflicts, keep CPU optimization as a step for when GPU is in a good state.”* | **MEASURED STARTING FACTS (audited 2026-09-21):** the CPU lane runs **exactly ONE Worker** (`public/app.js:729`) so a 16-core machine uses ~1 core; progressive refinement **copies the whole buffer per pass** (`public/fractalWorker.js:48` `job.prior.slice()` plus `{...job, chunk}` spreads) and the final frame is the only transferred one; the escape-time loop is the dependency-free `public/fractalKernel.js`. All three are already recorded as deferred in `MODERNIZATION.md` §3. | **THE TRICKS ARE REAL AND MOSTLY UNEXPLOITED, in payoff order:** (1) **multi-worker tiling** — the pixels are embarrassingly parallel with zero inter-pixel communication, so this is a `hardwareConcurrency` multiplier and the single biggest win (SharedArrayBuffer+Atomics, or N tile workers merged on the main thread); (2) **SIMD** — a branchless escape test lets V8 auto-vectorise (the `break`/early-escape branch is what defeats it), and explicit **WebAssembly SIMD** is the credible fixed-width route for this workload; (3) buffer reuse + transferables on non-final passes; cache/loop ordering. **HONEST LIMITS, recorded so it is not oversold:** SIMD buys a CONSTANT factor and cannot follow depth — the iteration count grows with `log10(1/scale)` (D2), so at extreme depth the CPU lane's sequential dependency dominates regardless of vector width; and under the GPU-only invariant (`FORK-RESOLVED` row) CPU speed only improves the GENUINELY GPU-LESS case, so this is a quality-of-fallback investment, NOT a competitor to the GPU lane. | PROBE DISPATCHED 2026-09-21: read-only measurement probe `dfff36bb-0bbc-4135-b7b4-888cf6511d9c` — **touches no code in this repo, takes no gate lock, binds no :3000**, working in `/tmp/fv-cpu-probe/`, so it cannot collide with the in-flight GPU writer. It is to report: baseline ms/frame by depth, the real tiling scaling curve, branchless/WASM-SIMD gain WITH evidence that vectorisation actually happened, and the BigInt-orbit-vs-per-pixel cost split (which decides whether vectorising pixels matters at depth at all).
PUBLISHED | row=GPU-ARBITRARY | 2026-09-21 | **the FIRST deploy on this host that reached visitors with NO Cloudflare purge**, because the versioned asset URLs were already in place — which was the entire purpose of `PUBLISH-CACHE`/`scripts/stage.sh`. Staged with the script (exit 0: `18 files in dist/, 11 versioned, verified content-addressed`). **VERIFIED ON THE LIVE CDN, real browser:** `zoomCap = 1000000`, `minScale = 1e-6`, the per-full-image readout exists (`renderTimeMs()` ≈ 46.4 ms on the default view), **`#zoomCapOffer` is ABSENT** (the automatic CPU offer is gone per the owner invariant), all eight module assets load in their VERSIONED names (`app.36b89e2e1da3.js`, `webglFractal.473b602ab56e.js`, …), and **non-2xx: NONE, ERRORS: NONE**. Raw log `/tmp/fv-live-gpu.mjs`. | **`scripts/stage.sh` GREW A FIX FROM THIS PUBLISH, and it is the WEBGPU-WIRING gap arriving early:** the first stage run FAILED with `unversioned script would be served under a cached name: capabilities.js / capabilitiesModule.js / webgpuProbe.js` — the WEBGPU-GROUNDWORK files exist in `public/` but nothing references them, so they are neither in the module graph nor versioned. The rule was wrong, not the files: reachability is what makes an unversioned name a stale-cache HAZARD, so unreachable files are now REPORTED as notes and reachable ones still fail. **Recorded honestly: two attempted control arms for this widened rule were BOTH VOID** — emptying the reachable set turns every file into an orphan (so the hazard branch is never reached), and claiming a bare reachable name cannot fire because the versioner never writes a bare reachable name in the first place. The hazard branch is therefore an **INVARIANT ASSERTION, not a tested guard**, and the script now says so in place; its green must not be read as stale-name protection having been exercised. | FORWARD: the `DEEP-LANE-SWITCH` defect below is now **LIVE for users** — the cap lift is what made the 1e-4 boundary reachable, so a visitor zooming past 1e4x will see the 31.6-unit colour jump. That is a regression in USER-VISIBLE behaviour even though no previously-working code changed, and it should be the first thing the next slice fixes.
DISPATCHER-VERIFY | row=GPU-ARBITRARY | 2026-09-21 | **landed at `ab47737`** (rebased onto `origin/main` `15344b8`; the writer's own gated code commit was `b531308`, and its docs commit became the rebase resolution). | **THE REBASE WAS NOT A FORMALITY — this branch predated the WEBGPU-GROUNDWORK landing, so `git diff origin/main..HEAD` initially showed 5 files as DELETED (`capabilities.js`, `capabilitiesModule.js`, `webgpuProbe.js`, `webgpu-feasibility.spec.js`, `RENDERER-CONTRACT.md`). A fast-merge of the branch AS RECEIVED would have silently reverted that whole slice.** Resolved by rebasing onto main and union-resolving the two docs conflicts: `docs/DECISIONS.md` had a real NUMBERING COLLISION (main owns 1–56, the writer appended 48–52), resolved by keeping main's 1–56 and renumbering the writer's rows to **57–61** — verified 61 rows, all unique, no gaps; `docs/STATE.md` resolved by keeping main's board and applying only the writer's row change. **Verified after the rebase: the four code files are byte-identical to the gated `b531308`/`7f7b12e` (`webglFractal.js` `473b602ab56e`, `app.js` `36b89e2e1da3`, `index.html` `f01d6e979f94`, `gpu-arbitrary.spec.js` `4e9ecb130857`), the WebGPU files are PRESENT and not in the diff, and a post-landing check on `main` confirms all four still exist.** | **MY OWN full gate, at the rebased landing `7f7b12e` → exit 0 GREEN, 65 passed / 2 skipped / 6.1m** (`/tmp/fracvibe-gate/gate-full-7f7b12e-20260921T210400Z.log`); 65 = the writer's 64 plus the WebGPU feasibility pin that main now carries. The landing `ab47737` differs from the gated tree by **`docs/STATE.md` only**, so the gate stands for it — stated rather than re-run, and the code hashes above are the proof. | **MY OWN independent probe found a VISIBLE DEFECT the writer's pins do not cover: `DEEP-LANE-SWITCH`** (own row above) — a **31.6-unit mean-red jump in one zoom step** exactly where the perturbation lane takes over at 1e-4 (`9.60e-5 → 1.00e-4`), against ~0.01–3.4 units for neighbouring steps. Raw log `/tmp/fv-boundary2.log`. The writer recorded the same boundary as a hop FRACTION but bounded its colour sweep above it, so the colour delta across the switch was unmeasured until this probe. | WRITER'S CLAIMS CHECKED, not taken on report: mechanism = uniform split (`u_scale ← scale·2^shift`, `u_scaleShift`, `S = exp2(-u_scaleShift)`), with shift 0 reducing byte-for-byte to the old expressions; depth 1e-40 at 0 misclassified vs an independent BigInt reference; no-regression pin 4 (shallow lane byte-identical) is green in my gate; cap `WEBGL_ZOOM_CAP` 1e4 → 1e6; CPU-switch offer DELETED per the owner invariant; per-full-image readout exposed. | PROCESS NOTES: (1) the writer left a literal **`SHAPENDING`** placeholder in the board row, which I replaced with the real sha — a successor must never inherit a placeholder; (2) `git log` now carries a `wip: gpu-arbitrary (pre-gate checkpoint)` commit (`11dcbd5`) that is noise from the writer's own checkpointing; harmless, recorded rather than rewritten; (3) **stale comments found in the writer's own test edits** — `tests/iter-budget.spec.js:256` and `tests/smooth-color.spec.js:250` both say the cap moved “to 1e-20” while the shipped constant is `1e6` (`1e-6`). Comments only, no behavioural effect; recorded so a successor is not misled.
QUEUE-CLOSED | row=DEEP-LANE-SWITCH | **CLOSED 2026-09-22 by `session-lane-continuity` at sha `e57f9b4`** — the discontinuity is REMOVED and the sweep is flat: shipped boundary step **0.019** at `9.60e-5 -> 1.00e-4`, against the removed rule's **31.600** measured in the same pin; the sweep's max step (11.834) is a genuine non-boundary step, and a FIXED-budget arm measures the lane switch's own colour delta at **0.013**, so the lane was never the defect — D2's budget floor turning on was. The floor is now continuous (`iterBudgetForScale` ramps it in over the decade below 1e-4; the deep anchors 1e-5 2560 / 1e-8 4096 / 1e-15 7680 are unchanged). The app's zoom cap is REMOVED (`WEBGL_ZOOM_CAP = null`, owner directive row 62, no hard stop), and the limits are now stated separately: cap removed / delta RANGE fixed by the exponent split / delta MANTISSA = the CURRENT single-factor solver's 24-bit limit (correct to 1e-40, 7.4 % at 1e-42), to be widened next. **FULL gate at `e57f9b4` → exit 0 GREEN, 70 passed / 2 skipped / 9.8m** (`/tmp/fracvibe-gate/gate-full-e57f9b4-20260921T223102Z.log`); new pin `tests/lane-continuity.spec.js` (4 pins) with the failing baseline in-pin. **CORRECTION to this row's own narrative: the measured budget step was 512 -> 2058 (4.02x), NOT 50 -> 2048 (41x)** — the running app's slider floor is 512 (`FractalViewer` sets `maxIterFloor = 512`; `index.html`'s `value="256"` is overwritten), and the dispatcher's own raw log shows `cap=512` above the boundary. The 31.600-unit colour jump itself is unchanged and was reproduced exactly. | **A VISIBLE COLOUR JUMP AT THE 1e-4 LANE BOUNDARY — the owner's original hard constraint (“the zoom needs to stay visually as smooth as possible”, no step-to-step spike) is VIOLATED at one specific scale, and it only became reachable because GPU-ARBITRARY lifted the cap from 1e4 to 1e6.** `public/webglFractal.js:945` selects the perturbation (deep) lane with `view.scale < FractalKernel.ITER_BUDGET_MIN_SCALE` = `1e-4`, so crossing 1e-4 swaps the WHOLE PROGRAM (plain float32 lane ↔ perturbation lane) and simultaneously starts D2's auto-budget: the effective maxIter steps from the user's floor (50) to `ceil(512·log10(1/1e-4))` = 2048 in ONE wheel step. **MY OWN MEASUREMENT (not the writer's — theirs bounded its colour sweep ABOVE the boundary and recorded only a 0.469 sub-iteration fraction):** log-spaced real renders through the real path at the COAST view, mean red channel over the frame — `9.00e-5: 254.418`, `9.60e-5: 254.425`, **`1.00e-4: 222.825`**, `1.05e-4: 222.690`, `1.20e-4: 219.502`, `1.50e-4: 207.668`; **MAX STEP 31.60 at 9.60e-5 → 1.00e-4**, against ~0.01–3.4 units for every neighbouring step. The jump is ONE-SIDED and sits exactly on the switch, and the writer independently recorded the same boundary (`0.469` hop fraction at 6.73e-5, “driven by the cap jumping 50→2137 there”). **The measured quantum is a 4.02× budget discontinuity (512→2058) — corrected by the dispatcher; the 41× figure was my error, from reading the slider's min="50" attribute instead of fractalViewer.js:33's maxIterFloor=512; the colour jump is its visible consequence.** LIMIT OF THIS MEASUREMENT, stated: it is a MEAN over the frame, which UNDERSTATES the per-pixel maximum, and it is one view — the defect's per-pixel magnitude and its behaviour across other views/palettes are not yet bounded. | WHY IT MATTERS AND WHY IT IS NOT A REGRESSION: above the boundary the pre-change plain lane is byte-identical (the writer's pin 4, four shallow views), so nothing that worked before broke. What changed is that the boundary is now REACHABLE — it used to sit exactly at the old cap, so the app could never cross it and the jump was invisible. Lifting the cap exposed it. The writer's own note defers “making the switch invisible” to the refinement slice. | DELIVERABLE: make the transition either invisible or unnecessary — candidates, to be chosen BY MEASUREMENT: (a) raise the perturbation lane's START so the budget does not step (D2's floor already begins at exactly this scale — the two mechanisms fire together and are deliberately pinned equal, so decoupling them is a real design decision, not a constant bump); (b) ramp the effective maxIter smoothly across the boundary instead of switching a floor on; (c) have the refinement slice's coarse-to-fine passes hide the step behind a convergent image. Acceptance = the same log-spaced sweep shows no outlier step at the boundary, with the pre-fix outlier measured in-pin as the failing baseline. | Footprint: `public/webglFractal.js` (lane selection), `public/fractalKernel.js` (`ITER_BUDGET_MIN_SCALE` / `iterBudgetForScale`), `tests/smooth-color.spec.js` — ALL OF WHICH THE REFINEMENT SLICE ALSO TOUCHES, so SERIALIZE against it.
QUEUE | row=WEBGPU-WIRING (next, NOT dispatched, gated on GPU-ARBITRARY landing) | **Wire the WebGPU tier into the live dispatch.** The groundwork shipped the MECHANISM only (`public/capabilities.js` + `docs/RENDERER-CONTRACT.md`); nothing references it yet, deliberately, so the live site is unchanged. This slice must: (a) consume `selectTier(report)` to pick the backend — and the `api-present-no-adapter` case must fall through to the WebGL rung, never select WebGPU; (b) implement the missing `docs/RENDERER-CONTRACT.md` §5 gaps (progressive frames on the WebGPU path, `device.lost`, shared orbit cache, `window.__fv` observables); (c) carry the owner's other two requirements that are STILL UNBUILT — **coarse-to-fine refinement on the GPU with zoom possible mid-render**, and the **per-full-image render-time readout**; (d) **teach `scripts/stage.sh` about the new files** — it versions exactly the graph reachable from `app.js`/`fractalWorker.js`, so once `index.html`/`app.js` reference `capabilities.js` (or a WebGPU backend) the stage step must include them, or they would publish unversioned and be cache-stuck. | Precondition: **GPU-ARBITRARY must land first** — it owns `public/app.js` and `public/webglFractal.js`, which this slice must edit, and two writers must never share those files (AGENTS.md: a shared file means SERIALIZE). Sequence so far: `origin/main` = `a5beb94`; the GPU writer is still in flight at base `ab59aaa`, so on landing it needs a rebase onto main — **no code overlap is expected** (its files are `app.js`/`webglFractal.js`/tests; mine are 5 new files + docs), so a clean rebase/re-gate is the expectation, to be PROVEN by reading the trees, not predicted.
QUEUE | row=TOOLBOX-PORT (new, 2026-09-21, owner-sequenced after the GPU writers, NOT dispatched) | **Owner: “the expert project created an (untested so far) toolbox for duplicate code detections”** → decision = **COPY IT HERE AND ADAPT OUR COPY** (owner's explicit choice over fixing it in Expert; consequence accepted: the two copies diverge and a fix must be carried manually). Source=`/home/administrator/projects/Expert/tools/` (`README.md` + `duplicate-candidates/{find-duplicate-candidates.mjs, .test.mjs, README.md}`). It finds Type-4 clone CANDIDATES — function-like declarations sharing an exact / normalised / near name across files, which `jscpd` cannot see — is dependency-free (Node built-ins only), read-only, ships its own test, is byte-deterministic, and prints its OWN pruning counters. | **SMOKE-TESTED 2026-09-21 IN /tmp (nothing entered this repo): IT ALREADY WORKS ON OUR JS.** The only blocker is the extension filter (`if (!entry.name.endsWith('.ts')) continue;`); patching that one line and running against `public/` gave **11 files scanned, 2158 function-like declarations, 0 skipped/unparseable**, exit 0 — so the TS-tuned extractor handles `app.js`/`webglFractal.js`/`bigOrbit.js` fine. Result: **12 exact-name pairs, 0 normalised, 112 near; 630 pairs filtered as ubiquitous (`constructor` 621, `init` 4, `render` 3, `clear` 2) and all counted in the report.** | **WHAT THE PORT MUST ADD (measured needs, not guesses):** (1) accept `.js`/`.mjs`/`.cjs` AND adapt `SKIP_FILE_SUFFIXES` (currently `.d.ts`/`.test.ts`/`.spec.ts`) to our `*.spec.js`, or every test file enters the scan; (2) **an EXCLUDE option — the tool has `--src/--report/--max/--ignore/--no-ignore` but NO way to exclude a path, and `public/three.module.js` alone accounts for 7 of our 12 exact pairs (207 KB vendored library)**; excluding it leaves 5, of which `onMouseDown/Up/Move/Wheel` between `app.js` and `fractal3d.js` are thin delegators and `setColorOffset` is a real shared-name pair worth a look; (3) port its own test with JS fixtures (`node --test`) and keep `npm test` green; (4) scan root `public` + `tests` (the walker already skips `node_modules`/`dist*`, so `dist/` and its versioned copies are safe); (5) add `/reports/` to `.gitignore`. **Do NOT wire it into `scripts/gate.sh`** — the gate is one command with four exit codes and a lock; this is a discovery aid, not a gate. | **DOCUMENTED LIMIT — do not oversell it as enforcing DECISIONS row 52:** the tool is NAME-based, so it catches a duplicated copy of `iterBudgetForScale`/`smoothIterationValue`/`clampMaxIter`, but **NOT** the same arithmetic re-implemented under a different name in a second backend — which is the specific drift the three-backend ladder risks. It raises the odds against exact-name copy-paste (the S3 “four copies” failure mode) and proves nothing about renamed copies. | WHY IT IS WORTH PORTING: we currently have NO mechanical check for the shared-math rule in DECISIONS row 52, and this is a proven-on-our-code one that costs one line of adaptation.
QUEUE-CLOSED | row=GPU-ARBITRARY | **LANDED 2026-09-21 at `7f7b12e`** (branch `feat/gpu-arbitrary`, writer `session-gpu-arbitrary`; base `ab59aaa`, rebased onto `c9c2618`). **THE MECHANISM (measured, not asserted):** the float32 delta-coordinate RANGE wall is removed by a UNIFORM SPLIT — the deep lane seeds its per-pixel delta in float32's NORMAL range (`u_scale <- scale * 2^shift`) and carries the scale's exponent in the rescaled representation's own `S` (`u_scaleShift <- shift`, `S = exp2(-u_scaleShift)`), so `S * dc` is EXACTLY the pre-fix physical offset while neither the delta nor `S` is subnormal. Rebasing/rescaling/glitch detection untouched. **THE MEASUREMENT THAT CHOSE IT:** through the REAL render path at 128x96 vs an independent per-pixel BigInt iteration — the pre-fix seed collapses at 2e-38 (100% misclassified, ONE distinct value; still structured at 1e-35, breaking at 1e-37), and the shipped mechanism measures **1e-30/1e-35/1e-40 all 0.00000% misclassified** (1e-40: mean \|Δ escape index\| 0.003, worst 2) with the SAME view's pre-fix arm at 100% and one value. A two-factor `S` and a fully boxed `S` were IMPLEMENTED AND MEASURED WORSE (32% misclassified at 1e-40; mean 2109 vs 0.613 at 1e-30) and are NOT shipped; the single-factor form's own limit is measured (7.4% at 1e-42) and stated. **CAP:** `WEBGL_ZOOM_CAP` 1e4 -> **1e6** (scale 1e-6), from the measured COST curve (14 ms/image at 1e-4 ... 285 ms at 1e-20 ... 640 ms at 1e-40, 640x480, SwiftShader SOFTWARE rasteriser — not hardware numbers); the depth where the wall used to be (2e-38) is no longer special (not the cap, not a lane boundary, renders correctly through it). **READOUT:** per-FULL-IMAGE ms is visible (`#renderTime`) and observable (`__fv.renderTimeMs()`/`fullImagePasses()`), and its measurement is a ONE-PIXEL readback sync chosen by measurement (`gl.finish()` does NOT sync here: 0.2 ms against a true 288 ms; a 1x1 readback costs ~7% of an already-227 ms frame). **INVARIANT:** the automatic CPU-switch OFFER is DELETED (`#zoomCapOffer` + its buttons removed); the cap reports a brief non-modal notice and the renderer never changes itself; the manual checkbox and the WebGL-failure CPU path are untouched (S1/S2 pins green). **NO REGRESSION, byte-for-byte:** the shallow plain lane's FNV-1a readback hashes are IDENTICAL to the PRISTINE pre-change build at scale 3, 1e-4, 1e-8 and 1.05e-4 (two consecutive pre-fix captures are identical too), and P1's 1e-15 pins stay green. **RECORDED, not hidden:** with the cap lifted, the deep lane's own boundary at 1e-4 is a PROGRAM SWITCH inside the reachable range and measures a 0.469 hop fraction at 6.7e-5 (13404 of 13404 sub-iteration pixels) — the hop sweep was bounded above the boundary and the switch is recorded with its number; making it invisible belongs with the coarse-to-fine refinement slice. | pins=`tests/gpu-arbitrary.spec.js` (4 new) plus the S6/D2/D1 pins amended for the new cap | docs=`docs/DECISIONS.md` rows 48-52, `docs/PLAN-DEEPZOOM.md` P4/P5, this row | verify=**MY OWN local full-suite run in two batches against a worktree-local server: 61 passed + 4 passed = the whole suite green** (`/tmp/fv-arb/all1.log`, `/tmp/fv-arb/ga2.log`); **the OFFICIAL host-wide gate was REFUSED (exit 9, VOID) because a PEER WRITER held the shared lock and :3000 throughout** (`worktrees/webgpu-groundwork`, head `b55aa56`) — so this row claims a green LOCAL suite, NOT a green gate, and the gate is owed by whoever next holds the lock. |
QUEUE | row=PUBLISH-CACHE (new, 2026-09-21, NOT dispatched) | **version the asset URLs so a deploy cannot serve stale bytes.** Root cause of the 16:15Z incident and of the earlier UNEXPLAINED curl/browser discrepancy: this origin sends **no cache headers**, so Cloudflare's default **4 h per-URL edge TTL** applies, and FracVibe ships **unhashed filenames** — so every deploy serves the previous build's bytes for up to four hours. Fix = rewrite the asset URLs to carry a content hash at STAGE time (`dist/` is generated and published; `public/` stays unversioned for the dev server), because this app has **no bundler and every path is relative plus reached by `import`**, so hashing only the `index.html` tags would NOT be enough. The full request graph a writer must cover, anchors checked 2026-09-21: `public/index.html:7` (`styles.css`), `:8` (`styles/fractVibeSplash.css` — a static `<link>`, NOT fetched dynamically), `public/app.js:7` (`import './fractalKernel.js'`), `public/app.js:714` (`new Worker('fractalWorker.js')`), `public/fractalWorker.js:5` (`importScripts('fractalKernel.js')`), `public/fractalViewer.js:6` (`import './fractalKernel.js'`), plus whatever `index.html` loads for three.js. **A Cache Rule lowering the TTL is NOT the fix — it hides it.** Verification must go through **:8082 with per-ASSET cache-busters**, never a page-URL nonce. **SERIALIZE after P2 — NOW SATISFIED:** P2 landed at `a2fe502` and its writer is retired, so the publish-path slice is free to dispatch once its landing is verified (it edits `public/app.js`, which P2 also touched, so it is dispatched from P2's landing, never from a pre-P2 base).
QUEUE-CLOSED | row=1 | push Phase 0 + machinery to origin/main | CLOSED 2026-09-21T08:35Z at sha 5d86f40, verified equal SHAs and `ls-remote` | the earlier "BLOCKED on credentials" status was a host-credentials problem, never a repo or work problem
QUEUE | row=S2 | **NEXT, not dispatched** — worker truth (B6 + the probe's findings): real cancellation, `worker.onerror`, applied `progress` frames, and a payload guard | src=docs/PLAN.md S2 | base on `origin/main` = `cea74cb`
QUEUE-CLOSED | row=B4 | WebGL canvas never resized / HiDPI blur | CLOSED by S1 at `cea74cb` — one resize path sizes both backing stores to CSS × dpr, 3D sets pixel ratio; pinned by tests/canvas-dpr.spec.js
QUEUE-CLOSED | row=B9 | WebGL failure left a blank canvas; `destroy()` leaked programs/buffers/context | CLOSED by S1 at `cea74cb` — one `handleWebGLFailure`/`handleWebGLLoss` exit starts a real CPU calc and reports through the non-modal `#appMessage`; `destroy()` is idempotent, deletes program + shaders + buffer, releases the context and accounts for live renderers; the REAL `webglcontextlost` wiring is pinned
QUEUE-CLOSED | row=B5 | iteration cap mismatch: slider allows 2000, the fragment shader loops to 1024 → silently mis-coloured pixels above 1024 | CLOSED by S3 at `590945c` — ONE cap constant `MAX_ITER = 2000` (`public/fractalKernel.js:33`), the slider max derived from it (`app.js:59`), and the shader TEMPLATED (`#define MAX_ITER`, `webglFractal.js:67`) so its four loop sites (89,100,111,122) and the `u_maxIter` uniform share that bound; pinned by tests/kernel-parity.spec.js pin 1, which the dispatcher turned RED
QUEUE-CLOSED | row=B6 | worker abort is a no-op (the flag can only be set between jobs) | CLOSED by S2 at `63f19c8` — `cancelJob()` is now `terminateWorker()` + respawn with the worker generation accounted; the `abort` post is retained only for the between-jobs case and is no longer the cancel path; pinned in tests/worker-lifecycle.spec.js
QUEUE-CLOSED | row=B7 | HTML injection via imported location names (`innerHTML`) | CLOSED by S4 at `55eada7` — the sink (`app.js:180`, which interpolated FIVE fields, not just `name`) is DELETED for `createElement` + `textContent` (`appendLocationInfo` `app.js:275`); per-record schema validation in `public/memoryRepository.js:85`/`:134`; bounded import (200 records / 1 MiB / 200-char names); malformed JSON via `#appMessage`; `getAll()` sort is total; pinned by tests/import-validation.spec.js, which the dispatcher turned RED
QUEUE-CLOSED | row=B8 | 3D colour cycling rebuilds the whole heightmap per frame | CLOSED by S5 at `3f473c8` — `evaluateHeightmap()` (`fractal3d.js:130`) is the ONLY kernel call and is reached only from `regenerateMesh()`; `setColorOffset`/`setColorScheme` (`:231`/`:238`) call `applyColors()` (`:146`), which rewrites the cached heights' colour attribute reusing the live buffer and marks it for upload. Pinned 3 ways in tests/3d-truth.spec.js: zero fractal evaluations across a real cycle tick (counted — the old code scored **7**), the palette VALUES, and the palette UPLOAD (three.js attribute `version`). **NOTE: colour cycling is OFF BY DEFAULT** (`app.js:813` forces the checkbox false despite `index.html`), so B8 was reachable only after the user enables Cycle Colors. The 3D init-failure half (§2 fold-in) is closed too: `enter3DMode` (`app.js:512`) awaits `init()` and reports a failed/GPU-less init through `#appMessage`, restoring 2D via `restore2DFrom3D()` (`:504`) instead of a black screen.
QUEUE | row=B9 | WebGL-init failure leaves a blank canvas; `WebGLFractalRenderer.destroy()` leaks programs/buffers/context | src=MODERNIZATION.md §2 fold-in
QUEUE-CLOSED | row=P1 (kernel + tables) | one fractal kernel in four copies (`fractalEngine.js` is dead, overridden by the worker) + duplicated type/palette maps | CLOSED by S3 at `590945c` — ONE kernel `public/fractalKernel.js` with no `import`/`export`, so the CLASSIC worker (`importScripts`) and the ES-module main thread load the SAME file; `fractalEngine.js` and `fractalEngineMain.js` DELETED, the worker's inline override gone (127 → 69 lines), tables in the kernel and templated into GLSL as `#define FT_*`/`#define CS_*`; pinned by tests/kernel-parity.spec.js pins 2–3. S1 closed the zoom-cap third of this row
QUEUE | row=P1 (remaining) | still open from MODERNIZATION §3: per-frame LUT/`ImageData` allocation in `FractalViewer.render`, the per-pass buffer copy in the worker plus the single-worker design (no pool/tiles/transferables) | src=MODERNIZATION.md §3
QUEUE | row=P2 | Vite, `three` from npm, Node 24, lint/format/CI, ESM dev server | src=MODERNIZATION.md §4
QUEUE | row=P3 | `<dialog>` + a11y, pointer/touch input, persisted locations | src=MODERNIZATION.md §5

## TRAP
TRAP | `dist/` at the repo root is a byte-identical, UNTRACKED copy of `public/` (`diff -rq public dist` → no differences). It is the source of the LIVE apps-host site (see the PUBLISHED section) — never edit it, and after any change to `public/` re-stage it with `rsync -a --delete`.
TRAP | `MODERNIZATION.md` §2 says the fixes live "in this working tree", but they are committed (`f9a016e`, `001cec9`); §1 is explicitly the PRE-fix revision. Read the sha, not the prose.
TRAP | `webServer.reuseExistingServer` was `true`: a server left behind by a dev session or another worktree gets reused, so a "green" suite can report on files the run never loaded. Now `false`, and the gate refuses on a busy `:3000` (exit 9). Do not "helpfully" restore reuse.
TRAP | A `git worktree add` checks out the COMMITTED tree. A control run from a fresh worktree against an uncommitted script silently tested nothing (rc=127) and still printed a reassuring line. Commit before you probe, and check the probe's own exit code.
TRAP | PARITY: the tier must come from what CHANGED, not from what is uncommitted. A diff against `origin/main` is EMPTY once everything is pushed, so "docs-only, skip the suite" can silently become a full run — or an unverified push.
TRAP | `netlify.toml` pins `NODE_VERSION = "18"` (EOL 2025-04-30). `command = ""` means nothing builds today, so it is latent, not live — it bites the moment a build step is added.
TRAP | Git history carries `node_modules` across ~14 commits (~1.2MB `.git`). Purging needs `git filter-repo` **and a force-push**; it is not needed for correctness. Never do it as a side effect of another landing.
TRAP | Every commit is authored by the placeholder `User <user@example.com>`; no `user.name`/`user.email` is set in this repo.
TRAP | **A pin can assert the DATA and miss the EFFECT — it has happened TWICE.** S1: the context-loss pin called `handleWebGLLoss` directly and stayed GREEN when the real `webglcontextlost` listener was deleted. S5: the palette pin read the CPU-side `color` array and stayed GREEN when `attr.needsUpdate` was removed, so colours were computed but never uploaded — a frozen mesh behind a green suite. Ask of EVERY pin: does it observe the mechanism that produces the USER-VISIBLE result, or a proxy one step upstream? Both times the fix was to pin the effect itself (dispatch the REAL event; assert the attribute `version` advances).
TRAP | **The dispatcher's OWN error, twice:** `docs/BRIEF.md:51` requires every brief to include "any claim in `MODERNIZATION.md` your change makes false", but the S3 and S4 briefs I actually sent OMITTED that clause. Both writers therefore left the audit doc listing B5/P1/B7 as open, and I amended it by hand each time. Two writers not doing a required thing is not two coincidences — it is a brief that dropped the requirement. The S5 brief carried it, and the S5 writer amended the audit doc unprompted: the fix is verified, not assumed.
TRAP | **The dispatcher's OWN error:** the S3 brief and `docs/PLAN.md` asserted that "the dead override in `fractalEngine.js` is deleted". WRONG — `fractalEngine.js` was a dead **copy**; the live override lived in `fractalWorker.js`. The writer caught it and corrected the plan wording. Read the file before naming what is dead. Recorded rather than quietly fixed.
TRAP | **THIS HOST'S CHROME RUNS WEBGL ON SWIFTSHADER** (ANGLE/Vulkan software rasteriser). Every "GPU" pin in this suite therefore compares the CPU kernel against a SOFTWARE implementation of the shader, not real hardware — the pins remain valid for correctness, but they say nothing about GPU performance, and a hardware-only GLSL defect could pass here. Reported by the S3 writer.
TRAP | S3's pin-1 threshold is MEASURED, not generous: the correct mean \|CPU−GPU\| is 0.911 against a `<6` bound, so the threshold sits only ~6.6× above the noise floor. The dispatcher's own injection (uniform pinned to 100) tripped it at **6.53** — barely. A subtler GPU/CPU divergence could pass. The residual is the pre-existing half-pixel CPU-corner vs GPU-centre sampling mismatch, deliberately NOT fixed (out of S3's scope), costed into the thresholds and recorded in `docs/DECISIONS.md` row 17.
TRAP | **NEVER write a mechanism into a brief or a record from plausibility.** The probe asserted "`new Int32Array(undefined)` throws"; it does NOT — it yields an empty array. The unverified claim reached the probe report, `docs/PLAN.md`, this board, AND a writer brief before the S2 writer ran it in node and falsified it. Run the runtime semantics you are about to assert, or mark the claim UNVERIFIED in the text itself.
TRAP | **A probe that removes only SOME of the nets proves nothing.** The dispatcher's first two attempts to make S2's cancel pin fail were VOID: attempt 1 missed the generation gate in `makeWorkerMessageHandler`; attempt 2 missed `terminateWorker`'s own `progressiveState` clear. Only removing all four turned the pin RED. Remove the WHOLE mechanism, and never report an incomplete arm as a gap — a pin that "passes a broken build" is far more likely to be a broken injection.
TRAP | The GitHub credential is an `x-access-token` in `~/.git-credentials` (mode 600), read by `credential.helper=store` in `~/.gitconfig`. It **expires**: a push that works today can fail tomorrow with 403 / an auth failure. The fix is to ask the owner for a fresh token in the SAME format — never to hunt for one, never to print/echo/commit it, and never to touch `~/.openclaw`. `git ls-remote --heads origin` is the cheap pre-flight proof; see the `github-push` skill.
TRAP | `git add -A` is not allowed while another actor may be mid-flight: assert what is staged with `git status --short` and add EXPLICIT paths. This session used `git add -A` twice while it was the only writer — safe both times, recorded here as a correction rather than a defence.
TRAP | There is no `.github/workflows`, so a push here triggers **no CI**. The deploy path is Netlify's own Git integration, and `https://fracvibe.netlify.app/` answers HTTP 404, so the live URL is UNVERIFIED from this host. Report that `origin/main` was fixed; do not claim the live site was.
TRAP | A pin that invokes a HANDLER directly through a test hook does NOT cover the WIRING that calls it. S1's original context-loss pin ("a lost WebGL context degrades…") PASSED with the real `webglcontextlost` listener deleted, because it called `window.__fv.simulateContextLoss()` → `handleWebGLLoss` and never touched `attachContextLossGuard`. When a pin's name claims a real-world event, dispatch the REAL event and assert an observable side effect (there: `dispatchEvent()` returns `false` because `preventDefault()` ran). Fixed forward at `42ce3b6`; the hook test is renamed to say what it actually covers.
TRAP | `git worktree add` inside the repo trees the new worktree OUTSIDE `.gitignore` unless `worktrees/` is listed — and a worktree's symlinked `node_modules` is not matched by a `node_modules/` pattern (trailing slash = directories only). Both are now handled; see `.gitignore`.

## GUARD
GUARD | suite-cost | the full tier is **55 tests, ~3.1m** after P1 (2026-09-21; the stale "39 tests, ~1.8m" figure was the pre-S3 state); the compile tier is ~1s over 24 parsed files. Both go through the same one gate; the compile tier still takes no lock.
GUARD | one-gate | `bash scripts/gate.sh` is the only gate. The full tier takes an atomic `mkdir` lock at `$GIT_COMMON/fracvibe-gate.lock`, shared by every worktree; the compile tier reads nothing shared, so it takes no lock and runs alongside a peer's suite. Exit codes in-file and in AGENTS.md: 0 green · 1 red · 2 compile-only (suite did not run) · 9 refused (VOID) · 10 killed (VOID) · 64 usage.
GUARD | gate-controls | exercised 2026-09-21 at c6c87e2 — full green exit 0 · compile exit 2 (16 files parsed) · BUSY exit 9 with the winner's lock left INTACT · dead-owner stale lock reaped, then green · from a probe worktree: full exit 9 refused on the shared lock, compile exit 2 while that lock was held. Raw logs /tmp/fracvibe-gate/.
GUARD | per-tree | a syntax error injected into a probe worktree's `public/app.js` turned THAT worktree's compile gate RED while the main tree stayed green — so the gate gates the tree it lives in, and a worktree run cannot silently validate the main tree. (Control log /tmp/fracvibe-gate/gate-compile-c6c87e2-20260921T082745Z.log.)
GUARD | host-floor | the full tier refuses to start below 500MB `MemAvailable` (exit 10, VOID). Override with `GATE_MIN_AVAIL_MB`.
GUARD | wrong-tree | the gate refuses when `:3000` is already served (exit 9) rather than verifying another tree's files; `reuseExistingServer: false` is the second line of the same defence.

## RECOVERY
RECOVERY | PUBLISH (current: deployed code = P1's gated tree, `origin/main` = `97ee9e1`, 2026-09-21T16:15Z): the live site is `https://apps.futuremagic.de/fracvibe/` (public, no login), served from `~/apps/fracvibe` → the repo's `dist/`. Republish = `rsync -a --delete --exclude '.git*' public/ dist/`; verify = `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8082/fracvibe/` (expect 200) **and** hash the served assets against `dist/` with a cache-buster (`curl -s "<url>?cb=$RANDOM" | sha256sum`), because bare CDN URLs can serve a stale edge copy for up to 4 h (`max-age=14400`). Full published-subpath suite: `npx playwright test --config /tmp/fv-publish.config.js`. The pre-publish `dist/` is backed up at `/tmp/fv-dist-publish-20260921T1620/`. See the `apps-publish` skill; `~/apps` is world-readable, so build output only.
RECOVERY | S6 evidence: dispatcher gate `/tmp/fracvibe-gate/gate-full-310c4ae-20260921T112006Z.log`; the dispatcher's confirm-path probe `/tmp/fv-armS6.log` (dialog pin RED). Landing sha `310c4ae` — the FINAL campaign sha. Two modal surfaces now live in `index.html` (`#saveLocationPanel`, `#zoomCapOffer`); the render path is `appendLocationInfo` (`app.js:275`); the ONE gate is `scripts/gate.sh`.
RECOVERY | S5 evidence: dispatcher gates `/tmp/fracvibe-gate/gate-full-ae94753-…` and `gate-full-3f473c8-…`; the dispatcher's probe that FOUND the gap `/tmp/fv-armS5-palette.log` (palette pin GREEN while the upload was removed) and the round-2 re-run `/tmp/fv-armS5r2.log` (upload pin RED, 1 failed / 2 passed). Landing sha `3f473c8`. 3D seams: `fractal3d.js:130` `evaluateHeightmap`, `:146` `applyColors`, `:231`/`:238` the colour setters; `app.js:512` `enter3DMode` (awaits init), `:504` `restore2DFrom3D`, `:1290` `__fv.colorAttributeVersion`.
RECOVERY | S4 evidence: dispatcher gate `/tmp/fracvibe-gate/gate-full-55eada7-20260921T102954Z.log`; the dispatcher's escaping-helper probe `/tmp/fv-armS4.log` (3 RED). Landing sha `55eada7`. Read `public/memoryRepository.js:85`/`:134` (the ONE schema) and `app.js:143` (the ONE import path) first; the render path is `appendLocationInfo` (`app.js:275`).
RECOVERY | S3 evidence: dispatcher gates `/tmp/fracvibe-gate/gate-full-afe7601-…` and `gate-full-590945c-…`; the dispatcher's uniform-path probe `/tmp/fv-armS3-pin1.log` (RED) and `/tmp/fv-armS3-pin2.log` (GREEN, isolation); the writer's out-of-tree snapshot `/tmp/s3-new` with its six arm hashes. Landing sha `590945c`. The single kernel to read first is `public/fractalKernel.js`; the cap constant is its line 33.
RECOVERY | S2 evidence: dispatcher gates `/tmp/fracvibe-gate/gate-full-446f746-…` and `gate-full-63f19c8-…`; the four-net probe that went RED `/tmp/fv-armS2c.log`; the two VOID incomplete attempts `/tmp/fv-armS2.log` and `/tmp/fv-armS2b.log`. Landing sha `63f19c8`.
RECOVERY | S1's writer-side evidence survives at `/tmp/s1-evidence/gate/` (its own FINAL green logs, its armF/armG red logs, its iteration logs). The dispatcher's own pin evidence is `/tmp/fv-arm1.log`, `/tmp/fv-arm2.log`, `/tmp/fv-arm2b-real.log`, `/tmp/fv-arm2b-hook.log`, and the gates under `/tmp/fracvibe-gate/`. S1 landed at `cea74cb`.
RECOVERY | a successor starts here, then runs: `git fetch && git rev-list --left-right --count origin/main...main`, `git worktree list`, `git branch -a`, and reads `MODERNIZATION.md`. Raw gate logs: /tmp/fracvibe-gate/.
RECOVERY | machinery that must exist before any dispatch is on disk as of c6c87e2: this board, `docs/DECISIONS.md`, `docs/BRIEF.md`, `scripts/gate.sh`, `AGENTS.md`. Established 2026-09-21 by session-f6d26a74.
RECOVERY | dispatcher errors made while establishing it, recorded rather than quietly corrected: (a) the full suite was run in the FOREGROUND of the operator's session once, having forgotten `GATE_TIER=compile` — 6.2s, harmless here, still a rule breach; (b) the first draft of `gate.sh` released the lock from an EXIT trap without checking ownership, so a REFUSED run would have deleted the winner's lock — fixed before use and covered by the BUSY control; (c) the first cross-worktree lock probe was VOID (see TRAP on `git worktree add`) and its "lock INTACT" line proved nothing, which was reported before the error was noticed.
TRAP | **A DECLARED 404 is not a broken reference — identify the REQUEST before believing it.** On 2026-09-21 the OpenClaw admin reported that P2's `app.js` "references `fractal_locations.json`" and that the edge returned 404, inferring a missing build input. **FALSE POSITIVE, and it reached me as a claim to fix:** the only occurrence is `a.download = 'fractal_locations.json'` (`public/app.js:184`), a filename string for the Export button's download attribute — **never fetched, never a URL**. Measured on the live site: 14 requests total, **zero** matching `fractal_locations`. The string also predates P2 (`aa39f1c:179`). A declared filename inside JS is invisible to a grep-for-the-name audit; ask what the request actually was.
TRAP | **The suite's mysterious 404 is `favicon.ico` at the HOST ROOT and is not the app's.** CDP `Network.responseReceived` names it: `https://apps.futuremagic.de/favicon.ico → 404`. It appeared in every P1 and P2 verification run as a single console error and was previously recorded as unexplained. It is a host-root convention request, unaffected by anything under `fracvibe/`; `index.html` linked no icon. Harmless, but it is why a bare "one console error" reading is not evidence of an app defect. **FIXED 2026-09-21 at `7735d36`** — see the FAVICON row below; the app now declares its own icon, so the browser no longer asks the host root.
FAVICON | LANDED 2026-09-21 | sha=`7735d36` | owner asked for it after this trap was reported | delivered=`public/favicon.svg` (hand-authored: dark `#181818` tile, accent-yellow `#ffe066` orbit arc + its escaping dot, no external font or image so it renders at 16 px — checked by rendering at 16/32/64/100 px on BOTH a light and a dark strip before committing); `public/index.html` `<link rel="icon" type="image/svg+xml" href="favicon.svg">` **relative**, like every other path in this app (a root-absolute href would 404 under the published subpath); one pin in `tests/hygiene.spec.js` holding **each link in the chain** — declared, on disk, served, AND **really received by a browser** (declaration alone passes while the file 404s, which is the exact shape of this bug) | verify=**MY OWN**: FULL gate at `bcf9ebe` (the tree WITH the change) → **exit 0 GREEN, 59 passed / 3.4m** (/tmp/fracvibe-gate/gate-full-bcf9ebe-20260921T165216Z.log); **MY OWN control**: `href` repointed at a missing file → the pin **RED** at `favicon-does-not-exist.svg is not on disk` (/tmp/fv-fav-arm.log), restored byte-identically; **published and verified on the LIVE URL**: the bare CDN serves `favicon.svg` → **200 `image/svg+xml`**, the browser declares `favicon.svg` and receives **200**, and **ALL non-2xx = NONE** — the host-root 404 that had been unexplained for the whole campaign is gone | published=re-staged `dist/` (mirror verified) and **both new/changed files were FRESH at the edge on their bare URLs immediately — NO purge needed this time**, because `favicon.svg` is a brand-new URL and `index.html` was already uncached (`cf-cache-status: DYNAMIC`) | note=this was safe to do while the WORKER-CANCEL writer is in flight: its worktree was **verifiably clean** of `public/index.html`, so the two footprints are disjoint (sequencing PROVEN by reading its tree, not predicted).
TRAP | **NEVER `pkill -f 'http.server'` — the apps host's static server matches that pattern, and killing it 502s EVERY app on `apps.futuremagic.de`, not just this one.** Done once, 2026-09-21, during my own cleanup of a THROWAWAY debug server I had started on :8099: `pkill -f 'http.server 8099'` was safe, but the broad pattern killed the host's `python3 -m http.server 8082 --bind 127.0.0.1 --directory /home/administrator/apps` (PID 2115031) too. The owner saw `502 Bad Gateway` on `https://apps.futuremagic.de/fracvibe/`. It had **already warned me twice**: the same pattern matched my OWN shell command line and killed my shell (a `[killed by signal: SIGTERM]` result I moved past). **There is NO systemd unit for it** (`systemctl` and `/etc/systemd/system` name nothing for 8082 or apps) — it was a bare manual process, so it does not survive a reboot and nothing restarts it. `cloudflared` stayed up (PID 1676589); the 502 was purely the dead origin. **The lesson generalises: kill by the PID you started, never by a pattern that can match a service you did not.**
RECOVERY-APPS-HOST | **`https://apps.futuremagic.de/` is served by a plain static server on `127.0.0.1:8082` with NO supervisor, behind the `cloudflared` tunnel.** If EVERY app on the host 502s, check `ss -ltn | grep 8082` and `pgrep -af 'http[.]server 8082'`; if it is gone, restart it detached so it outlives the session: `cd /home/administrator/apps && setsid nohup python3 -m http.server 8082 --bind 127.0.0.1 --directory /home/administrator/apps > /tmp/apps-host-8082.log 2>&1 < /dev/null & disown` — then verify `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8082/fracvibe/` → 200 AND the same for `/` (the hub), `/expert/` and `/apps.index.json`, so a partial restore is visible. Confirmed working 2026-09-21T18:16Z. **OPEN HOST-LEVEL ASK (not mine to make unilaterally): this service has no supervision** — a reboot or one careless signal takes down every app; a systemd unit (or supervision by the OpenClaw side, which owns this host) would make it self-healing.
TRAP | **A `502` is an ORIGIN/transport failure, not an app defect — check the local origin BEFORE reading the app.** `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8082/fracvibe/` returning `000` proves the origin is down (`000` = no connection at all, a different diagnosis from any `4xx`/`5xx` the app itself could produce). The CDN's `502` is the tunnel reporting that same fact.

## 🔬 GPU-DIAGNOSTIC — the hardware-certification instrument (DECISIONS 68)
INSTRUMENT | 2026-09-21 | branch=`feat/gpu-diagnostic` · worktree=`worktrees/gpu-diagnostic` · base=`origin/main` = `b300413` | delivered=`public/gpuDiagnostic.js` (**NEW**: identity + unmasked renderer, the feature gates including a REAL float-texture sample test, the DOUBLE-SINGLE RESIDUAL probe on a STANDALONE shader, the real deep lane vs an in-page CPU float64 reference (1e-6) and a direct BigInt reference (1e-20) reporting % misclassified / mean `|Δ|` / `orbitSource`, `gl.getError()` + readback plausibility, and `renderTimeMs()` at 1e-4/1e-6/1e-8) · `public/index.html` + a `Test` button, a `#gpuDiagStatus` span and a module `<script src="gpuDiagnostic.js">` · `tests/gpu-diagnostic.spec.js` (asserts the report's STRUCTURE only, plus button wiring and safe re-entry) · DECISIONS row 68 | **WHY: this host runs SwiftShader for WebGL AND WebGPU**, so the k-component expansion ladder (DECISIONS 66/67) can be BUILT and MEASURED here but NOT CERTIFIED on a real compiler and driver — **the owner presses `Test` on their GPU and reports the console, and that press is the certification instrument this record has been missing.** It integrates ONLY through the frozen `window.__fv` observables and touches NONE of `app.js`/`webglFractal.js`/`fractalKernel.js` (owned by `feat/lane-continuity`). | **verify=MY OWN FULL gate at the landing sha `b5eae26` → exit 0 GREEN, 68 passed / 2 skipped / 8.4m** (`/tmp/fracvibe-gate/gate-full-b5eae26-20260921T230334Z.log`); my three tests are `✓ 13` (button → one structural report, 44.8s), `✓ 14` (safe re-entry + rerun, 1.9m) and `✓ 15` (browser-free non-vacuity control, 15ms). The spec asserts STRUCTURE only, never the machine-specific values. **The gate log carries the exact 33-line block** (echoed by the spec) — see the BASELINE-GPU-DIAGNOSTIC row. Sixteen earlier attempts were `exit 9 REFUSED` because `lane-continuity`/`precision-chain`/`expansions` held the host-wide gate; those runs are VOID, never failures, and the lock was never forced.
BASELINE-GPU-DIAGNOSTIC | 2026-09-21T23:03Z | the SwiftShader baseline the owner's hardware report is compared against, captured VERBATIM in the gate log above | `renderer="ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (Subzero) (0x0000C0DE)), SwiftShader driver)" kind=SOFTWARE` · `floatTex=SAMPLES` (`wrote=[0.25,0.5,0.75,1] read=[64,128,191] maxDelta=0`) · `oes_texture_float`/`_linear` present, `MAX_TEXTURE_SIZE=8192 px`, `MAX_VERTEX_TEXTURE_IMAGE_UNITS=32`, `highp=23 bits / mediump=10 bits` · **`dsLow=ZERO-IGNORED`: the LITERAL-OPERAND `TwoSum(1, 2^-25).lo` reads back **0** (rawByte=0) while the VARIABLE-OPERAND compensated accumulation still moves the result (`compensated=1.0000019 naive=1.0000000 -> YES`) and the Dekker `TwoProd` residual survives (`5.960e-8 -> NONZERO`).** That is the research §7 warning measured on a real compiler: the literal form is folded away, the loop-carried form is not — so the instrument's low-component probe is a genuine compiler-portability signal, and the `dsLow` SUMMARY token must be read together with `ds_low_changes_result` and `ds_twoprod_low`, not alone. · deep lane both engaged and correct: at 1e-6 `src=float64 misclassified=0.032% mean|dn|=2.269` (capIter 3072) and at 1e-20 `src=bigint bits=192 misclassified=0.000% mean|dn|=0.176` (capIter 8192, `orbitTexels=8192`) · `gl_error=NO_ERROR(0)`, `readpixels_plausible` · cost (SOFTWARE bounds, DECISIONS 47, measured under load ≈5-8): 1e-4 **70.9 ms** (lane=none), 1e-6 **7397.5 ms** (float64), 1e-8 **3306.9 ms** (float64) | **limit, stated:** these are this host's numbers on a software rasteriser; the owner's press is what certifies hardware, and one press samples one driver/OS/browser.
## MANTISSA-LADDER — the expansion arithmetic layer (NEW FILES ONLY, 2026-09-21)
SESSION | id=session-expansions | role=WRITER · row=MANTISSA-LADDER | state=dispatched 2026-09-21 by session-0e97db24-5f1e-49f6-8e63-18a285c027a0 · worktree=`worktrees/expansions` · branch=`feat/expansions` · base=`origin/main`=`ca14755` · scope=the k-component expansion arithmetic layer for arbitrary depth, per DECISIONS 66 | **NEW FILES ONLY** (`public/expansions.glsl.js`, `public/expansions.js`, `tests/expansions.spec.js`) plus appends to `docs/DECISIONS.md` (68-70) and this row; **`public/webglFractal.js`, `public/fractalKernel.js`, `public/app.js` are UNTOUCHED** because the LANE-CONTINUITY writer is mid-flight in the shader and kernel, and integration of the ladder is a LATER slice | verify=see GATE rows below
DELIVERED | `public/expansions.glsl.js` — the GLSL EMITTER, k a build parameter: `TwoSum` (Moller-Knuth), `FastTwoSum`, `DekkerSplit`, `TwoProd`, Priest distillation, `EX_ADD/SUB/MUL/SQR/SCALE/SCALE_POW2/DOUBLE` and `EX_DELTA_STEP` for the recurrence `w -> 2 Z w + S w^2 + d` | `public/expansions.js` — the facade plus the JS float32 REFERENCE (`Math.fround` after every op) and the exact cost model; no `import`/`export`, one frozen global each (DECISIONS 14) | `tests/expansions.spec.js` — 9 pins against an INDEPENDENT BigInt dyadic oracle, each with an in-spec collapsed-build RED control
MEASURED-K1-IDENTITY | the k=1 rung is BIT-IDENTICAL to the plain float32 expressions: 5 ops x 96 scalars and a 384-px delta recurrence, **0 mismatches** (pin 1). This is the property that lets the existing shallow-lane byte-identity pins keep passing after integration.
MEASURED-COMPILER | the emitted GLSL matches the JS float32 reference **bit-for-bit** on ANGLE/SwiftShader for add/sub/mul/sqr at k=1..4 and across a 3-op chain at k=4 (**0 of 128** component values differ). The rounding points the transforms need SURVIVE this compiler — on SOFTWARE (see UNVERIFIED).
MEASURED-ACCURACY | vs an independent **512-bit** BigInt fixed-point reference, 32x24 at the P2 centre, f32 orbit transport: **delta relative error** at 1e-6/20 steps k1 `1.80e-7` k2 `2.95e-12` k4 `2.94e-12`; at 1e-20/64 steps k1 `1.84e-7` k2 `1.00e-14` k4 `2.56e-18`. **Escape-index misclassification** (512-bit DIRECT reference) at 1e-6: k1 **4/768 (0.5208%)**, k2 **0/768**, k4 **0/768**; at 1e-20: k1 **4/768 (0.5208%)**, k2 **2/768 (0.2604%)**, k4 **2/768 (0.2604%)**. **FINDING: at a deep view k=4 buys nothing over k=2 for the escape index because the 24-bit f32 ORBIT TRANSPORT dominates both** — DECISIONS 67's second mechanism, arriving on schedule; the next slice is the multi-component orbit transport, not more rungs.
MEASURED-COST | structural counts per delta step (TwoSums/TwoProds): k1 `0/0`, k2 `138/20`, k3 `594/39`, k4 `1740/64` (pinned exactly). Wall time on the software rasteriser, 32x24x512: k1 `~21-33 ms`, k2 `~106-137 ms`, k4 `~2059-2447 ms`; a 64x `EX_MUL`/`EX_ADD` chain at 64 px: k1 `~14-28 ms`, k2 `~30-44 ms`, k4 `~360-412 ms`.
PRECONDITIONS-PINNED | `FastTwoSum` requires `|a| >= |b|` (unordered it loses the residual **2/2**, while `TwoSum` stays exact) and `TwoProd`'s Dekker split requires `|operand| < 2^128/4097 = 8.306e34` (beyond it the residual is NaN on the GPU and in the JS reference alike). Both are exported as `SPLITTER_MAX`; neither binds the recurrence (operands O(1)). Subnormal operands are excluded and measured to be non-exact here (`1e-38 + 1e30`), which is why the shipped `u_scaleShift` seed exists.
UNVERIFIED | hardware. Every number above is ANGLE + **SwiftShader** (software). The bit-exact compiler result is evidence about THIS compiler, NOT that a hardware driver preserves the transforms; certification needs hardware this host does not have (RESEARCH §7 warning 2, DECISIONS 70).
GATE | row=MANTISSA-LADDER | sha=`098bc6a` | **FULL gate at the landing head: exit 0 GREEN, 74 passed / 2 skipped / 6.1m**, raw log `/tmp/fracvibe-gate/gate-full-098bc6a-20260921T231551Z.log` — the 74 = the 65 the tree already carried plus this slice's **9** expansion pins, and the 2 skips are the existing WebGPU adapter-gated feasibility tests (documented). **VOID runs, recorded not hidden:** two earlier full-gate attempts at `c2c242d`/`098bc6a` were **exit 9 REFUSED** (the gate lock was held by `worktrees/lane-continuity` then `worktrees/gpu-diagnostic` then `worktrees/precision-chain`) and one was killed by the harness's 10-minute command cap after a 20x20 s wait loop (log `/tmp/fv-exp-probe/gate-full-2.log`, incomplete) — none of those is evidence. **A green FULL gate at `c2c242d` also exists** (`/tmp/fracvibe-gate/gate-full-c2c242d-20260921T225722Z.log`, 74 passed / 2 skipped) from before the pin-7 strengthening.
CONTROL | row=MANTISSA-LADDER | **two named injections into the NEW emitter, both watched RED and both restored BYTE-IDENTICALLY (`public/expansions.glsl.js` sha256 `8c04715d0dca4d007905a54e09f6ad839347590ff879b6f02a11744476b1dbb6` before and after).** (1) **collapse `EX_MUL` to float32** (`Exp(a.c0*b.c0, 0, ...)`) → **pin 5 RED** ("the emitted GLSL must match the JS float32 reference bit-for-bit across a chain"); **pin 7 stayed GREEN and that is recorded as a LIMIT, not a success** — the delta step's linear term is an `EX_SCALE`, so a mul-only collapse is not numerically significant at these views. (2) **collapse the distillation** (zero every non-leading component, i.e. the exact "silently collapsed to f32" failure the research warns about) → **pins 3, 4, 5 and 7 RED**, pin 7 on **"the low component must be nonzero across the deep view"** — the REQUIRED-NONZERO assertion the mission names, actually doing its job. In-spec collapsed-build controls also run on every gate: pin 2 rejects 7/10 sums and 3/10 products, pin 3 rejects 2/2 recovered residuals, pin 4 rejects 4/4.
TRAP | **A `502` is an ORIGIN/transport failure, not an app defect — check the local origin BEFORE reading the app.** `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8082/fracvibe/` returning `000` proves the origin is down (`000` = no connection at all, a different diagnosis from any `4xx`/`5xx` the app itself could produce).
## 🎯 PRECISION-CHAIN (writer `feat/precision-chain`, base `ca14755`, NEW FILES ONLY — 2026-09-21)
PRECISION-CHAIN | row=AUDIT (mechanisms 67) | status=**DELIVERED** · `docs/PRECISION-CHAIN.md` (new) is the measured stage-by-stage map from the view centre to the pixel, with `file:line` anchors, significand bits and exponent range at 1e-30, and the ranked limiter list at 1e-30 and 1e-40. **HEADLINE: the dispatcher's 24-bit orbit-transport claim is CONFIRMED as a width (mean delivered significand 25.78 bits; `toF32` keeps 53 then frounds to 24) and FALSIFIED as the depth cap** — against an independent per-pixel direct-BigInt reference the shipped 24-bit transport is **0.0000 % misclassified, mean \|Δ\| 0.007–0.017** from 1e-20 to 1e-40, and extra BigInt precision changes **0.0000 %** of pixels because the perturbation only ever samples the first **364** orbit indices (first transported-word divergence at index 612). **What binds first is the float32 DELTA mantissa** (0.003 at 1e-30 / 0.005 at 1e-40 incremental; breakdown 1e-42 / 7.4 %, DECISIONS 57), so DECISIONS 66's k-component ladder is correctly next. **NEW GAP FOUND: `centerXExact` has NO production producer** — the only one is the `__fv.setDeepView` test hook (`app.js:1620`), so the BigInt orbit is unreachable from the shipped UI; a serialisable exact centre is a prerequisite for "arbitrary in both paths" and was not named by 67 or the queue. **RECORD CORRECTION, measured: the CPU lane is clamped to scale ≥ 1e-6, not the 1e-4 on the board** (`WEBGL_ZOOM_CAP` was lifted to 1e6 and one shared clamp serves both lanes); DECISIONS 63's conclusion is unchanged. | mechanism map: (a) orbit transport = stage 3, second-order; (b) delta mantissa = stage 4, the next wall; (c) CPU port = stage 5.
CPU-PERTURB | row=PROTOTYPE (mechanism 67c) | status=**WORKING PROTOTYPE, new files only** · `public/fractalPerturb.js` + `tests/fractal-perturb.spec.js` (2 pins) · consumes `BigOrbit.computeOrbitFixed` for the orbit and `FractalKernel.smoothIterationValue` for the ONE colour rule; delegates every view it cannot improve straight to `FractalKernel.calcFractalChunk`. **CORRECTNESS (independent direct-BigInt reference, cap 8192): 0.00000 % misclassified at 1e-30 (mean 0.023), 1e-40 (mean 0.013) and 1e-120 — where the shipped lane's own code is 2.78–4.17 % misclassified with mean ~230–300 iterations and ~19 distinct colours (the in-pin failing baseline).** [probe] matches to 1e-250, degrades ~1e-300 (float64 offset underflow). **COST (browser, 320×240, one frame): 118 ms at 1e-20, 187 ms at 1e-30, 251 ms at 1e-40 (1.53–3.27 ms/kpx); 30/47/63 ms at 160×120; orbit built once per view (5–11 ms).** USABLE on one core; tiling + the existing 8→4→2→1 refinement are the next multipliers. **NOT genuinely arbitrary yet: the ORBIT is (BigInt, depth-scaled bits), the per-pixel DELTA is float64 (53-bit)** — mechanism (b) applies to the CPU too. Rebasing is implemented and fires 46k–190k×/frame but is nearly inert here with float64 deltas. REMAINING: wire `calcPerturbChunk` into the worker/dispatch, exact-centre producer, a wide orbit accessor in `bigOrbit.js`, delta ladder on both lanes, tiling. **Do NOT edit `bigOrbit.js` from this slice** — it is the expansions writer's / a later slice's file.
PRECISION-CHAIN | UNVERIFIED | the host is **SwiftShader** for WebGL (and WebGPU): every GPU precision number is a correct-software-rasteriser number, NOT hardware; a hardware-only GLSL defect or an FMA/reassociation difference could still pass here. The float32 delta emulation in the audit is a non-contracted `Math.fround` model (GLSL may fuse). The deep reach sweep (1e-50…1e-300) is a throw-away Node probe, not a committed pin; the committed pins are 1e-30/1e-40/1e-120.
PRECISION-CHAIN | VERIFY | **gate=FULL exit 0 GREEN, 67 passed / 2 skipped / 6.0m at head `1bda851`**, raw log `/tmp/fracvibe-gate/gate-full-1bda851-20260921T225119Z.log` (suite 65+2 -> 67+2; the 2 skips are the WebGPU no-adapter tests, unchanged). **Both new pins RAN (not skipped) and passed**: `tests/fractal-perturb.spec.js:225` (7.7s) and `:265` (4.7s). Gate-log numbers at 1e-120: PORT mis=0.00000, mean \|Δ smooth\| 0.0020, distinct 652/653, maxM 1572, 32 ms; BASELINE mis=0.02778, mean 188.2, distinct 19. Worktree holds only tracked files; the slice is NEW FILES + docs only (no forbidden file touched), so it was safe beside the in-flight `feat/lane-continuity` writer. The CDN's `502` is the tunnel reporting that same fact.
